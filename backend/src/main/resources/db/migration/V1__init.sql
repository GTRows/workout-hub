-- V1__init.sql
-- Placeholder migration. Real schema (users, exercises, workout_plans, ...)
-- lands in PHASE 1. Keeping an empty migration here so Flyway has a
-- baseline to report on first boot.
SELECT 1;
