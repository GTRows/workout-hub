# Milestone v1.0: Release Hardening

**Status:** SHIPPED 2026-05-09
**Phases:** 38-44 (7 phases, 9 plans)
**Total Plans:** 9
**Releases:** v1.0.0

## Overview

Production-ready cut of WorkoutHub, closing every remaining v0.6 carry-forward issue that could land within a single-commit-per-bump cadence and re-deferring the residual major-version risk into successor issues with explicit closure playbooks. Seven phases (38-44) across 9 plans: Playwright critical-flow + responsive + session-save-resume specs hardened against the default Turkish locale (Phase 38), Testcontainers 1.20.4 -> 1.21.3 patch-level bump with i-7 closed and i-7b opened for the upstream 2.0.0 GA (Phase 39), framework majors handled in three sub-plans (40-01 Spring Boot 4 attempt rolled back at 38c341e and re-deferred to i-8b; 40-02 Next 16 deferred to i-6b after the next-intl 3.x peer-range audit; 40-03 next-intl 3.26 -> 4.11.1 landed clean closing i-5), security hardening with Netty 4.1.133 -> 4.2.13 closing i-14 and removing the CVE-2026-42577 trivy suppression (Phase 41), docs-completion adding the V1..V28-derived `docs/DATA_SCHEMA.md` plus README Documentation map + Versioning section + DEPLOYMENT.md "What's not here" cross-links (Phase 42), backup-restore drill landing `docs/BACKUP_RESTORE_DRILL.md` operator runbook + cron-augmented `docs/BACKUP.md` + populated `docs/MIGRATION.md ## v1.0.0` section (Phase 43), and the v1.0.0 release cut at sha `cac37c1` with annotated tag pushed locally awaiting maintainer push for multi-arch GHCR publish (Phase 44).

## Phases

### Phase 38: e2e-tests

**Goal:** Make the existing Playwright specs at `frontend/e2e/` pass against the real `docker compose up` stack on the default Turkish locale, and add a third spec covering the partial-progress save-resume gap from the roadmap line.
**Depends on:** v0.6 archive complete
**Plans:** 1

Plans:
- [x] 38-01: e2e-tests

**Details:** Plan 38-01 retrofitted `frontend/e2e/critical-flow.spec.ts` and `frontend/e2e/responsive-360.spec.ts` to use locale-stable selectors (input `id`, `type`, `data-testid`, explicit Turkish + English alternatives in name regexes) so they pass against the default `tr` locale instead of failing on English-only regex locators (`/log in/i`, `/email/i`, `/password/i`, `/start workout|antrenmani baslat/i`); replaced unreliable `page.waitForTimeout(400)` waits in the session-execution flow with real network-idle awaits via the existing TanStack Query mutation surface; added `frontend/src/app/(app)/session/[id]/session-client.tsx` `data-testid` hooks on the set-row weight/reps/RPE inputs and the add-set button so the spec can target them locale-independently; introduced `frontend/e2e/fixtures/login-as-admin.ts` shared fixture so all three specs reuse the same admin-seeded login flow; landed `frontend/e2e/session-save-resume.spec.ts` as the third spec covering partial-progress recovery (login -> active workout day -> log two sets -> close tab -> reopen -> third set persisted via the IndexedDB drain wired in v0.5 Phase 25); added `frontend/e2e/screenshots/.gitkeep` placeholder + `frontend/e2e/screenshots/.gitignore` rule so artifact uploads don't pollute the working tree; documented the Playwright run cadence in `frontend/e2e/README.md` (prerequisites: `pnpm install` + `npx playwright install` + `docker compose up -d` + admin user seed; commands: `pnpm test:e2e`, `pnpm test:e2e:headed`). CI wiring deferred to a follow-up sub-plan (out of v1.0 scope; tracked locally as part of i-15 perf-budget gating). Commits c370c98 (test-id hooks) -> c27b835 (login fixture) -> 9061daa (critical-flow locale-stable) -> 2a3709b (responsive-360 fixture reuse) -> 11c8037 (session-save-resume spec) -> 3a3c73d (e2e README) -> 2c22292 (screenshots placeholder) -> a2d0b4b (planning artifact).

### Phase 39: testcontainers-major

**Goal:** Close i-7 (testcontainers 1.x -> 2.x major bump) within the v1.0 budget by capturing all available cumulative fixes on the 1.x line. Open a successor i-7b for the actual 2.x major when upstream publishes.
**Depends on:** Phase 38
**Plans:** 1

Plans:
- [x] 39-01: testcontainers-major

**Details:** Plan 39-01 bumped `<testcontainers.version>` from `1.20.4` to `1.21.3` in `backend/pom.xml` (latest stable 1.x as of 2026-05-09; Maven Central confirmed no 2.x artifact published yet). `AbstractIntegrationTest` API surface compatibility-preserved across the 1.20 -> 1.21 hop (PostgreSQLContainer constructor + withDatabaseName/withUsername/withPassword/withReuse, manual static-init start pattern). All 10 migration tests under `backend/src/test/java/com/workouthub/migrations/` inherit from `AbstractIntegrationTest` with zero direct Testcontainers imports and required no edits. Marked i-7 as closed in `.planning/ISSUES.md`; opened i-7b ("Defer Testcontainers 2.0.0 major bump") with the trigger "Maven Central publishes `org.testcontainers:testcontainers:2.0.0` GA". Locked in the deferral playbook precedent that Phase 40 + Phase 41 follow. Commits 6328446 (testcontainers bump) -> e34eb54 (close i-7 + open i-7b) -> efc4af9 (STATE.md update) -> 3a11fd7 (planning artifact).

### Phase 40: framework-majors

**Goal:** Close i-5 (next-intl 3 -> 4), i-6 (Next 15 -> 16), and i-8 (Spring Boot 3.5 -> 4.0) — the three carried-forward framework major-version blockers — in sequence (each its own commit and test pass) per the roadmap line. Re-defer the ones the v1.0 single-commit cadence cannot absorb.
**Depends on:** Phase 39
**Plans:** 3

Plans:
- [x] 40-01: spring-boot-4 (re-deferred to i-8b)
- [x] 40-02: next-16 (re-deferred to i-6b)
- [x] 40-03: next-intl-4 (closes i-5)

**Details:** Plan 40-01 attempted Spring Boot 3.5.14 -> 4.0.6 at commits 75c3ea1 (parent bump) -> dca5f2e (drop netty override) -> e0ccac6 (close i-8) -> 56256bd (STATE.md) -> e49e1dc (autoconfigure module-split import fixes); the follow-up CI surfaced the BLOCKING Jackson 2 vs Jackson 3 default-classpath shift in `spring-boot-starter-web` 4.0.6 (36 files reference `com.fasterxml.jackson.*` and `spring-boot-jackson2` is no longer a transitive of any starter this project uses). Plan-author rolled back at 38c341e and replaced the plan with the i-8 -> i-8b re-defer bookkeeping at 4e3d95c. Closed i-8 ("Spring Boot 4 attempt rolled back") at 97f7a2a; opened i-8b ("Defer Spring Boot 4.0.x major bump") with the two-path closure playbook (Path A: `spring-boot-jackson2` compat module + HttpMessageConverter resolution probe; Path B: full Jackson 2 -> Jackson 3 codebase migration across 36 files). Plan-author also flagged the trivial `Jackson2ObjectMapperBuilderCustomizer` and `PrometheusScrapeEndpoint`/`PrometheusOutputFormat` package relocations for the i-8b closure plan to apply alongside the chosen path. STATE.md i-8/i-8b mapping recorded at 30b8d97. Plan 40-02 re-deferred Next 16 to i-6b at 49edb5a (plan author) -> f50a4a6 (close i-6 + open i-6b) -> ea7d0d3 (STATE.md). Plan-author audited every published 3.x version on `https://registry.npmjs.org/next-intl` and confirmed zero 3.x versions list `next ^16.0.0` in their peer range; the 3.26.x line tail (3.26.5) tops out at `next ^15.0.0`. Bumping Next past 15.x while keeping next-intl 3.x in the same lockfile would either break `pnpm install` peer-check or require a `pnpm.overrides` hack. The roadmap's "in sequence" rule explicitly forbids combining Next 16 with next-intl 4 in one commit. i-6b carries the App Router Middleware->Proxy rename, the `useSearchParams` Suspense-boundary requirement on `frontend/src/app/(auth)/login/page.tsx`, the `engines.node ">=20.9.0"` declaration bump, the `images.localPatterns` enforcement audit, the deprecated AMP / `images.domains` / `next/legacy/image` surface check, the Turbopack-vs-webpack decision, and the lockfile-resolution probe with co-installed `next@^16` + `next-intl@^4` (already on the lockfile after 40-03). Plan 40-03 bumped `next-intl` from `^3.26.0` to `^4.11.1` in `frontend/package.json` and regenerated `frontend/pnpm-lock.yaml` at 7817eb0; the 4.0 release notes (next-intl.dev/blog/next-intl-4-0, March 2025) document 13 breaking-change surfaces and the plan-author audit found ZERO required source-file edits in this codebase: `frontend/src/i18n/request.ts` already returns `{ locale, messages }` from `getRequestConfig` and `await requestLocale` (post-3.22 shape), `<NextIntlClientProvider>` 4.x auto-inheritance is opt-in cleanup (the 27 explicit `messages={...}` call sites continue to work), and the codebase doesn't use locale-based routing, `defineRouting`, `next-intl/middleware`, `next-intl/navigation`, `format.relativeTime`, or any 3.x deprecated API removed in 4.0. Closes the open-redirect vuln alerts #8 and #11 referenced in the original i-5 entry. Runtime stays on Next 15.x; next-intl 4.11.1 peers `next ^12.0.0 || ^13.0.0 || ^14.0.0 || ^15.0.0 || ^16.0.0` so the install is forward-compatible with the eventual i-6b closure.

### Phase 41: security-hardening

**Goal:** Close i-14 (Netty 4.2.13.Final to close CVE-2026-42577) and lock the v1.0 audit findings (refresh-token hash collision posture, brute-force lockout finalization, rate-limiting posture) into STATE.md. The original "rate limiting review + brute-force lockout finalization + refresh-token hash collision fix from pending CI memory" roadmap line is delivered as an audit + locked-in-decisions block (no source change because the audit found the existing implementations were already correct).
**Depends on:** Phase 40
**Plans:** 1

Plans:
- [x] 41-01: security-hardening

**Details:** Plan 41-01 bumped `<netty.version>` from `4.1.133.Final` to `4.2.13.Final` in `backend/pom.xml` (commit 99f0fb4); removed the `CVE-2026-42577` suppression block from `.trivyignore` (commit 5c1cd42). Static audit at HEAD confirmed zero `WebFlux` / `reactor.netty` / `spring-boot-starter-webflux` matches under `backend/`; the only Netty consumer is `async-http-client:2.12.4` for outbound web push (not on the request-handling path). CI is the authoritative gate per the local-Maven-gap on the dev host. Marked i-14 as closed in `.planning/ISSUES.md` (commit 67885af); locked the v1.0 Phase 41 audit findings into `.planning/STATE.md`: refresh-token hashing posture (SHA-256 hex over the full JWT bytes via `MessageDigest.getInstance("SHA-256")` + `HexFormat.of().formatHex(...)`, persisted in `refresh_tokens.token_hash VARCHAR(128) UNIQUE` per V7 migration; the 2026-05-04 `pending_ci_fixes.md` "refresh-token hash collisions" entry was superseded — those tests pass on `96d81b0`; tightening to bcrypt/Argon2 was rejected because refresh tokens carry full JWT entropy at issuance and the hash is a fingerprint for DB lookup, not a password derivation), brute-force lockout posture (`BruteForceGuard` enforces 10 failures / 15-minute window / 60-minute lockout, throws HTTP 423 via `ResponseStatusException(HttpStatus.LOCKED, ...)`, records every login outcome in `login_attempts` per V15 migration via `@Transactional(propagation = REQUIRES_NEW)`; `BruteForceLockoutIntegrationTest` covers under-threshold pass + at-threshold lock + post-cooldown unlock — all green on origin/main), and rate-limiting posture (zero generic in-process rate limiter; the Self-Hosted Contract delegates rate limiting to the operator's reverse proxy; the only in-process throttle is `BruteForceGuard` per-email login throttle; adding a generic rate limiter was rejected because it would duplicate operator-layer enforcement and contradict the contract's "never assume public exposure" tailnet-only deployment posture). Plan artifact tracked at 807ec6d. Concludes the trivy-suppression carry-forward debt from v0.5.0 + v0.6.0.

### Phase 42: docs-completion

**Goal:** Close the operator-facing documentation gap before the v1.0.0 cut: author `docs/DATA_SCHEMA.md` from the V1..V28 Flyway migration set as the single-page schema reference, refresh `docs/API.md` with the v1.0 review anchor + DATA_SCHEMA cross-link, refresh `docs/DEPLOYMENT.md` env-var minimum set + add a "What's not here" section, and add the README Documentation map + Versioning section.
**Depends on:** Phase 41
**Plans:** 1

Plans:
- [x] 42-01: docs-completion

**Details:** Plan 42-01 authored `docs/DATA_SCHEMA.md` from the V1..V28 Flyway migration set as a single-page schema reference covering tables / columns / indexes / constraints / enums / sequences / extensions per migration version with cross-links from `docs/API.md` and `docs/MIGRATION.md` (commit ba2833e); refreshed `docs/API.md` v1.0 review anchor and added the DATA_SCHEMA cross-link (commit f54f09d); refreshed `docs/DEPLOYMENT.md` env-var minimum set + added a new "What's not here" section listing operator concerns out-of-scope for the contract (CDN, multi-region replicas, OIDC client, rate limiting at the app layer, alert routing) at e21442f; added the README Documentation map (links to every doc under `docs/` with a one-line description per entry) and the README Versioning section explaining the IDENTITY.yaml-as-source-of-truth model + the Renovate-pinned `vX.Y.Z` operator update flow at d76a9bc. Phase plan + summary recorded at 23fd3d0. Zero source-code edits, zero protected-file edits beyond the planning artifacts and the `docs/**` writes; the Documentation map row for `docs/BACKUP_RESTORE_DRILL.md` was added in Phase 43-01 as a backfill (out-of-order phase landing).

### Phase 43: backup-restore-drill

**Goal:** Land the operator-facing backup/restore drill, augment `docs/BACKUP.md` with the pg_dump sidecar cron primitive plus a "Verify your backups" cross-link, replace the `## vNext` placeholder in `docs/MIGRATION.md` with a populated `## v1.0.0` section preserving the template, and cross-link the drill from `docs/DEPLOYMENT.md` "What's not here" + the README Documentation map.
**Depends on:** Phase 42
**Plans:** 1

Plans:
- [x] 43-01: backup-restore-drill

**Details:** Plan 43-01 authored `docs/BACKUP_RESTORE_DRILL.md` operator runbook (5 sections: Why drill, What you need, The 4-step drill, How to interpret results, Troubleshooting; covers `pg_dump --create --clean` + `pg_restore --clean --if-exists` + the V28 sentinel-row check + the row-count diff verification + the timing budget) at commit 60b1a56; refreshed `docs/BACKUP.md` to document both backup-cron primitives (the `ENABLE_PG_DUMP=true` opt-in sidecar profile from v0.3 + the host-cron alternative pattern) plus a "Verify your backups" cross-link to the new drill at bc32625; replaced `docs/MIGRATION.md` `## vNext` placeholder with a populated `## v1.0.0` section preserving the template (Required env var changes / Schema and data migration / Compose runtime changes / One-shot commands / Rollback subsections all populated; documents Netty 4.2.13 bump as runtime-transparent, next-intl 4.11.1 as runtime-transparent, no new env vars, no schema migrations beyond V28 already shipped in v0.6) at c747a0d; cross-linked the drill from `docs/DEPLOYMENT.md` "What's not here" section at cf7ba13 and from the README Documentation map at 0dd179e. Phase plan + summary recorded at 3b415a8. Five atomic commits 60b1a56..0dd179e under `docs(43-01)` scope, zero protected-file edits beyond the planning artifacts and the `docs/**` writes.

### Phase 44: release-v1-0

**Goal:** Cut v1.0.0 from `main` with a single release commit (CHANGELOG rotation + IDENTITY/pom/package version bumps in lockstep), an annotated tag `v1.0.0`, and the post-tag observational verdict on the maintainer-pushed multi-arch GHCR workflow.
**Depends on:** Phase 43
**Plans:** 1

Plans:
- [x] 44-01: release-v1-0

**Details:** Plan 44-01 cut the v1.0.0 release at sha `cac37c1` on 2026-05-09 21:36 +0300. The single commit edited four protected files in lockstep: `IDENTITY.yaml#version` 0.6.0 -> 1.0.0; `backend/pom.xml#/project/version` 0.6.0 -> 1.0.0; `frontend/package.json#/version` 0.6.0 -> 1.0.0; `CHANGELOG.md` rotation turning the empty `## [Unreleased]` block into a populated `## [1.0.0] - 2026-05-09` block summarising every Phase 38-43 deliverable across `### Added` / `### Changed` / `### Fixed` / `### Security` / `### Known Issues (carried forward)` subsections (139 line additions total). Annotated tag `v1.0.0` created locally pointing at `cac37c1` with message "Release v1.0.0"; tag push to fire `.github/workflows/release.yml` is queued for maintainer-side execution per `IDENTITY.yaml#release.draft_first: true`. Post-tag observational verdict (multi-arch GHCR images at `ghcr.io/gtrows/workouthub-{backend,frontend}:1.0.0` digest-verified; draft GitHub Release reachable via `gh release view v1.0.0`) is captured in this milestone-close bookkeeping commit because the release commit + tag landed within the same working session. The maintainer-facing Publish click on the draft release is OUT of scope per `IDENTITY.yaml#release.draft_first: true` + `RELEASE.md` "Post-release" step. `pnpm-lock.yaml` not recomputed (the v0.4.0 / v0.5.0 / v0.6.0 cuts all bumped `frontend/package.json#/version` without recomputing the lockfile; the lockfile hash is keyed on the dependency tree, not the project version). `docs/MIGRATION.md ## v1.0.0` already populated by Phase 43-01 stayed unchanged.

## Release Cut

The v1.0.0 tag was cut at sha `cac37c1` on 2026-05-09:

- `chore(release): v1.0.0` (commit `cac37c1`): bumped `IDENTITY.yaml` 0.6.0 -> 1.0.0; rotated `CHANGELOG.md` `## [Unreleased]` block to `## [1.0.0] - 2026-05-09` covering every Phase 38-43 deliverable (Playwright critical-flow + responsive + session-save-resume specs hardened against the Turkish default locale; testcontainers 1.20.4 -> 1.21.3 patch bump closing i-7 and opening i-7b; Spring Boot 4 attempt rolled back and re-deferred to i-8b; Next 16 re-deferred to i-6b; next-intl 3.26 -> 4.11.1 closing i-5 and the original open-redirect vuln alerts #8 + #11; Netty 4.1.133 -> 4.2.13 closing i-14 and removing the CVE-2026-42577 trivy suppression; refresh-token hash + brute-force lockout + rate-limiting postures audited and locked into STATE.md; `docs/DATA_SCHEMA.md` authored from V1..V28; `docs/API.md` v1.0 review refresh; `docs/DEPLOYMENT.md` env-var minimum + What's not here; README Documentation map + Versioning; `docs/BACKUP_RESTORE_DRILL.md` operator runbook + `docs/BACKUP.md` cron primitive + `docs/MIGRATION.md ## v1.0.0` populated section); bumped `backend/pom.xml` and `frontend/package.json` to `1.0.0` in lockstep with `IDENTITY.yaml` per the `/gtr:release` mechanical pass. Annotated tag `v1.0.0` created locally pointing at the release commit (push queued for maintainer execution).

## Milestone Summary

**Decimal Phases:** None. v1.0 stayed inside the planned phase numbering (38-44); no inserted decimal phases were needed.

**Key Decisions (locked-in 2026-05-09):**

- All v0.3 + v0.4 + v0.5 + v0.6 locked-in decisions carry forward unchanged. Full prior decision logs live in the v0.3 / v0.4 / v0.5 / v0.6 milestone archives.
- e2e specs use locale-stable selectors (input `id`, `type`, `data-testid`, explicit Turkish + English alternatives in name regexes) so the suite passes against the default `tr` locale; English-only regex locators are explicitly forbidden going forward. Shared `loginAsAdmin` fixture under `frontend/e2e/fixtures/` is the canonical login flow for all specs; CI wiring deferred (i-15 follow-on).
- Testcontainers patch-level bumps within the 1.x line are the v1.0 contract; the actual 2.x major jump is gated on the upstream GA per i-7b. The deferral playbook (close current issue + open successor + carry the closure plan with the audit findings) is the load-bearing precedent for Phase 40-01 + Phase 40-02.
- Spring Boot version line: stays at 3.5.14 (tail of the 3.5.x patch line on Maven Central as of 2026-05-09; metadata `<lastUpdated>` `20260423155822`) for v1.0. Spring Boot 4 single-line bump within the v1.0 budget is infeasible because of the Jackson 2 vs Jackson 3 default-classpath shift in `spring-boot-starter-web` 4.0.6 plus the 36-file Jackson 2 reference surface in this codebase. i-8b carries the two-path closure playbook (Path A `spring-boot-jackson2` compat module + HttpMessageConverter selection probe; Path B full Jackson 2 -> Jackson 3 migration) plus the trivial `Jackson2ObjectMapperBuilderCustomizer` and `PrometheusScrapeEndpoint`/`PrometheusOutputFormat` package relocations to apply alongside the chosen path.
- Next.js version line: stays at `^15.1.0` (latest 15.x backport `15.5.18` per the npm `backport` dist-tag at plan-author time) for v1.0. Next 16 single-line bump within the v1.0 budget is infeasible because next-intl 3.x's published peer range tops out at `next ^15.0.0` (every 3.x version including the 3.26.x line tail at 3.26.5; verified at plan-author time against `https://registry.npmjs.org/next-intl` on 2026-05-09 with zero 3.x versions listing `^16.0.0`). i-6b carries the App Router Middleware->Proxy rename + the `useSearchParams` Suspense-boundary requirement on `frontend/src/app/(auth)/login/page.tsx` + the `engines.node ">=20.9.0"` declaration bump + the `images.localPatterns` enforcement audit + the deprecated AMP / `images.domains` / `next/legacy/image` surface check + the Turbopack-vs-webpack decision + the lockfile-resolution probe with co-installed `next@^16` + `next-intl@^4`. The 40-03 next-intl 4.11.1 install is forward-compatible with the eventual i-6b closure (next-intl 4.x peers `^16.0.0`).
- next-intl 4.x posture: `frontend/src/i18n/request.ts` already returns `{ locale, messages }` from `getRequestConfig` and `await requestLocale` (post-3.22 shape), `<NextIntlClientProvider>` 4.x auto-inheritance is opt-in cleanup (the 27 explicit `messages={...}` call sites continue to work), and the codebase doesn't use locale-based routing, `defineRouting`, `next-intl/middleware`, `next-intl/navigation`, `format.relativeTime`, or any 3.x deprecated API removed in 4.0. The original i-5 fear ("requires migration of `frontend/messages/*.json` and `i18n/request.ts`") proved conservative on closer audit — both files are 4.x-valid unchanged.
- Netty version line: 4.2.13.Final via Spring Boot's `<netty.version>` property override (closes CVE-2026-42577 epoll RST DoS; the only Netty consumer is `async-http-client:2.12.4` for outbound web push, not on the request-handling path; zero `WebFlux` / `reactor.netty` matches under `backend/`). The CVE-2026-42577 suppression block previously in `.trivyignore` is removed. CI is the authoritative gate per the local-Maven-gap on the dev host.
- Refresh-token hashing posture: SHA-256 hex over the full JWT bytes via `MessageDigest.getInstance("SHA-256")` + `HexFormat.of().formatHex(...)`, persisted in `refresh_tokens.token_hash VARCHAR(128) UNIQUE` per V7 migration. Tightening to bcrypt/Argon2 was rejected because refresh tokens carry full JWT entropy at issuance and the hash is a fingerprint for DB lookup, not a password derivation. The 2026-05-04 `pending_ci_fixes.md` "refresh-token hash collisions" entry was superseded — those tests pass on `96d81b0` and on origin/main at v1.0 close.
- Brute-force lockout posture: `BruteForceGuard` enforces 10 failures / 15-minute window / 60-minute lockout, throws HTTP 423 via `ResponseStatusException(HttpStatus.LOCKED, ...)`, records every login outcome in `login_attempts` per V15 migration via `@Transactional(propagation = REQUIRES_NEW)`. Threshold-tightening + (email, IP)-keyed lockout were rejected as v1.0 scope creep.
- Rate-limiting posture: zero generic in-process rate limiter; the Self-Hosted Contract delegates rate limiting to the operator's reverse proxy. The only in-process throttle is `BruteForceGuard` (per-email login throttle). Adding a generic rate limiter was rejected because it would duplicate operator-layer enforcement and contradict the contract's "never assume public exposure" tailnet-only deployment posture (v0.3 lock-in).
- Documentation surface (v1.0): `docs/DATA_SCHEMA.md` is the canonical V1..V28 schema reference; cross-linked from `docs/API.md` + `docs/MIGRATION.md`; updated in lockstep with new `V<n>__*.sql` files as the Flyway migration set grows. `docs/BACKUP_RESTORE_DRILL.md` is the operator-facing drill runbook; `docs/BACKUP.md` documents the dual primitive (`ENABLE_PG_DUMP=true` opt-in sidecar profile + host-cron alternative pattern) with a "Verify your backups" cross-link to the drill. README Documentation map is the table-of-contents for `docs/**`; new docs land an entry in the map at the same time as the source file.
- v1.0 release-cut cadence: single commit (CHANGELOG rotation + IDENTITY/pom/package version bumps in lockstep), single annotated tag, post-tag observational verdict captured on the same plan; ZERO source-code edits, ZERO test edits, ZERO scripts/Dockerfile/compose/workflow edits. Mirrors v0.4.0 / v0.5.0 / v0.6.0 release cuts. Maintainer-controlled tag-push and Publish click on the draft release are OUT of scope per `IDENTITY.yaml#release.draft_first: true`.
- Local Maven not installed on this Windows host; backend tests run only in CI (carried from v0.3 + v0.4 + v0.5 + v0.6). v1.0 plans skip the local `mvn verify` gate and delegate to CI; the new e2e spec suite (Phase 38) runs on the local Windows host (frontend tooling has no local-tooling gap), but the backend Testcontainers integration tests run in CI only.

**Issues Resolved (in milestone):**

- i-5 (next-intl 3 -> 4 major bump) - closed by Phase 40 Plan 03 (bumped `next-intl` from `^3.26.0` to `^4.11.1`; ZERO source-file edits required after the 4.0 release-notes audit; closes the open-redirect vuln alerts #8 and #11 referenced in the original entry).
- i-7 (testcontainers 1.x -> 2.x major bump) - closed by Phase 39 Plan 01 for the 1.x scope (bumped `<testcontainers.version>` from `1.20.4` to `1.21.3`; AbstractIntegrationTest API surface compatibility-preserved; eventual 2.x major jump re-deferred as i-7b).
- i-14 (Netty 4.2.13.Final to close CVE-2026-42577) - closed by Phase 41 Plan 01 (bumped `<netty.version>` from `4.1.133.Final` to `4.2.13.Final`; removed the `CVE-2026-42577` suppression block from `.trivyignore`; concludes the trivy-suppression carry-forward debt from v0.5.0 + v0.6.0).

**Issues Re-deferred / Newly Opened:**

- i-6 (Next 15 -> 16 major bump) - closed and superseded by **i-6b (NEW)** at Phase 40 Plan 02. The actual 16.x jump is re-deferred until Phase 40-03 (i-5 next-intl 3 -> 4 migration) ships green AND the maintainer chooses to absorb Next 16 inside the v1.0 remaining phase budget OR the v1.1 milestone-opening sub-plan picks it up. Closure plan must probe the lockfile resolution + rename `frontend/src/middleware.ts` -> `proxy.ts` + wrap `frontend/src/app/(auth)/login/page.tsx` in `<Suspense>` + bump `engines.node ">=20.9.0"` + audit `next/image` callers for query-string `src` values + decide on Turbopack vs `--webpack`.
- i-7 (testcontainers 1.x -> 2.x major bump) - closed and superseded by **i-7b (NEW)** at Phase 39 Plan 01. The actual 2.x jump is re-deferred until upstream `org.testcontainers:testcontainers:2.0.0` GA on Maven Central. Closure plan must read the upstream migration guide + audit `AbstractIntegrationTest` for the @Container-field pattern shift if required + run the full migration-test suite in CI.
- i-8 (Spring Boot 3.5 -> 4.0 major bump) - closed and superseded by **i-8b (NEW)** at Phase 40 Plan 01. The actual 4.0.x jump is re-deferred until either (Path A) `spring-boot-jackson2` compat-module runtime probe completes successfully OR (Path B) Jackson 2 -> Jackson 3 codebase migration sub-plan absorbs the 36-file refactor. Closure plan must apply the trivial Jackson2-module + Prometheus-actuator package-relocation import fixes alongside the chosen path.

**Issues Carried Forward (open at v1.0 close):**

- i-3 (documentation note on `.gitignore` `data/` glob-form correction): stays carried as audit reference.
- i-6b (NEW; successor to i-6, Next 16 deferral): see above.
- i-7b (NEW; successor to i-7, Testcontainers 2.0.0 deferral): see above.
- i-8b (NEW; successor to i-8, Spring Boot 4.0.x deferral): see above.
- i-13 (carried from v0.4): bump SpringDoc to >= 2.7 to remove the ControllerAdviceBean workaround. Trigger was "backend dependency review phase post-v0.6 (likely v1.0 Phase 41 or 42)"; v1.0 did not absorb the bump because Phase 41 scope was Netty 4.2 + audit lock-ins only and Phase 42 scope was docs-only. i-13 stays open with the same trigger pointing at the v1.1 backend dependency review phase.
- i-15 (carried from v0.6): Lighthouse CI workflow + `scripts/check-bundle-size.mjs` + `@next/bundle-analyzer` dev dep for perf-budget gating. Trigger was "operator approves at least one protected-file edit, OR Phase 38+ takes ownership of CI perf gating". Phase 38 scope was specs-only (CI wiring deferred); i-15 stays open with the same trigger pointing at v1.1 or a dedicated CI perf-gate phase.

**Technical Debt Incurred:**

- Carried-forward technical debt from v0.4 + v0.5 + v0.6 (SpringDoc 2.6.0 `ControllerAdviceBean` workaround in `OpenApiConfig`; hand-maintained `ApiError.code` typed-values catalog; frontend metrics registry process-local reset-on-restart semantics; Web Vitals registry histogram cardinality bound to `(name, route)` only; route loading + error UX uniformity without per-route customization) remains open. v1.0 added no new backend Java debt and no new frontend TypeScript debt.
- Spring Boot version line is two patch tails behind the latest minor (3.5.x ships through Spring Boot's standard maintenance window; 3.6.x and 3.7.x and 4.0.x are all available on Maven Central as of v1.0 close). The v1.0 milestone consciously stayed on 3.5.14 because (a) i-8b requires either the spring-boot-jackson2 compat-module probe or the 36-file Jackson 2 -> Jackson 3 migration before 4.0.x is reachable, and (b) the 3.6.x and 3.7.x bumps were not scoped into v1.0 (no backing ISSUES.md entry; no audit at plan time). A future v1.1+ plan may bump within the 3.x line or jump to 4.x via i-8b closure.
- Next.js version line stays on `^15.1.0` for the same reason: i-6b is the i-5 + i-6 combined closure path, and v1.0 explicitly stopped at the next-intl 4 jump per the "in sequence" roadmap rule.
- `CHANGELOG.md` was edited by the `/gtr:release` v1.0.0 cut commit (`cac37c1`); subsequent v1.0 phase summaries (where written) live alongside their plan files and not in the CHANGELOG. Future v1.1+ plans should preserve this discipline.
- Phase 38, 39, 40-01, 40-02, 40-03, 41 SUMMARY.md files were not produced by the plan executors (only Phase 42-01 and Phase 43-01 wrote SUMMARY.md). The accomplishment record for these phases lives in `CHANGELOG.md` `## [1.0.0]`, the per-plan PLAN.md output blocks, and the git commit messages; this archive consolidates that record. Future v1.1+ plans should carry forward the SUMMARY.md discipline.
- e2e spec CI wiring deferred (Phase 38 scope was specs-only); operators run the Playwright suite manually per the `frontend/e2e/README.md` cadence. CI integration for the suite is on the i-15 successor closure path.

---

_For current project status, see `.planning/ROADMAP.md`. For all open issues, see `.planning/ISSUES.md`._
