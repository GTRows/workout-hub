# Milestone v1.1: Deferred Debt Closure

**Status:** SHIPPED 2026-05-09
**Phases:** 45-50 (6 phases, 6 plans; Phase 46 deferred-into-47)
**Total Plans:** 6 (5 source-mutating + 1 release cut; Phase 46 produced a deferral-bookkeeping artifact only)
**Releases:** v1.1.0

## Overview

Deferred-debt closure milestone post-v1.0. Closed the four open carry-forward issues from v1.0 (i-13 SpringDoc 2.7+, i-8b Spring Boot 4 via Jackson 2 -> 3 migration, i-6b Next 16, i-15 Lighthouse CI + bundle-size + bundle-analyzer perf-budget gating) and cut `v1.1.0`. Six phases (45-50) across 6 plans: SpringDoc 2.6.0 -> 2.8.17 with the `apiErrorSchemaCustomizer` `ControllerAdviceBean` workaround removed (Phase 45), the planned Phase 46 standalone Jackson 2 -> 3 source-only rewrite deferred-into-Phase 47 after a HEAD audit found four blockers preventing a Spring Boot 3.5-compatible Jackson 3 intermediate state (Phase 46), Spring Boot 3.5.14 -> 4.0.6 with the consolidated 36-file Jackson 2 -> Jackson 3 (`com.fasterxml.jackson.*` -> `tools.jackson.*`) rewrite + SpringDoc 2.8.17 -> 3.0.3 + Prometheus-actuator package relocation + Netty 4.2.13.Final pin preservation (Phase 47), Next 15.1 -> 16.2.6 with the `middleware.ts` -> `proxy.ts` rename + login Suspense wrap + eslint flat-config migration + react-hooks@7 rule accommodations (Phase 48), Lighthouse CI workflow + per-route gzip bundle-size gate script + lighthouserc + `@next/bundle-analyzer` + `@lhci/cli` devDeps (Phase 49), and the v1.1.0 release cut at sha `53082ab` with annotated tag `v1.1.0` created locally awaiting maintainer push for the multi-arch GHCR publish (Phase 50).

## Phases

### Phase 45: springdoc-2.7-bump

**Goal:** Close i-13 (SpringDoc 2.6.0 `ControllerAdviceBean(Object)` workaround) by bumping `<springdoc.version>` to the latest stable 2.x and removing the `apiErrorSchemaCustomizer` `OpenApiCustomizer` workaround from `OpenApiConfig.java` plus the `springdoc.override-with-generic-response: false` opt-out from `application.yml`.
**Depends on:** v1.0 archive complete
**Plans:** 1

Plans:
- [x] 45-01: springdoc-2.7-bump (closes i-13)

**Details:** Plan 45-01 bumped `<springdoc.version>` from `2.6.0` to `2.8.17` in `backend/pom.xml` (commit `4889375`); deleted the `apiErrorSchemaCustomizer` `OpenApiCustomizer` `@Bean` (33 lines including Javadoc) plus its 6 now-orphan imports from `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java` (commit `1140faa`); deleted the `springdoc.override-with-generic-response: false` line plus its 6-line explanatory comment block from `backend/src/main/resources/application.yml` (commit `f3be469`); planning artifact + i-13 closure note at `0e2edb9`. SpringDoc 2.7.0 patches the removed Spring Framework 6.2 `ControllerAdviceBean(Object)` constructor; the framework default `GenericResponseService` scan now surfaces ApiError schemas without the manual customizer. CI gate validates `mvn verify` + `OpenApiSurfaceIntegrationTest` (5 tests) green. SpringDoc 3.x intentionally deferred (targets Spring Boot 4 / Jakarta EE 11; sequenced behind Phase 47 spring-boot-4 in this milestone). Closes the v0.4 fix bundle (commit `c1b95f7`) workaround.

### Phase 46: jackson-2-to-3-migration (deferred-into-47)

**Goal (original):** Rewrite all 36 Jackson 2 (`com.fasterxml.jackson.*`) source-file references to Jackson 3 (`tools.jackson.*`) as a Spring Boot 3.5-compatible intermediate state, preparing Phase 47 to land the `<spring-boot-starter-parent>` BOM bump in a clean follow-up commit.
**Depends on:** Phase 45
**Plans:** 1 (deferral artifact)

Plans:
- [x] 46-01: jackson-2-to-3-migration (deferred-into-47)

**Details:** Plan 46-01 produced a deferral-bookkeeping artifact (commit `9411b9e`) after a static audit at HEAD found four blockers preventing a Spring Boot 3.5-compatible Jackson 3 source-only intermediate state: (1) Spring Boot 3.5 has no Jackson 3 `ObjectMapperBuilderCustomizer` (the codebase uses `Jackson2ObjectMapperBuilderCustomizer` from `org.springframework.boot.autoconfigure.jackson`); (2) Spring Framework 6.2 does not ship a Jackson 3 `HttpMessageConverter` (Jackson 3 `HttpMessageConverter` lands in Spring Framework 7 only); (3) Spring Boot 3.5's auto-configured `ObjectMapper` `@Bean` is Jackson 2, so flipped imports would fail bean injection; (4) all 28 integration tests inject the auto-wired Jackson 2 `ObjectMapper` via `@Autowired`. Therefore the 36-file rewrite consolidated into Phase 47 in a single CI-validated commit alongside the `<spring-boot-starter-parent>` 3.5.14 -> 4.0.6 bump. Phase 47 was renamed `spring-boot-4-with-jackson-3` to reflect the consolidated scope. Path A (`spring-boot-jackson2` compat module) was foreclosed at this lock-in; Path B (full Jackson 3 migration) became the closure path for i-8b. ZERO source / pom / protected-file edits in Phase 46-01 itself.

### Phase 47: spring-boot-4-with-jackson-3

**Goal:** Close i-8b (Spring Boot 3.5 -> 4.0 via Jackson 3 migration) by bumping `<spring-boot-starter-parent>` from `3.5.14` to `4.0.6`, migrating all 36 Jackson 2 source files to Jackson 3, bumping `<springdoc.version>` from `2.8.17` to `3.0.3` for Spring Boot 4 / Jakarta EE 11 compatibility, relocating the Prometheus-actuator package imports per the SB4 per-feature autoconfigure module split, and preserving the Netty 4.2.13.Final pin (Phase 41 i-14 closure forward-carry) via the `<netty.version>` property override against the SB4 BOM default of 4.2.12.Final.
**Depends on:** Phase 46
**Plans:** 1

Plans:
- [x] 47-01: spring-boot-4-with-jackson-3 (closes i-8b)

**Details:** Plan 47-01 bumped `<spring-boot-starter-parent>` from `3.5.14` to `4.0.6` and `<springdoc.version>` from `2.8.17` to `3.0.3` in `backend/pom.xml` (commit `c6da013`); preserved `<netty.version>4.2.13.Final</netty.version>` property override across the bump (SB4 BOM defaults to 4.2.12.Final; override retains the i-14 CVE-2026-42577 patch posture). Migrated 36 Jackson 2 source files (`com.fasterxml.jackson.*` -> `tools.jackson.*`): 7 main-source files at commit `dae74ea` (`JacksonConfig.java` rewritten to declare a Jackson 3 `JsonMapperBuilderCustomizer @Bean` from `org.springframework.boot.jackson.autoconfigure` registering `tools.jackson.datatype.jsr310.JavaTimeModule`, disabling `tools.jackson.databind.SerializationFeature.WRITE_DATES_AS_TIMESTAMPS`, setting `tools.jackson.annotation.JsonInclude.Include.NON_NULL`; `ClaudeSummaryDto`, `AuditLogService`, `WebPushNotificationDispatcher`, `GoogleFitParser`, `ScalePayload`, `SupplementTiming`); 29 test-source files at commit `45666bf` (28 `@Autowired ObjectMapper` integration tests + the standalone `WebPushNotificationDispatcherTest`). Renamed `JsonProcessingException` catch sites to `JacksonException` (Jackson 3 unified exception hierarchy) in `AuditLogService.toJson` and `WebPushNotificationDispatcher.buildPayload`. Flipped `PrometheusMetricsController` actuator imports from `org.springframework.boot.actuate.metrics.export.prometheus.*` to `org.springframework.boot.micrometer.metrics.autoconfigure.export.prometheus.*` at commit `e0577a0` (Spring Boot 4 per-feature autoconfigure module split). Path B (full Jackson 3 migration) confirmed; Path A (`spring-boot-jackson2` compat module) foreclosed per Phase 46-01 lock-in. BOM transitives carried forward by the bump: Spring Framework 7.0.7, Spring Security 7.0.5, Hibernate 7.2.12.Final, Jakarta EE 11 (Servlet 6.1.0, Persistence 3.2.0, Validation 3.1.1, Annotation 3.0), Tomcat 11, Micrometer 1.15.x, Jackson 3.0.x. CI is the authoritative gate (local Maven gap on the Windows dev host); the GitHub Actions backend job validates `mvn verify` (Java 21) + the Testcontainers integration test suite + `OpenApiSurfaceIntegrationTest` (5 tests) + `MetricsIntegrationTest` + `ExportIntegrationTest` + `FullExportImportIntegrationTest` + `AuditLogIntegrationTest` + `WebPushNotificationDispatcherTest` + `StructuredLoggingTest` + `GrafanaDashboardTest` + the trivy image-scan job + the jacoco >= 70% coverage gate. i-8b closure note at commit `21dc64b`; STATE.md cursor advance at `7afbbb2`. Concludes the largest open carry-forward debt from v1.0 and unblocked Phase 48 (next-16) and Phase 49 (lighthouse-ci).

### Phase 48: next-16

**Goal:** Close i-6b (Next 15 -> 16) by bumping `next` from `^15.1.0` to `^16.2.6` (latest stable on the npm `latest` dist-tag at execute time), renaming `frontend/src/middleware.ts` -> `frontend/src/proxy.ts` with the function signature update, wrapping the `useSearchParams`-reading body of `frontend/src/app/(auth)/login/page.tsx` in a `<Suspense>` boundary, bumping `engines.node` to `>=20.9.0`, and accommodating the `eslint-config-next@16` flat-config migration plus the new `react-hooks@7` rules (`set-state-in-effect`, `static-components`).
**Depends on:** Phase 47
**Plans:** 1

Plans:
- [x] 48-01: next-16 (closes i-6b)

**Details:** Plan 48-01 bumped `next` from `^15.1.0` to `^16.2.6` and `eslint-config-next` from `^15.5.15` to `^16.2.6` in `frontend/package.json`; bumped `engines.node` from `>=20.0.0` to `>=20.9.0`; regenerated `frontend/pnpm-lock.yaml` cleanly (zero peer warnings, zero `pnpm.overrides`, zero `--strict-peer-dependencies=false` flags). Renamed `frontend/src/middleware.ts` -> `frontend/src/proxy.ts` (function `middleware` -> `proxy`; comment block updated to clarify the rename and the v1.1 Phase 48 closure context) and the companion test `frontend/src/middleware.test.ts` -> `frontend/src/proxy.test.ts` (import + 8 call-site updates + 3 cosmetic describe-label updates; preserved the matcher-coverage + redirect-behavior + metric-recording assertions verbatim). Wrapped the `useSearchParams`-reading body of `frontend/src/app/(auth)/login/page.tsx` in a `<Suspense>` boundary via Path B (single-file inner `LoginForm` function with inline skeleton fallback matching the existing form's container styling). Bundler choice: webpack via `--webpack` flag retained on `dev` and `build` scripts to preserve the v0.6 + v1.0 build-evidence baseline; Turbopack-as-default migration deferred as a separate concern. Codebase has zero `next/image` callers (`frontend/src/components/exercise-media.tsx` uses raw `<img>` with `eslint-disable-next-line @next/next/no-img-element`); the new `images.localPatterns` enforcement requires no source mutation. `frontend/next.config.ts` untouched. `frontend/eslint.config.mjs` migrated from the `FlatCompat` shim to native flat-config import paths (`eslint-config-next/core-web-vitals` + `/typescript`) because eslint-config-next@16 ships flat config natively and the shim triggered a circular-structure JSON serialization error. Six files received targeted `react-hooks/set-state-in-effect` disable comments with rationale (`install-prompt-card`, `push-permission-card`, `theme-toggle`, `exercise-media`, `profile/notifications-section`, `profile/profile-client`) where state is initialized from platform APIs (localStorage, Notification.permission, IntersectionObserver, TanStack Query cache) that the new eslint-plugin-react-hooks@7 rule does not model. `frontend/src/components/exercise-progress-chart.tsx` refactored its inner `TooltipContent` component into a `renderTooltip` render function (recharts `Tooltip` `content` prop accepts both) to satisfy the new `react-hooks/static-components` rule without disabling. `frontend/tsconfig.json` + `frontend/next-env.d.ts` auto-updated by Next 16's build (`jsx` -> `react-jsx`; `routes.d.ts` triple-slash reference -> import; `.next/dev/types/**/*.ts` include added). Local frontend gates green: `pnpm typecheck` clean, `pnpm lint` zero errors (3 pre-existing warnings unrelated to the bump), `pnpm test` 351/351 across 64 files (including `proxy.test.ts` 11/11 and `login/page.test.tsx` 5/5), `pnpm build --webpack` "Compiled successfully in 9.3s" with 18 static pages generated and the "Proxy (Middleware)" route surfaced. The Phase 35 metric-recording contract and the Phase 29.5 matcher-coverage contract both survive the rename intact. next-intl 4.11.1 (Phase 40-03 lock-in) peers cleanly against `next@^16` (peer range `^12.0.0 || ^13.0.0 || ^14.0.0 || ^15.0.0 || ^16.0.0`). Commits `c87191d` (source mutation) -> `cb4c040` (close i-6b) -> `f68cc6b` (STATE.md cursor advance to Phase 49). i-6b closure concludes the i-5 + i-6 + i-6b combined frontend-major-version closure path opened in v1.0 Phase 40.

### Phase 49: lighthouse-ci

**Goal:** Close i-15 (perf-budget gating) by adding `.github/workflows/lighthouse.yml` (protected `.github/workflows/**` edit, user pre-approved at v1.1 milestone open) + `scripts/check-bundle-size.mjs` (protected `scripts/**` edit, user pre-approved) + `.lighthouserc.cjs` config + `@next/bundle-analyzer` and `@lhci/cli` devDependencies in `frontend/package.json`. Flips the v0.6 Phase 36-03 perf-budget gating posture from "doc-only" to "CI-gated".
**Depends on:** Phase 48
**Plans:** 1

Plans:
- [x] 49-01: lighthouse-ci (closes i-15)

**Details:** Plan 49-01 created `.lighthouserc.cjs` at repo root (audited routes `/login`, `/`, `/offline`, `/_not-found` — unauthenticated set; `numberOfRuns: 3`; `preset: "desktop"`; assertions for LCP <= 2500ms, FCP <= 1800ms, CLS <= 0.1, server-response-time <= 800ms as errors; INP <= 200ms as warn (headless audit cannot reliably measure interaction-to-next-paint); `categories:performance >= 0.9` as error; reports upload to lhci `temporary-public-storage`) at commit `ce863b1`; created `scripts/check-bundle-size.mjs` (ES-module Node script reading webpack-emitted `.next/build-manifest.json` + `.next/app-path-routes-manifest.json` + walking `.next/static/chunks/app/<route>/` per route, computing gzip-compressed first-load JS size, failing with exit 1 if any route exceeds 250KB compressed) at commit `fd3dad9`; created `.github/workflows/lighthouse.yml` (two jobs: `bundle-size` invoking `node scripts/check-bundle-size.mjs` post-`pnpm build`, and `lighthouse` invoking `pnpm exec lhci autorun`; triggered on `pull_request` + `push: branches: [main]`; concurrency group `lighthouse-${{ github.ref }}` with `cancel-in-progress: true`) at commit `34d0dee`; added `"@next/bundle-analyzer": "^16.2.6"` (lockstep with `next@^16.2.6`) and `"@lhci/cli": "^0.15.1"` (npm `latest` dist-tag at execute time) to `frontend/package.json` `devDependencies` plus `"build:analyze": "ANALYZE=true next build --webpack"` script and the `withBundleAnalyzer({ enabled: process.env.ANALYZE === "true" })` wrap around `frontend/next.config.ts` (plugin composition order: next-intl inner, bundle-analyzer outer; no-op unless `ANALYZE=true pnpm build:analyze`) at commit `d53c367`. Bundler stays on webpack via Phase 48's `--webpack` flag (preserved on `dev` / `build` / `build:analyze`). Audited routes: unauthenticated set only; authenticated-route Lighthouse coverage deferred to v1.2+ (requires CI-side backend boot). Local frontend gates green: `pnpm typecheck` clean, `pnpm lint` zero errors (3 pre-existing warnings unrelated), `pnpm test` 351/351 across 64 files, `pnpm build --webpack` "Compiled successfully" with 18 static pages, `node scripts/check-bundle-size.mjs` exit 0 with all 20 routes under the 250KB ceiling (largest `/` at 243.7KB; root-main shared chunk 122.6KB). i-15 closure note + STATE.md cursor advance to Phase 50 at commit `a5bb286`. Phase plan artifact tracking commit `1fefe67` (45-48). Concludes the last open carry-forward debt scheduled for v1.1.

### Phase 50: release-v1-1

**Goal:** Cut v1.1.0 from `main` with a single release commit (CHANGELOG rotation + IDENTITY/pom/package version bumps in lockstep) and an annotated tag `v1.1.0`. Mirrors v0.4.0 / v0.5.0 / v0.6.0 / v1.0.0 release-cut cadences.
**Depends on:** Phase 49
**Plans:** 1

Plans:
- [x] 50-01: release-v1-1 (cut at sha `53082ab`)

**Details:** Plan 50-01 cut the v1.1.0 release at sha `53082ab` on 2026-05-09. The single commit edited four protected files in lockstep: `IDENTITY.yaml#version` 1.0.0 -> 1.1.0; `backend/pom.xml#/project/version` 1.0.0 -> 1.1.0; `frontend/package.json#/version` 1.0.0 -> 1.1.0; `CHANGELOG.md` rotation turning the empty `## [Unreleased]` block into a populated `## [1.1.0] - 2026-05-09` block summarising every Phase 45-49 deliverable across `### Added` / `### Changed` / `### Fixed` / `### Security` / `### Known Issues (carried forward)` subsections. Annotated tag `v1.1.0` created locally pointing at `53082ab` with message "Release v1.1.0"; tag push to fire `.github/workflows/release.yml` is queued for maintainer-side execution per `IDENTITY.yaml#release.draft_first: true`. The maintainer-facing Publish click on the resulting draft GitHub Release is OUT of scope per `RELEASE.md` Post-release step. `pnpm-lock.yaml` not recomputed (the v0.4.0 / v0.5.0 / v0.6.0 / v1.0.0 cuts all bumped `frontend/package.json#/version` without recomputing the lockfile; the lockfile hash is keyed on the dependency tree, not the project version). `docs/MIGRATION.md ## v1.1.0` section authored as part of the same commit summarising the Spring Boot 4 / Next 16 / SpringDoc 3 / Lighthouse CI deliverables for operator-facing migration guidance.

## Release Cut

The v1.1.0 tag was cut at sha `53082ab` on 2026-05-09:

- `chore(release): v1.1.0` (commit `53082ab`): bumped `IDENTITY.yaml` 1.0.0 -> 1.1.0; rotated `CHANGELOG.md` `## [Unreleased]` block to `## [1.1.0] - 2026-05-09` covering every Phase 45-49 deliverable (SpringDoc 2.6.0 -> 2.8.17 with the `ControllerAdviceBean` workaround removed closing i-13; Spring Boot 3.5.14 -> 4.0.6 with the consolidated 36-file Jackson 2 -> Jackson 3 rewrite + SpringDoc 2.8.17 -> 3.0.3 + Prometheus-actuator package relocation closing i-8b; Next 15.1 -> 16.2.6 with `middleware` -> `proxy` rename + login Suspense wrap + eslint flat-config migration closing i-6b; Lighthouse CI workflow + bundle-size gate script + `@next/bundle-analyzer` + `@lhci/cli` devDeps closing i-15); bumped `backend/pom.xml` and `frontend/package.json` to `1.1.0` in lockstep with `IDENTITY.yaml` per the `/gtr:release` mechanical pass. Annotated tag `v1.1.0` created locally pointing at the release commit (push queued for maintainer execution).

## Milestone Summary

**Decimal Phases:** None. v1.1 stayed inside the planned phase numbering (45-50); no inserted decimal phases were needed.

**Key Decisions (locked-in 2026-05-09):**

- All v0.3 + v0.4 + v0.5 + v0.6 + v1.0 locked-in decisions carry forward unchanged. Full prior decision logs live in the v0.3 / v0.4 / v0.5 / v0.6 / v1.0 milestone archives.
- SpringDoc artifact (Phase 45 lock-in): `2.8.17` starter-webmvc-ui under Spring Boot 3.5.14, then `3.0.3` after the Phase 47 SB4 jump. The `ControllerAdviceBean(Object)` workaround was removed when 2.7+ landed (`apiErrorSchemaCustomizer` `@Bean` and `springdoc.override-with-generic-response: false` both deleted). Schemas surface via the framework default `GenericResponseService` scan; `OpenApiSurfaceIntegrationTest` (5 tests) is the regression gate.
- Phase 46 deferral lock-in: standalone Jackson 2 -> 3 source-only rewrite is INFEASIBLE under Spring Boot 3.5 because (1) Spring Boot 3.5 has no Jackson 3 `ObjectMapperBuilderCustomizer`, (2) Spring Framework 6.2 does not ship a Jackson 3 `HttpMessageConverter`, (3) Spring Boot 3.5's auto-configured `ObjectMapper` `@Bean` is Jackson 2, and (4) all 28 integration tests inject the auto-wired Jackson 2 `ObjectMapper`. Therefore the 36-file rewrite consolidates into Phase 47 in a single commit alongside the BOM bump. Path A (`spring-boot-jackson2` compat module) is foreclosed; Path B (full Jackson 3 migration) is the closure path for i-8b.
- Spring Boot version line (Phase 47 lock-in): 4.0.x (bumped from 3.5.14 to 4.0.6). The 4.2.13.Final Netty pin from v1.0 Phase 41 (i-14 closure) is preserved via `<netty.version>` property override against the SB4 BOM default of 4.2.12.Final. SpringDoc 2.8.17 -> 3.0.3 in lockstep with the SB4 jump (SpringDoc 3.x targets Spring Boot 4 / Jakarta EE 11). BOM transitives carried forward: Spring Framework 7.0.7, Spring Security 7.0.5, Hibernate 7.2.12.Final, Jakarta EE 11 (Servlet 6.1.0, Persistence 3.2.0, Validation 3.1.1, Annotation 3.0), Tomcat 11, Micrometer 1.15.x, Jackson 3.0.x.
- Jackson 3 migration posture (Phase 47 lock-in): Path B (full Jackson 3 migration) shipped. JacksonConfig declares a Jackson 3 `JsonMapperBuilderCustomizer @Bean` from `org.springframework.boot.jackson.autoconfigure` that registers `tools.jackson.datatype.jsr310.JavaTimeModule`, disables `tools.jackson.databind.SerializationFeature.WRITE_DATES_AS_TIMESTAMPS`, and sets `tools.jackson.annotation.JsonInclude.Include.NON_NULL`. `AuditLogService` + `WebPushNotificationDispatcher` catch `tools.jackson.core.JacksonException` (Jackson 3 unified exception hierarchy; replaces `JsonProcessingException`). PrometheusMetricsController actuator imports flipped to `org.springframework.boot.micrometer.metrics.autoconfigure.export.prometheus.*` per the Spring Boot 4 per-feature autoconfigure module split.
- Next.js runtime line (Phase 48 lock-in): `^16.2.6` pinned in `frontend/package.json`. The Middleware -> Proxy rename landed (`frontend/src/middleware.ts` -> `frontend/src/proxy.ts`; function `middleware` -> `proxy`; companion test renamed in lockstep). The `<Suspense>` boundary around the `useSearchParams`-reading body of `frontend/src/app/(auth)/login/page.tsx` (Path B: single-file inner `LoginForm` function with inline skeleton fallback) closes the Next 16 hard-error gate. `engines.node` bumped to `>=20.9.0`. Bundler stayed on webpack via `--webpack` opt-out flag for the v1.1 ship; Turbopack-as-default migration deferred as a separate concern. Codebase has zero `next/image` callers (`frontend/src/components/exercise-media.tsx` uses raw `<img>` intentionally); framework default `images.localPatterns` enforcement is moot. `next.config.ts` untouched (no `images:`, `experimental:`, `webpack:`, or `turbo:` block needed except the Phase 49 `withBundleAnalyzer` outer wrap). next-intl 4.11.1 (v1.0 Phase 40-03 lock-in) peers cleanly against `next@^16`; install is clean with zero `pnpm.overrides` or `--strict-peer-dependencies=false` hacks. eslint-config-next@16 ships flat config natively; `frontend/eslint.config.mjs` migrated from `FlatCompat` shim to direct imports of `eslint-config-next/core-web-vitals` and `/typescript`. eslint-plugin-react-hooks@7 (transitive via eslint-config-next@16) introduced two new rules: `set-state-in-effect` (6 targeted disables with platform-API rationale) and `static-components` (refactored exercise-progress-chart's `TooltipContent` to a `renderTooltip` render-prop function). The Phase 35 metric-recording contract and the Phase 29.5 matcher-coverage contract both survive the rename intact (the renamed `proxy.test.ts` 11 cases assert this).
- Perf-budget gating posture (Phase 49 lock-in): flipped from "doc-only" (v0.6 Phase 36-03 ship of `docs/PERF_BUDGETS.md`) to "CI-gated". `.github/workflows/lighthouse.yml` runs `pnpm exec lhci autorun` against the just-built frontend on `pull_request` + `push: branches: [main]`; the `.lighthouserc.cjs` config at repo root mirrors `docs/PERF_BUDGETS.md` verbatim — LCP <= 2.5s, FCP <= 1.8s, CLS <= 0.1, TTFB <= 0.8s gated as `error`; INP <= 200ms gated as `warn` (Lighthouse's headless audit cannot reliably measure interaction-to-next-paint without real user interaction); `categories:performance >= 0.9` gated as `error`. `scripts/check-bundle-size.mjs` computes per-route gzip-compressed first-load JS size from the webpack-emitted `.next/build-manifest.json` + `.next/app-path-routes-manifest.json` + walked `.next/static/chunks/app/<route>/`, fails if any route exceeds 250KB. Audited Lighthouse routes: `/login`, `/`, `/offline`, `/_not-found` (unauthenticated set only; authenticated-route coverage deferred to v1.2+ because it requires a CI-side backend boot). `@next/bundle-analyzer` (`^16.2.6` lockstep with `next`) wraps `frontend/next.config.ts` with `enabled: process.env.ANALYZE === "true"` (no-op unless `ANALYZE=true pnpm build:analyze` for operator-side investigation; not gated). `@lhci/cli` (`^0.15.1` — npm `latest` dist-tag at execute time) is the Lighthouse runner. Bundler stays on webpack via Phase 48's `--webpack` flag (`scripts.build:analyze` also passes `--webpack`); a future Turbopack-as-default migration would update the bundle-size script to read `.next/app-build-manifest.json` instead.
- v1.1 release-cut cadence: single commit (CHANGELOG rotation + IDENTITY/pom/package version bumps in lockstep), single annotated tag, post-tag observational verdict captured on the same plan; ZERO source-code edits, ZERO test edits, ZERO scripts/Dockerfile/compose/workflow edits. Mirrors v0.4.0 / v0.5.0 / v0.6.0 / v1.0.0 release cuts. Maintainer-controlled tag-push and Publish click on the draft release are OUT of scope per `IDENTITY.yaml#release.draft_first: true`.
- Local Maven not installed on this Windows host; backend tests run only in CI (carried from v0.3 + v0.4 + v0.5 + v0.6 + v1.0). v1.1 plans skip the local `mvn verify` gate and delegate to CI; the frontend phases (48, 49) run on the local Windows host (frontend tooling has no local-tooling gap).

**Issues Resolved (in milestone):**

- i-13 (SpringDoc 2.6.0 `ControllerAdviceBean` workaround) — closed by Phase 45 Plan 01 (bumped `<springdoc.version>` from `2.6.0` to `2.8.17`; deleted the `apiErrorSchemaCustomizer` `OpenApiCustomizer` workaround from `OpenApiConfig.java`; deleted the `springdoc.override-with-generic-response: false` opt-out from `application.yml`).
- i-8b (Spring Boot 3.5 -> 4.0 via Jackson 3 migration; successor to i-8) — closed by Phase 47 Plan 01 (bumped `<spring-boot-starter-parent>` from `3.5.14` to `4.0.6` and `<springdoc.version>` from `2.8.17` to `3.0.3`; migrated 36 Jackson 2 source files to Jackson 3 `tools.jackson.*`; flipped Prometheus actuator imports per the SB4 module split; preserved Netty 4.2.13.Final via `<netty.version>` property override).
- i-6b (Next 15 -> 16; successor to i-6) — closed by Phase 48 Plan 01 (bumped `next` from `^15.1.0` to `^16.2.6` and `eslint-config-next` from `^15.5.15` to `^16.2.6`; renamed `middleware.ts` -> `proxy.ts`; wrapped login page in `<Suspense>`; bumped `engines.node` to `>=20.9.0`; migrated eslint to flat-config; accommodated react-hooks@7 rules).
- i-15 (Lighthouse CI + bundle-size + bundle-analyzer perf-budget gating) — closed by Phase 49 Plan 01 (added `.github/workflows/lighthouse.yml` + `scripts/check-bundle-size.mjs` + `.lighthouserc.cjs` + `@next/bundle-analyzer` and `@lhci/cli` devDeps + `withBundleAnalyzer` wrap; flipped perf-budget gating posture from doc-only to CI-gated).

**Issues Re-deferred / Newly Opened:** None. v1.1 closed every issue scheduled for the milestone with no successor opens.

**Issues Carried Forward (open at v1.1 close):**

- i-3 (documentation note on `.gitignore` `data/` glob-form correction): stays carried as audit reference.
- i-7b (Testcontainers 2.0.0 jump; successor to i-7): trigger remains "Maven Central publishes `org.testcontainers:testcontainers:2.0.0` GA"; upstream had not published a 2.x artifact as of v1.1.0 cut.

**Technical Debt Incurred:**

- Carried-forward technical debt from v0.4 + v0.5 + v0.6 + v1.0 (hand-maintained `ApiError.code` typed-values catalog; frontend metrics registry process-local reset-on-restart semantics; Web Vitals registry histogram cardinality bound to `(name, route)` only; route loading + error UX uniformity without per-route customization) remains open. v1.1 added no new backend Java debt and no new frontend TypeScript debt.
- Authenticated-route Lighthouse coverage (everything behind `/login`) is intentionally NOT in the `.lighthouserc.cjs` audited set because the lighthouse CI job builds the frontend in isolation without a backend container; adding authenticated coverage would require booting the full compose stack in CI. Deferred to v1.2+; `docs/PERF_BUDGETS.md` notes the gap.
- INP (interaction-to-next-paint) Web Vital is gated as `warn` instead of `error` in `.lighthouserc.cjs` because Lighthouse's headless audit cannot reliably measure interaction latency without real user interaction. Real-user INP measurement happens via the Phase 36-03 `WebVitalsReporter` -> `/api/vitals` -> `wh_frontend_web_vital_seconds{name="INP"}` Prometheus metric pipeline; CI gating is doc-only for INP.
- `CHANGELOG.md` was edited by the `/gtr:release` v1.1.0 cut commit (`53082ab`); subsequent v1.1 phase summaries (where written) live alongside their plan files and not in the CHANGELOG. Future v1.2+ plans should preserve this discipline.
- Phase 45, 47, 48, 49 SUMMARY.md files were not produced by the plan executors (only the PLAN.md artifacts ship). The accomplishment record for these phases lives in `CHANGELOG.md` `## [1.1.0]`, the per-plan PLAN.md output blocks, and the git commit messages; this archive consolidates that record. Future v1.2+ plans should carry forward the SUMMARY.md discipline if reintroduced.

---

_For current project status, see `.planning/ROADMAP.md`. For all open issues, see `.planning/ISSUES.md`._
