---
phase: 12-release-v0.3.0
plan: 01
type: execute
domain: release
---

<objective>
Cut v0.3.0. Populate CHANGELOG.md from Phase 1-11 work, bump versions in IDENTITY.yaml + pom.xml + frontend/package.json, single release commit, tag v0.3.0, push tag (release.yml fires multi-arch GHCR publish + draft release).

Purpose: First contract-aligned release. Operator can pull `ghcr.io/gtrows/workouthub-{backend,frontend}:v0.3.0` after this lands.

Output: CHANGELOG.md updated, version files bumped, signed-off release commit, v0.3.0 git tag pushed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@docs/SELF_HOSTED_CONTRACT.md
@CHANGELOG.md
@IDENTITY.yaml
@docs/MIGRATION.md
@.planning/STATE.md

**Constraining decisions:**
- Tag triggers release.yml (multi-arch GHCR publish + draft release).
- All version files bumped in lockstep: IDENTITY.yaml + backend/pom.xml + frontend/package.json (and the latter's lockfile if present).
- Single release commit; single tag.

**Open issues acknowledged in release notes:**
- i-1, i-2 (test infrastructure failures, local-Maven-required)
- i-4 through i-8 (frontend metrics scope + 4 deferred majors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate CHANGELOG v0.3.0 from Phase 1-11 work</name>
  <files>CHANGELOG.md</files>
  <action>
Replace the empty `## [Unreleased]` block with a populated `## [0.3.0] - 2026-05-03` block summarizing every Phase 1-11 deliverable. Then add a fresh empty `## [Unreleased]` ABOVE the v0.3.0 block.

Sections per Keep a Changelog:
- Added: BIND_ADDR / BACKEND_PORT / FRONTEND_PORT, named workouthub-net, healthcheck endpoints, ECS structured logging, /metrics, forward-auth filter + TRUSTED_PROXIES, .env.example rewrite, docker-compose.override.yml.example, pg_dump opt-in sidecar, README rewrite, MIGRATION.md, 7 CI gates, multi-arch GHCR publish.
- Changed: bind-mount db data, healthcheck retarget, log format prod profile.
- Deprecated: none.
- Removed: in-app OIDC client (per contract section 7), `:latest` floating tag.
- Fixed: 12 of 19 pre-existing test failures (refresh-token jti, brute-force REQUIRES_NEW, ResponseStatusException handler, etc.).
- Security: bouncycastle 1.77 -> 1.84 (closes 7 CVE alerts incl. 1 HIGH).

Reference docs/MIGRATION.md from the v0.3.0 entry head.
  </action>
  <verify>
- `grep -c "^## \[0.3.0\]" CHANGELOG.md` returns 1
- `grep -c "^## \[Unreleased\]" CHANGELOG.md` returns 1 (empty section above v0.3.0)
- All 6 Keep-a-Changelog subsections present in v0.3.0 entry
  </verify>
  <done>CHANGELOG ready for release.yml to extract.</done>
</task>

<task type="auto">
  <name>Task 2: Bump version in IDENTITY.yaml + pom.xml + frontend/package.json</name>
  <files>IDENTITY.yaml, backend/pom.xml, frontend/package.json</files>
  <action>
- IDENTITY.yaml: change `version: 0.1.0` to `version: 0.3.0` (single line edit).
- backend/pom.xml: locate the project version line (`<version>0.1.0</version>` near the top, NOT the parent or dependency versions), bump to 0.3.0.
- frontend/package.json: change `"version": "0.1.0"` to `"version": "0.3.0"`.

Do NOT touch lockfiles (pnpm-lock.yaml hash is unaffected by package.json version field).
  </action>
  <verify>
- `grep -E "^version: 0.3.0" IDENTITY.yaml` matches
- `grep -E "<version>0\.3\.0</version>" backend/pom.xml` matches the project version line
- frontend/package.json shows `"version": "0.3.0"`
  </verify>
  <done>All three version files in sync at 0.3.0.</done>
</task>

<task type="auto">
  <name>Task 3: Release commit + tag v0.3.0 + push tag</name>
  <files>(git ops)</files>
  <action>
Single release commit:
```
git add CHANGELOG.md IDENTITY.yaml backend/pom.xml frontend/package.json
git commit -m "chore(release): v0.3.0"
git tag -a v0.3.0 -m "Release v0.3.0 - self-hosted contract alignment"
git push origin main
git push origin v0.3.0
```

Tag push triggers `.github/workflows/release.yml`. Wait for the multi-arch build + draft release. The release ends up as a draft - operator publishes manually after reviewing.
  </action>
  <verify>
- `git tag -l v0.3.0` shows the tag
- `git log v0.3.0` shows the release commit
- `gh run list --workflow release.yml --branch main --limit 1` shows the new run
  </verify>
  <done>v0.3.0 cut and pushed; release workflow firing.</done>
</task>

</tasks>

<success_criteria>
- 1 release commit on main
- v0.3.0 tag pushed
- release.yml run triggered (visible in gh run list)
- v0.3 milestone closed; ready to invoke /gsd:complete-milestone
</success_criteria>

<output>Standard SUMMARY.md.</output>
