---
phase: 27-history-view
plan: 01
type: execute
domain: frontend
---

<objective>
Phase 27 (history-view) ProjectBrief-mandated surface (`PB:357-359`) is already shipped: `frontend/src/app/(app)/history/history-client.tsx` (198 lines) renders a Monday-first calendar with completed days highlighted (lines 73-105), a `SelectedSessionCard` per-session detail card (lines 126-163) showing sets list + a "view full detail" link to `/session/{id}`, and four passing vitest cases in `history-client.test.tsx`. The Phase 21 audit `Section 5` rows for `History` mark both ProjectBrief items `Implemented`, and `Section 6 row 27` issues a "narrow" verdict: "link from per-session detail back into a duplicate this workout flow, OR `defer-v0.6` if the calendar is sufficient. 0-1 plans." The orchestrator picks the narrow scope (one plan, this plan); the alternative is bookkeeping-only.

This plan delivers exactly the audit's narrow recommendation: a "Repeat workout" call-to-action on the per-session detail card that starts a new session from the same `workoutDayId`. The CTA is the user-visible loop that closes ProjectBrief Phase 5's history surface (PB:357-359 list "calendar" + "per-session detail"; the CTA turns the detail card into an actionable jump-off rather than a dead-end view), avoiding a second trip to `/dashboard` -> "today's workout" -> "start session". Backend support already exists: `POST /api/sessions/start` accepts an optional `{ workoutDayId }` body (verified at `frontend/src/lib/api/endpoints.ts:144-151`), and the v0.5 typed-`ApiError` helper from Phase 22.5 (`frontend/src/lib/api/api-error-codes.ts`, consumed in `session-client.tsx:19-23`) already exposes `SESSION_ALREADY_ACTIVE` branching, which is the only realistic 409 a "Repeat" press can produce.

Outputs:

1. **Repeat-workout button on `SelectedSessionCard`.**
   - `frontend/src/app/(app)/history/history-client.tsx` (MODIFIED): inside the existing `SelectedSessionCard` component, add a second `<Button>` next to the current "View full detail" link (around line 158-160). The button is rendered only when `summary.workoutDayId` is non-null (free-form sessions started without a workout-day reference are not repeatable; the audit verdict scopes to "duplicate this workout" which presupposes the source workout-day exists). When `workoutDayId` is null, only the existing "View full detail" link renders - identical to today.
   - The button label uses a new translation key `history.repeatWorkout` (TR: `"Antrenmani tekrarla"`, EN: `"Repeat workout"`).
   - Click behaviour: a TanStack Query `useMutation` calls `startSession(summary.workoutDayId)` from `endpoints.ts:144`; on success, the router navigates to `/session/{newSessionId}` via `useRouter().push`. On `ApiErrorCode.SESSION_ALREADY_ACTIVE` (the user already has an in-flight session), the handler reads `err.activeSessionId` (already populated by `parseApiError` in the typed-error helper, used identically by `dashboard-client.tsx` per the recent commit `5ad3fb9`) and navigates to `/session/{activeSessionId}` instead, so the user is sent to resume rather than dead-locked. Any other ApiError surfaces via the existing `errors.api.generic` toast pattern; offline / network errors short-circuit to the same generic toast.
   - The mutation is in `pending` state for the duration of the request: the button is disabled and renders the existing `history.loading` ("Yukleniyor..." / "Loading...") label to give a deterministic visual signal without introducing a new spinner primitive.
   - The mutation is local to the component (no module-scope `useMutation`), so a second click on a different day does not collide; the button calls a stable handler bound to the currently-rendered `summary`.

2. **Translation keys in BOTH `frontend/messages/tr.json` and `frontend/messages/en.json` under the existing `history` namespace** (lines 358-380 in both files; the namespace already exists with 17 keys). Add:
   - `repeatWorkout` (TR: `"Antrenmani tekrarla"`, EN: `"Repeat workout"`).
   - `repeatWorkoutPending` (TR: `"Baslatiliyor..."`, EN: `"Starting..."`).

   Reuse without re-adding: `errors.api.sessionAlreadyActive` already exists at `messages/{tr,en}.json:27` ("Aktif bir antrenmanin zaten var. Devam ekranina yonlendiriliyorsun." / "You already have an active session. Redirecting to resume.") - the SESSION_ALREADY_ACTIVE branch reuses that key via the `apiErrorCodeMessageKey` helper from `api-error-codes.ts`, matching the dashboard's redirect-on-409 UX.

3. **Tests:** extend `frontend/src/app/(app)/history/history-client.test.tsx` with three new `it` blocks (the file already has four passing cases and the fetch-stub + intl scaffolding at lines 1-69). The `useRouter` mock (line 9) gains an explicit `push` capture so the assertions can read it. New cases:
   1. **CTA is rendered only when `summary.workoutDayId` is non-null.** Stub `/api/sessions/history` to return one summary with `workoutDayId: "wd-1"` and one with `workoutDayId: null` on different days; click each day; assert the "Repeat workout" button is present in the first card and absent in the second.
   2. **Click triggers `POST /api/sessions/start` with the correct `workoutDayId` and navigates.** Stub `/api/sessions/start` to return `{ id: "new-session-id", ... }`; assert (a) the POST body contains `{"workoutDayId":"wd-1"}`, (b) the captured `router.push` was called with `/session/new-session-id`, (c) the button transitions through its disabled+pending state (assertion via `expect(button).toBeDisabled()` after click + before resolve, then re-enabled after).
   3. **`SESSION_ALREADY_ACTIVE` redirects to the existing active session id.** Stub `/api/sessions/start` to return `409 { error: { code: "SESSION_ALREADY_ACTIVE", activeSessionId: "active-id", message: "..." } }`; assert `router.push` was called with `/session/active-id` (NOT `/session/new-session-id`); assert no thrown error escapes the React tree.

   Test-only utility: the existing helper `jsonResponse(status, body)` at line 54 already covers the 409 case; no new helper. Pattern mirrors `frontend/src/app/(app)/dashboard/dashboard-client.test.tsx` which already exercises the same SESSION_ALREADY_ACTIVE redirect flow on the dashboard's start-mutation (commits `5ad3fb9` + `9bb901a`); this plan reuses that test pattern verbatim, adapted to the history surface.

The plan is intentionally narrow: one component file modified, two i18n keys added per locale, three new test cases. No backend, no migrations, no protected-file touch (no `package.json`, no `compose.yml`, no `Dockerfile`, no `IDENTITY.yaml`, no `CHANGELOG.md`, no workflow file). No new dependency. No `client.ts` change. No `endpoints.ts` change (the existing `startSession` wrapper is consumed as-is). No new shadcn/ui primitive. No middleware change. No route addition (the per-session full detail page already exists at `/session/[id]` and is unchanged by this plan).

Out of scope:
- A standalone `/history/[id]` route. The audit accepts the in-place per-session card as ProjectBrief-complete (`Section 5` row "Per-session detail" verdict `Implemented`); creating a route purely for symmetry would duplicate `/session/[id]` without new content.
- A weekly/monthly aggregation card (volume per week, total sessions). That is Phase 31 (charts-stats) territory in v0.6 per `ROADMAP.md:84` ("Phase 31: charts-stats - volume, 1RM (Epley), weight change, frequency heatmap, PR list, streak").
- A streak counter on the calendar grid. Same v0.6 Phase 31 scope.
- Pagination beyond the current 200-item fetch. The audit Section 4 does not flag this and `fetchSessionHistory(0, 200)` covers the foreseeable per-user dataset for v0.5; if scale forces it, that is a v0.6 follow-up.
- Filter chips (date range, mood, energy level). Not in ProjectBrief PB:357-359; would expand surface beyond the "narrow" verdict.
- Backend `code` emission changes. Backend is unchanged; this plan only consumes existing typed codes.
- Free-form session repeat (no `workoutDayId`). Backend `POST /api/sessions/start` accepts an empty body for that, but "Repeat workout" implies a definite plan; without a `workoutDayId` the user gets the same shape via the dashboard's existing "Start session" (no value-add to duplicate). The CTA is hidden in that branch by design.
- Playwright e2e for the calendar -> detail -> back navigation flow. Audit Section 7 lists this as `needs admin-seeded fixtures` and the v0.5 milestone has no e2e harness yet (v1.0 Phase 38 introduces Playwright critical flows). vitest component coverage is sufficient at this milestone.
- Touching `session-client.tsx`. The audit verdict explicitly notes "25 (session detail unchanged)" as the dependency relation; this plan honours it.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md
@.planning/phases/21-frontend-audit/21-01-AUDIT.md
@.planning/phases/22.5-typed-apierror-helper/22.5-01-PLAN.md
@.planning/phases/23-dashboard/23-01-PLAN.md
@.planning/phases/26-exercise-catalog/26-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@frontend/src/app/(app)/history/page.tsx
@frontend/src/app/(app)/history/history-client.tsx
@frontend/src/app/(app)/history/history-client.test.tsx
@frontend/src/app/(app)/dashboard/dashboard-client.tsx
@frontend/src/app/(app)/dashboard/dashboard-client.test.tsx
@frontend/src/app/(app)/session/[id]/session-client.tsx
@frontend/src/lib/api/client.ts
@frontend/src/lib/api/endpoints.ts
@frontend/src/lib/api/schemas.ts
@frontend/src/lib/api/api-error-codes.ts
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/button.tsx
@frontend/messages/tr.json
@frontend/messages/en.json

**Tech stack already wired (no install required):**
- Next.js 15 App Router + React 19 + TypeScript strict; `"use client"` boundary already at `history-client.tsx:1`. No SSR change.
- TanStack Query 5: `useQuery` + `useMutation` already imported across the (app) routes; `Providers` instantiates the shared `QueryClient`.
- next-intl 3.26 with the existing `history` namespace at `messages/{tr,en}.json:358-380` (17 keys today; +2 from this plan = 19).
- shadcn/ui `Button` (`@/components/ui/button`) and `Card` already imported in `history-client.tsx:9-10`. No new primitive.
- `useRouter` from `next/navigation` is already used in `dashboard-client.tsx` and `session-client.tsx`; this plan adds the same `import { useRouter } from "next/navigation"` line to `history-client.tsx`.
- Vitest 2.1 + Testing Library + jsdom; existing `history-client.test.tsx` already has the `useRouter` mock at lines 8-12 and the fetch-stub + intl-provider scaffolding at lines 1-69. New tests reuse that scaffolding verbatim.

**Backend endpoints consumed (read once at plan time):**
- `POST /api/sessions/start` - `startSession(workoutDayId?)` in `endpoints.ts:144-151`; returns `SessionDetail`. Existing wrapper. Accepts `{ workoutDayId }` body when defined, empty body otherwise.
- `GET /api/sessions/history` - `fetchSessionHistory(0, 200)` in `endpoints.ts:277-286`; the `SessionSummary` row in the response carries `workoutDayId: string | null` (verified via `schemas.ts:156` `sessionSummaryPageSchema` and audit Section 4 row "GET /api/sessions/history -> Wrapped+typed"). The component already binds `summary.workoutDayId` is null elsewhere (line 36 has `if (!s.finished) continue;`); the new CTA reads the same field directly.

**Established patterns (verified at plan time):**
- The dashboard's "Start session" mutation at `dashboard-client.tsx` calls `startSession(workoutDayId)` and on success navigates to `/session/{id}`; on `SESSION_ALREADY_ACTIVE` it navigates to `/session/{activeSessionId}` instead. The same pattern is reused by this plan; the helper code path is identical so the typed-error UX is consistent across `/dashboard` and `/history`.
- `apiErrorCodeMessageKey(code)` returns `"errors.api.<camelCase>"` (e.g. `"errors.api.sessionAlreadyActive"`); both keys already exist in both message files. No new error key.
- The `history-client.tsx` file is at 198 lines today; the plan adds approximately 25 lines (button JSX + handler + import) keeping it well under the 200-line CLAUDE.md ceiling. If a future addition pushes it past 200, splitting `SelectedSessionCard` to its own file is the obvious next step, but no split is needed for this plan.

**Verification gate (the executor MUST run all four):**
1. `pnpm --filter ./frontend typecheck` - tsc strict, `--noEmit`. Must exit 0. Confirms no `any`, no implicit `any` on the new mutation handler, no missing import.
2. `pnpm --filter ./frontend lint` - ESLint over the modified component and test files. Must exit 0.
3. `pnpm --filter ./frontend test -- src/app/\(app\)/history` - vitest over `history-client.test.tsx` (4 existing + 3 new = 7 cases). Must all pass. Watch the assertion that `router.push` is called exactly once per case (a leaked-mock symptom of a misbound handler).
4. `pnpm --filter ./frontend test` (full suite) - regress neighbour suites, especially `dashboard-client.test.tsx` which exercises the shared SESSION_ALREADY_ACTIVE redirect path; a refactor here that drifts the helper would surface there too.
</context>
