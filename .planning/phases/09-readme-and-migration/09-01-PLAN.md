---
phase: 09-readme-and-migration
plan: 01
type: execute
domain: docs
---

<objective>
Bring `README.md` to contract section 11 spec (one-paragraph description, quick start, configuration table, exposure examples, data and backup hooks, updating). Create `docs/MIGRATION.md` with the v0.3.0 entry covering the named-volume to bind-mount migration plus the OIDC client removal.

Purpose: Operators need a single entry point to run the stack and a migration document that captures every breaking change per release. Contract sections 11 and 10.2 require both.

Output: Rewritten root `README.md` and new `docs/MIGRATION.md`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@docs/SELF_HOSTED_CONTRACT.md
@.env.example
@compose.yml
@docker-compose.override.yml.example
@.planning/STATE.md

**No code changes. Pure docs.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md per contract section 11</name>
  <files>README.md</files>
  <action>
Replace the entire README.md (currently the template README, not project README) with the contract-aligned structure: One-paragraph description, Quick Start (smallest copy-pasteable docker compose recipe), Configuration (env var groups summarized; full reference points at .env.example), Exposure (Caddy + Traefik + nginx snippets as examples for operators), Data and Backup (./data/ layout + opt-in pg_dump sidecar), Updating (CHANGELOG + MIGRATION pointers), Reference Links section linking docs/SELF_HOSTED_CONTRACT.md / API.md / DATA_SCHEMA.md / EXPORT_FORMAT.md / OBSERVABILITY.md / BACKUP.md / DEPLOYMENT.md.

Keep snippets short and operator-focused. Reverse-proxy examples use `${PUBLIC_HOSTNAME}` and `127.0.0.1:8080` / `:3000` upstreams.
  </action>
  <verify>README.md has all 6 contract section 11 subsections present; markdown lint clean.</verify>
  <done>README is project-specific, operator-facing, contract-compliant.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/MIGRATION.md with v0.3.0 entry</name>
  <files>docs/MIGRATION.md</files>
  <action>
Create new `docs/MIGRATION.md` per contract section 10.2. Top-level template explains structure (one entry per release, listing required env var changes, schema migrations, one-shot operator commands, rollback recipe, plus an explicit "No migration steps." line for releases that need none).

First entry: `## v0.3.0` covering:
- Compose port bindings now parametric: copy the new `.env.example` and set BIND_ADDR/BACKEND_PORT/FRONTEND_PORT
- DB volume migration: named volume `db_data` -> bind mount `./data/postgres/`. If upgrading from a v0.2.x deployment, run pg_dump from the old container, drop the named volume, create `./data/postgres/`, restore the dump. Provide exact commands.
- OIDC client removed (Plan 05-01); operators using it must move SSO to a reverse-proxy forward-auth setup. Set APP_AUTH_MODE=forward-auth in `.env`.
- New env vars introduced (BIND_ADDR, BACKEND_PORT, FRONTEND_PORT, AUTH_MODE, TRUSTED_PROXIES, headers, ENABLE_PG_DUMP, PG_DUMP_*, VAPID, REMINDERS).
- Healthcheck endpoints renamed: probes now use `/healthz` / `/api/healthz` instead of `/actuator/health` / `/`. Reverse-proxy upstream selection updated accordingly.
- Logs in production profile now emit ECS JSON to stdout (Plan 03-01); update log shipper config if needed.
- /metrics endpoint added on main listener (port 8080 backend, 3000 frontend); Prometheus scrape config can switch from /actuator/prometheus to /metrics.
- Rollback recipe: revert merge SHA in operator's deployment repo (Renovate-PR-pinned) and Komodo redeploys previous tag; restore last pre-deploy pg_dump if state changed.
  </action>
  <verify>docs/MIGRATION.md exists with v0.3.0 entry containing all required subsections.</verify>
  <done>Migration doc seeded; release workflow has somewhere to extract operator notes.</done>
</task>

</tasks>

<success_criteria>Phase 9 closes; Phase 10 (CI gates) unblocked.</success_criteria>

<output>Standard SUMMARY.md.</output>
