---
phase: 18-export-refinement
plan: 03
type: execute
domain: backend
---

<objective>
Apply per-record `@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)` on `ClaudeSummaryDto` and each of its 9 nested records, then flip every `ExportIntegrationTest` jsonPath assertion from camelCase to snake_case so the wire shape of `GET /api/export/claude-summary` matches the canonical `ProjectBrief.md:510-573` example. Closes 18-01-AUDIT Section 6 `naming-convention` bucket via Direction C (claude-summary only; full-export and per-slice DTOs stay camelCase).

Purpose: Plan 18-02 closed the `claude-summary-fields` bucket - 10 records / ~37 components now carry the right field set, but all property names still serialize in camelCase (`totalWorkouts`, `displayName`, `bodyMetrics`, `currentStreakDays`, `dayName`, etc.). The brief's LLM-paste example uses snake_case literals (`total_workouts`, `display_name`, `body_metrics`, `current_streak_days`). Direction C from the 18-01-AUDIT Section 4 Part A verdict is "snake_case on `ClaudeSummaryDto` only; full-export round-trip stays camelCase to keep prior backups importable." Section 5 Part E declared zero pre-existing `@JsonNaming` usage in `backend/src` - this plan establishes the precedent. The per-record annotation is required (Jackson's `SnakeCaseStrategy` does NOT recurse into nested record types unless they are also annotated). 18-02-SUMMARY ships at sha 576cc6f6 with `ClaudeSummaryDto` declaring 10 records; this plan adds 10 annotations and changes zero field declarations.

Output: `ClaudeSummaryDto.java` grows by 2 imports + 10 class-level annotations (1 on the top-level + 9 on nested records). Zero field, component, or method changes; zero new records. `ExportIntegrationTest.java` flips ~14 jsonPath assertion strings across 5 content-emitting test methods so every camelCase key becomes snake_case (e.g. `$.summary.totalWorkouts` -> `$.summary.total_workouts`, `$.user.displayName` is not currently asserted but `$.bodyMetrics` -> `$.body_metrics`, `$.consistency.currentStreakDays` -> `$.consistency.current_streak_days`). Single-word keys (`weight`, `reps`, `email`, `age`, `goals`, `type`, `prs`, `period.days`) stay literally identical. NO change to `FullExportDto`, `ImportResultDto`, `CsvImportResultDto`, `JacksonConfig`, `ExportService`, `FullExportService`, `FullImportService`, `ImportValidator`, `CsvExportService`, `IcsExportService`, frontend, docs, or migrations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-export-refinement/18-01-AUDIT.md
@.planning/phases/18-export-refinement/18-01-SUMMARY.md
@.planning/phases/18-export-refinement/18-02-PLAN.md
@.planning/phases/18-export-refinement/18-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@ProjectBrief.md
@backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java
@backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java

**Tech stack available:** Java 21 records, Jackson 2.x annotations (`com.fasterxml.jackson.databind.annotation.JsonNaming`, `com.fasterxml.jackson.databind.PropertyNamingStrategies`, `PropertyNamingStrategies.SnakeCaseStrategy`), Spring MVC `MockMvc` + `JSONPath` assertions, JUnit 5, `serializationInclusion = NON_NULL` already wired (18-02-SUMMARY confirmed; absent fields strip cleanly).

**Established patterns (carried from prior plans):**
- DTOs are records, immutable, declared in `com.workouthub.exports.dto`. The 18-02 plan introduced 3 new nested records (`PrEntry`, `BodyMetricSummary`, `Consistency`); 18-03 leaves the components alone and only annotates the type.
- Annotations on records (Java 21): class-level annotations on a record declaration apply to the type itself; Jackson reads `@JsonNaming` from the class. Per-component `@JsonProperty` overrides are a code smell and not used here.
- Test discipline: `ExportIntegrationTest` uses `MockMvc` + `jsonPath()` against the canonical wire shape. The 18-02 plan grew `@Test` count from 4 to 6 and added 28 assertions; 18-03 only flips path strings, no method count change.
- Jackson `SnakeCaseStrategy` translation rules (deterministic for our component names):
  - `displayName` -> `display_name`
  - `heightCm` -> `height_cm`
  - `weightKg` -> `weight_kg`
  - `healthNotes` -> `health_notes`
  - `goals` -> `goals` (single word)
  - `age` -> `age`
  - `from` / `to` / `days` -> unchanged (single word)
  - `totalWorkouts` -> `total_workouts`
  - `plannedWorkouts` -> `planned_workouts`
  - `adherencePercent` -> `adherence_percent`
  - `totalVolumeKg` -> `total_volume_kg`
  - `avgSessionDurationMin` -> `avg_session_duration_min`
  - `weightChangeKg` -> `weight_change_kg`
  - `date` / `type` / `mood` / `energy` -> unchanged
  - `durationMin` -> `duration_min`
  - `userNotes` -> `user_notes`
  - `nameTr` -> `name_tr`
  - `nameEn` -> `name_en`
  - `sets` / `weight` / `reps` / `exercise` -> unchanged
  - `bodyMetrics` -> `body_metrics`
  - `consistency` -> `consistency`
  - `currentStreakDays` -> `current_streak_days`
  - `missedDays` -> `missed_days`
  - `missedReasons` -> `missed_reasons`
  - `prs` / `period` / `summary` / `user` / `workouts` / `exercises` -> unchanged

**Constraining decisions (from 18-01-AUDIT Section 4 Part A and Section 6 naming-convention bucket):**
- **Direction C only.** `@JsonNaming(SnakeCaseStrategy)` lands on `ClaudeSummaryDto` and its 9 nested records ONLY. Do NOT touch `FullExportDto` (66 components), `ImportResultDto` (8 components), `CsvImportResultDto` (4 components), or any other DTO. The audit explicitly rejects Direction A (wholesale flip) for backup-restore stability and rejects Direction B (camelCase + docs deviation note) for LLM-paste ergonomics.
- **Per-record annotation.** Jackson's `@JsonNaming` does NOT cascade to nested record types declared inside an annotated outer record. Each of the 10 records must carry its own annotation: `ClaudeSummaryDto`, `UserSummary`, `Period`, `Totals`, `WorkoutEntry`, `ExerciseEntry`, `SetEntry`, `PrEntry`, `BodyMetricSummary`, `Consistency`. Verified by zero `@JsonNaming` usage in `backend/src` today (18-01-AUDIT Section 5 Part E), so no precedent contradicts this.
- **No global JacksonConfig change.** `JacksonConfig` (referenced in 18-02 context) sets `JavaTimeModule`, `WRITE_DATES_AS_TIMESTAMPS=false`, `serializationInclusion=NON_NULL`. Adding `PropertyNamingStrategies.SNAKE_CASE` globally would flip `FullExportDto` and break import round-trip on any prior backup file - explicitly out of scope per the audit. Plan 18-03 does NOT edit `JacksonConfig.java`.
- **No `@JsonProperty` per-field overrides.** The class-level `@JsonNaming` strategy auto-derives snake_case from camelCase component names; per-field `@JsonProperty("...")` shadows the strategy and creates lock-in (18-02 context warned against this). Annotation-level only.

**Constraining decisions (this planner):**
- **Single import addition style.** `ClaudeSummaryDto.java` already imports `java.math.BigDecimal`, `java.time.LocalDate`, `java.util.List`. Add exactly two imports: `com.fasterxml.jackson.databind.PropertyNamingStrategies` and `com.fasterxml.jackson.databind.annotation.JsonNaming`. Do NOT use `import static`; the annotation reads `@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)` everywhere consistently.
- **Annotation placement.** The annotation goes on the line immediately preceding each `public record` declaration. For the top-level `ClaudeSummaryDto`, the annotation goes BETWEEN the closing `*/` of the class-level Javadoc (lines 7-12) and the `public record ClaudeSummaryDto(...)` declaration. For nested records that have no Javadoc, the annotation lives on the line above `public record`.
- **No reformatting.** Only diffs are: 2 import lines added, 10 annotation lines added. Do NOT re-wrap component lists, change the existing Javadoc, or alter trailing braces. The current file is 70 lines; after the plan, 82 lines (2 imports + 10 annotations).
- **Test split into two atomic edits.** Tasks 2 and 3 partition the test edits along test-method boundaries: Task 2 owns the 4 inherited-from-18-01 tests (`unauthenticatedIsRejected`, `summaryBundlesRecentFinishedSessionsAndTotalsForTheCaller`, `summaryDoesNotIncludeOtherUsersSessions`, `daysQueryParamClampedBelowOneReturns400`); Task 3 owns the 2 added-in-18-02 tests (`summaryIncludesAgeGoalsArrayAndWorkoutTypeWhenProfileAndPlanAreSeeded`, `summaryIncludesBodyMetricsAndWeightChangeWhenMetricsSeededInWindow`). The split keeps each task's diff scope obvious to the executor and matches the natural test-grouping in the file.
- **`energyLevel` -> `energy_level` doesNotExist flip.** 18-02 introduced `$.workouts[0].energyLevel` `doesNotExist()` (the rename to `energy` already happened in the DTO; the camelCase variant `energyLevel` should NOT leak). After Direction C, the snake_case form is `energy_level`; flip the `doesNotExist()` assertion target to `$.workouts[0].energy_level`. The actual field is `energy`; both `energyLevel` AND `energy_level` should be absent. This assertion belongs to Task 3 (it lives in the 18-02-added test).
- **`weight` and `reps` are single-word.** Snake_case strategy leaves them unchanged: `set.getWeightKg()` populates `SetEntry.weight` which serializes as `"weight"` either way. Path stays `$.workouts[0].exercises[0].sets[0].weight` and `.reps`.
- **`prs[0].weight` and `prs[0].reps` and `prs[0].date` and `prs[0].exercise` all single-word.** Unchanged after flip.
- **`bodyMetrics` (camelCase top-level field name) -> `body_metrics` (snake_case wire); `bodyMetrics[i].weightKg` -> `body_metrics[i].weight_kg`.**
- **`consistency.currentStreakDays` -> `consistency.current_streak_days`; `consistency.missedReasons` -> `consistency.missed_reasons`.**
- **`summary.plannedWorkouts` / `adherencePercent` / `weightChangeKg` / `totalWorkouts` / `totalVolumeKg` -> snake_case forms.**
- **`user.email` is `doesNotExist()` and stays unchanged (single-word).**
- **`workouts[0].type` stays unchanged (single-word).**
- **`period.days` stays unchanged (single-word).**
- **No new tests in 18-03.** The 6 tests inherited from 18-02 cover the field set; the snake_case flip is mechanical. Adding a 7th test would inflate scope.
- **Test data discipline carries from 18-02.** No seed changes; only path-string changes inside `jsonPath()` calls. The `addSet` / `updateProfile` / `upsertMetric` / `createDay` private helpers post `repsDone` / `weightKg` / `displayName` / `birthDate` / `dayOfWeek` etc. as REQUEST bodies to non-claude-summary endpoints (`/api/sessions/.../sets`, `/api/users/me`, `/api/metrics`, `/api/workout-plans/.../days`); those payload keys are NOT touched here because the receiving endpoints are NOT annotated with `@JsonNaming` (Direction C scope).

**Issues being addressed:**
- 18-01-AUDIT Section 4 Part A: JSON property naming convention. Direction C verdict.
- 18-01-AUDIT Section 5 Part E: zero pre-existing `@JsonNaming` usage; this plan declares the precedent.
- 18-01-AUDIT Section 6 `naming-convention` bucket: closed by this plan.
- ROADMAP Phase 18 deliverable: "Align /api/export/claude-summary output with the ProjectBrief example" - the snake_case wire shape is the final contract-alignment piece.

**Out of scope (deferred to other plans):**
- `FullExportDto`, `ImportResultDto`, `CsvImportResultDto` snake_case migration - explicitly rejected by Direction C; backup-restore stability mandates camelCase on these.
- `JacksonConfig` global naming strategy - rejected; would silently flip non-target DTOs.
- `is_pr` round-trip preservation, `ImportValidator.validateSessionDayIds` rule - Plan 18-04 (`roundtrip-stability`).
- `docs/EXPORT_FORMAT.md` claude-summary section (snake_case wire format documentation, goals delimiter, adherence-no-clamp note) - Phase 19 (`api-contract-docs`).
- Frontend changes - none; the claude-summary surface is download-only and no client code reads its keys.
- Migrations - none needed.
- New endpoint, new DTO, new field, new helper, new test - none added.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate ClaudeSummaryDto and 9 nested records with @JsonNaming(SnakeCaseStrategy)</name>
  <files>backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java</files>
  <action>
Add 2 imports and 10 class-level annotations to `ClaudeSummaryDto.java`. Zero field, component, method, or Javadoc changes.

**Imports to add (in alphabetical order, with `com.fasterxml.jackson.*` placed BEFORE `java.*` per repo Spring/Jackson convention):**

```java
import com.fasterxml.jackson.databind.PropertyNamingStrategies;
import com.fasterxml.jackson.databind.annotation.JsonNaming;
```

The import block after edit reads:

```java
package com.workouthub.exports.dto;

import com.fasterxml.jackson.databind.PropertyNamingStrategies;
import com.fasterxml.jackson.databind.annotation.JsonNaming;
import java.math.BigDecimal;
import java.time.LocalDate;
import java.util.List;
```

**Annotations to add (10 total).** Each annotation goes on the line directly above the `public record` keyword for that record. The class-level Javadoc on `ClaudeSummaryDto` (lines 7-12 in the current file) stays unchanged; the annotation goes BETWEEN the closing `*/` of the Javadoc and the `public record ClaudeSummaryDto(...)` declaration.

Place exactly this annotation on each:

```java
@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)
```

Targets, in declaration order from the current file:

1. `ClaudeSummaryDto` (top-level, line 13 today) - between Javadoc closing `*/` and `public record`.
2. `UserSummary` (line 22) - directly above `public record UserSummary(...)`.
3. `Period` (line 30) - directly above `public record Period(...)`.
4. `Totals` (line 32) - directly above `public record Totals(...)`.
5. `WorkoutEntry` (line 40) - directly above `public record WorkoutEntry(...)`.
6. `ExerciseEntry` (line 49) - directly above `public record ExerciseEntry(...)`.
7. `SetEntry` (line 54) - directly above `public record SetEntry(...)`.
8. `PrEntry` (line 56) - directly above `public record PrEntry(...)`.
9. `BodyMetricSummary` (line 62) - directly above `public record BodyMetricSummary(...)`.
10. `Consistency` (line 66) - directly above `public record Consistency(...)`.

Final file: 70 lines (current) + 2 import lines + 10 annotation lines = 82 lines. Zero changes elsewhere.

**Avoid and WHY:**
- Do NOT add `@JsonNaming` to `FullExportDto`, `ImportResultDto`, or `CsvImportResultDto`. Direction C verdict explicit; doing so breaks backup-restore round-trip on prior payloads.
- Do NOT add `@JsonNaming` to `JacksonConfig.java` as a global strategy. Same reason; would silently flip non-target DTOs.
- Do NOT add `@JsonProperty` annotations on individual record components. The class-level `@JsonNaming` strategy auto-derives the snake_case wire form from the camelCase Java component names; `@JsonProperty` overrides shadow the strategy and create lock-in.
- Do NOT use `@JsonNaming(PropertyNamingStrategies.SNAKE_CASE)` (the static instance form). The strategy must be passed as `SnakeCaseStrategy.class` (a `Class<? extends PropertyNamingStrategy>`) per the `@JsonNaming(value = ...)` element type. Both forms work in current Jackson minor versions but the `.class` form is the canonical one and avoids surprises if the codebase pins an older Jackson minor.
- Do NOT rename Java component names to snake_case. Records' Java component names stay camelCase (matches Java conventions); Jackson handles the wire-format translation.
- Do NOT relocate or re-wrap the existing class-level Javadoc on `ClaudeSummaryDto`. The Javadoc text is descriptive of the wire contract; flipping the wire to snake_case does not invalidate the prose.
- Do NOT add a `@JsonNaming` annotation on the package-info or on the file header. The Jackson `@JsonNaming` annotation has `@Target({ANNOTATION_TYPE, METHOD, TYPE})` - it MUST sit on the type. Per-record placement is the ONLY valid form for record types.
  </action>
  <verify>
- `rg -n "@JsonNaming" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns exactly 10 hits.
- `rg -n "PropertyNamingStrategies.SnakeCaseStrategy" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns exactly 10 hits (one per annotation).
- `rg -n "import com.fasterxml.jackson.databind.PropertyNamingStrategies;" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns 1 hit.
- `rg -n "import com.fasterxml.jackson.databind.annotation.JsonNaming;" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns 1 hit.
- `rg -n "public record" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` still returns 10 hits (no record additions/removals).
- `rg -n "@JsonProperty" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns 0 hits.
- `rg -n "@JsonNaming" backend/src/main/java/com/workouthub/exports/dto/FullExportDto.java backend/src/main/java/com/workouthub/exports/dto/ImportResultDto.java backend/src/main/java/com/workouthub/exports/dto/CsvImportResultDto.java` returns 0 hits (Direction C scope respected).
- `git diff --stat backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` shows ~12 added lines, 0 removed.
- Compile gate delegated to CI per local-maven-gap memory.
  </verify>
  <done>
- 2 Jackson imports added.
- 10 `@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)` annotations added (one per record type).
- Zero record component, method, or Javadoc changes.
- `FullExportDto`, `ImportResultDto`, `CsvImportResultDto`, `JacksonConfig` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip jsonPath assertions to snake_case in the 4 tests inherited from 18-01</name>
  <files>backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java</files>
  <action>
Mechanical path-string flips inside `jsonPath("...")` calls in the 4 inherited-from-18-01 tests. Zero new tests, zero new helpers, zero seed changes. Only response-side jsonPath strings change; request-payload JSON in private helpers stays camelCase.

**Method 1: `unauthenticatedIsRejected` (around line 41-44).** No assertions touched (only checks status 401). Skip.

**Method 2: `summaryBundlesRecentFinishedSessionsAndTotalsForTheCaller` (around line 46-84).** Flip the following jsonPath strings inside `jsonPath(...)` calls. Match the existing surrounding `andExpect(...)` chain, change ONLY the path string and nothing else (matchers, expected values, line ordering, surrounding code stay identical):

| Before (jsonPath arg) | After (jsonPath arg) |
| --- | --- |
| `$.user.email` | `$.user.email` (unchanged - single word) |
| `$.period.days` | `$.period.days` (unchanged) |
| `$.summary.totalWorkouts` | `$.summary.total_workouts` |
| `$.summary.totalVolumeKg` | `$.summary.total_volume_kg` |
| `$.summary.plannedWorkouts` | `$.summary.planned_workouts` |
| `$.summary.adherencePercent` | `$.summary.adherence_percent` |
| `$.summary.weightChangeKg` | `$.summary.weight_change_kg` |
| `$.workouts.length()` | `$.workouts.length()` (unchanged) |
| `$.workouts[0].exercises[0].sets.length()` | `$.workouts[0].exercises[0].sets.length()` (unchanged) |
| `$.workouts[0].exercises[0].sets[0].reps` | `$.workouts[0].exercises[0].sets[0].reps` (unchanged - single word) |
| `$.workouts[0].exercises[0].sets[0].weight` | `$.workouts[0].exercises[0].sets[0].weight` (unchanged - single word) |
| `$.prs` | `$.prs` (unchanged) |
| `$.prs.length()` | `$.prs.length()` (unchanged) |
| `$.prs[0].weight` | `$.prs[0].weight` (unchanged) |
| `$.prs[0].reps` | `$.prs[0].reps` (unchanged) |
| `$.bodyMetrics` | `$.body_metrics` |
| `$.bodyMetrics.length()` | `$.body_metrics.length()` |
| `$.consistency.currentStreakDays` | `$.consistency.current_streak_days` |
| `$.consistency.missedReasons` | `$.consistency.missed_reasons` |
| `$.consistency.missedReasons.length()` | `$.consistency.missed_reasons.length()` |

Distinct path-string changes in this method: 8.

**Method 3: `summaryDoesNotIncludeOtherUsersSessions` (around line 86-103).** Flip 1 path:

| Before | After |
| --- | --- |
| `$.summary.totalWorkouts` | `$.summary.total_workouts` |
| `$.workouts.length()` | `$.workouts.length()` (unchanged) |

Distinct path-string changes in this method: 1.

**Method 4: `daysQueryParamClampedBelowOneReturns400` (around line 105-113).** No content assertions; status 400 only. Skip.

**Total path-string changes for Task 2: 9.**

**Reminder lines NOT to touch (private helper bodies with REQUEST payloads):**
- `addSet` (around line 206-216): the request body string `"\"repsDone\":" + reps + ",\"weightKg\":" + weight + "}"` posts to `/api/sessions/{id}/sets`, which is NOT a claude-summary endpoint and NOT annotated. Stay verbatim.
- `startSession` / `finishSession` / `seedExercise`: not touched.

**Avoid and WHY:**
- Do NOT change the matcher expected values. `value(50.0)` for `weight` stays `value(50.0)`; only path strings flip.
- Do NOT remove any assertion lines. Every existing line stays; only the path string inside `jsonPath(...)` changes.
- Do NOT change request-payload JSON. Only response-shape jsonPath strings flip.
- Do NOT change `$.user.email` to a snake_case variant - `email` is single-word, unchanged. The `doesNotExist()` semantic stays; the path stays.
- Do NOT touch the 18-02-added tests in this task. Those are scoped to Task 3.
  </action>
  <verify>
- `rg -n "totalWorkouts|totalVolumeKg|plannedWorkouts|adherencePercent|weightChangeKg|bodyMetrics|currentStreakDays|missedReasons|missedDays" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java | rg "jsonPath"` returns 0 hits within the 4 inherited tests (lines 41-113 region).
- `rg -n "total_workouts|total_volume_kg|planned_workouts|adherence_percent|weight_change_kg|body_metrics|current_streak_days|missed_reasons" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java | rg "jsonPath"` returns at least 9 hits within the 4 inherited tests.
- The 4 inherited tests still exist as `@Test`s (`unauthenticatedIsRejected`, `summaryBundlesRecentFinishedSessionsAndTotalsForTheCaller`, `summaryDoesNotIncludeOtherUsersSessions`, `daysQueryParamClampedBelowOneReturns400`).
- `rg -n "\"repsDone\":|\"weightKg\":" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java` still returns hits inside the `addSet` helper body (request payloads stay camelCase).
- Test execution gate delegated to CI per local-maven-gap memory.
  </verify>
  <done>
- 9 distinct path-string flips applied across 2 content-emitting tests (`summaryBundlesRecentFinishedSessionsAndTotalsForTheCaller` + `summaryDoesNotIncludeOtherUsersSessions`).
- Inherited test method names and `@Test` count for this group are unchanged (4).
- Request-payload JSON in private helpers (`addSet`) untouched.
- 18-02-added tests left alone for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip jsonPath assertions to snake_case in the 2 tests added by 18-02</name>
  <files>backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java</files>
  <action>
Mechanical path-string flips inside `jsonPath("...")` calls in the 2 added-in-18-02 tests. Zero new tests, zero new helpers, zero seed changes. Only response-side jsonPath strings change; request-payload JSON in private helpers (`updateProfile`, `createPlan`, `createDay`, `activatePlan`, `upsertMetric`) stays camelCase since those endpoints are not annotated.

**Method 5: `summaryIncludesAgeGoalsArrayAndWorkoutTypeWhenProfileAndPlanAreSeeded` (around line 115-156).** Flip the following:

| Before (jsonPath arg) | After (jsonPath arg) |
| --- | --- |
| `$.user.email` | `$.user.email` (unchanged - single word) |
| `$.user.age` | `$.user.age` (unchanged) |
| `$.user.goals.length()` | `$.user.goals.length()` (unchanged) |
| `$.user.goals[0]` | `$.user.goals[0]` (unchanged) |
| `$.user.goals[1]` | `$.user.goals[1]` (unchanged) |
| `$.workouts[0].type` | `$.workouts[0].type` (unchanged) |
| `$.workouts[0].energyLevel` | `$.workouts[0].energy_level` |
| `$.summary.plannedWorkouts` | `$.summary.planned_workouts` |
| `$.summary.adherencePercent` | `$.summary.adherence_percent` |

Distinct path-string changes in this method: 3.

The `$.workouts[0].energy_level` assertion keeps the `.doesNotExist()` matcher: the actual field is `energy` (after 18-02 rename), so neither the camelCase rename-survivor `energyLevel` nor the snake_case form `energy_level` should appear in the wire payload. Asserting absence on the snake_case form is the right post-Direction-C contract.

**Method 6: `summaryIncludesBodyMetricsAndWeightChangeWhenMetricsSeededInWindow` (around line 158-178).** Flip the following:

| Before (jsonPath arg) | After (jsonPath arg) |
| --- | --- |
| `$.bodyMetrics.length()` | `$.body_metrics.length()` |
| `$.bodyMetrics[0].weightKg` | `$.body_metrics[0].weight_kg` |
| `$.bodyMetrics[1].weightKg` | `$.body_metrics[1].weight_kg` |
| `$.summary.weightChangeKg` | `$.summary.weight_change_kg` |

Distinct path-string changes in this method: 4.

**Total path-string changes for Task 3: 7.**

**Reminder lines NOT to touch (private helper bodies with REQUEST payloads):**
- `updateProfile` (around line 226-232): JSON body parameter is opaque to this task; helper itself stays verbatim. The CALLER-side string at lines 124-132 contains a hand-written JSON request body with keys `displayName`, `heightCm`, `weightKg`, `birthDate`, `goals` - these are REQUEST payload keys consumed by `/api/users/me`, not annotated. Stay verbatim.
- `upsertMetric` (around line 267-274): the request body `"\"recordedDate\":\"" + date + "\",\"weightKg\":" + weightKg + "}"` posts to `/api/metrics`, not annotated. Stay verbatim.
- `createDay` (around line 245-258): the request body uses `dayOfWeek`, `name`, `focus`, `estimatedDurationMin` - posts to `/api/workout-plans/{id}/days`, not annotated. Stay verbatim.
- `createPlan`, `activatePlan`: not touched.

**Avoid and WHY:**
- Do NOT add a 7th test. The 6 existing tests cover the same fields the snake_case flip touches; a 7th test would only re-assert the same paths in a different shape.
- Do NOT change the matcher expected values. `value("Fit vucut")`, `value(26)`, `value(78.5)`, `value(-1.50)` all stay; only path strings flip.
- Do NOT change `$.workouts[0].energyLevel`'s matcher away from `.doesNotExist()`. Replace ONLY the path string with `$.workouts[0].energy_level` and keep `.doesNotExist()`.
- Do NOT add a `$.workouts[0].energy` assertion (positive). The 18-02 test does not seed an `energyLevel` value on the session, so `energy` would be absent (or null, stripped by NON_NULL); asserting on it adds noise.
- Do NOT change request-payload JSON in `updateProfile` callers (lines 124-132). The receiving endpoint `/api/users/me` is not annotated with `@JsonNaming`; flipping its accepted keys to snake_case here would break the helper.
- Do NOT touch the 4 inherited tests in this task. Those are scoped to Task 2.
  </action>
  <verify>
- `rg -n "energyLevel|bodyMetrics|weightKg|weightChangeKg|plannedWorkouts|adherencePercent" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java | rg "jsonPath"` returns 0 hits within the 18-02-added tests region (lines ~115-178).
- `rg -n "energy_level|body_metrics|weight_kg|weight_change_kg|planned_workouts|adherence_percent" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java | rg "jsonPath"` returns at least 7 hits within the 18-02-added tests region.
- Both 18-02-added tests still exist as `@Test`s (`summaryIncludesAgeGoalsArrayAndWorkoutTypeWhenProfileAndPlanAreSeeded`, `summaryIncludesBodyMetricsAndWeightChangeWhenMetricsSeededInWindow`).
- `rg -c "@Test" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java` returns 6 (combined with Task 2; no test added or removed).
- `rg -n "\"displayName\":|\"heightCm\":|\"weightKg\":|\"birthDate\":|\"recordedDate\":|\"dayOfWeek\":|\"estimatedDurationMin\":" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java` still returns hits inside REQUEST payloads (helper bodies and inline JSON in the calling tests). These stay camelCase per Direction C scope.
- `git diff --stat backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java` (combined with Task 2) shows ~14-16 lines changed, 0 added/removed.
- Test execution gate delegated to CI per local-maven-gap memory.
  </verify>
  <done>
- 7 distinct path-string flips applied across 2 content-emitting tests (`summaryIncludesAgeGoalsArrayAndWorkoutTypeWhenProfileAndPlanAreSeeded` + `summaryIncludesBodyMetricsAndWeightChangeWhenMetricsSeededInWindow`).
- `$.workouts[0].energy_level` is the new `doesNotExist()` target (replaces 18-02's camelCase form).
- Combined with Task 2: 16 distinct path strings flipped, 6 `@Test` count preserved, request payloads untouched.
- Inherited tests left alone (handled by Task 2).
  </done>
</task>

</tasks>

<verification>
Before declaring plan 18-03 complete:
- [ ] `rg -n "@JsonNaming" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns 10.
- [ ] `rg -n "@JsonNaming" backend/src/main/java/com/workouthub/exports/dto/FullExportDto.java backend/src/main/java/com/workouthub/exports/dto/ImportResultDto.java backend/src/main/java/com/workouthub/exports/dto/CsvImportResultDto.java` returns 0.
- [ ] `rg -n "PropertyNamingStrategies" backend/src/main/java/com/workouthub/common/JacksonConfig.java` returns 0 (no global change).
- [ ] `rg -n "public record" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns 10 (record count unchanged from 18-02).
- [ ] `rg -n "@JsonProperty" backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` returns 0 (no per-field overrides).
- [ ] `rg -c "@Test" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java` returns 6 (test count unchanged from 18-02).
- [ ] `rg -n "totalWorkouts|totalVolumeKg|plannedWorkouts|adherencePercent|weightChangeKg|bodyMetrics|currentStreakDays|missedReasons|missedDays|energyLevel" backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java | rg "jsonPath"` returns 0 hits across the entire file (no camelCase response-path tokens left).
- [ ] `git diff --stat` touches exactly 2 files: `ClaudeSummaryDto.java`, `ExportIntegrationTest.java`.
- [ ] No migration file added under `backend/src/main/resources/db/migration/`.
- [ ] No change to `FullExportDto`, `FullExportService`, `FullImportService`, `ImportValidator`, `ExportController`, `ExportService`, `CsvExportService`, `IcsExportService`, `JacksonConfig`, frontend, or docs.
- [ ] CI build green (delegated; no local mvn).
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- 18-01-AUDIT Section 4 Part A Direction C verdict realized exactly: snake_case on claude-summary only, full-export and per-slice DTOs untouched.
- 18-01-AUDIT Section 5 Part E precedent established: first `@JsonNaming` usage in `backend/src` lands on `ClaudeSummaryDto` and 9 nested records.
- 18-01-AUDIT Section 6 `naming-convention` bucket closed.
- ROADMAP Phase 18 progress: 3/4 plans shipped (18-01 audit + 18-02 fields + 18-03 naming). Remaining: 18-04 roundtrip-stability.
- v0.4 milestone progress moves from "Phase 18 in progress (2/4)" to "Phase 18 in progress (3/4)".
- Plan 18-04 stays focused on FullExportDto round-trip; this plan does not bleed into is_pr or session.workoutDayId validator territory.
</success_criteria>

<output>
After completion, create `.planning/phases/18-export-refinement/18-03-SUMMARY.md`:

---
phase: 18-export-refinement
plan: 03
subsystem: exports
tags: [claude-summary, json-naming, snake-case, jackson-annotation, direction-c]

requires:
  - phase: 18-export-refinement
    plan: 01
    reason: 18-01 audit Direction C verdict (Section 4 Part A) prescribes snake_case on claude-summary only
  - phase: 18-export-refinement
    plan: 02
    reason: 18-02 fixed the DTO field set to 10 records; 18-03 annotates them with @JsonNaming

provides:
  - ClaudeSummaryDto and 9 nested records carry @JsonNaming(SnakeCaseStrategy)
  - /api/export/claude-summary wire shape now matches ProjectBrief example literally (snake_case keys)
  - First @JsonNaming usage in backend/src; FullExportDto, ImportResultDto, CsvImportResultDto unaffected

affects:
  - 18-export-refinement/18-04 (roundtrip-stability operates on FullExportDto, untouched here)
  - 19-api-contract-docs (claude-summary section in docs/EXPORT_FORMAT.md will document the snake_case wire shape)

tech-stack:
  added:
    - com.fasterxml.jackson.databind.annotation.JsonNaming (per-record annotation)
    - com.fasterxml.jackson.databind.PropertyNamingStrategies.SnakeCaseStrategy
  patterns:
    - per-record @JsonNaming(SnakeCaseStrategy) on records (does NOT cascade to nested types; each record annotated explicitly)

key-files:
  created: []
  modified:
    - backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java
    - backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java

key-decisions:
  - "Direction C: snake_case on ClaudeSummaryDto ONLY; FullExportDto, ImportResultDto, CsvImportResultDto stay camelCase to preserve backup-restore round-trip on prior payloads."
  - "Per-record @JsonNaming required because Jackson's strategy does not cascade into nested record types. 10 annotations total."
  - "No global JacksonConfig change; localized annotation keeps the rest of the API surface untouched."
  - "Java component names stay camelCase; Jackson translates to snake_case at serialization time. No field rename, no @JsonProperty per-field overrides."
  - "ExportIntegrationTest jsonPath strings flip to snake_case on the response side; request payloads in private helpers (addSet, updateProfile, upsertMetric, createDay) stay camelCase since the receiving endpoints are not annotated."
---

# Phase 18 Plan 03: naming-convention Summary

[One-liner: e.g. "ClaudeSummaryDto and 9 nested records annotated with @JsonNaming(SnakeCaseStrategy); ExportIntegrationTest jsonPath strings flipped to snake_case on response paths only; first @JsonNaming usage in backend/src; closes 18-01-AUDIT Section 6 naming-convention bucket via Direction C."]

## Accomplishments

- 2 Jackson imports + 10 `@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)` annotations added to `ClaudeSummaryDto.java`.
- 16 jsonPath response-side assertion strings flipped to snake_case across 4 content-emitting tests in `ExportIntegrationTest.java`.
- `FullExportDto`, `ImportResultDto`, `CsvImportResultDto`, `JacksonConfig` untouched.
- Zero record-component, method, helper, seed, or test-count changes.
- First `@JsonNaming` usage in `backend/src`; precedent declared per 18-01-AUDIT Section 5 Part E.

## Files Created/Modified

- `backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java` - 2 imports, 10 annotations.
- `backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java` - 16 jsonPath response strings flipped.

## Decisions Made

- Direction C scope honored: claude-summary only.
- Per-record annotation, not global JacksonConfig.
- `@JsonNaming(SnakeCaseStrategy.class)` form used (not `.SNAKE_CASE` static instance).
- No `@JsonProperty` per-field overrides.
- `$.workouts[0].energy_level` is the new `doesNotExist()` target (replaces 18-02's camelCase form).

## Issues Encountered

[Fill at completion]

## Next Phase Readiness

- Plan 18-04 (`roundtrip-stability`) is unblocked - operates on a different DTO (`FullExportDto`) and a different surface (full-export round-trip + ImportValidator rule).
- Phase 19 (`api-contract-docs`) gains content to document: the snake_case wire shape on claude-summary, the goals delimiter (from 18-02), the adherence-no-clamp note (from 18-02), and the camelCase-on-full-export stance (Direction C non-target).
</output>
