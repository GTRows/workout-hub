---
phase: 18-export-refinement
plan: 01
type: execute
domain: backend
---

<objective>
Inventory the `backend/src/main/java/com/workouthub/exports/` package, map `/api/export/claude-summary` output against the canonical example in ProjectBrief lines 510-573 (period, summary, prs, body_metrics, consistency sections; user.age and goals[] shape; per-exercise/per-set "weight"/"reps" key naming), audit the `/api/export/import` round-trip path against the post-i-2 fix in `FullExportImportIntegrationTest.importRoundTripPreservesPlansFromExport` (the test-side seed addition from Plan 14-02 left several production round-trip gaps unverified), and recommend a concrete Phase 18 plan-02+ hardening scope with explicit plan boundaries.

Purpose: Phase 18 cannot plan "what to land" because `ExportService`, `FullExportService`, `FullImportService`, `ImportValidator`, `ExportController` are all already wired (14 source files / ~1100 lines under `exports/`, 6 endpoints across full + slice + ICS + CSV). Phase 18 must plan "which JSON fields to add to `ClaudeSummaryDto` so it matches the brief example (prs, consistency, planned_workouts, adherence_percent, weight_change_kg, body_metrics)", "which JSON-property casing to fix (currently camelCase via Jackson default vs the brief's snake_case `total_workouts`/`weight_kg`/`current_streak_days`)", "which round-trip gaps remain after the Plan 14-02 test-side fix (sessions reference `workoutDayId` on the export side but `FullImportService.insertSessions` does not validate that the referenced day still exists post-`replacePlans`; supplements `timing` enum tolerance via `parseTiming` may silently downgrade unknown values; the `importedAt` field is asymmetric with `exportedAt`)", and "which cross-cutting decisions must be made before plan 18-02+" (snake_case migration: keep camelCase via `JacksonConfig` global override vs add per-field `@JsonProperty`; PR computation source: reuse `analytics/PrDetector.epleyOneRm` per-set scan vs persist via SessionSet.isPr from Plan 16-04 vs aggregate from `AnalyticsService.prs(...)`; consistency source: reuse existing `analytics/StreakCalculator` vs new computation in `ExportService`; whether to expose the missed_reasons gap as a NEW V28 column on a yet-to-exist `missed_workouts` table or defer entirely). This audit produces the single source of truth for those decisions.

Output: `.planning/phases/18-export-refinement/18-01-AUDIT.md` containing six sections (exports package inventory, endpoint + DTO catalog, ProjectBrief example vs current `ClaudeSummaryDto` field-by-field gap matrix, round-trip behavioral analysis, cross-package coupling map for prs + consistency + body_metrics + plan-count verdicts, recommended Phase 18 plan-02+ scope with explicit plan boundaries). No Java source files, migrations, or tests are modified.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/ISSUES.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/13-workouts-audit/13-01-AUDIT.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/phases/16-sessions-analytics/16-01-AUDIT.md
@.planning/phases/16-sessions-analytics/16-04-SUMMARY.md
@.planning/phases/17-body-metrics/17-01-AUDIT.md
@ProjectBrief.md
@backend/src/main/java/com/workouthub/exports/ExportController.java
@backend/src/main/java/com/workouthub/exports/ExportService.java
@backend/src/main/java/com/workouthub/exports/FullExportService.java
@backend/src/main/java/com/workouthub/exports/FullImportService.java
@backend/src/main/java/com/workouthub/exports/ImportValidator.java
@backend/src/main/java/com/workouthub/exports/CsvExportService.java
@backend/src/main/java/com/workouthub/exports/CsvImportService.java
@backend/src/main/java/com/workouthub/exports/CsvRowReader.java
@backend/src/main/java/com/workouthub/exports/IcsExportService.java
@backend/src/main/java/com/workouthub/exports/ExerciseNameMatcher.java
@backend/src/main/java/com/workouthub/exports/dto/ClaudeSummaryDto.java
@backend/src/main/java/com/workouthub/exports/dto/FullExportDto.java
@backend/src/main/java/com/workouthub/exports/dto/ImportResultDto.java
@backend/src/main/java/com/workouthub/exports/dto/CsvImportResultDto.java
@backend/src/test/java/com/workouthub/exports/ExportIntegrationTest.java
@backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java
@backend/src/test/java/com/workouthub/exports/ExportFormatExampleTest.java
@backend/src/main/java/com/workouthub/common/JacksonConfig.java
@backend/src/main/java/com/workouthub/analytics/PrDetector.java
@backend/src/main/java/com/workouthub/analytics/StreakCalculator.java
@backend/src/main/java/com/workouthub/analytics/AnalyticsService.java

**Tech stack available (carried from Phase 17):** Java 21, Spring Boot 3.5.x, Spring Web, Spring Data JPA, Hibernate `@UuidGenerator`, Jackson with `serializationInclusion = NON_NULL` (per `JacksonConfig.java:18`), Flyway migrations (last applied: V27 `session_sets.is_pr` per Phase 16-04; V28 is the next free slot), JUnit 5 + Testcontainers Postgres, typed `ApiError.code` envelope from Phase 15-04, Bean Validation via `jakarta.validation` on request DTOs, `analytics/PrDetector.epleyOneRm` + `SessionSet.isPr` durable PR persistence from Plan 16-04, `analytics/StreakCalculator` + `AnalyticsService` already exposing `/api/analytics/{volume,one-rm,streak,prs,heatmap}` from pre-GSD work, Phase 17 `MetricsService.UpsertResult(dto, wasCreated)` precedent for service-side branch wrappers.

**Established patterns (carried from prior audits):**
- Feature-sliced packages (`exports/` already conforms: top-level services + `dto/` subpackage, no nested controllers).
- Controller -> Service -> Repository, transactions on services, DTOs at HTTP boundary.
- Jackson `NON_NULL` global serialization; optional record components stripped from JSON when null. Same convention applies to any new optional field on `ClaudeSummaryDto` (e.g. `prs` and `bodyMetrics` empty lists vs null - audit must pick one).
- Hand-written mappers as private static methods on the producing service (`ExportService.toEntry`, `FullExportService.toPlanSection`, `FullExportService.toMetricRow`, etc.). No separate `ExportMapper` class.
- ProjectBrief example uses `snake_case` JSON property names (`total_workouts`, `weight_kg`, `current_streak_days`); current `ClaudeSummaryDto` and `FullExportDto` records emit Jackson default `camelCase` (`totalWorkouts`, `weightKg`); `JacksonConfig.java:14-21` does NOT set `PropertyNamingStrategies.SNAKE_CASE`. Audit must pick a stance: (A) brief is authoritative, migrate to snake_case (cross-DTO breaking change), (B) brief is illustrative, keep camelCase, document the deviation in `docs/EXPORT_FORMAT.md` (low-cost), (C) split: keep `FullExportDto` camelCase for backup/restore (machine consumer) but flip `ClaudeSummaryDto` to snake_case via `@JsonNaming` (LLM consumer benefits from matching the brief's example).
- Phase 14 lesson: explicit child-repo `saveAndFlush(child)` after `parent.addChild()` to materialize `@UuidGenerator` ids; `FullImportService.replacePlans` calls `plans.save(plan)` with `cascade = ALL` on `WorkoutPlan -> WorkoutDay -> WorkoutDayExercise`. Audit must confirm Plan 14-01's lesson is applied or note where it is not (parent-only `save` vs explicit child saves).
- Phase 14-02 i-2 fix: was test-side (added a `POST /api/workout-plans` seed before the export GET). Audit must confirm no production-side gap was masked by the test-side fix and that the round-trip end-to-end (export -> import -> re-export -> diff) actually returns identical JSON.
- Phase 16-04 PR durability: `SessionSet.isPr` is now persisted; `analytics/PrDetector.epleyOneRm(weightKg, repsDone)` produces the Epley projection; `AnalyticsService.prs(userId)` returns `List<PrDto>`. `ClaudeSummaryDto` does NOT yet expose any `prs` array. Audit must catalog the integration shape.
- Phase 17-03 time-series range: `MetricsService.list(userId, from, to)` exists. `ClaudeSummaryDto` does NOT yet expose a `body_metrics` array. Audit must catalog the integration shape.

**Constraining decisions (sourced from STATE.md + prior summaries):**
- ROADMAP Phase 18 description (`.planning/ROADMAP.md:88-93`): "Align `/api/export/claude-summary` output with the ProjectBrief example (period, summary, prs, consistency sections); make the import endpoint round-trip-stable per the i-2 fix." Plan-count flagged as TBD in roadmap; this audit decides.
- ProjectBrief example (lines 510-573) is the canonical `ClaudeSummaryDto` shape. Audit must do a field-by-field drift check between the brief's JSON keys and the current `ClaudeSummaryDto` record components. The brief example's TOP-LEVEL keys are: `user`, `period`, `summary`, `workouts`, `prs`, `body_metrics`, `consistency`. Current code emits: `user`, `period`, `summary`, `workouts`. Three TOP-LEVEL gaps (prs, body_metrics, consistency) plus subkey drift inside `user` (`age` missing; `goals` is `String` not `String[]`; `current_weight_kg` vs `weightKg`) and inside `summary` (`planned_workouts`, `adherence_percent`, `weight_change_kg` missing).
- ProjectBrief import paragraph (line 577-579): "Aynı format geri yüklenebilsin ki bir makineden diğerine taşınabilsin." The roundtrip target is the FULL export format (`/api/export/full` -> `/api/export/import`), NOT `claude-summary` (which is summarization, not backup). Audit must split the two contracts: claude-summary is read-only "for-LLM-paste"; full-export is the round-trip backup format.
- `ddl-auto: validate` - schema cannot drift; any new column requires a new `V<n>__*.sql`. Phase 16-04 consumed V27; **V28 is the next free Flyway slot**. The `consistency.missed_reasons` example field has NO source today - there is no `missed_workouts` table. Audit must decide: (A) introduce V28 + a `missed_workouts` table to support the brief example literally, (B) emit `missed_reasons: []` always (placeholder, brief-illustrative), (C) drop `missed_reasons` and document the deviation in `docs/EXPORT_FORMAT.md`. Decision drives plan-count.
- Phase 15-02 + 17-04 status-code split precedent: applies to `POST /api/export/import` if-and-only-if the import has both create and replay semantics. Today the import is delete-then-reinsert (replace semantics) and returns 200; no status-code drift. Audit must confirm this and explicitly NOT carry the status-code-split bucket forward.
- Phase 14-02 closed i-2 with a test-side fix (a seed plan POST before the export GET). The PRODUCTION code at `FullImportService.replacePlans` (`FullImportService.java:212-269`) sets `plan.setUserId(userId)` and uses `cascade = ALL` on the entity tree but the cascade-id Plan 14-01 lesson (`daysRepo.saveAndFlush(day)`) is NOT applied here. Audit must confirm whether this is a latent bug masked by the test-side fix or whether the import path's cascade is safe because the import does not READ the cascaded ids back into a DTO before the transaction commits (the import returns `plansInserted` count, not a `PlanSection` echo).
- ProjectBrief example uses `"weight": 7, "reps": 10` for set entries; current `ClaudeSummaryDto.SetEntry` uses `repsDone` and `weightKg`. The keys are different AND the order is reversed. Audit must catalog this as part of Section 3.

**Issues being addressed:**
- i-2 closed by Plan 14-02 test-side fix. Audit must confirm no residual production-side gap and CLOSE the loop on i-2 explicitly. If a gap is found (cascade-id, post-import re-export drift), it must be raised as a new issue in `ISSUES.md` and consumed by plan 18-02+.
- Open issues i-3..i-9 belong to other phases per the issue-to-phase mapping in STATE.md (i-4 v0.6, i-5/i-6/i-8 v1.0, i-7 v1.0, i-9 v0.6).
- The audit may surface NEW issues (e.g. snake_case mismatch raised to issue-level if the verdict picks Direction A; consistency.missed_reasons schema gap; prs source coupling). Recording new issues is in scope; opening `ISSUES.md` edits is out of scope for this audit and belongs to plan 18-02+.

**Out of scope for this plan:**
- Writing Java fixes (plan 18-02+).
- Adding new endpoints, columns, DTOs, or migration files.
- Modifying ProjectBrief, ROADMAP, or any STATE/ISSUES file.
- Touching `analytics/`, `metrics/`, `sessions/`, `workouts/` packages beyond reading the entry points that the export currently does or could consume.
- Touching `frontend/src/app/(app)/export/`, `docs/EXPORT_FORMAT.md`, or `docs/examples/full-export-example.json`. The audit may RECOMMEND changes to those files; plan 18-02+ owns landing them.
- Running `mvn` (local-maven-gap memory; the audit produces no test-runnable output).
- Picking the snake_case verdict via Direction A wholesale - that is a Section 4 decision; the audit walks all three directions and lands on one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory exports package and catalog endpoints + DTOs</name>
  <files>.planning/phases/18-export-refinement/18-01-AUDIT.md</files>
  <action>
  Read every file under `backend/src/main/java/com/workouthub/exports/` (14 source files: `ExportController.java`, `ExportService.java`, `FullExportService.java`, `FullImportService.java`, `ImportValidator.java`, `CsvExportService.java`, `CsvImportService.java`, `CsvRowReader.java`, `IcsExportService.java`, `ExerciseNameMatcher.java`, plus the 4 DTOs under `dto/`: `ClaudeSummaryDto.java`, `FullExportDto.java`, `ImportResultDto.java`, `CsvImportResultDto.java`) and the three test classes `backend/src/test/java/com/workouthub/exports/{ExportIntegrationTest, FullExportImportIntegrationTest, ExportFormatExampleTest}.java`. Also open `docs/EXPORT_FORMAT.md` and `docs/examples/full-export-example.json` if present (verify file existence first via Glob; if either is absent, note as a Section 6 documentation gap).

  Create `18-01-AUDIT.md` with frontmatter matching the 17-01-AUDIT shape (`phase: 18-export-refinement`, `plan: 01`, `deliverable: audit`). Write Sections 1 and 2.

  Section 1 - Exports Package Inventory:
  Tree of `exports/`-owned files grouped by role (Controller, Read-side services [Export, FullExport, CsvExport, IcsExport], Write-side services [FullImport, CsvImport], Helpers [ImportValidator, CsvRowReader, ExerciseNameMatcher], DTOs, Tests). For each file: one-line responsibility plus line count measured 2026-05-06. Same shape as `17-01-AUDIT.md` Section 1 and `16-01-AUDIT.md` Section 1.

  Sub-table for the V6 / V20 / V26 / V27 SQL touchpoints that the export currently reads through repositories (no new SQL):
    | Source repository | Method called from exports | Tables touched | Migration source |
  Rows: `UserRepository.findById`, `UserProfileRepository.findById`, `WorkoutPlanRepository.findByUserIdOrderByCreatedAtAsc`, `WorkoutSessionRepository.findFinishedSince`, `WorkoutSessionRepository.findByUserIdAndEndedAtIsNotNullOrderByStartedAtDesc`, `BodyMetricRepository.findByUserIdOrderByRecordedDateDesc`, `SupplementRepository.findByUserIdOrderByCreatedAtAsc`. For each: cite the migration that defines the underlying table (`V1` for users + user_profile, `V2/V3/V4/V5/V6` schedule per `13-01-AUDIT.md` Section 1 if needed).

  Total file/line count for `exports/`-owned code (~1100 lines expected) at the bottom of the section.

  Section 2 - Endpoint + DTO Catalog:
  One row per HTTP endpoint exposed by `ExportController`. Columns: HTTP method, path, controller method name (file:line), request DTO, response DTO, auth requirement (`@AuthenticationPrincipal AppUserPrincipal`), 2xx status code, idempotency notes, query parameters with defaults and bounds, content-disposition header behavior.

  Eleven rows expected (`ExportController.java:56-184`):
    - `GET /api/export/claude-summary` -> `claudeSummary` (`:56-67`). Request: `days` query param, defaults `30`, bounds `[1, 365]`. Response: `ClaudeSummaryDto`. 200. Idempotent. Content-Disposition: `attachment; filename="workouthub-claude-<ISO>.json"`.
    - `GET /api/export/full` -> `fullDump` (`:69-79`). Response: `FullExportDto`. 200. Idempotent. Content-Disposition: attachment.
    - `POST /api/export/import` -> `importDump` (`:81-86`). Request: `FullExportDto`. Response: `ImportResultDto`. 200. Idempotent at the GIVEN payload (delete-then-reinsert; running it twice with the same payload converges; running it twice with DIFFERENT payloads each time wins).
    - `GET /api/export/profile` (`:88-91`). Slice. Response: `UserSection`. 200.
    - `GET /api/export/plans` (`:93-96`). Slice. Response: `List<PlanSection>`.
    - `GET /api/export/sessions` (`:98-102`). Slice. Response: `List<SessionSection>`.
    - `GET /api/export/metrics` (`:104-107`). Slice. Response: `List<MetricRow>`.
    - `GET /api/export/supplements` (`:109-113`). Slice. Response: `List<SupplementRow>`.
    - `POST /api/export/import/profile` (`:115-121`), `POST /api/export/import/metrics` (`:123-129`), `POST /api/export/import/supplements` (`:131-137`), `POST /api/export/import/plans` (`:139-145`), `POST /api/export/import/sessions` (`:177-183`). Per-slice import paths.
    - `GET /api/export/plan.ics` (`:147-156`). Response: `text/calendar; charset=utf-8`. 200.
    - `POST /api/export/import/csv` (`:158-163`) consumes `text/csv`; response `CsvImportResultDto`.
    - `GET /api/export/csv/sessions` (`:165-175`). Response: `text/csv; charset=utf-8`.

  Then a DTO sub-section: list all 4 top-level components of `ClaudeSummaryDto` (`user`, `period`, `summary`, `workouts`) and recurse into all nested records (`UserSummary` 6 components, `Period` 3 components, `Totals` 3 components, `WorkoutEntry` 6 components, `ExerciseEntry` 3 components, `SetEntry` 2 components). Cite `ClaudeSummaryDto.java:13-48`. Repeat for `FullExportDto` (`schemaVersion`, `exportedAt`, `user`, `plans`, `sessions`, `bodyMetrics`, `supplements`) and recurse through `UserSection` 9 components, `PlanSection` 4 components, `DayRow` 6 components, `DayExerciseRow` 9 components, `SessionSection` 8 components, `SetRow` 8 components, `MetricRow` 10 components, `SupplementRow` 5 components. Cite `FullExportDto.java:9-92`. Cite `ImportResultDto.java:5-25` for the 8-component result + 6-component back-compat constructor.

  Test sub-section: list every test method across the three test classes with file:line and a one-line outcome:
    - `ExportIntegrationTest`: `unauthenticatedIsRejected` (`:38`), `summaryBundlesRecentFinishedSessionsAndTotalsForTheCaller` (`:43`), `summaryDoesNotIncludeOtherUsersSessions` (`:71`), `daysQueryParamClampedBelowOneReturns400` (`:90`).
    - `FullExportImportIntegrationTest`: `unauthenticatedFullExportReturns401` (`:30`), `fullExportReturnsSchemaVersionAndCurrentUserSlices` (`:35`), `importRejectsUnsupportedSchemaVersion` (`:67`), `importRejectsPayloadsWithMultipleActivePlans` (`:89`), `importRoundTripPreservesPlansFromExport` (`:114` - this is the post-Plan-14-02 test), `importRoundTripReplacesMetricsAndSupplements` (`:144`).
    - `ExportFormatExampleTest`: `documentedExampleImportsAndBecomesTheCurrentExportState` (`:41`).
  Eleven tests total.

  Constraint: read-only on Java source. Do not run mvn or modify any source file. The only file written by this task is `18-01-AUDIT.md`.
  </action>
  <verify>
  - `18-01-AUDIT.md` exists with frontmatter and Sections 1 and 2.
  - Section 1 lists all 14 source files plus the repository touchpoint table.
  - Section 2 has exactly eleven endpoint rows with `file:line` citations.
  - DTO sub-section lists `ClaudeSummaryDto` 4 top-level + 6 nested record breakdowns AND `FullExportDto` 7 top-level + 8 nested record breakdowns AND `ImportResultDto` 8-component shape.
  - Test sub-section lists all 11 tests across the 3 test classes with line numbers.
  - `git diff --stat` shows zero changes under `backend/`.
  </verify>
  <done>
  Sections 1 and 2 give the reader a complete view of the exports package's current scaffold without opening any source file.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProjectBrief example field-by-field gap matrix and round-trip behavioral analysis</name>
  <files>.planning/phases/18-export-refinement/18-01-AUDIT.md</files>
  <action>
  Read `ProjectBrief.md` lines 510-573 (canonical claude-summary example) and lines 577-579 (import roundtrip statement) and `.planning/ROADMAP.md` lines 88-93 (Phase 18 goal). Append Sections 3 and 4 to the audit file.

  Section 3 - ProjectBrief Claude-Summary Example vs Current `ClaudeSummaryDto` Gap Matrix:
  Five-part section.

  Part A - Top-level keys gap matrix (ProjectBrief lines 513, 522, 527, 535, 555, 563, 567 vs `ClaudeSummaryDto.java:13-17`):
    | Top-level key | ProjectBrief example | Current DTO component | Status |
  Rows: `user`, `period`, `summary`, `workouts`, `prs`, `body_metrics`, `consistency`. First four resolve to OK at the top-level shape (drift is in subkeys, see Part B+); last three resolve to MISSING (not present on `ClaudeSummaryDto`). Status column uses `OK` / `Subkey-drift` / `MISSING`.

  Part B - `user` subkey drift (ProjectBrief lines 514-521 vs `UserSummary` `ClaudeSummaryDto.java:19-25`):
    | Brief key | Current component | Drift |
  Rows: `name` -> `displayName` (key rename), `age` -> MISSING (Brief has `26`, current DTO has no derived field; computed from `UserProfile.birthDate` if present), `height_cm` -> `heightCm` (case), `current_weight_kg` -> `weightKg` (key rename + case), `health_notes` -> `healthNotes` (case), `goals` -> `goals` (BUT Brief has `["Fit vücut", "Karaciğer iyileştirme"]` ARRAY; current `UserProfile.goals` is `TEXT` -> `String` -> single string). Status: `goals` SHAPE drift (string vs array) is the most surgical concern; either split on newline/semicolon at export time, or rename to `goals_text` to match the schema, or migrate `user_profile.goals` to a `text[]` SQL column (V28 candidate, breaking change).

  Part C - `summary` subkey drift (ProjectBrief lines 528-533 vs `Totals` `ClaudeSummaryDto.java:29-32`):
    | Brief key | Current component | Status |
  Rows: `total_workouts` -> `totalWorkouts` (case), `planned_workouts` -> MISSING (source: count active plan's days in the period), `adherence_percent` -> MISSING (computed from `total_workouts / planned_workouts`), `total_volume_kg` -> `totalVolumeKg` (case), `avg_session_duration_min` -> `avgSessionDurationMin` (case), `weight_change_kg` -> MISSING (source: latest `BodyMetric` minus earliest `BodyMetric` in window).

  Part D - `workouts[]` and nested entries:
  Compare ProjectBrief workouts shape (lines 536-553) vs `WorkoutEntry` `ClaudeSummaryDto.java:34-40` and nested:
    | Brief key | Current component | Status |
  Rows for `workouts[i]`: `date` -> `date` (OK), `type` -> MISSING (source: `WorkoutSession.workoutDayId` -> `WorkoutDay.name`), `duration_min` -> `durationMin` (case), `exercises` -> `exercises` (OK), `user_notes` -> `userNotes` (case), `mood` -> `mood` (OK), `energy` -> `energyLevel` (key rename).
  Rows for `workouts[i].exercises[j]`: `name` -> `nameTr` AND `nameEn` (Brief uses single key `name` set to English; current DTO splits TR + EN. Pick: keep both via `nameTr` + `nameEn` as additive vs collapse to brief's single `name`). Status: `Sub-key drift` with verdict deferred to Section 4.
  Rows for `workouts[i].exercises[j].sets[k]`: Brief is `{"weight": 7, "reps": 10}` (line 544); current `SetEntry` is `(short repsDone, BigDecimal weightKg)` (`ClaudeSummaryDto.java:47`). KEY drift (`weight` vs `weightKg`, `reps` vs `repsDone`) AND ORDER drift (Brief weight-first, current reps-first via record component order). Note: record component order does NOT affect Jackson JSON-property order under default settings; key drift is the meaningful concern.

  Part E - `prs[]`, `body_metrics[]`, `consistency` (the three TOP-LEVEL gaps from Part A):
  For each missing top-level key, document:
    - `prs[]` (Brief lines 555-562): each entry has `exercise`, `weight`, `reps`, `date`. Source candidates: (1) `analytics/PrDetector.epleyOneRm` per-set scan in window, (2) `SessionSet.isPr=true` rows in window joined to exercise, (3) reuse `AnalyticsService.prs(userId)` and filter by `period.from..period.to`. Verdict deferred to Section 5.
    - `body_metrics[]` (Brief lines 563-566): each entry has `date`, `weight_kg`. Source: `MetricsService.list(userId, from, to)` from Plan 17-03; subset to `(date, weightKg)` projection. Lighter shape than the full `BodyMetricDto`; verdict on whether to expose all fields or only `(date, weight_kg)` deferred to Section 5.
    - `consistency` (Brief lines 567-571): `current_streak_days`, `missed_days[]`, `missed_reasons[]`. Source candidates: `analytics/StreakCalculator` already computes streak; `missed_days` can be derived from the active plan's day-of-week schedule minus actual session dates in the window; `missed_reasons` has NO data source (no `missed_workouts` table). Verdict deferred to Section 5 + Section 6 plan-count.

  Section 4 - Behavioral Analysis:

  Part A - JSON property naming convention drift:
  Walk three directions:
    - Direction A: migrate ALL export DTOs (`ClaudeSummaryDto` AND `FullExportDto` AND `ImportResultDto`) to snake_case via class-level `@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)` on each record. Pros: matches the brief example literally; ergonomic for LLM consumers. Cons: BREAKING CHANGE on `FullExportDto` which is ALSO the import payload; `docs/examples/full-export-example.json` and `ExportFormatExampleTest` must update in lockstep; existing client (frontend `(app)/export/export-client.tsx`) reads `bodyMetrics`/`supplements` keys in test code (`export-client.test.tsx`) - those flip too. Touches: 4 DTO files, 1 example JSON, 1 doc, 2 frontend files, 4 test files.
    - Direction B: keep camelCase everywhere; document the brief example as illustrative-not-canonical; update `docs/EXPORT_FORMAT.md` (or create it if absent) to declare camelCase as the wire format. Pros: smallest change; preserves backward compat; one-doc-edit. Cons: wire-format diverges from the brief on the ProjectBrief LLM-paste workflow.
    - Direction C: split per consumer. `ClaudeSummaryDto` flips to snake_case via `@JsonNaming` (LLM consumer benefits from matching the brief; it is a one-way READ-only DTO so no import path). `FullExportDto` + `ImportResultDto` stay camelCase (machine-shaped; round-trip-stable). Pros: LLM consumer matches the brief AND backup/restore is unaffected. Cons: two conventions in one package; mild cognitive load.
  Recommendation: pick one. Lean Direction C unless evidence surfaces that frontend code or `docs/examples/full-export-example.json` already uses snake_case (Direction A is then required).

  Part B - Missing top-level fields: scope of work per direction:
  For each of `prs`, `body_metrics`, `consistency`, document:
    - "If we add it to `ClaudeSummaryDto`": net change = 1 record component on `ClaudeSummaryDto`, 1 nested record per array element type, 1 new mapper method on `ExportService`, 1 cross-package read (analytics/metrics).
    - "If we leave it out": divergence stays; Phase 18 only delivers period+summary+user+workouts; ROADMAP "prs, consistency sections" deliverable is partial.
  Recommendation: ADD all three. ROADMAP literally lists `prs` and `consistency` as Phase 18 deliverables; `body_metrics` is a brief-level driver (the LLM coach needs weight trend to advise on body-recomp). Plan-count 3 buckets minimum (one per missing top-level key) plus 1 audit + 1 cross-cutting (snake_case + planned_workouts/adherence/weight_change subkey gaps). Validate against Section 6 plan-count.

  Part C - `summary` subkey gaps `planned_workouts`, `adherence_percent`, `weight_change_kg`:
  Walk source-of-truth options for each:
    - `planned_workouts`: count of `WorkoutDay`s in the user's active plan that fall on the calendar days within `[period.from, period.to]`. Caveat: rest days (`focus = REST`) should not count. Cross-package read: `WorkoutPlanRepository.findActiveByUserId(userId)` -> filter days by day-of-week mapping. Adds one repo call per export; cheap.
    - `adherence_percent`: `Math.round(totalWorkouts * 100.0 / plannedWorkouts)` with safety on zero-divide. Pure derived; no new SQL.
    - `weight_change_kg`: latest `BodyMetric` in window minus earliest `BodyMetric` in window. Cross-package read: `BodyMetricRepository.findByUserIdAndRecordedDateBetweenOrderByRecordedDateDesc` (Plan 17-03 provides this method). If list is empty, emit `null` and let Jackson `NON_NULL` strip the key.
  These three are cheap to add and fold into the same plan as the `summary` subkey case migration if Direction C is chosen for naming. Suggest one plan covers all three.

  Part D - `user.age` derivation and `user.goals` array shape:
  Walk:
    - `age`: derived from `UserProfile.birthDate` and `LocalDate.now(ZoneOffset.UTC)`. If `birthDate` is null, emit null. Pure read-side derivation.
    - `goals` array: today `UserProfile.goals` is `TEXT`. Three options: (1) split client-side at export time on a delimiter (newline or `;`); (2) add a parallel `goals_text` keeping the SQL column shape and emit `goals: text.split(...)` at export time only for the brief example; (3) migrate `user_profile.goals` SQL column to `text[]` in V28 plus entity + DTO + form changes (BREAKING for the profile UI). Recommend option (1) for v0.4 budget. Avoids a V28 migration and keeps the existing profile form intact. The split delimiter must be documented in `docs/EXPORT_FORMAT.md`.

  Part E - `workouts[i].type` derivation:
  `WorkoutSession.workoutDayId` is nullable (sessions can be ad-hoc / detached after a plan reset, see `FullImportService.replacePlansSection` `:128` `sessions.detachSessionsFromDays(userId)`). When non-null, fetch the `WorkoutDay.name` (e.g. "Üst Vücut İtme"). When null, emit `null` or `"Ad-hoc"`. Recommend null + Jackson NON_NULL strips. Costs: one extra repo call per session (cache `Map<UUID, String>` for the window's distinct `workoutDayId` set).

  Part F - Round-trip stability re-check (post-Plan 14-02 i-2 closure):
  Walk the export -> import -> re-export pipeline:
    1. `GET /api/export/full` produces `FullExportDto` JSON. Today this includes `schemaVersion`, `exportedAt`, full slices.
    2. `POST /api/export/import` calls `FullImportService.importDump`. Sequence (`FullImportService.java:71-109`): (a) ImportValidator runs; if errors -> 422. (b) Profile upserted. (c) Metrics deleted-then-reinserted. (d) Supplements deleted-then-reinserted. (e) Sessions wiped (active sub + finished). (f) Plans deleted-then-reinserted (cascade-all). (g) Sessions reinserted with `workoutDayId` references restored from the import payload.
    3. `GET /api/export/full` again. Compare JSON.
  Drift candidates:
    - `exportedAt` is a fresh `Instant.now()` per `FullExportService.build` (`:64`). Round-trip will NEVER produce byte-identical JSON because of this field. Audit must declare this is acceptable (timestamp is a metadata field, not a data field) and recommend a re-export comparison strategy that ignores `exportedAt`.
    - `WorkoutDay.id` is preserved on import via `if (dr.id() != null) day.setId(dr.id())` (`FullImportService.java:240`). `WorkoutPlan.id` same (`:232`). `WorkoutDayExercise.id` same (`:250`). `BodyMetric.id` is NOT preserved on metric reimport (`replaceMetrics` `:155-178` does not call `m.setId(r.id())`); `Supplement.id` not preserved (`replaceSupplements` `:180-198`); `WorkoutSession.id` IS preserved (`:277`). This is asymmetric. After a round-trip, `BodyMetric.id` and `Supplement.id` change while `WorkoutPlan.id`, `WorkoutDay.id`, `WorkoutDayExercise.id`, `WorkoutSession.id`, `SessionSet.id` are stable. `is_pr` is NOT explicitly preserved on `SessionSet` reinsert (`insertSessions` :271-305 omits `set.setPr(r.isPr())` because `SetRow` does not even carry `isPr` - the `FullExportDto.SetRow` record has 8 fields and none is `isPr`). After round-trip, all `is_pr` values flip to `false`; subsequent reads will see no PRs until a PR-recompute pass runs. **This is a NEW production gap surfaced by Plan 16-04's persistence change**, only partially relevant to i-2's scope but produced by the same code path. Audit must raise this as a NEW ISSUE candidate (i-10) and recommend Plan 18-04 `pr-roundtrip` to address: (1) add `Boolean isPr` to `FullExportDto.SetRow` (9th field), (2) preserve on `insertSessions` if present, (3) recompute via `SessionSetsService.recomputePrForExerciseHistory` (Plan 16-04) for all touched `(user, exercise)` pairs at the END of the import transaction as a safety net (in case the import payload was authored by a different schema version).
    - `ImportValidator` does NOT reject sessions whose `workoutDayId` does not appear in the `plans` slice. After import, those sessions become orphans. The current `replacePlansSection` `:128` calls `sessions.detachSessionsFromDays` on the per-slice path BUT the WHOLE-DUMP `importDump` does NOT call this helper before `replacePlans` (`:92-94`); it relies on `wipeSessions(userId)` followed by `replacePlans` followed by `insertSessions`. The rebuilt sessions DO carry the original `workoutDayId` reference; if that day no longer exists post-`replacePlans` (because the payload's `plans` slice was edited), the FK violation surfaces at insert time. Audit must note this is correctly detected (FK violation -> 500-class error -> transaction rollback) but NOT validated upfront with a 422 message; preferable to add an `ImportValidator` rule "every session's `workoutDayId` must reference a `plan.day.id` from the same payload OR be null."
    - `goals` field round-trip stability: `FullExportDto.UserSection.goals` is `String` (matches `UserProfile.goals` SQL column TEXT). Round-trip is stable. Direction Part D option (1) "split at claude-summary export time" does not affect the full export shape; it only affects the brief LLM-paste view. Audit confirms full-export round-trip is unaffected by Direction Part D.

  Constraint: no Java files modified.
  </action>
  <verify>
  - Section 3 Part A is a 7-row top-level matrix with `prs`, `body_metrics`, `consistency` as MISSING.
  - Section 3 Part B-E enumerate every subkey drift in `user`, `summary`, `workouts[i]`, `workouts[i].exercises[j]`, `workouts[i].exercises[j].sets[k]`.
  - Section 3 Part E walks source-of-truth options for `prs`, `body_metrics`, `consistency`.
  - Section 4 Part A walks three naming-convention directions and ends with one recommendation (Direction C).
  - Section 4 Part B has per-missing-key cost-of-add tables.
  - Section 4 Part C walks all three summary subkey gaps with sources of truth.
  - Section 4 Part D walks `user.age` derivation and `user.goals` array shape with a v0.4-friendly recommendation.
  - Section 4 Part E walks `workouts[i].type` derivation.
  - Section 4 Part F walks round-trip stability and surfaces the `is_pr` round-trip drop as a NEW ISSUE candidate (i-10).
  - `git diff --stat` shows only `.planning/phases/18-export-refinement/18-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 3 and 4 give plan 18-02+ a field-level fix list, a snake_case direction, and a flagged round-trip gap on `is_pr`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-package coupling map for prs/consistency/body_metrics, recommended Phase 18 plan-02+ scope, plan-count verdict</name>
  <files>.planning/phases/18-export-refinement/18-01-AUDIT.md</files>
  <action>
  Append Sections 5 and 6 to the audit file.

  Section 5 - Cross-Package Coupling Map and Source-of-Truth Verdicts:

  Part A - PRs source-of-truth (closes Section 3 Part E `prs[]` open verdict):
  Three options walked in Section 3; evaluate each against the post-Plan-16-04 codebase:
    - Option 1: per-set Epley scan via `analytics/PrDetector.epleyOneRm(weightKg, repsDone)`. Pros: pure derivation; matches the in-window-only filter in the brief example exactly. Cons: O(N) on session_sets; runs on every export call. With ~300 sets per 30-day window for a power user, single export latency stays under 50ms.
    - Option 2: query `SessionSet.isPr=true` rows in window via a new repo method `SessionSetRepository.findPrsByUserAndDateRange(userId, from, to)`. Pros: single-row read per PR; persisted from Plan 16-04. Cons: `is_pr` is global (across all history), not per-window; "PRs in this window" requires extra logic to filter "PR row whose `created_at` falls in the window AND was beaten by a row outside the window" - non-trivial. Rejects Option 2 unless the brief is reinterpreted as "all-time PRs" (it is not - the period section is the window scope).
    - Option 3: reuse `AnalyticsService.prs(userId)` and filter by `created_at` in window. Same problem as Option 2: `AnalyticsService.prs` returns all-time top-1-per-exercise; window filter would drop legitimate "PR set in this window even if all-time PR is older." Rejects.
  Verdict: Option 1. Add `ExportService.computePrsInWindow(sessions)` private static helper that walks the same `Map<UUID, ExerciseGroup>` already built in `toEntry` and tracks the highest Epley per exercise-and-window; emit `(exercise nameEn, weight_kg, reps_done, date)` for each. Cross-package: import `analytics/PrDetector.epleyOneRm` (cheap; PrDetector is in `com.workouthub.analytics` per Plan 16-02).

  Part B - Body metrics source-of-truth (closes Section 3 Part E `body_metrics[]` open verdict):
  Single option walked: `MetricsService.list(userId, period.from, period.to)` from Plan 17-03 returns `List<BodyMetricDto>`. Project to `(recordedDate, weightKg)` for the brief example. The brief shows `weight_kg` only; do NOT expose `body_fat_percent`, measurements, photo_url in the claude-summary (the LLM coach asks for trend; full data is available via `/api/export/full`). Verdict: Option 1 only. Cross-package: depend on `metrics/MetricsService` directly.

  Part C - Consistency source-of-truth (closes Section 3 Part E `consistency` open verdict):
  Three sub-fields:
    - `current_streak_days`: reuse `analytics/StreakCalculator.calculateStreak(...)` if it accepts a window or run a new call passing the relevant sessions list. Cite `StreakCalculator` line range in audit. If the existing API is "all-time streak," extend it OR re-derive from the `recent` sessions list local to `ExportService.buildSummary`. Verdict: re-derive locally (avoid coupling on a method whose semantic does not match the window scope).
    - `missed_days`: derive from the user's active plan's `day_of_week` set within the window minus the actual session dates. Cross-package: `WorkoutPlanRepository.findActiveByUserId`. Empty list if no active plan.
    - `missed_reasons`: NO source today. Three options:
      - Option C-1: emit `[]` always. Reduces brief example to illustrative-not-canonical for this field. Document in `docs/EXPORT_FORMAT.md`.
      - Option C-2: introduce V28 `missed_workouts(user_id, date, reason TEXT)` table + per-day "I skipped because..." UI affordance + endpoint. Phase-level scope; out of v0.4 budget.
      - Option C-3: drop `missed_reasons` from the DTO entirely. Shrinks the brief example but reduces wire-format expectation.
    Verdict: Option C-1 (emit `[]`). Document in `docs/EXPORT_FORMAT.md`. Mark Option C-2 as a Phase 31 (charts-stats) or v0.6 candidate. Open as ISSUES.md item (i-11) "missed_workouts table for skip reasons."

  Part D - Round-trip preservation gaps (extends Section 4 Part F):
  Five gaps cataloged:
    - `is_pr` round-trip drop (Section 4 Part F): NEW issue (i-10). Plan 18-04 candidate.
    - `BodyMetric.id` and `Supplement.id` not preserved on import: Acceptable for v0.4 since downstream FKs do not depend on these ids and round-trip semantics are "data-equal" not "byte-equal." Document in `docs/EXPORT_FORMAT.md`.
    - `exportedAt` timestamp drift: Acceptable (metadata, not data). Document.
    - `ImportValidator` does NOT pre-validate `session.workoutDayId` against the payload's `plans.days[].id` set: NEW validator rule recommendation. Plan 18-04 candidate (or fold into the same plan that touches `is_pr`).
    - `Supplement.timing` enum tolerance via `parseTiming(s)` -> `OTHER` on unknown: silent downgrade. Currently `ImportValidator.validateSupplements` warns but does not error. Acceptable; brief does not fix the timing vocabulary as a strict contract. Document.

  Part E - JsonConfig stance:
  Current `JacksonConfig.java:14-21` configures `JavaTimeModule`, `WRITE_DATES_AS_TIMESTAMPS=false`, `serializationInclusion=NON_NULL`. Does NOT set `PropertyNamingStrategies.SNAKE_CASE`. If Direction A or C from Section 4 Part A is chosen, the per-DTO `@JsonNaming` annotation is the surgical landing site (no global config change). Confirm via cross-search that no other DTO in the project uses `@JsonNaming` today (run `rg "@JsonNaming" backend/src` inside the audit-write step; if any pre-existing usage exists, document it as the precedent shape; if none, this audit declares the precedent for Phase 18).

  Section 6 - Recommended Phase 18 plan-02+ Scope:

  Bulleted list of concrete tasks plan 18-02+ should plan, each with: target files, expected change shape, and which test file picks up the regression coverage. Group by bucket:

  - `claude-summary-fields` - extend `ClaudeSummaryDto` with new top-level keys `prs`, `bodyMetrics`, `consistency` (camelCase if Direction C; snake_case via `@JsonNaming` if Direction A). Add `summary` subkeys `plannedWorkouts`, `adherencePercent`, `weightChangeKg`. Add `user.age`. Add `workouts[i].type`. Wire the existing `analytics/PrDetector` (Part A verdict), `metrics/MetricsService.list` (Part B verdict), `analytics/StreakCalculator` (Part C verdict), `workouts/WorkoutPlanRepository.findActiveByUserId`, `workouts/WorkoutDayRepository`-equivalent for the `type` derivation. Targets: `dto/ClaudeSummaryDto.java` (record-component growth from 4 to 7 top-level + 3 to 6 summary fields + 1 user field + 1 workouts field), `ExportService.java` (mappers for each new field, a `computePrsInWindow` helper, a `computeConsistency` helper), `ExportIntegrationTest.java` (new assertions for each new field + `prs`/`bodyMetrics`/`consistency` arrays). Depends on Section 3 Parts A-E and Section 5 Parts A-C verdicts.
  - `naming-convention` - apply Direction C: `@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)` on `ClaudeSummaryDto` ONLY; `FullExportDto`, `ImportResultDto`, `CsvImportResultDto` stay camelCase. Update `ExportIntegrationTest` assertions from `$.summary.totalWorkouts` to `$.summary.total_workouts` etc. Frontend `(app)/export/export-client` is unaffected (claude-summary is download-only on the UI; the JSON is server-rendered for paste). Targets: `dto/ClaudeSummaryDto.java` (1 class annotation), `ExportIntegrationTest.java` (~4 jsonPath flips). Depends on Section 4 Part A verdict.
  - `goals-array-shape` - split `UserSummary.goals` from a single `String` to a `List<String>` at claude-summary export time only. `FullExportDto.UserSection.goals` stays a `String` (matches SQL column shape). Delimiter: newline-or-semicolon (regex split); document in `docs/EXPORT_FORMAT.md`. Targets: `dto/ClaudeSummaryDto.java` (UserSummary.goals: `String` -> `List<String>`), `ExportService.java` (split + null-safe + filter-empty); test extension. Depends on Section 4 Part D Option (1) verdict.
  - `roundtrip-stability` - close the `is_pr` round-trip drop and the `workoutDayId` validator gap (NEW issue i-10). Add `Boolean isPr` to `FullExportDto.SetRow` (9th component); preserve on `FullExportService.toSessionSection` (cite `:148`); preserve on `FullImportService.insertSessions` (cite `:286-300`); call `SessionSetsService.recomputePrForExerciseHistory(userId, exerciseId)` for each distinct touched exercise at the END of `importDump` as a safety net. Add `ImportValidator.validateSessionDayIds(plans, sessions, errors)` rule. Targets: `dto/FullExportDto.java` (SetRow grows 8 to 9), `FullExportService.java` (1 line), `FullImportService.java` (1 set + 1 recompute call + 1 transaction-end hook), `ImportValidator.java` (1 new rule), `FullExportImportIntegrationTest.java` (2 new tests: `prSurvivesRoundTrip`, `importRejectsSessionWithUnknownWorkoutDayId`). Depends on Section 4 Part F + Section 5 Part D verdicts. NEW ISSUE i-10 candidate.
  - `defer` - explicit non-goals for v0.4: `missed_workouts` table (Section 5 Part C Option C-2, Phase 31 / v0.6 candidate; opens NEW ISSUE i-11), full snake_case migration of `FullExportDto` (Direction A; rejected because Direction C is sufficient), `BodyMetric.id`/`Supplement.id` stable preservation on import (acceptable per Section 5 Part D), `FullExportDto` schemaVersion bump to 2 (no schema-breaking changes in plan 18-02..18-04; `is_pr` addition is additive and absent-on-old-payload defaults to `false` which matches the V27 column default).

  For each `claude-summary-fields`, `naming-convention`, `goals-array-shape`, `roundtrip-stability` entry, write a one-paragraph rationale tied to a specific section verdict.

  Plan-count recommendation: how many plans (18-02, 18-03, ...) the hardening warrants given comprehensive depth (`config.json: depth: comprehensive`) and the Phase 14/15/16/17 precedent (one bug-shape per plan; cross-cutting touch in its own plan when it crosses package boundaries). Phase 18 has NO migration since `is_pr` already exists (V27) and no new tables are introduced.

  Suggested boundary template (the audit may revise):
  - Plan 18-02: claude-summary-fields (largest plan; closes the prs + body_metrics + consistency + summary subkeys + user.age + workouts.type gaps; ~3 file touches: `ClaudeSummaryDto.java`, `ExportService.java`, `ExportIntegrationTest.java`; ~6 new test assertions; cross-package reads from `analytics/`, `metrics/`, `workouts/`).
  - Plan 18-03: naming-convention (smallest plan; class-level annotation flip + 4 jsonPath updates; ~2 file touches).
  - Plan 18-04: roundtrip-stability (closes new issue i-10; touches `FullExportDto`, `FullExportService`, `FullImportService`, `ImportValidator`, `FullExportImportIntegrationTest`; ~5 file touches + 2 new tests; medium-sized plan).
  - Plan 18-05 (optional): goals-array-shape (smallest plan; either fold into 18-02 if scope budget allows or stand alone if 18-02 already exceeds 4 file touches).

  If `goals-array-shape` is folded into 18-02, mark it as "Plan 18-02 absorbs goals-array-shape" rather than its own plan slot.

  Constraint: do not edit any Java file. Do not propose architectural rewrites - the goal is the smallest set of changes that closes the gaps and respects the v0.4 budget.

  Final length check: target under 500 lines for the entire `18-01-AUDIT.md`. Trim verbose narrative; tables and ranked lists belong here, not prose.
  </action>
  <verify>
  - Section 5 Part A picks Option 1 with rationale rejecting Options 2 and 3.
  - Section 5 Part B confirms a single source-of-truth (Option 1) for `body_metrics[]`.
  - Section 5 Part C catalogs three sub-fields and picks Option C-1 (`missed_reasons: []`) with Option C-2 deferred to a NEW ISSUE i-11.
  - Section 5 Part D catalogs five round-trip gaps, marks `is_pr` as NEW ISSUE i-10.
  - Section 5 Part E confirms there is no pre-existing `@JsonNaming` precedent in the codebase OR documents the precedent if found.
  - Section 6 has one entry per applicable bucket; empty buckets are stated explicitly as "None - no findings" or "Deferred to Phase 31 / v0.6."
  - Plan-count recommendation is explicit: `Plan 18-02: ...`, `Plan 18-03: ...`, etc., each with a one-line objective.
  - Every `claude-summary-fields`, `naming-convention`, `goals-array-shape`, `roundtrip-stability` rationale paragraph references the section that produced the verdict.
  - Total `18-01-AUDIT.md` length is under 500 lines.
  - `git diff --stat` shows only `.planning/phases/18-export-refinement/18-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 5 and 6 give plan 18-02+ a file-level scope, an unambiguous naming-convention verdict (Direction C), an unambiguous PR-source verdict (Option 1), an unambiguous consistency-source verdict (Option C-1 with i-11 deferral), and explicit plan boundaries.
  </done>
</task>

</tasks>

<verification>
Before declaring the plan complete:
- [ ] `18-01-AUDIT.md` exists and contains Sections 1-6.
- [ ] Every controller method, service method, DTO record, repository query, and migration column referenced cites `file:line` (or migration line).
- [ ] Section 1 lists every file in `exports/` plus the cross-package repository touchpoint table.
- [ ] Section 2 has all 11 endpoints, all DTO record-component breakdowns (`ClaudeSummaryDto`, `FullExportDto`, `ImportResultDto`), and all 11 test methods.
- [ ] Section 3 Part A is a 7-row top-level matrix with `prs`, `body_metrics`, `consistency` as MISSING.
- [ ] Section 3 Parts B-E enumerate all subkey drift inside `user`, `summary`, `workouts[i]`, `workouts[i].exercises[j].sets[k]`, plus per-missing-key source-of-truth options.
- [ ] Section 4 Part A delivers a single naming-convention verdict (Direction C) with all three directions walked.
- [ ] Section 4 Parts B-F walk per-missing-key cost, summary subkey gaps, user.age + goals derivation, workouts.type derivation, and round-trip stability.
- [ ] Section 5 Parts A-E deliver source-of-truth verdicts for `prs`, `body_metrics`, `consistency`, round-trip preservation, JsonConfig stance.
- [ ] Section 6 buckets are concrete and either populated with file-level entries or stated as empty.
- [ ] Plan-count recommendation in Section 6 is explicit so plan 18-02+ structure is decided.
- [ ] Total length of `18-01-AUDIT.md` is under 500 lines.
- [ ] `git diff --stat` shows only `.planning/phases/18-export-refinement/` changes.
</verification>

<success_criteria>
- All three tasks complete.
- `18-01-AUDIT.md` is the single Java-free deliverable.
- Phase 18 plan-02+ has a concrete, file-level fix list bucketed by intent.
- Naming-convention direction (Direction A wholesale snake_case vs Direction B keep camelCase + doc vs Direction C split per-consumer) is settled with rationale.
- PRs source-of-truth (Option 1 PrDetector per-set scan vs Option 2 isPr persisted vs Option 3 AnalyticsService) is settled with rationale.
- Body metrics source-of-truth is settled (Option 1 MetricsService.list).
- Consistency source-of-truth is settled (Option C-1 missed_reasons:[] with i-11 deferral).
- Round-trip stability gap on `is_pr` is raised as NEW ISSUE i-10 candidate and bucketed in Plan 18-04.
- Plan-count for Phase 18 hardening (18-02..18-0N) is explicit.
- No Java sources, migrations, tests, or other planning files are modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/18-export-refinement/18-01-SUMMARY.md` with frontmatter and body following the Phase 17-01 audit summary shape:

---
phase: 18-export-refinement
plan: 01
subsystem: exports
tags: [audit, gap-analysis, contract-drift, cross-package-coupling, round-trip, naming-convention, planning]

requires:
  - phase: 17-body-metrics
    plan: 03
    reason: MetricsService.list(userId, from, to) is the body_metrics source-of-truth in Section 5 Part B
  - phase: 16-sessions-analytics
    plan: 04
    reason: SessionSet.isPr persistence and PrDetector.epleyOneRm are referenced for Section 4 Part F (is_pr round-trip drop) and Section 5 Part A (prs source-of-truth)
  - phase: 16-sessions-analytics
    plan: 02
    reason: PrDetector lives in com.workouthub.analytics post-extraction; cross-package import path
  - phase: 14-workouts-hardening
    plan: 02
    reason: i-2 closure was test-side; Section 4 Part F re-checks production-side round-trip stability

provides:
  - exports package inventory (14 source files + 4 DTOs + 3 test classes + repository touchpoint table)
  - endpoint + DTO catalog (11 endpoints, ClaudeSummaryDto + FullExportDto + ImportResultDto record-component breakdowns)
  - ProjectBrief example field-by-field gap matrix (top-level + user + summary + workouts + sets + missing-keys)
  - JSON property naming convention verdict (Direction A wholesale OR Direction B doc-only OR Direction C per-consumer)
  - PRs source-of-truth verdict (Option 1 PrDetector OR Option 2 isPr OR Option 3 AnalyticsService)
  - body_metrics source-of-truth (Option 1 MetricsService.list)
  - consistency source-of-truth (Option C-1 missed_reasons:[] with i-11 deferral)
  - round-trip stability re-check post-Plan-14-02 (is_pr drop surfaces as NEW ISSUE i-10 candidate)
  - recommended Phase 18 plan-02+ scope (claude-summary-fields / naming-convention / goals-array-shape / roundtrip-stability / defer buckets)
  - explicit plan boundaries with one-line objective per plan

affects:
  - 18-export-refinement/18-02
  - 18-export-refinement/18-03
  - 18-export-refinement/18-04 (or whatever the final plan-count resolves to)
  - 19-api-contract-docs (export wire-format documentation lands in docs/EXPORT_FORMAT.md before Phase 19)
  - ISSUES.md (i-10 round-trip is_pr drop, i-11 missed_workouts table - NEW issue candidates)

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/18-export-refinement/18-01-AUDIT.md
  modified: []

key-decisions:
  - "[Naming-convention verdict: Direction A wholesale snake_case OR Direction B keep camelCase + doc OR Direction C per-consumer @JsonNaming on ClaudeSummaryDto only]"
  - "[PRs source verdict: Option 1 PrDetector per-set scan OR Option 2 SessionSet.isPr persisted OR Option 3 AnalyticsService.prs(...)]"
  - "[Consistency source verdict: Option C-1 missed_reasons:[] OR Option C-2 V28 missed_workouts table OR Option C-3 drop missed_reasons]"
  - "[goals array shape verdict: Option 1 split-at-export OR Option 2 parallel column OR Option 3 V28 text[] migration]"
  - "[Round-trip is_pr drop verdict: raise as NEW issue i-10 and bucket in Plan 18-04 OR defer entirely]"
  - "[Plan-count for Phase 18 hardening: N plans (18-02..18-0N) with explicit boundaries]"
---

# Phase 18 Plan 01: export-refinement audit Summary

**[One-line audit outcome - what landed, what is now planning-ready]**

## Accomplishments
- Inventory complete: 14 exports source files + 4 DTOs + 3 test classes + repository touchpoint table.
- Endpoint + DTO catalog: 11 endpoints, 3 export DTOs with components enumerated, 11 tests cataloged.
- ProjectBrief example gap matrix: top-level (3 missing: prs, body_metrics, consistency), user (2 missing: age, goals-array-shape), summary (3 missing: planned_workouts, adherence_percent, weight_change_kg), workouts subkey drift cataloged.
- Naming-convention verdict: [Direction A / B / C] with rationale.
- PRs source-of-truth verdict: [Option 1 / 2 / 3] with rationale.
- Consistency source-of-truth verdict: [Option C-1 / C-2 / C-3] with rationale.
- Round-trip stability: post-Plan-14-02 re-check surfaces is_pr drop as NEW ISSUE i-10 candidate; missed_workouts table as NEW ISSUE i-11 candidate.
- Phase 18 plan-02+ scope: K claude-summary-fields, L naming-convention, M goals-array-shape, N roundtrip-stability, P defer.
- Plan-count recommendation: 18-02..18-0N with one-line objective each.

## Files Created/Modified
- `.planning/phases/18-export-refinement/18-01-AUDIT.md` - audit deliverable.

## Decisions Made
- Naming-convention direction.
- PRs source-of-truth.
- body_metrics source-of-truth.
- Consistency source-of-truth.
- goals array shape direction.
- Round-trip is_pr drop bucket.
- Phase 18 plan boundaries.

## Issues Encountered
- NEW ISSUE i-10 candidate: is_pr round-trip drop on full-export import path (post-Plan-16-04 surface).
- NEW ISSUE i-11 candidate: missed_workouts table absent (consistency.missed_reasons has no source).

## Next Phase Readiness
- Ready for Phase 18 plan-02+ planning: yes / no with reason.
- Open questions for plan 18-02+: list any.
</output>
</content>
</invoke>