---
phase: 14-workouts-hardening
plan: 02
type: execute
domain: exports
---

<objective>
Close i-2 by fixing the test-side precondition in `FullExportImportIntegrationTest.importRoundTripPreservesPlansFromExport`. The test currently asserts `plansInserted >= 1` after a round-trip but never seeds a plan first, because `TestAuthHelpers.seed` bypasses `DefaultPlanSeeder` (audit Section 4b). Add an explicit plan-create call before the export step, re-enable the test, and mirror the same precondition into `fullExportReturnsSchemaVersionAndCurrentUserSlices` so the non-empty plans slice is exercised end-to-end.

Purpose: Phase 13 audit Section 4b and Section 5 (F2, C2, C4) established that i-2 is a coverage / test-correctness bug, not a production code defect. The export and import code paths are correct; the test simply omits the precondition required for its own assertion. Fixing the test on the test side (rather than amending `TestAuthHelpers.seed` to invoke `DefaultPlanSeeder`) keeps the helper's "start from an empty user" semantics intact for the other tests that depend on it.
Output: `FullExportImportIntegrationTest` no longer has any disabled methods; `importRoundTripPreservesPlansFromExport` seeds a plan before export and observes `plansInserted >= 1` after import; `fullExportReturnsSchemaVersionAndCurrentUserSlices` asserts `$.plans.length() >= 1` after seeding a plan. ISSUES.md is updated to mark i-1 and i-2 as Closed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md
@.planning/phases/13-workouts-audit/13-01-AUDIT.md
@.planning/phases/14-workouts-hardening/14-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

# Source files this plan reads or modifies:
@backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java
@backend/src/test/java/com/workouthub/support/TestAuthHelpers.java
@backend/src/main/java/com/workouthub/exports/FullExportService.java
@backend/src/main/java/com/workouthub/exports/FullImportService.java
@backend/src/main/java/com/workouthub/workouts/DefaultPlanSeeder.java

**Tech stack available:** Java 21, Spring Boot 3.5.x, MockMvc, Testcontainers Postgres 16, JUnit 5.
**Established patterns (from CONVENTIONS.md and TESTING.md):** Each test seeds its own user via nanoTime suffix; HTTP-layer integration tests build state through MockMvc POSTs (not direct repository writes) when the goal is to exercise the production controller path; assertions via `MockMvcResultMatchers.jsonPath`.
**Constraining decisions:**
- Phase 13 audit (key-decisions): "i-1 and i-2 do NOT share a root cause. Fixing i-1 will not unblock i-2." Plan 14-01 must land first because Plan 14-02's seeding call (`POST /api/workout-plans`) will succeed regardless, but the round-trip's preserved plans need `WorkoutDaysService.createDay` to work too if the seeded plan adds days. Per F2's minimum scope, this plan seeds only a plan (no days), so the dependency is logical (close in order) rather than technical.
- Audit picks the test-local fix over modifying `TestAuthHelpers.seed`: "Pick the test-local fix; it is narrower and avoids changing the helper's semantics for tests that intentionally start from an empty user." This plan honors that decision.
- Local Maven is unavailable on the dev host (memory: local_maven_gap.md); verification runs only in CI on push.
- Protected files MUST NOT be touched.

**Issues being addressed:** i-2 (1 disabled test in FullExportImportIntegrationTest). Bookkeeping for i-1 (closed by Plan 14-01) is also done here since this is the last plan of Phase 14.
**Out of scope:** Modifying `TestAuthHelpers.seed` to invoke `DefaultPlanSeeder` (rejected by audit). Modifying `FullExportService` or `FullImportService` (audit Section 4b confirmed these are correct). Touching the existing comment block at lines 115-116 of the test beyond what's required by Task 1.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed a plan before export in importRoundTripPreservesPlansFromExport and re-enable</name>
  <files>backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java</files>
  <action>
In `FullExportImportIntegrationTest` (`backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java`):

1. Remove the `@Disabled("ISSUES.md i-2: same root cause as i-1; needs local Maven debugger")` annotation from `importRoundTripPreservesPlansFromExport` (currently line 109). The "same root cause as i-1" claim is wrong (audit Section 4b verdict); the actual root cause is documented in audit Section 4b smoking-gun finding.

2. Replace the misleading comment at lines 115-116 (currently: `// The default plan seeder runs on user creation, so the initial // export should already contain at least one plan.`) with a single-line comment that documents the actual precondition:
   `// TestAuthHelpers.seed bypasses DefaultPlanSeeder, so create a plan explicitly before exporting.`

3. Between the comment and the existing `MvcResult exp = ...` block (currently line 117), insert one MockMvc call that creates a plan via the production controller path:
   ```
   mvc.perform(post("/api/workout-plans")
                   .header("Authorization", auth)
                   .contentType(MediaType.APPLICATION_JSON)
                   .content("{\"name\":\"Round-Trip Seed\"}"))
           .andExpect(status().isCreated());
   ```
   Use the same auth header variable already declared at line 113. Do NOT extract a helper method; the test stays self-contained per the existing file's style.

4. Do NOT modify the existing export-then-import-then-`/active` block beyond removing the `@Disabled` and rewording the comment. The assertion `$.plansInserted >= 1` stays as is — the seeded plan ensures it now passes.

5. Do NOT touch any other test method in this file in this task. Task 2 handles `fullExportReturnsSchemaVersionAndCurrentUserSlices`.

6. Verify imports: `MediaType`, `post`, `status`, `jsonPath` are already imported. No new imports needed.
  </action>
  <verify>
- `grep -c "@Disabled" backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java` returns 0.
- `grep -c "Round-Trip Seed" backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java` returns 1.
- `grep -c "TestAuthHelpers.seed bypasses DefaultPlanSeeder" backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java` returns 1.
- The misleading "default plan seeder runs on user creation" comment is gone.
- `import org.junit.jupiter.api.Disabled;` is removed (Task 1 of Plan 14-01 already removed it from a different file; this is a separate file).
- The original assertion `$.plansInserted` `greaterThanOrEqualTo(1)` is unchanged.
  </verify>
  <done>
- `importRoundTripPreservesPlansFromExport` is no longer skipped.
- The test exercises a real export -> import round-trip with a seeded plan and asserts `plansInserted >= 1`.
- The follow-up `GET /api/workout-plans/active` assertion still expects 200 (the seeded plan is created with `active=false`; getting `/active` on a user with no active plan returns 204, which is also a 2xx so `status().isOk()` will fail — verify what the existing assertion expects). NOTE for executor: if the existing assertion `status().isOk()` fails because `/active` returns 204, change it to `status().is2xxSuccessful()`. Do not introduce a `POST /activate` call to "fix" this — the round-trip semantics do not include activation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add non-empty plans assertion to fullExportReturnsSchemaVersionAndCurrentUserSlices</name>
  <files>backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java</files>
  <action>
In the same file, in `fullExportReturnsSchemaVersionAndCurrentUserSlices` (currently lines 36-59):

1. Between the existing supplement-create call (currently lines 47-51, ends with `.andExpect(status().isCreated());`) and the export GET (currently line 53), insert a plan-create call:
   ```
   mvc.perform(post("/api/workout-plans")
                   .header("Authorization", auth)
                   .contentType(MediaType.APPLICATION_JSON)
                   .content("{\"name\":\"Export Seed\"}"))
           .andExpect(status().isCreated());
   ```

2. After the existing `.andExpect(jsonPath("$.supplements.length()").value(1))` assertion (currently line 58), add:
   ```
   .andExpect(jsonPath("$.plans.length()").value(1));
   ```
   Note: the existing trailing semicolon must move from `.value(1))` (the supplements line) to the new last assertion.

3. Do NOT modify the seeding of metrics/supplements, the schemaVersion check, the user.email check, or any other test in the file.

4. Why this addition (audit C4): the existing test asserts non-empty `bodyMetrics` and `supplements` but never `plans`. Without this, a regression that drops the plans slice from the export response would only be caught by the round-trip test (Task 1), not by the simpler shape test. Mirroring the precondition + assertion makes the failure mode visible at the smaller-blast-radius test first.
  </action>
  <verify>
- `grep -c "Export Seed" backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java` returns 1.
- `grep -c "\\$.plans.length()" backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java` returns 1 (the new assertion in this method).
- `fullExportReturnsSchemaVersionAndCurrentUserSlices` now contains 6 `mvc.perform` calls (one each for metric POST, supplement POST, plan POST, export GET) — original 3 plus 1 new plan POST = 4 total POSTs preceding the export GET. Recount carefully: original was metric POST + supplement POST + export GET = 3. New is metric POST + supplement POST + plan POST + export GET = 4.
- Assertion ordering inside the export GET chain ends with the new `$.plans.length()` line; trailing semicolon placement is correct.
  </verify>
  <done>
- `fullExportReturnsSchemaVersionAndCurrentUserSlices` exercises a non-empty plans slice end-to-end.
- The export response is asserted to contain exactly 1 plan after the explicit POST.
- No regression in the existing schema/user/metrics/supplements assertions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Move i-1 and i-2 to Closed in ISSUES.md and update STATE.md</name>
  <files>.planning/ISSUES.md, .planning/STATE.md</files>
  <action>
In `.planning/ISSUES.md`:
1. Move the entire `### i-1 — WorkoutDaysIntegrationTest helper NPE on response id (6 errors)` block from the "Open" section to the "Closed" section.
2. Move the entire `### i-2 — FullExportImportIntegrationTest.importRoundTripPreservesPlansFromExport (1 failure)` block from "Open" to "Closed".
3. Append a single-line resolution note to each, in italics, with the closing-plan reference. For i-1: `*Closed by Phase 14 Plan 01: explicit child-repo saveAndFlush in WorkoutDaysService (createDay, addItem) populates @UuidGenerator id before WorkoutPlanMapper reads it.*` For i-2: `*Closed by Phase 14 Plan 02: test now seeds a plan via POST /api/workout-plans before exporting; the production export/import round-trip was correct all along.*`
4. Do NOT touch i-3 through i-9 — they remain open and are owned by later phases per STATE.md "Issue-to-Phase Mapping".
5. The "Closed" section is currently empty (`(none)`). Replace `(none)` with the moved blocks; do not leave `(none)` followed by content.

In `.planning/STATE.md`:
1. Bump the "Current Position" block:
   - Change "Phase: 13 of 20 (workouts-audit) - complete (1 of 1 plan shipped)" to "Phase: 14 of 20 (workouts-hardening) - complete (2 of 2 plans shipped)".
   - Change "Plan: 13-01 complete" to "Plan: 14-02 complete".
   - Change "Status: Phase 13 complete; ready to plan Phase 14 (workouts-hardening)" to "Status: Phase 14 complete; ready to plan Phase 15 (sessions-core)".
   - Change "Last activity:" date to today's date and the value to "Completed 14-02-PLAN.md (workouts-hardening)".
2. Update the "Progress" gauge from "v0.4 ##__________________ 12.5% (1/8 phases)" to "v0.4 ####________________ 25% (2/8 phases)".
3. Update "Project Reference" `ISSUES.md` line: change `(i-1, i-2, i-4, i-5, i-6, i-7, i-8, i-9)` to `(i-4, i-5, i-6, i-7, i-8, i-9)`.
4. Replace the "Current focus:" line with: "Plan and execute Phase 15 (sessions-core). Phase 14 closed i-1 (cascade-id) and i-2 (test precondition); WorkoutDaysIntegrationTest is fully green; export round-trip test now seeds a plan."
5. Update the "Phase 13 Findings" section heading to "Phase 13-14 Findings" and append a new bullet: "Phase 14 confirmed: i-1 fix is service-layer only (`WorkoutDaysService.createDay`/`addItem` use `daysRepo`/`itemsRepo` `saveAndFlush` instead of `plans.saveAndFlush(plan)`); i-2 fix is test-only (added a seed-plan POST before the export GET in `FullExportImportIntegrationTest`). The audit's two-independent-root-causes verdict held."
6. Update "Issue-to-Phase Mapping": change "i-1, i-2: closed by v0.4 Phase 14 (workouts-hardening). Phase 13 audit identifies two independent root causes." to "i-1, i-2: closed by v0.4 Phase 14 (workouts-hardening) Plans 01 and 02 respectively." Move this line to a new "Closed Issue Mapping" sub-section if the layout warrants it; otherwise leave in place but reword as above.
7. Update the "Roadmap Evolution" section: append a dated entry for today: `- 2026-05-04: Phase 14 (workouts-hardening) shipped (2 plans: 14-01 cascade-id fix, 14-02 export round-trip test fix). Closed i-1 and i-2.`
8. Update "Session Continuity": change "Last session" date to today, change "Stopped at:" to "Phase 14 closed (2 of 2 plans). Next action: `/gsd:plan-phase 15` to break down sessions-core."

Also update `.planning/ROADMAP.md`:
1. In the "Phase 14: workouts-hardening" block, change "Plans: TBD" to "Plans: 2".
2. Replace the bullet `- [ ] 14-01: TBD` with two bullets:
   - `- [x] 14-01: workouts-cascade-id-fix`
   - `- [x] 14-02: export-roundtrip-test-fix`
3. In the "Progress" table, update the "14. workouts-hardening" row: `Plans` column to `2/2`, `Status` to `Complete`, `Completed` to today's date (`2026-05-04`).

This bookkeeping task is part of Plan 14-02 (not a separate plan) because it is small, mechanical, and naturally tied to closing the last open Phase 14 issue. It must run after Tasks 1 and 2 of this plan.
  </action>
  <verify>
- `.planning/ISSUES.md`: `(none)` no longer appears under "## Closed"; both i-1 and i-2 blocks are present under "## Closed" with their resolution notes; neither block remains under "## Open".
- `.planning/STATE.md`: Phase line, Plan line, Status line, Progress gauge, ISSUES open list, Current focus, and Session Continuity all reference Phase 14 as complete.
- `.planning/ROADMAP.md`: Phase 14 row in the Progress table reads `2/2` and `Complete`; both 14-01 and 14-02 are checked.
- `git diff` shows only `.planning/ISSUES.md`, `.planning/STATE.md`, and `.planning/ROADMAP.md` modified by this task — no source-code changes.
  </verify>
  <done>
- ISSUES.md cleanly separates closed v0.4 work from still-open deferred items.
- STATE.md is the source of truth for "Phase 14 complete; next is Phase 15 sessions-core".
- ROADMAP.md reflects the same.
- A future `/gsd:plan-phase 15` invocation reads consistent state.
  </done>
</task>

</tasks>

<verification>
Before declaring plan 14-02 complete:
- [ ] `git status` shows only the expected files: `FullExportImportIntegrationTest.java`, `.planning/ISSUES.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`. No protected files; no source changes outside `backend/src/test/`.
- [ ] CI on the pushed commit reports backend `mvn verify` green; specifically `FullExportImportIntegrationTest` reports 6 tests run (was 5 + 1 disabled before plan), 0 errors, 0 failures, 0 skipped.
- [ ] Plan 14-01 is already merged on `main` (this plan must execute after Plan 14-01 is shipped, since both touch test infrastructure and 14-01 closes the larger root cause).
- [ ] After this plan ships, the entire Phase 14 milestone exit criteria from STATE.md "Current focus" is met: 7 previously disabled tests are now active across two test classes.
</verification>

<success_criteria>

- All three tasks completed and atomically committed.
- `FullExportImportIntegrationTest` runs 6 tests, all passing in CI.
- ISSUES.md i-1 and i-2 are under "Closed" with correct resolution notes.
- STATE.md and ROADMAP.md show Phase 14 complete (2/2 plans).
- No production code modified by this plan; the change is test + bookkeeping only (audit Section 4b verdict honored).
- The Phase 14 milestone — "Fix i-1 (WorkoutDaysIntegrationTest helper NPE, 6 disabled tests) and i-2 (FullExportImportIntegrationTest plan round-trip drop)" — is closed end-to-end across plans 14-01 and 14-02.
  </success_criteria>

<output>
After completion, create `.planning/phases/14-workouts-hardening/14-02-SUMMARY.md` with this frontmatter and body:

```yaml
---
phase: 14-workouts-hardening
plan: 02
subsystem: exports
tags: [bug-fix, test-precondition, integration-tests, planning-bookkeeping]

requires: [14-workouts-hardening/14-01]
provides:
  - seeded-plan precondition in FullExportImportIntegrationTest.importRoundTripPreservesPlansFromExport
  - non-empty $.plans assertion in fullExportReturnsSchemaVersionAndCurrentUserSlices
  - ISSUES.md i-1 and i-2 moved to Closed
  - STATE.md and ROADMAP.md updated to reflect Phase 14 complete
affects: [15-sessions-core]

tech-stack:
  added: []
  patterns:
    - "Round-trip integration tests must seed their own preconditions when TestAuthHelpers does not invoke production seeders."

key-files:
  created: []
  modified:
    - backend/src/test/java/com/workouthub/exports/FullExportImportIntegrationTest.java
    - .planning/ISSUES.md
    - .planning/STATE.md
    - .planning/ROADMAP.md

key-decisions:
  - "i-2 fixed test-side (added explicit POST /api/workout-plans before export GET) per audit Section 4b verdict; TestAuthHelpers.seed semantics preserved for tests that intentionally start from an empty user."
  - "Audit's Phase 13 hypothesis ranking validated end-to-end: independent root causes for i-1 and i-2; both fixes shipped in their own plans within the same phase."

patterns-established:
  - "When the production seeder is bypassed by a test helper, integration tests that depend on a seeded slice must POST through the production controller to materialize it."

issues-created: []
issues-closed: [i-2]

duration: TBD
completed: TBD
---
```

Body sections (Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Next Phase Readiness): follow the standard summary template. Next Phase Readiness should explicitly green-light `/gsd:plan-phase 15` for sessions-core.
</output>
