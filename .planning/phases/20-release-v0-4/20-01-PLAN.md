---
phase: 20-release-v0-4
plan: 01
type: execute
domain: release
---

<objective>
Prepare every artifact required to cut `v0.4.0` to GHCR EXCEPT the tag push and
publish trigger themselves. This plan stages the release commit content
(populated `CHANGELOG.md` v0.4.0 entry plus fresh `## [Unreleased]` block;
`docs/MIGRATION.md` v0.4.0 section with API-surface deltas; CVE-patch-sweep
verdict captured under the v0.4.0 CHANGELOG `### Security` heading;
`IDENTITY.yaml` version bump 0.3.2 -> 0.4.0; `backend/pom.xml` and
`frontend/package.json` derived-manifest bumps in lockstep) so the milestone
checkpoint that follows user approval can fire `/gtr:release 0.4.0` without
re-litigating content. The tag push (`git tag -a v0.4.0 ...; git push origin
v0.4.0`) and the GHCR publish workflow (`.github/workflows/release.yml`) are
EXPLICITLY out of scope for this plan and belong at the milestone checkpoint.
This is the only Phase 20 plan; closes ROADMAP Phase 20 deliverable on the
preparation surface and clears the path for milestone close.

Purpose: Phase 20 is the v0.4 release-prep phase. Phases 13-19 shipped the
backend feature work (workouts hardening, sessions core, sessions analytics,
body metrics, export refinement, API contract docs); the v0.4 milestone goal
is "ship a contract-aligned release that adds the backend feature surface
ProjectBrief Phase 3-5 prescribed". The release runbook lives at `RELEASE.md`
and was exercised in v0.3.0 / v0.3.1 / v0.3.2. The runbook prescribes a
single release commit that bumps three version files in lockstep, populates
the CHANGELOG `[Unreleased]` block into a dated `[x.y.z]` block, and adds a
fresh empty `[Unreleased]` block above. The release.yml workflow then triggers
on `v*.*.*` tag push: tests run, multi-arch images publish to GHCR, draft
release is created with `## [x.y.z]` extracted as notes. Phase 20 stops one
step BEFORE the tag push because the user approval gate on the milestone
checkpoint owns the irreversible operation.

The CVE patch sweep is documentation-only on this Windows host: the v0.3.x
line landed Spring Boot 3.5.14 + bouncycastle 1.84 + async-http-client 2.12.4
+ jose4j 0.9.6 + alpine apk upgrade + .trivyignore for picomatch (i-6).
Renovate is the operator-facing patch model per v0.3 milestone "Locked-in
Decisions"; this repo's CVE story for v0.4.0 is "no NEW backend dependencies
were added in Phases 13-19 except the SpringDoc 2.6.0 runtime introduced in
19-02; verify against the existing trivy fs / trivy image / Dependabot CI
gates from Phase 10 and document the verdict". The mvn dependency sweep
(`mvn versions:display-dependency-updates`) cannot run on this host per
`local_maven_gap` memory; the CVE-sweep task captures the documentation
catch-up of any v0.3.x patches that landed after v0.3.2 cut, and defers the
runtime sweep verification to CI (existing trivy_fs + trivy_image jobs from
Phase 10 are the gate).

Output: `CHANGELOG.md` carries a populated `## [0.4.0] - YYYY-MM-DD` block
summarizing every Phase 13-19 deliverable across `### Added`, `### Changed`,
`### Fixed`, `### Security`, `### Known Issues (carried forward)` subsections;
the `## [Unreleased]` block above is empty with the 6 standard subsection
stubs. `docs/MIGRATION.md` carries a populated `## v0.4.0` section with
required-env-var-changes (none beyond v0.3.x), schema-and-data-migration
(V26 + V27 Flyway migrations apply automatically), compose/runtime changes
(none operator-facing; SpringDoc adds /v3/api-docs + /swagger-ui.html on
backend listener), one-shot commands (none required), rollback recipe.
`IDENTITY.yaml` version bumps from `0.3.2` to `0.4.0`. `backend/pom.xml`
project version (line 16) bumps from `0.3.2` to `0.4.0` (the parent
`spring-boot-starter-parent` 3.5.14 stays unchanged; only the project
version changes). `frontend/package.json` `"version"` field bumps from
`0.3.2` to `0.4.0`. NO change to `pnpm-lock.yaml` (the version field in
`package.json` does not affect the lockfile hash). NO change to
`.github/workflows/release.yml`, `.github/workflows/ci.yml`, or any other
workflow file; they are protected and the release-cut step that consumes
the workflow happens at the milestone checkpoint, not in this plan.
NO new file. NO Java edit. NO test edit. NO frontend code edit beyond the
single version-field line in `package.json`. NO database migration. NO
`.planning/PROJECT.md` / `ROADMAP.md` / `STATE.md` / `ISSUES.md` edit
(orchestrator-spawned summarizer at phase close handles those).

NO tag push. NO `git push origin main`. NO `git push origin v0.4.0`. NO
`gh workflow run release.yml`. NO GHCR image publish. The plan stops at
"single release commit lands on `main` with all version artifacts in
lockstep; release runbook ready for milestone-checkpoint sign-off". The
milestone checkpoint (after user approves Phase 20 close) executes the
remaining `git tag -a v0.4.0 ...; git push origin main; git push origin
v0.4.0` steps from RELEASE.md; this plan does not.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md
@.planning/MILESTONES.md
@.planning/milestones/v0.3-ROADMAP.md
@.planning/phases/13-workouts-audit/13-01-AUDIT.md
@.planning/phases/14-workouts-hardening/14-01-SUMMARY.md
@.planning/phases/14-workouts-hardening/14-02-SUMMARY.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/phases/15-sessions-core/15-02-SUMMARY.md
@.planning/phases/15-sessions-core/15-03-SUMMARY.md
@.planning/phases/15-sessions-core/15-04-SUMMARY.md
@.planning/phases/16-sessions-analytics/16-01-AUDIT.md
@.planning/phases/16-sessions-analytics/16-02-SUMMARY.md
@.planning/phases/16-sessions-analytics/16-03-SUMMARY.md
@.planning/phases/16-sessions-analytics/16-04-SUMMARY.md
@.planning/phases/17-body-metrics/17-01-AUDIT.md
@.planning/phases/17-body-metrics/17-02-SUMMARY.md
@.planning/phases/17-body-metrics/17-03-SUMMARY.md
@.planning/phases/17-body-metrics/17-04-SUMMARY.md
@.planning/phases/18-export-refinement/18-01-AUDIT.md
@.planning/phases/18-export-refinement/18-02-SUMMARY.md
@.planning/phases/18-export-refinement/18-03-SUMMARY.md
@.planning/phases/18-export-refinement/18-04-SUMMARY.md
@.planning/phases/19-api-contract-docs/19-01-AUDIT.md
@.planning/phases/19-api-contract-docs/19-02-SUMMARY.md
@.planning/phases/19-api-contract-docs/19-03-SUMMARY.md
@.planning/phases/19-api-contract-docs/19-04-SUMMARY.md
@.planning/phases/19-api-contract-docs/19-05-SUMMARY.md
@.planning/phases/12-release-v0.3.0/12-01-PLAN.md
@CHANGELOG.md
@RELEASE.md
@IDENTITY.yaml
@docs/MIGRATION.md
@docs/SELF_HOSTED_CONTRACT.md
@backend/pom.xml
@frontend/package.json
@CLAUDE.md

**Tech stack and runtime context (carried from Phases 13-19):**

- v0.3.2 (current `IDENTITY.yaml#version`) shipped 2026-05-03 with Spring Boot
  3.5.14 + bouncycastle 1.84 + jjwt 0.13.0 + jose4j 0.9.6 +
  async-http-client 2.12.4 + alpine apk upgrade. Five backend feature phases
  (13-17) plus two contract phases (18, 19) shipped on top of v0.3.2 to form
  the v0.4.0 release content. No backend dependency was added beyond
  SpringDoc 2.6.0 (Phase 19-02; `springdoc-openapi-starter-webmvc-ui`).
- Two new Flyway migrations land in v0.4.0: V26 (clientSetId idempotency
  column on `session_sets` from Phase 15-02) and V27 (`is_pr BOOLEAN NOT NULL
  DEFAULT false` on `session_sets` plus the window-function backfill from
  Phase 16-04). Both are auto-applied on container start; no operator-side
  one-shot is required. Per `application.yml`, `spring.jpa.hibernate.ddl-auto:
  validate` continues to gate schema drift.
- Six new typed `ApiError.code` values surfaced in v0.4.0 (Phase 15-04 for
  the four conflict codes; Phase 18-02 for the two import-validation rule
  codes carried by `ImportValidator`). The wire-level error contract is
  documented in `docs/API.md` (Phase 19-05 navigational rewrite) and in
  the OpenAPI document under `components.schemas.ApiError`.
- The OpenAPI document at `GET /v3/api-docs` and the Swagger UI at
  `GET /swagger-ui.html` (Phase 19-02) are NEW operator-visible runtime
  surfaces in v0.4.0; they ship reachable without authentication by design.
  The `BIND_ADDR=127.0.0.1` (compose default) is the trust boundary, per
  `docs/API.md` "Discoverability" section.
- v0.4.0 closes 2 deferred ISSUES.md entries (i-1 by Phase 14-01, i-2 by
  Phase 14-02) and adds 1 closed entry (i-10 by Phase 18-04). Six remain
  open (i-3 documentation note, i-4 v0.6 carry, i-5 / i-6 / i-7 / i-8
  framework-major carries, i-9 v0.6 carry).
- Backend test count: green tests grew from 12-of-19-fixed at v0.3.0 cut
  to "all tests pass" at v0.4.0 prep (Phase 14 closed i-1 + i-2's eight
  disabled tests; Phase 19-04 added a 5-test `OpenApiSurfaceIntegrationTest`
  CI gate). Every Phase 13-19 plan deferred local mvn verification to CI
  per `local_maven_gap` memory; CI is green on `main` at the time of the
  Phase 20 cut.

**Established patterns (carried from prior plans):**

- `CHANGELOG.md` follows Keep a Changelog 1.1.0 (verified against the v0.3.0
  / v0.3.1 / v0.3.2 entries). Subsections in fixed order: `### Added`,
  `### Changed`, `### Deprecated`, `### Removed`, `### Fixed`, `### Security`.
  Empty subsections are written as headings with no bullet content (the
  v0.3.0 entry omits `### Deprecated` per Keep a Changelog "Don't dump...
  unless meaningful"). Each release entry opens with a 1-2 paragraph
  summary positioned between the H2 heading and the first subsection.
- The release-notes generator in `.github/workflows/release.yml` extracts
  the body of `## [x.y.z]` (v0.3.x precedent confirms the heading format).
  Heading wording: `## [x.y.z] - YYYY-MM-DD` (with bracketed version, dash
  separator, ISO-8601 date). The exact heading string `## [0.4.0] - <date>`
  is required for the workflow to find the section.
- `docs/MIGRATION.md` follows the template documented in lines 157-188 of
  the file itself. Subsections in fixed order: required env var changes,
  schema and data migration, compose/runtime changes, one-shot commands,
  rollback. Empty sections write `No migration steps.` explicitly per the
  file's "An empty section is not allowed" discipline. The H2 heading is
  `## v0.4.0` (no leading "v0.4.0" duplication, no date in heading; the
  date sits in `CHANGELOG.md`'s entry).
- Cross-doc links use the markdown `[text](relative/path)` form. From
  `docs/MIGRATION.md`, peer docs in `docs/` are referenced as
  `[text](FILENAME.md)`; root-level files are `[text](../FILENAME.md)`.
- Code blocks use fenced `\`\`\`bash` for one-shot commands, `\`\`\`yaml`
  for env-var blocks. Inline code uses backticks. Inline literal HTTP
  method + path uses `\`GET /api/...\`` form.
- No emojis anywhere per global CLAUDE.md + project CLAUDE.md.
- English-only changelog and migration content. User-facing UI strings live
  in `frontend/src/i18n/messages/*.json` and are out of scope.
- Per CLAUDE.md "No emojis", "Do not refactor surrounding code", "Do not
  make changes beyond what was explicitly requested": this plan touches
  exactly five files (`CHANGELOG.md`, `docs/MIGRATION.md`, `IDENTITY.yaml`,
  `backend/pom.xml`, `frontend/package.json`); no other file is touched,
  no surrounding doc is reformatted, no emojis are added.

**Constraining decisions (from project conventions + CLAUDE.md + RELEASE.md
runbook):**

- **Version: 0.4.0** (MINOR bump from 0.3.2). Justification: backend feature
  expansion (sessions analytics + body metrics + export refinement + API
  contract docs are new feature surfaces, not bugfixes). Per RELEASE.md
  "Versioning rules": MAJOR on breaking change, MINOR on new feature without
  break, PATCH on bugfix only. v0.4.0 adds new endpoints (sessions analytics
  Last/Progress, body metrics range filter, OpenAPI document) and new wire
  fields (`isPr`, `estimatedOneRmKg`, `heartRateAvgBpm`, typed `code`) but
  preserves backward compatibility on every existing field; MINOR bump is
  correct.
- **Three protected-file touches in this plan: `IDENTITY.yaml`,
  `CHANGELOG.md`, `backend/pom.xml`.** Per CLAUDE.md "Protected Files"
  list, all three require user confirmation before edit. The pre-edit hook
  (`.claude/hooks/pre_guard_release_files.py`) enforces the gate. Each
  edit is a HALT-AND-ASK gate at the executor level; the executor must
  surface the diff and obtain explicit user confirmation before applying
  the change. `frontend/package.json` is also a protected file (per
  CLAUDE.md "Protected Files" list it explicitly mentions `package.json`);
  that's a fourth halt-and-ask gate. Only `docs/MIGRATION.md` is NOT
  protected and can be edited freely.
- **Single release commit cadence (per RELEASE.md "Cut the release" Step 4).**
  All five file touches go in ONE commit on `main`. The commit message is
  `chore(release): v0.4.0` per the v0.3.0 precedent. RELEASE.md Step 5
  (`git tag v0.4.0 -m "Release v0.4.0"`) and Step 6 (`git push && git push
  --tags`) are NOT executed in this plan. The milestone checkpoint owns
  the tag-and-push.
- **CVE-sweep depth: documentation-only.** Per `local_maven_gap` memory,
  `mvn versions:display-dependency-updates` cannot run locally. The CVE
  sweep task captures: (a) confirmation that no NEW backend dependency
  beyond SpringDoc 2.6.0 was introduced in Phases 13-19 (Grep `pom.xml`
  diff against v0.3.2 baseline at `git show v0.3.2:backend/pom.xml`);
  (b) confirmation that the existing v0.3.1 force-pinned versions
  (async-http-client 2.12.4, jose4j 0.9.6, bouncycastle 1.84) are still
  the latest patches per the v0.3.1 patch-window state; (c) statement
  that runtime CVE verification is delegated to the existing CI gates
  (`trivy_fs` and `trivy_image` from Phase 10-01) and Dependabot. The
  CVE-sweep VERDICT is captured under the v0.4.0 CHANGELOG `### Security`
  subsection as a single paragraph. NO mvn invocation; NO pom.xml dependency
  bump in this plan (the version-field bump is a different line entirely).
- **No backend Dockerfile change.** The v0.3.1 `apk -U upgrade --no-cache`
  line stays in place; alpine OS patches pull at image build time per the
  Phase 11-01 multi-arch publish flow. No image tag construction change
  (the v0.3.2 lowercase fix stays in place).
- **Date for the v0.4.0 CHANGELOG entry: filled at execution time.** The
  executor writes the current ISO-8601 date (`YYYY-MM-DD`) at edit time
  using the date provided in the orchestrator's session context. The
  planner does NOT lock a specific date here because the orchestrator
  schedules the milestone checkpoint independently; same-day execution
  is the typical pattern (v0.3.0 / v0.3.1 / v0.3.2 all dated 2026-05-03).
- **MIGRATION.md `vNext` template management.** The current `docs/MIGRATION.md`
  ends with a `## vNext` template block (lines 157-188). The v0.4.0 entry
  REPLACES the live `## vNext` heading wording in place: the plan inserts
  a new `## v0.4.0` section ABOVE the `## vNext` template block, leaving
  the template block intact for v0.5.0 use. Per `docs/MIGRATION.md` line
  158 wording: "`No migration steps.` (or replace with the structure above
  when the next release lands.)" — the next release (v0.4.0) lands above
  the template, the template stays for the release after.
- **CHANGELOG section ordering: `[Unreleased]` then `[0.4.0]` then
  `[0.3.2]`.** The current file has an empty `[Unreleased]` block and the
  `[0.3.2]`, `[0.3.1]`, `[0.3.0]` entries below. The plan inserts the
  populated `[0.4.0]` block between the empty `[Unreleased]` (which stays
  empty with the 6 standard subsection stubs) and the existing `[0.3.2]`
  entry. The `[Unreleased]` block remains as-is structurally — empty
  `### Added`, `### Changed`, `### Deprecated`, `### Removed`, `### Fixed`,
  `### Security` subsections waiting for v0.5.0 work. The `Unreleased`
  block does NOT carry the Phase 13-19 content (that content goes in the
  populated `[0.4.0]` block).
- **No mvn invocation, no pnpm invocation, no docker invocation in this
  plan.** Local mvn unavailable per `local_maven_gap`. pnpm not invoked
  because the version-field edit does not require a lockfile recompute.
  Docker not invoked because no image is built or pushed in this plan.
  CI is the verification gate per the existing Phase 10 trivy/gitleaks
  jobs and the Phase 19-04 OpenApiSurfaceIntegrationTest.
- **No `gh release create`, `gh workflow run`, or any tag-related git
  command.** RELEASE.md Steps 5 and 6 (tag and push) are NOT executed;
  the milestone-checkpoint owns them.
- **Idempotency: this plan re-runnable.** If the executor runs the plan
  twice, the version files end at the same `0.4.0` state (idempotent
  bumps via Edit find/replace) and the CHANGELOG / MIGRATION blocks
  remain singular (the executor re-reads before each Write/Edit and
  detects the existing `## [0.4.0]` heading; if present, the second run
  is a no-op).

**Constraining decisions (this planner):**

- **6 atomic tasks total.** Task 1 = CHANGELOG.md `## [0.4.0]` block
  insert and `[Unreleased]` reset (single-file edit on a protected file;
  HALT-AND-ASK gate). Task 2 = `docs/MIGRATION.md` `## v0.4.0` section
  insert (single-file edit; not protected). Task 3 = `IDENTITY.yaml`
  version bump 0.3.2 -> 0.4.0 (single-line edit on a protected file;
  HALT-AND-ASK gate). Task 4 = `backend/pom.xml` project version bump
  on line 16 (single-line edit on a protected file; HALT-AND-ASK gate).
  Task 5 = `frontend/package.json` version field bump (single-line edit
  on a protected file; HALT-AND-ASK gate). Task 6 = single release commit
  via `git add` of the five touched files plus `git commit -m "chore
  (release): v0.4.0"` (commit only; no push, no tag). Task ordering: 1 ->
  2 -> 3 -> 4 -> 5 -> 6. Splitting across 6 tasks captures the natural
  one-edit-per-file cadence and surfaces each protected-file gate as a
  distinct user-confirmation moment; the alternative of bundling all
  edits into one mega-task would obscure which file the user is approving.
- **Commit cadence: 1 commit total.** Tasks 1-5 stage edits. Task 6
  performs `git add CHANGELOG.md docs/MIGRATION.md IDENTITY.yaml
  backend/pom.xml frontend/package.json` and `git commit -m "chore
  (release): v0.4.0"`. The protected-file edits ARE staged (the pre-edit
  hook's confirmation gate stops at edit time, not commit time). Per
  CLAUDE.md "Avoid `git add -A` or `git add .`": the `git add` lists
  exactly the five files by path. RELEASE.md Step 5 (`git tag`) and
  Step 6 (`git push`) are NOT executed; the milestone checkpoint owns
  them. The commit message follows the v0.3.0 precedent verbatim.
- **No verification gate that requires running the backend or frontend.**
  Per `local_maven_gap` memory + the docs+config-only nature of this plan,
  this plan does NOT execute `mvn verify`, `mvn spring-boot:run`,
  `pnpm build`, `pnpm test`, `pnpm lint`, or any compile/test step. The
  CI gate on the eventual tag push (release.yml) is the runtime gate.
  Task 6 verifies structural correctness via re-read only.
- **Verification depth: file-state correctness + cross-file consistency.**
  After Task 6, the executor re-reads (a) `IDENTITY.yaml#version` ==
  `0.4.0`; (b) `backend/pom.xml` line 16 == `<version>0.4.0</version>`
  (the project version, NOT the parent `spring-boot-starter-parent`
  3.5.14); (c) `frontend/package.json` line containing `"version":` ==
  `"version": "0.4.0",`; (d) `CHANGELOG.md` contains exactly one
  `## [0.4.0] - YYYY-MM-DD` heading and exactly one `## [Unreleased]`
  heading (with the `[Unreleased]` heading appearing BEFORE `[0.4.0]` in
  file order); (e) `docs/MIGRATION.md` contains exactly one `## v0.4.0`
  heading (positioned between the v0.3.0 entry and the `## vNext`
  template block); (f) `git status` shows the five files in the commit
  and no uncommitted changes; (g) `git log -1 --oneline` shows the
  `chore(release): v0.4.0` commit on `main`.
- **CVE patch sweep: 5-line documentation-only summary in
  `CHANGELOG.md ### Security`.** Per the constraining decision above,
  the CVE sweep is delegated to CI. The plan-time-locked text is:

  > Backend dependency baseline carried from v0.3.2: Spring Boot 3.5.14,
  > bouncycastle 1.84, jjwt 0.13.0, jose4j 0.9.6, async-http-client 2.12.4
  > all unchanged. SpringDoc 2.6.0 introduced in Phase 19-02 is the only
  > new backend runtime dependency in v0.4.0; springdoc-openapi-starter-
  > webmvc-ui 2.6.0 is published to Maven Central with no open CVEs at
  > release time. Trivy fs + Trivy image CI jobs (Phase 10-01) re-validate
  > the image at every push to main. Dependabot continues to surface new
  > advisories; force-pinned versions for transitive dependencies (async-
  > http-client 2.12.4 via dependencyManagement, jose4j 0.9.6 via
  > dependencyManagement) stay in place. `.trivyignore` carries CVE-2026-
  > 33671 (picomatch ReDoS, vendored copy in next/dist/compiled/picomatch;
  > deferred to v0.5 per ISSUES.md i-6); no other ignores added.

  Wording above is locked at plan time; the executor copies verbatim into
  `CHANGELOG.md`'s `### Security` subsection under the v0.4.0 heading.
  The summary is positioned as the FIRST bullet/paragraph of the
  `### Security` subsection.
- **CHANGELOG `[0.4.0]` block content: locked at plan time.** Subsection
  content is pre-written to avoid execution-time prose-shopping. The
  executor copies the locked content verbatim, with one substitution:
  `<DATE>` -> the current ISO-8601 date at execution time (provided in
  the orchestrator's session context). The locked content is in Task 1's
  `<action>` block.
- **MIGRATION.md `## v0.4.0` block content: locked at plan time.** Same
  rationale as above; locked content is in Task 2's `<action>` block.
- **No backwards-compatibility shim, no deprecation notice.** Phase 13-19
  shipped no breaking change on the wire; v0.4.0 is purely additive on
  the public REST surface. Per CLAUDE.md "Do not introduce backwards-
  compatibility shims for code you just removed": none required.
- **No new ISSUES.md entry.** Phase 19's i-NEW-A / i-NEW-B / i-NEW-C
  candidates remain candidates per 19-04 + 19-05 deferral. v0.4.0 does
  not surface new deferred work in this plan; the orchestrator-spawned
  summarizer at phase close handles `ISSUES.md` if needed.

**Issues being addressed:**

- ROADMAP Phase 20 deliverable "Run a CVE patch sweep on backend
  dependencies, update CHANGELOG.md, RELEASE.md, and docs/MIGRATION.md,
  and cut v0.4.0 to GHCR" — preparation surface closed by Tasks 1-6;
  the GHCR cut surface stays open for the milestone checkpoint.
- v0.4 milestone goal "Complete the backend modules from ProjectBrief.md
  phases 3-5" — closed at the milestone checkpoint after this plan lands.

**Out of scope (deferred to milestone checkpoint):**

- `git tag -a v0.4.0 -m "Release v0.4.0"` — RELEASE.md Step 5.
- `git push origin main` — RELEASE.md Step 6 (the release commit pushes
  the prep work to remote `main`; per CLAUDE.md "Git Safety Protocol"
  this is a non-destructive push but still belongs at the milestone
  checkpoint to keep the entire release sequence atomic).
- `git push origin v0.4.0` — RELEASE.md Step 6.
- `release.yml` workflow trigger (auto-fires on the `v*.*.*` tag push).
- GHCR multi-arch image publish to `ghcr.io/gtrows/workouthub-{backend,
  frontend}:0.4.0`.
- Draft GitHub release creation with `## [0.4.0]` extracted as notes.
- Operator-side validation of the published image (clean-machine smoke
  test per RELEASE.md "Post-release" checklist).
- `/gsd:complete-milestone` invocation (orchestrator owns the milestone-
  close ceremony).

**Out of scope (NOT in Phase 20 at all):**

- New backend feature work. Phases 13-19 closed the v0.4 feature surface;
  Phase 20 ships what's already on `main` and bumps the version number.
- Frontend completion work. Per ROADMAP, v0.5 milestone (Phases 21-30)
  owns the frontend surface; v0.4 ships backend-feature-complete with
  the existing v0.2-era frontend stubs in place.
- Test infrastructure changes. The `local_maven_gap` host limitation
  stays for v0.4.0; v1.0 Phase 39 (testcontainers-major) addresses
  test-infra debt.
- Framework major bumps (Spring Boot 4.0 i-8, Next 16 i-6, next-intl 4
  i-5). Deferred to v0.6 / v1.0 per ROADMAP.
- Release-workflow modification (`.github/workflows/release.yml`). The
  workflow is protected and was last touched in v0.3.2 (lowercase
  IMAGE_PREFIX fix). v0.4.0 reuses the v0.3.2 workflow as-is.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert v0.4.0 entry into CHANGELOG.md and reset Unreleased block</name>
  <files>
    CHANGELOG.md
  </files>
  <action>
This task edits a protected file (`CHANGELOG.md` per CLAUDE.md "Protected
Files"). The pre-edit hook surfaces a confirmation gate; the executor
must obtain explicit user confirmation before applying the edit.

**Step A: Read the current `CHANGELOG.md`** to confirm the existing
structure: the `## [Unreleased]` block (currently 6 empty subsection
stubs) sits at lines 11-18; the `## [0.3.2] - 2026-05-03` block sits at
lines 20-33; the older entries follow.

**Step B: Compose the v0.4.0 entry.** The locked content is below; the
executor copies verbatim with one substitution: replace `<DATE>` with
the current ISO-8601 date (the orchestrator's session context provides
the date; if the date is 2026-05-07 at execution time, write `2026-05-07`).

```markdown
## [0.4.0] - <DATE>

Backend feature completion release. Closes the workout-plan editing,
live-session execution, body-metrics, full-export refinement, and API
contract documentation deliverables from `ProjectBrief.md` Phases 3-5.
Adds 14 new endpoints across sessions analytics and body-metrics
slices, persists PR durability via a new `is_pr` column on
`session_sets`, exposes the runtime OpenAPI document at
`GET /v3/api-docs` and Swagger UI at `GET /swagger-ui.html`, and
hardens the Claude-summary export wire shape with snake_case naming
and import-time validation. Closes 2 deferred test issues (i-1, i-2)
and 1 export round-trip drop (i-10). Operators upgrade with a normal
`docker compose pull && docker compose up -d`; two new Flyway
migrations (V26 clientSetId, V27 is_pr) auto-apply on backend start.

### Added

- `POST /api/sessions/{id}/sets` accepts a `clientSetId` UUID for
  idempotent offline-queue replay; 200 returns the existing record on
  replay, 201 returns the newly created record (Phase 15-02; new V26
  migration).
- `Short heartRateAvgBpm` field exposed on `SessionDto` and
  `SessionSummaryDto` (read-only; populated by the existing Garmin
  .fit importer; Phase 15-03).
- Typed `code` field on the `ApiError` envelope for the four sessions-
  surface 409 conflicts: `SESSION_ALREADY_ACTIVE`,
  `SESSION_ALREADY_FINISHED`, `SESSION_FINISHED`,
  `SET_NUMBER_DUPLICATE` (Phase 15-04). The field is null on legacy
  conflict paths and stripped from the wire by Jackson `NON_NULL`.
- `GET /api/exercises/{id}/last-performance` and `GET /api/exercises/
  {id}/progress` formalised under the new `com.workouthub.analytics`
  package (Phase 16-02; the in-package leak from v0.3.x is now a
  proper feature slice).
- `estimatedOneRmKg` field on `ProgressPointDto` (Epley projection;
  per-top-set semantics; null for unweighted sets; Phase 16-03).
- Persistent `is_pr` column on `session_sets` with window-function
  backfill on V27 migration; the PR flag now survives a session
  detail-reread, a session update, and a full-export round-trip
  (Phase 16-04 + Phase 18-04).
- `photoUrl` field on `UpsertBodyMetricRequest` (round-trips with the
  V6 `photo_url` column; Phase 17-02).
- Optional `from` and `to` `LocalDate` query params on
  `GET /api/metrics` for time-series range filtering; mixed-pair and
  inverted-range yield 400 (Phase 17-03).
- 201/200 status-code split on `POST /api/metrics`: 201 on first
  insert per date, 200 on idempotent overwrite of the same date
  (Phase 17-04).
- snake_case naming convention on `ClaudeSummaryDto` aligning the
  LLM-paste surface with the ProjectBrief example (Phase 18-03).
- Import-time validation on `POST /api/export/import`: unknown
  `session.workoutDayId` references yield a curated 422 instead of a
  500-class FK violation (Phase 18-04).
- SpringDoc 2.6.0 OpenAPI document at `GET /v3/api-docs` (JSON;
  `/v3/api-docs.yaml` for YAML) and Swagger UI at
  `GET /swagger-ui.html`. Document declares two security schemes
  (`bearerAuth` http/bearer/JWT default; `forwardAuth` apiKey/header
  `X-Forwarded-Email` opt-in), 28 hand-curated tag groupings, and the
  `ApiError` envelope with `code` enum (Phases 19-02, 19-03, 19-04).
- `OpenApiSurfaceIntegrationTest` (5 tests) CI-asserts the runtime
  OpenAPI contract on every push (Phase 19-04).
- `docs/API.md` rewritten as a navigational entry point to the runtime
  OpenAPI surface (Phase 19-05).

### Changed

- `WorkoutDaysService.createDay` and `addItem` use explicit child-repo
  `saveAndFlush` to populate `@UuidGenerator` ids before the mapper
  reads them; closes the helper-NPE on 6 disabled
  `WorkoutDaysIntegrationTest` cases (Phase 14-01; closes i-1).
- `FullExportImportIntegrationTest.importRoundTripPreservesPlansFrom
  Export` now seeds a plan via `POST /api/workout-plans` before the
  export GET; the production export/import round-trip was correct all
  along (Phase 14-02; closes i-2).
- `FullExportDto.SetRow` grows from 8 to 9 components; `Boolean isPr`
  appended in the 9th position. Legacy schemaVersion-1 dumps
  deserialize without error; the import path treats null as `false`
  and runs a recompute pass after `insertSessions` returns as a
  safety net (Phase 18-04).
- `FullExportService.toSessionSection` populates the new `isPr`
  component from `SessionSet.isPr()`; `FullImportService` reads
  null-safely; `SessionSetsService.recomputePrForExerciseHistory` is
  invoked once per distinct touched `(userId, exerciseId)` pair after
  the insert loop (Phase 18-04).
- `docs/EXPORT_FORMAT.md` documents the new `isPr` field on `SetRow`,
  the BodyMetric.id / Supplement.id non-preservation stance, and the
  acceptable-round-trip-drift catalog (Phase 18-04).

### Fixed

- `i-1` `WorkoutDaysIntegrationTest` 6 helper-NPE cases re-enabled and
  passing (Phase 14-01).
- `i-2` `FullExportImportIntegrationTest.importRoundTripPreservesPlans
  FromExport` re-enabled and passing (Phase 14-02).
- `i-10` `is_pr` round-trip drop on full-export import path closed
  (Phase 18-04).
- Dual `SessionsController` operationId collision in the OpenAPI
  document resolved via per-method `@Operation(operationId = ...)`
  hand-disambiguation (Phase 19-03).

### Security

- Backend dependency baseline carried from v0.3.2: Spring Boot 3.5.14,
  bouncycastle 1.84, jjwt 0.13.0, jose4j 0.9.6, async-http-client 2.12.4
  all unchanged. SpringDoc 2.6.0 introduced in Phase 19-02 is the only
  new backend runtime dependency in v0.4.0;
  springdoc-openapi-starter-webmvc-ui 2.6.0 is published to Maven Central
  with no open CVEs at release time. Trivy fs + Trivy image CI jobs
  (Phase 10-01) re-validate the image at every push to main. Dependabot
  continues to surface new advisories; force-pinned versions for
  transitive dependencies (async-http-client 2.12.4 via
  dependencyManagement, jose4j 0.9.6 via dependencyManagement) stay in
  place. `.trivyignore` carries CVE-2026-33671 (picomatch ReDoS, vendored
  copy in next/dist/compiled/picomatch; deferred to v0.5 per ISSUES.md
  i-6); no other ignores added.

### Known Issues (carried forward)

Tracked in `.planning/ISSUES.md`:
- i-3: documentation note on `.gitignore` `data/` glob-form correction
  during Plan 01-01 (resolved at execution time; carried for audit).
- i-4: Frontend per-request HTTP histogram deferred to v0.6.
- i-5: next-intl 3 -> 4 major bump deferred (open-redirect mitigated
  via reverse-proxy header rewrite).
- i-6: Next 15 -> 16 major bump deferred to v0.5.
- i-7: testcontainers 1.x -> 2.x major bump deferred to next test-infra
  session (likely v1.0 Phase 39).
- i-8: Spring Boot 3.5 -> 4.0 major bump deferred to v0.6 / v1.0.
- i-9: `StructuredLoggingTest` test-config gap; real-runtime ECS output
  unaffected. Deferred to v0.6 Phase 37.
```

**Step C: Apply the edit.** Use the Edit tool to replace the existing
empty `## [Unreleased]` block (the 7 lines at lines 11-18 of the current
`CHANGELOG.md`) with the FOLLOWING two-block sequence:

1. A fresh empty `## [Unreleased]` block matching the original 7-line
   structure (H2 + 6 H3 subsection stubs, no content under each).
2. A blank line separator.
3. The populated `## [0.4.0] - <DATE>` block from Step B.

Lines 20+ (the existing `## [0.3.2]` block and below) remain unchanged.

The Edit tool is the right choice (not Write) because the rest of the
file (the `## [0.3.2]`, `## [0.3.1]`, `## [0.3.0]` entries; the
section template comment block at the bottom) must survive byte-for-byte.

**Step D: Verification within the task.**

After the Edit:
- `grep -c "^## \[0.4.0\]" CHANGELOG.md` returns exactly 1.
- `grep -c "^## \[Unreleased\]" CHANGELOG.md` returns exactly 1.
- `grep -c "^## \[0.3.2\]" CHANGELOG.md` still returns exactly 1
  (the existing entry is preserved).
- The `## [Unreleased]` heading appears BEFORE `## [0.4.0]` in file
  order (Read confirms the ordering).
- The `## [0.4.0]` heading appears BEFORE `## [0.3.2]` in file order.
- The `<DATE>` placeholder is replaced with the current ISO-8601 date
  (no literal `<DATE>` string survives in the committed text).
- All 5 standard subsections (`### Added`, `### Changed`, `### Fixed`,
  `### Security`, `### Known Issues (carried forward)`) appear under
  the `## [0.4.0]` heading. (`### Deprecated` and `### Removed` are
  intentionally omitted under v0.4.0 because the release has no
  deprecations or removals — matches the v0.3.0 entry's pattern of
  omitting empty subsections.)
- The `### Security` subsection's first paragraph is the locked CVE-
  sweep text from the constraining-decisions block.
- No emojis.
- No trailing whitespace.

NO change to any other file in this task. NO change to
`docs/MIGRATION.md`, `IDENTITY.yaml`, `pom.xml`, `package.json`, or
any code file.

This is a HALT-AND-ASK gate (`CHANGELOG.md` is protected per CLAUDE.md).
The pre-edit hook (`.claude/hooks/pre_guard_release_files.py`) will
surface a confirmation prompt; the executor must obtain explicit user
confirmation before the edit applies. Surface the diff to the user
with the locked content from Step B clearly identified.
  </action>
  <verify>
- `grep -c "^## \[0.4.0\]" CHANGELOG.md` returns 1
- `grep -c "^## \[Unreleased\]" CHANGELOG.md` returns 1
- `grep -c "^## \[0.3.2\]" CHANGELOG.md` returns 1
- The Unreleased heading precedes the 0.4.0 heading in file order
- The 0.4.0 heading precedes the 0.3.2 heading in file order
- No literal `<DATE>` string remains in the committed text
- 5 subsections under [0.4.0]: Added, Changed, Fixed, Security, Known Issues (carried forward)
  </verify>
  <done>CHANGELOG.md ready for release.yml to extract on the eventual tag push.</done>
</task>

<task type="auto">
  <name>Task 2: Insert v0.4.0 section into docs/MIGRATION.md</name>
  <files>
    docs/MIGRATION.md
  </files>
  <action>
This task edits `docs/MIGRATION.md`, which is NOT in the protected-files
list. No HALT-AND-ASK gate.

**Step A: Read the current `docs/MIGRATION.md`** to confirm the
existing structure: the `## v0.3.0` section ends at line 153 with the
`---` divider; the `## vNext` template block starts at line 157. The
v0.4.0 section inserts BETWEEN the `---` divider after v0.3.0 and the
`## vNext` heading.

**Step B: Compose the v0.4.0 section.** The locked content is below;
the executor copies verbatim:

```markdown
## v0.4.0

Backend feature completion release. Closes the workout-plan editing,
live-session execution, body-metrics, full-export refinement, and API
contract documentation deliverables from `ProjectBrief.md` Phases 3-5.
Operator-visible surface changes are limited to two new auto-applied
Flyway migrations (V26 clientSetId on `session_sets`, V27 `is_pr` on
`session_sets`), one new operator-reachable runtime surface (the
SpringDoc OpenAPI document and Swagger UI), and a refined
`/api/export/import` failure mode (curated 422 instead of 500-class
FK violation on unknown `workoutDayId`). No env var changes; no
compose-topology changes; no one-shot operator commands; rollback is
the standard `git revert` of the merge that bumped to v0.4.0.

### Required env var changes

No new env vars. No removed env vars. No env-default changes. The
v0.3.0 contract-aligned env surface (`BIND_ADDR`, `BACKEND_PORT`,
`FRONTEND_PORT`, `POSTGRES_PORT`, `APP_AUTH_*`, `APP_PUSH_VAPID_*`,
`APP_REMINDERS_*_CRON`, `ENABLE_PG_DUMP`, `PG_DUMP_*`) carries forward
unchanged.

### Schema and data migration

Two new Flyway migrations apply automatically on backend start:

- **V26** — adds `client_set_id UUID` (nullable, unique-per-session)
  to `session_sets` for offline-queue idempotent replay (Phase 15-02).
- **V27** — adds `is_pr BOOLEAN NOT NULL DEFAULT false` to
  `session_sets` plus a `ROW_NUMBER() OVER (PARTITION BY user_id,
  exercise_id ORDER BY weight*(1 + reps/30) DESC, created_at ASC)`
  backfill that marks the highest-Epley completed set per
  `(user_id, exercise_id)` (Phase 16-04).

Both migrations are forward-only and idempotent on first apply. The
backfill in V27 is bounded by existing `session_sets` rows; on a
fresh install it is a no-op.

`spring.jpa.hibernate.ddl-auto: validate` continues to gate schema
drift. If V26 or V27 fails to apply against an existing database (for
example, an operator with a manually-edited schema), the backend
container fails fast at boot with the offending migration in the
logs.

### Compose / runtime changes

- **OpenAPI document and Swagger UI now reachable on the backend
  listener.** SpringDoc 2.6.0 (introduced in Phase 19-02) serves
  `GET /v3/api-docs` (JSON), `GET /v3/api-docs.yaml` (YAML), and
  `GET /swagger-ui.html` (interactive UI). Both surfaces are reachable
  without authentication by design; the application's public-network
  exposure is bounded by `BIND_ADDR=127.0.0.1` (compose default) and
  the operator's reverse proxy. See `docs/API.md` "Discoverability"
  for the trust-boundary stance.
- **Operator-facing endpoints excluded from the OpenAPI document.**
  `/livez`, `/healthz`, `/metrics`, `/actuator/health`,
  `/actuator/info`, `/actuator/prometheus` are NOT in `/v3/api-docs`
  per `springdoc.paths-to-match=/api/**`. They stay documented in
  `docs/OBSERVABILITY.md`.
- **No compose-topology change.** No new service. The `pg_dump`
  sidecar profile from v0.3.0 stays opt-in.
- **No image registry change.** Multi-arch publish to
  `ghcr.io/gtrows/workouthub-{backend,frontend}:0.4.0` (amd64 +
  arm64) per the v0.3.2 release-workflow flow.

### One-shot commands

No one-shot commands required. Operators upgrade with the standard
two-step:

```bash
# 1. Pull the new image tags.
docker compose pull

# 2. Restart the stack. V26 + V27 apply at backend boot.
docker compose up -d
```

If the optional pg_dump sidecar is enabled (`COMPOSE_PROFILES=backup`),
the second step also restarts the sidecar against the same schema. No
data conversion is required for V26 (new nullable column) or V27 (new
column with default-and-backfill).

### Rollback

```bash
# 1. In your operator deployment repo, revert the merge that bumped to v0.4.0.
git revert <merge-sha>

# 2. The orchestrator (Komodo / Watchtower-replacement / manual `compose pull`)
#    redeploys the previously pinned tag (v0.3.2). Confirm the stack is back.

# 3. V26 + V27 columns survive the downgrade. v0.3.2 does not read
#    `client_set_id` or `is_pr`; the columns sit unused but cause no
#    harm. They re-activate automatically on the next forward upgrade
#    to v0.4.x or later.

# 4. Optional: take a fresh pg_dump after the rollback completes:
docker compose exec db pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" \
    --format=custom > ./data/backups/post-rollback-v0.3.2.dump
```

The bind-mount cluster directory at `./data/postgres/` is unchanged by
v0.4.0. No data conversion landed in v0.4.0; the rollback to v0.3.2 is
schema-additive-only and reversible without data loss.

---
```

**Step C: Apply the edit.** Use the Edit tool to insert the locked
v0.4.0 section between the existing `---` divider after the v0.3.0
section (line 155) and the `## vNext` heading (line 157). The exact
edit position is BEFORE the `## vNext` heading; the new content ends
with its own `---` divider so the `## vNext` template block visually
separates from v0.4.0.

**Step D: Verification within the task.**

After the Edit:
- `grep -c "^## v0.4.0" docs/MIGRATION.md` returns exactly 1.
- `grep -c "^## v0.3.0" docs/MIGRATION.md` still returns exactly 1.
- `grep -c "^## vNext" docs/MIGRATION.md` still returns exactly 1.
- The v0.3.0 heading appears BEFORE v0.4.0 in file order.
- The v0.4.0 heading appears BEFORE vNext in file order.
- All 5 standard MIGRATION subsections appear under `## v0.4.0`:
  Required env var changes, Schema and data migration, Compose /
  runtime changes, One-shot commands, Rollback. (Per the
  MIGRATION.md "An empty section is not allowed" discipline at line
  13: every subsection has content; the env-vars section explicitly
  states "No new env vars. No removed env vars." rather than being
  empty.)
- No emojis.
- No trailing whitespace.

NO change to any other file in this task.
  </action>
  <verify>
- `grep -c "^## v0.4.0" docs/MIGRATION.md` returns 1
- `grep -c "^## v0.3.0" docs/MIGRATION.md` returns 1
- `grep -c "^## vNext" docs/MIGRATION.md` returns 1
- v0.3.0 -> v0.4.0 -> vNext ordering confirmed via Read
- 5 subsections under v0.4.0 (env vars, schema, compose/runtime, one-shot, rollback)
  </verify>
  <done>docs/MIGRATION.md carries the v0.4.0 operator runbook entry.</done>
</task>

<task type="auto">
  <name>Task 3: Bump IDENTITY.yaml version 0.3.2 -> 0.4.0</name>
  <files>
    IDENTITY.yaml
  </files>
  <action>
This task edits a protected file (`IDENTITY.yaml` per CLAUDE.md
"Protected Files"). The pre-edit hook surfaces a confirmation gate.

**Step A: Read the current `IDENTITY.yaml`** to confirm the version
line: line 32 reads `version: 0.3.2`. The file's other contents (the
`identity:` block, the `release:` block) stay unchanged.

**Step B: Apply the edit.** Use the Edit tool to replace the single
line `version: 0.3.2` with `version: 0.4.0`. The `find`/`replace` is
exact-string-match; no surrounding context needs to change.

**Step C: Verification within the task.**

After the Edit:
- Read `IDENTITY.yaml` line 32; confirm it reads `version: 0.4.0`.
- `grep "^version:" IDENTITY.yaml` returns exactly `version: 0.4.0`.
- No other line in `IDENTITY.yaml` is touched (Read confirms
  `identity:` block on lines 10-28 and `release:` block on lines
  34-70 are byte-for-byte unchanged).

This is a HALT-AND-ASK gate (`IDENTITY.yaml` is protected). The
pre-edit hook surfaces a confirmation prompt; the executor must obtain
explicit user confirmation before the edit applies.
  </action>
  <verify>
- `grep "^version:" IDENTITY.yaml` returns `version: 0.4.0`
- No other line in IDENTITY.yaml changes
  </verify>
  <done>IDENTITY.yaml at 0.4.0; single source of truth ready for derived-manifest sync.</done>
</task>

<task type="auto">
  <name>Task 4: Bump backend/pom.xml project version 0.3.2 -> 0.4.0</name>
  <files>
    backend/pom.xml
  </files>
  <action>
This task edits a protected file (`pom.xml` per CLAUDE.md "Protected
Files" — the Maven backend manifest). The pre-edit hook surfaces a
confirmation gate.

**Step A: Read `backend/pom.xml` lines 1-20** to confirm the project
version line: line 16 reads `<version>0.3.2</version>`. This is the
PROJECT version, NOT the parent `spring-boot-starter-parent` version
(which sits on line 10 as `<version>3.5.14</version>` and stays
unchanged).

**Step B: Apply the edit.** Use the Edit tool to replace the single
line `    <version>0.3.2</version>` (line 16, with leading 4-space
indent matching the surrounding XML) with `    <version>0.4.0</
version>`. The `find`/`replace` is exact-string-match including the
surrounding whitespace; the Edit tool's exact-match guarantee prevents
collateral edits to the parent `<version>3.5.14</version>` on line 10.

The executor MUST verify the targeted line by including 1-2 lines of
context above and below in the find string to disambiguate from the
parent-version line:

```xml
    <artifactId>workouthub</artifactId>
    <version>0.3.2</version>
    <name>workouthub</name>
```

becomes:

```xml
    <artifactId>workouthub</artifactId>
    <version>0.4.0</version>
    <name>workouthub</name>
```

**Step C: Verification within the task.**

After the Edit:
- Read `backend/pom.xml` line 16; confirm it reads
  `    <version>0.4.0</version>`.
- Read `backend/pom.xml` line 10; confirm the parent
  `spring-boot-starter-parent` version still reads
  `        <version>3.5.14</version>` (the 8-space indent
  distinguishes the parent block from the project block).
- `grep -c "<version>0.4.0</version>" backend/pom.xml` returns
  exactly 1 (the project version; no other 0.4.0 references in
  pom.xml).
- `grep -c "<version>3.5.14</version>" backend/pom.xml` returns
  exactly 1 (the parent version; unchanged).

This is a HALT-AND-ASK gate (`pom.xml` is protected). The pre-edit
hook surfaces a confirmation prompt; the executor must obtain explicit
user confirmation before the edit applies.

The plan does NOT touch any `<dependency>` block or any `<properties>`
version declaration. All transitive-pin work (async-http-client,
jose4j, bouncycastle, jjwt, web-push, springdoc) carries forward from
v0.3.2 unchanged. The CVE-sweep verdict from Task 1's `### Security`
subsection states the rationale; no pom.xml dependency-bump is
required.
  </action>
  <verify>
- `grep -c "<version>0.4.0</version>" backend/pom.xml` returns 1
- `grep -c "<version>3.5.14</version>" backend/pom.xml` returns 1 (parent unchanged)
- `grep -c "<version>0.3.2</version>" backend/pom.xml` returns 0
  </verify>
  <done>backend/pom.xml project version at 0.4.0; transitive pins unchanged.</done>
</task>

<task type="auto">
  <name>Task 5: Bump frontend/package.json version 0.3.2 -> 0.4.0</name>
  <files>
    frontend/package.json
  </files>
  <action>
This task edits a protected file (`package.json` per CLAUDE.md
"Protected Files"). The pre-edit hook surfaces a confirmation gate.

**Step A: Read `frontend/package.json` lines 1-10** to confirm the
version field: line 4 reads `  "version": "0.3.2",`. The file's
`name`, `description`, `private`, `scripts`, `dependencies`,
`devDependencies` blocks stay unchanged.

**Step B: Apply the edit.** Use the Edit tool to replace the single
line `  "version": "0.3.2",` with `  "version": "0.4.0",`. The
`find`/`replace` is exact-string-match including the surrounding
whitespace and trailing comma.

**Step C: Verification within the task.**

After the Edit:
- Read `frontend/package.json` line 4; confirm it reads
  `  "version": "0.4.0",`.
- `grep "\"version\":" frontend/package.json` returns
  `  "version": "0.4.0",`.
- No other line in `frontend/package.json` is touched.
- `pnpm-lock.yaml` is NOT touched (per RELEASE.md / 12-01 precedent:
  "the version field in package.json does not affect the lockfile
  hash"). The plan does NOT invoke `pnpm install`, `pnpm install
  --lockfile-only`, or any pnpm command.

This is a HALT-AND-ASK gate (`package.json` is protected). The
pre-edit hook surfaces a confirmation prompt; the executor must obtain
explicit user confirmation before the edit applies.
  </action>
  <verify>
- `grep "\"version\":" frontend/package.json` returns `  "version": "0.4.0",`
- `pnpm-lock.yaml` is not modified
- No other line in frontend/package.json changes
  </verify>
  <done>frontend/package.json at 0.4.0; pnpm-lock.yaml untouched.</done>
</task>

<task type="auto">
  <name>Task 6: Single release commit on main; tag and push deferred to milestone checkpoint</name>
  <files>
    (git ops)
  </files>
  <action>
**Step A: `git status` to confirm staging area.**

Confirm exactly five files are modified relative to `HEAD`:
- `CHANGELOG.md` (Task 1)
- `docs/MIGRATION.md` (Task 2)
- `IDENTITY.yaml` (Task 3)
- `backend/pom.xml` (Task 4)
- `frontend/package.json` (Task 5)

If `git status` surfaces any other modified file, the executor STOPS
and surfaces the discrepancy as a blocker. The plan touches exactly
those five files.

**Step B: Stage the five files and commit.**

Per CLAUDE.md "Avoid `git add -A` or `git add .`": stage by explicit
path, not by wildcard.

```bash
git add CHANGELOG.md docs/MIGRATION.md IDENTITY.yaml backend/pom.xml frontend/package.json
git commit -m "chore(release): v0.4.0"
```

The commit message follows the v0.3.0 precedent verbatim
(12-01-PLAN.md Task 3 Step A: `git commit -m "chore(release):
v0.3.0"`). No `Co-Authored-By` trailer (CLAUDE.md "Git and Commits"
explicitly forbids it for this repo).

**Step C: Verification within the task.**

After the commit:
- `git log -1 --oneline` shows `chore(release): v0.4.0` on `main`.
- `git status` shows a clean working tree (no uncommitted changes).
- `git show --stat HEAD` shows exactly the five files in the diff.
- The diff contains only version-field changes plus the CHANGELOG/
  MIGRATION additions; no unintended edits.

**Step D: NO tag, NO push.** Per the constraining-decisions block:

```text
NO  git tag -a v0.4.0 -m "..."
NO  git push origin main
NO  git push origin v0.4.0
NO  gh workflow run release.yml
NO  release.yml workflow trigger
NO  GHCR image publish
NO  draft GitHub release creation
```

The release commit sits on local `main` waiting for the milestone
checkpoint to fire RELEASE.md Steps 5 and 6. The orchestrator owns the
checkpoint sequence:
1. User reviews this plan's diff (the five-file release commit) at
   the milestone checkpoint.
2. On user approval, the orchestrator (or the user via `/gtr:release`)
   executes `git tag -a v0.4.0 -m "Release v0.4.0 - backend feature
   completion"`.
3. `git push origin main` and `git push origin v0.4.0` push the
   commit and tag to GitHub.
4. The tag push triggers `.github/workflows/release.yml`: tests run,
   multi-arch images publish to GHCR, draft release lands.
5. A maintainer reviews the draft release on GitHub and clicks
   Publish per RELEASE.md "Post-release" checklist.

Steps 1-5 above are explicitly OUT OF SCOPE for this plan. The
executor signals task-complete after Step C verification; the
orchestrator-spawned summarizer writes the SUMMARY.md and the
milestone checkpoint takes over.

NOTE on commit cadence: this plan delivers exactly 1 commit on
`main`. The protected-file edits in Tasks 1, 3, 4, 5 ARE staged in
the same commit as the unprotected `docs/MIGRATION.md` edit from
Task 2 because the release runbook (RELEASE.md "Cut the release"
Step 4) prescribes a single release commit. The pre-edit hook
confirmations on the protected files happen at edit time (Tasks 1,
3, 4, 5); the commit at Task 6 is a single git operation.
  </action>
  <verify>
- `git log -1 --oneline` matches `chore(release): v0.4.0`
- `git status` reports a clean working tree
- `git show --stat HEAD` lists the five files: CHANGELOG.md, docs/MIGRATION.md, IDENTITY.yaml, backend/pom.xml, frontend/package.json
- No tag exists locally for v0.4.0 (`git tag -l v0.4.0` returns empty)
- No push occurred (the commit sits on local main only)
  </verify>
  <done>Release commit ready for milestone-checkpoint sign-off; tag and push deferred.</done>
</task>

</tasks>

<verification>

**Structural correctness (executor walks before signaling task-complete;
combined Task 1-6 checklist):**

- `IDENTITY.yaml#version` == `0.4.0` (Task 3).
- `backend/pom.xml` line 16 == `    <version>0.4.0</version>` (Task 4);
  parent `spring-boot-starter-parent` version unchanged at `3.5.14`.
- `frontend/package.json` line 4 == `  "version": "0.4.0",` (Task 5);
  `pnpm-lock.yaml` not modified.
- `CHANGELOG.md` carries exactly one `## [0.4.0] - <today>` heading and
  exactly one `## [Unreleased]` heading; the Unreleased heading precedes
  the 0.4.0 heading; the 0.4.0 heading precedes the existing 0.3.2
  heading; the populated section has 5 subsections (Added, Changed,
  Fixed, Security, Known Issues (carried forward)) plus the locked CVE-
  sweep paragraph as the first content of `### Security` (Task 1).
- `docs/MIGRATION.md` carries exactly one `## v0.4.0` heading positioned
  between the existing `## v0.3.0` and `## vNext` blocks; the populated
  section has 5 subsections matching the file's template (env vars,
  schema, compose/runtime, one-shot, rollback) (Task 2).
- `git log -1 --oneline` shows `chore(release): v0.4.0`; `git status`
  is clean; `git show --stat HEAD` lists exactly the five touched files
  (Task 6).
- No tag exists for v0.4.0 (`git tag -l v0.4.0` is empty).
- No push to remote (`git status -uno` shows the local main is ahead
  of origin/main by exactly 1 commit; the push is deferred to the
  milestone checkpoint).

**Cross-file consistency:**

- The version string `0.4.0` appears in: `IDENTITY.yaml#version` (1
  occurrence); `backend/pom.xml` line 16 (1 occurrence; parent stays
  3.5.14); `frontend/package.json` line 4 (1 occurrence);
  `CHANGELOG.md` H2 heading (1 occurrence in `## [0.4.0] - <date>`);
  `docs/MIGRATION.md` H2 heading (1 occurrence in `## v0.4.0`). All
  five locations agree.
- The version string `0.3.2` no longer appears in any of the five
  touched files (CHANGELOG.md still references `## [0.3.2]` in the
  preserved historical entry — that's a deliberate keep, not a drift).
- Each protected-file edit (`CHANGELOG.md`, `IDENTITY.yaml`,
  `backend/pom.xml`, `frontend/package.json`) was approved at the
  pre-edit hook gate by the user; the executor records the approval
  in the task-complete signal.

**CI gate (next push after the release commit lands):**

- The release commit on local `main` does NOT trigger CI in this plan
  (no `git push`). When the milestone checkpoint pushes the commit and
  tag, CI runs as normal:
  - `mvn verify` continues to pass (no Java change in this plan; the
    Phase 13-19 changes that ship in v0.4.0 already passed CI when
    they merged).
  - The `OpenApiSurfaceIntegrationTest` from Phase 19-04 continues to
    pass.
  - `trivy fs` and `trivy image` jobs from Phase 10-01 continue to
    pass against the 0.4.0 image build.
  - `gitleaks`, `hadolint`, `actionlint`, `shellcheck`, and
    `compose_config` from Phase 10-01 continue to pass.
  - On `v0.4.0` tag push: `release.yml` fires; tests run; multi-arch
    images publish to `ghcr.io/gtrows/workouthub-{backend,frontend}:
    0.4.0`; draft release is created with `## [0.4.0]` notes.

**Tests added in this plan: 0.** v0.4.0 ships with the Phase 13-19
test suite (already on main); no new test in Phase 20.

**Files touched:** 5 modified, 0 created, 0 deleted.

**Files NOT touched (audit + plan boundary):** `.github/workflows/*`,
`pnpm-lock.yaml`, `Dockerfile` (backend or frontend), `compose.yml`,
`docker-compose.override.yml.example`, `.env.example`,
`.trivyignore`, `application.yml`, `application-prod.yml`,
`OpenApiConfig.java`, `SecurityConfig.java`, `ApiError.java`,
`GlobalExceptionHandler.java`, all 31 `*Controller.java` files, all
DTOs, all services, all repositories, all mappers, all migrations
(V1 through V27 carry forward unchanged), all frontend code files,
all tests, `RELEASE.md` (read-only reference; the runbook itself
needs no edit because the v0.3.0 / v0.3.1 / v0.3.2 procedure
applies verbatim to v0.4.0), `docs/SELF_HOSTED_CONTRACT.md`,
`docs/EXPORT_FORMAT.md`, `docs/OBSERVABILITY.md`, `docs/API.md`,
`docs/DEPLOYMENT.md`, `docs/DATA_SCHEMA.md`, `docs/BACKUP.md`,
`README.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`,
`.planning/STATE.md`, `.planning/ISSUES.md`, `.planning/MILESTONES.md`,
all `.planning/codebase/*` files, all `.planning/phases/*` files
(other than this plan's own placement at `20-01-PLAN.md`).

</verification>

<success_criteria>

- The five file touches land in lockstep on a single release commit on
  local `main`, with version `0.4.0` consistent across `IDENTITY.yaml`,
  `backend/pom.xml`, `frontend/package.json`, `CHANGELOG.md` H2, and
  `docs/MIGRATION.md` H2.
- The CHANGELOG `## [0.4.0]` block summarizes every Phase 13-19
  deliverable (Added: 13 bullets covering V26 idempotency, heart-rate
  field, typed code, analytics package, Epley, V27 is_pr, photoUrl,
  range filter, status-code split, snake_case, import validation,
  SpringDoc + Swagger UI, OpenApiSurfaceIntegrationTest, docs/API.md
  rewrite; Changed: 5 bullets covering cascade-id fix, test-side fix,
  SetRow growth, FullExportService population, EXPORT_FORMAT.md
  documentation; Fixed: 4 bullets closing i-1, i-2, i-10, dual-
  controller operationId collision; Security: locked 1-paragraph CVE-
  sweep verdict; Known Issues: 7 carries i-3 through i-9). The
  Unreleased block is empty with the 6 standard subsection stubs.
- The MIGRATION.md `## v0.4.0` block covers the V26 + V27 schema
  changes, the SpringDoc operator-visible runtime surface, the
  no-env-var-change stance, the standard pull+up one-shot, and the
  schema-additive-only rollback recipe; the section sits between v0.3.0
  and the vNext template block, preserving the file's section ordering.
- The pre-edit hook gates fired exactly 4 times (one per protected-file
  touch: `CHANGELOG.md`, `IDENTITY.yaml`, `backend/pom.xml`,
  `frontend/package.json`). Each gate was answered with explicit user
  approval. The executor records the approval count in the task-
  complete signal.
- The 6 atomic tasks land in order: Task 1 (CHANGELOG) -> Task 2
  (MIGRATION) -> Task 3 (IDENTITY) -> Task 4 (pom.xml) -> Task 5
  (package.json) -> Task 6 (single commit).
- The release commit message is `chore(release): v0.4.0` per the v0.3.0
  precedent.
- No tag exists locally or remotely for v0.4.0; no push to remote
  occurred; no GHCR image was published; no GitHub release draft was
  created. The plan stops at "release commit on local main awaiting
  milestone-checkpoint sign-off".
- Phase 20 is closed on the preparation surface; ROADMAP can advance
  to milestone-checkpoint after this plan lands and the orchestrator-
  spawned summarizer writes `20-01-SUMMARY.md`.
- v0.4 milestone closes after the milestone checkpoint executes the
  RELEASE.md Step 5+6 tag-and-push sequence (out of scope here);
  `/gsd:complete-milestone` then archives the v0.4 phases under
  `.planning/milestones/v0.4-ROADMAP.md`.

</success_criteria>

<output>

- `CHANGELOG.md` carries the populated `## [0.4.0] - <DATE>` block
  summarizing 13 Added bullets, 5 Changed bullets, 4 Fixed bullets,
  the locked CVE-sweep Security paragraph, and 7 Known-Issues
  carries; the empty `## [Unreleased]` block stays in place above
  with the 6 standard subsection stubs (Task 1).
- `docs/MIGRATION.md` carries the populated `## v0.4.0` operator
  runbook section between v0.3.0 and the vNext template block
  (Task 2).
- `IDENTITY.yaml#version` == `0.4.0` (Task 3); `backend/pom.xml` line
  16 project version == `0.4.0` (Task 4); `frontend/package.json`
  line 4 version field == `0.4.0` (Task 5); pnpm-lock.yaml unchanged.
- A single release commit `chore(release): v0.4.0` lands on local
  `main` with exactly the five touched files in its diff (Task 6).
- No tag, no push, no GHCR publish, no draft release. The release
  commit sits on local `main` waiting for the milestone-checkpoint
  sign-off; the orchestrator owns the RELEASE.md Step 5+6 tag-and-
  push sequence after user approval.

After 20-01 ships and the milestone checkpoint fires the tag push:
v0.4 milestone closes end-to-end. The v0.4.0 release publishes to
`ghcr.io/gtrows/workouthub-{backend,frontend}:0.4.0` (multi-arch
amd64 + arm64) per the v0.3.2 release-workflow flow; the draft
GitHub release lands with `## [0.4.0]` extracted as notes; a
maintainer reviews and clicks Publish; `/gsd:complete-milestone`
archives Phases 13-20 under `.planning/milestones/v0.4-ROADMAP.md`
and ROADMAP advances to v0.5 (Phase 21 frontend-audit).

</output>
