---
phase: 11-ghcr-multi-arch-publish
plan: 01
type: execute
domain: github-actions
---

<objective>
Bring `.github/workflows/release.yml` into compliance with `docs/SELF_HOSTED_CONTRACT.md` sections 3.1 and 10. Add multi-arch builds (`linux/amd64` + `linux/arm64`), remove `:latest` floating alias, append the built image digest to the release notes, and fix the bundle step which references files that do not exist.

Purpose: Operators on ARM hosts (Pi 4, Apple Silicon, etc.) need to be able to pull the same image as x86_64 hosts. Contract section 3.1 forbids `:latest`. Section 10 wants digest pinning for traceability.

Output: Updated release.yml with multi-arch buildx + arm64 emulation, no `:latest` tag, digest appended to release notes, bundle scoped to actually-existing files.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@docs/SELF_HOSTED_CONTRACT.md
@.github/workflows/release.yml
@compose.yml
@docker-compose.override.yml.example

**Constraining decisions:**
- Multi-arch via docker buildx + QEMU emulation (`docker/setup-qemu-action@v3`).
- Single image tag per release: `vX.Y.Z` only. No `:latest`, no `:vX.Y` alias.
- Bundle step currently references `compose.prod.yml`, `nginx/`, `docs/DEPLOYMENT.md`, `docs/BACKUP.md`. compose.prod.yml and nginx/ don't exist; bundle should ship `compose.yml`, `docker-compose.override.yml.example`, `.env.example`, `docs/MIGRATION.md`, plus existing docs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update release.yml for multi-arch and contract compliance</name>
  <files>.github/workflows/release.yml</files>
  <action>
Make these specific edits to release.yml:

1. Add `docker/setup-qemu-action@v3` step BEFORE `docker/setup-buildx-action@v4` in build-and-push job.

2. Remove the `:latest` tag from the Compute image tags step. The conditional that currently appends `,$IMAGE:latest` for non-prerelease must be deleted entirely.

3. Add `platforms: linux/amd64,linux/arm64` to the build-push-action step.

4. Capture the digest output (`steps.build-push.outputs.digest`) and write it to a per-component file under `${{ runner.temp }}` so the release job can collect them.

5. In the release job, read those digest files and append a "Container image digests" subsection to RELEASE_NOTES.md showing both `ghcr.io/.../workouthub-backend@sha256:<digest>` and `ghcr.io/.../workouthub-frontend@sha256:<digest>`.

6. Fix the Bundle compose files step to reference only files that exist. Replace the file list with: `compose.yml`, `docker-compose.override.yml.example`, `.env.example`, `docs/MIGRATION.md`, and the existing `docs/DEPLOYMENT.md` / `docs/BACKUP.md`.

Do NOT change the verify-changelog or test jobs.
Do NOT add `:latest` back. The contract is explicit.
  </action>
  <verify>
- `.github/workflows/release.yml` has setup-qemu step
- build-push-action has platforms set to amd64+arm64
- No `:latest` substring in the tag computation
- Bundle step files all exist in repo root or docs/
  </verify>
  <done>
- Multi-arch builds wired
- :latest removed
- Digests in release notes
- Bundle scoped to existing files
  </done>
</task>

</tasks>

<success_criteria>
- 1 atomic task commit + 1 metadata
- Phase 11 closes; Phase 12 (Release v0.3.0) unblocked
</success_criteria>

<output>Standard SUMMARY.md.</output>
