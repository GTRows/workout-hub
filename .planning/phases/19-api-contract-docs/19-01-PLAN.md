---
phase: 19-api-contract-docs
plan: 01
type: execute
domain: backend
---

<objective>
Inventory the public REST surface that Phase 19 must document (every `@RestController` under `backend/src/main/java/com/workouthub/`, the security filter chain config, the `/livez` / `/healthz` / `/metrics` / actuator paths from the self-hosted contract, and the existing `docs/API.md`/`docs/EXPORT_FORMAT.md`/`docs/SELF_HOSTED_CONTRACT.md` content), confirm the SpringDoc starter artifact + version line compatible with the project's Spring Boot 3.5.14 / Java 21 baseline (`pom.xml:9-11`, `pom.xml:21`), audit how the surface aligns to `docs/SELF_HOSTED_CONTRACT.md` section 7 (built-in JWT default + `AUTH_MODE=forward-auth` opt-in; no OIDC client in this repo), and recommend a concrete Phase 19 plan-02+ scope with explicit plan boundaries for the OpenAPI generation work, the security-scheme documentation, the actuator/management exclusion, the `docs/API.md` refresh, and the contract-vs-actual drift report.

Purpose: Phase 19 cannot plan "what to land" because the wire surface, security filters, and per-endpoint shapes are already in place across 31 controllers (134 mapping annotations) and the v0.3 contract pinned the auth mode + health/metrics/actuator paths. Phase 19 must plan "which SpringDoc starter artifact to add (`springdoc-openapi-starter-webmvc-ui` 2.x line, with the exact compatible version verified against Spring Boot 3.5.14 docs)", "where to expose the generated OpenAPI JSON + Swagger UI (default `/v3/api-docs` + `/swagger-ui.html` vs operator-restricted via SecurityConfig)", "how to declare the two security schemes (JWT bearer + forward-auth header set) so the OpenAPI document accurately models both auth modes from contract section 7", "which actuator and management paths to suppress from the public surface so the operator's reverse proxy does not see internal endpoints documented", "what the refreshed `docs/API.md` deliverable should look like (hand-written reference vs static export of the generated OpenAPI vs both)", and "which contract-vs-actual drift cases must be raised as new issues vs deferred to v1.0 Phase 42 (docs-completion)". This audit produces the single source of truth for those decisions.

Output: `.planning/phases/19-api-contract-docs/19-01-AUDIT.md` containing six sections (REST surface inventory + controller catalog, current docs/API.md and docs/EXPORT_FORMAT.md state, SpringDoc 2.x artifact + version verification + integration shape, security-scheme + actuator-exclusion behavioral analysis, contract section 7 alignment + cross-package coupling map for the auth filters, recommended Phase 19 plan-02+ scope with explicit plan boundaries). No Java source files, migrations, tests, or documentation files are modified. The `pom.xml` change required to add the SpringDoc dependency is recommended but NOT applied — `pom.xml` is a protected file per `CLAUDE.md` "Protected Files" and any plan that touches it must call out the protection explicitly so the executor halts and asks for confirmation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/ISSUES.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md
@.planning/phases/13-workouts-audit/13-01-AUDIT.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/phases/16-sessions-analytics/16-01-AUDIT.md
@.planning/phases/17-body-metrics/17-01-AUDIT.md
@.planning/phases/18-export-refinement/18-01-AUDIT.md
@.planning/phases/18-export-refinement/18-04-SUMMARY.md
@docs/SELF_HOSTED_CONTRACT.md
@docs/API.md
@docs/EXPORT_FORMAT.md
@docs/DEPLOYMENT.md
@docs/OBSERVABILITY.md
@backend/pom.xml
@backend/src/main/resources/application.yml
@backend/src/main/resources/application-prod.yml
@backend/src/main/java/com/workouthub/common/config/SecurityConfig.java
@backend/src/main/java/com/workouthub/common/security/JwtAuthenticationFilter.java
@backend/src/main/java/com/workouthub/common/security/ForwardAuthFilter.java
@backend/src/main/java/com/workouthub/common/web/HealthController.java
@backend/src/main/java/com/workouthub/common/web/PrometheusMetricsController.java
@backend/src/main/java/com/workouthub/common/web/GlobalExceptionHandler.java
@backend/src/main/java/com/workouthub/common/web/ApiError.java
@ProjectBrief.md

**Tech stack available (carried from Phase 18):** Java 21, Spring Boot 3.5.14 (`pom.xml:9-11`), Spring Web, Spring Data JPA, Spring Security, Spring Boot Actuator (already on the dependency tree at `pom.xml:73-76`), Hibernate `@UuidGenerator`, Jackson with `serializationInclusion = NON_NULL`, JUnit 5 + Testcontainers Postgres, typed `ApiError.code` envelope from Phase 15-04, Bean Validation via `jakarta.validation` on request DTOs, `analytics/PrDetector.epleyOneRm` + `SessionSet.isPr` durable PR persistence, `metrics/MetricsService.list(UUID, LocalDate, LocalDate)` time-series range from Plan 17-03, `MetricsService.UpsertResult` 200/201 split from Plan 17-04, `ClaudeSummaryDto` snake_case via `@JsonNaming` from Plan 18-03, `FullExportDto.SetRow` 9 components incl. `isPr` from Plan 18-04, claude-summary `prs/body_metrics/consistency` exposure from Plan 18-02, `docs/EXPORT_FORMAT.md` round-trip drift section from Plan 18-04. The full v0.4 backend feature surface lands at Phase 18 close; Phase 19 is the documentation pass before the v0.4 release cut at Phase 20.

**Established patterns (carried from prior audits):**
- Feature-sliced packages (`auth/`, `users/`, `exercises/`, `workouts/`, `sessions/`, `metrics/`, `analytics/`, `nutrition/`, `supplements/`, `water/`, `achievements/`, `challenges/`, `notifications/`, `push/`, `webhooks/`, `health/`, `exports/`, `audit/`, `twofa/`, `admin/`, `common/`); Controller -> Service -> Repository, transactions on services, DTOs at HTTP boundary.
- 31 controllers across 22 feature packages (`Glob backend/src/main/java/com/workouthub/**/*Controller.java` returns 31 files; verified at audit time). 134 mapping annotations total per `Grep '@RequestMapping|@GetMapping|@PostMapping|@PutMapping|@DeleteMapping|@PatchMapping' --type java --output_mode count`. The audit must enumerate all 31 in Section 1 and decide which are "public" (operator + user-facing) vs "internal" (`HealthController`, `PrometheusMetricsController` already permitAll-ed in `SecurityConfig.java:43-45`).
- `ApiError.code` envelope from Phase 15-04: typed string codes for 409 paths (`SET_NUMBER_DUPLICATE`, `SESSION_ALREADY_FINISHED`, etc.). Audit must confirm `ApiError` is THE error response shape for the OpenAPI document; no other error envelope exists.
- Jackson `NON_NULL` global serialization; optional record components stripped from JSON when null (`JacksonConfig.java:14-21`). Audit must confirm OpenAPI schema generation respects this (SpringDoc 2.x reads Jackson config; null-stripping is fine to declare the field as `nullable: true` rather than `required: false` since the field is always declared but absent on the wire).
- `@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)` on `ClaudeSummaryDto` ONLY (Direction C from 18-03). All other DTOs across the project (16 controllers + their request/response DTOs) use Jackson default camelCase. Audit must confirm SpringDoc 2.x picks up the per-record `@JsonNaming` correctly so the OpenAPI document shows `total_workouts` for the claude-summary path and `totalWorkouts` everywhere else.
- Phase 18-04 lesson: optional fields with boxed Boolean (e.g. `SetRow.isPr`) deserialize null cleanly on legacy payloads. Audit must confirm OpenAPI marks these as `nullable: true` so client codegen does not break on absent fields from pre-18-04 dumps.
- Local mvn unavailable on this Windows host (memory: local_maven_gap); audit work is read-only on Java source. Any new dependency in `pom.xml` is verified by CI on the next push, not locally.

**Constraining decisions (sourced from STATE.md + prior summaries + contract):**
- ROADMAP Phase 19 description (`.planning/ROADMAP.md:98-107`): "Generate OpenAPI via SpringDoc, refresh `docs/API.md`, document JWT and forward-auth schemes, and verify the surface matches `docs/SELF_HOSTED_CONTRACT.md` section 7." Plan-count flagged as TBD in roadmap; this audit decides. Research flag: `Likely (SpringDoc 2.x with Spring Boot 3.5 + Java 21, JWT bearer auth scheme, actuator exclusion)`.
- `docs/SELF_HOSTED_CONTRACT.md` section 7.1: "The app handles login and (optionally) registration. Use a maintained library; never hand-roll JWT validation." Built-in JWT is the default. Section 7.2: `AUTH_MODE=forward-auth` opt-in; trust `X-Forwarded-User` / `X-Forwarded-Email` / `X-Forwarded-Groups` only when source IP is in `TRUSTED_PROXIES`. Section 7.3: "Use Redis (Postgres apps) or a dedicated SQLite file (SQLite apps)." NOTE: WorkoutHub uses Postgres + JJWT 0.13.0 (`pom.xml:22`); no Redis dependency exists today and no session table is materialized. Audit must NOT raise this as an issue — the contract says "use Redis" but stateless JWT is acceptable for the "Sessions" subsection (Spring Security stateless filter chain at `SecurityConfig.java:35`). The OpenAPI document must reflect stateless JWT, not session cookies.
- `docs/SELF_HOSTED_CONTRACT.md` section 9.1 + 9.2 (metrics + health): `GET /metrics` is exposed via `PrometheusMetricsController` which proxies to actuator (`pom.xml:73-76`); the actuator endpoint is `GET /actuator/prometheus`; both are permitAll-ed. `GET /healthz` (real readiness) and `GET /livez` (process up) are exposed via `HealthController.java`. The OpenAPI document must EXCLUDE all of these from the user-facing surface (operator-only paths) but Section 4 of this audit must confirm the SpringDoc include/exclude pattern matches.
- `docs/SELF_HOSTED_CONTRACT.md` section 0 ("scope of this file"): "What Claude does NOT do here: ... pick an identity provider." Audit must confirm Phase 19 deliverables do NOT add an OIDC client, an OAuth flow, or any identity-provider-specific code; SpringDoc is purely for documentation generation.
- `pom.xml` is a protected file per `CLAUDE.md` "Protected Files" section. Adding `springdoc-openapi-starter-webmvc-ui` requires a new `<dependency>` entry. Section 6 must call out that plan 19-02+ touches `pom.xml` and the executor MUST halt and ask for confirmation before applying the edit. The audit (this plan, 19-01) does NOT touch `pom.xml`.
- Phase 18 close summary (`.planning/phases/18-export-refinement/18-04-SUMMARY.md` `affects:`): "19-api-contract-docs (claude-summary section in docs/EXPORT_FORMAT.md gains the round-trip drift sub already documented; OpenAPI surface for /api/export/import gets the new SetRow.isPr field)". Phase 19 inherits the post-18-04 wire shape exactly; no re-litigation of export DTO shape.
- `docs/API.md` is a 2-line placeholder today (`docs/API.md:1-3`: "# API\n\nPlaceholder. The full REST surface is documented here starting in PHASE 1."). Phase 19 owns the rewrite; Phase 1 is closed and the placeholder text must be replaced.
- 31 controllers + 134 mappings is the high-water-mark scope. Phase 19 cannot enumerate every endpoint by hand in `docs/API.md`; the SpringDoc-generated `/v3/api-docs` JSON IS the canonical reference, and `docs/API.md` becomes a navigational + auth-guidance + operator-targeted README that LINKS to the OpenAPI document and Swagger UI. This stance must be confirmed (or rejected) in Section 4 Part C with an explicit verdict.
- The `local_maven_gap` memory means SpringDoc dependency addition cannot be smoke-tested locally — the executor must commit the change, push to CI, and observe the green build. The audit must surface this as a v0.4 risk and recommend a CI-gate strategy (e.g. add a smoke test that hits `/v3/api-docs` and asserts non-empty `paths` map).

**Issues being addressed:**
- None directly from `.planning/ISSUES.md`. Open issues i-3..i-9 belong to other phases per the issue-to-phase mapping in STATE.md (i-4 v0.6, i-5/i-6/i-8 v1.0, i-7 v1.0, i-9 v0.6). Phase 18 closed i-10 atomically.
- The audit may surface NEW issues (e.g. SpringDoc + Spring Boot 3.5.x version pin requiring `springdoc.api-docs.path` override; multiple `SessionsController` classes — `users.SessionsController` AND `sessions.SessionsController` — colliding on `/api/sessions` namespace requiring SpringDoc operationId disambiguation; `ApiError.code` not yet uniformly emitted on every 4xx/5xx path so the OpenAPI error schema understates reality). Recording new issues is in scope; opening `ISSUES.md` edits is out of scope for this audit and belongs to plan 19-02+.

**Out of scope for this plan:**
- Writing Java code or annotations (plan 19-02+).
- Editing `pom.xml` (protected file; ALL edits to it require user confirmation per `CLAUDE.md`).
- Editing `docs/API.md`, `docs/EXPORT_FORMAT.md`, or any other doc file.
- Modifying ProjectBrief, ROADMAP, STATE, or ISSUES files.
- Touching `frontend/` (the OpenAPI document is consumed at codegen time only by potential future tooling; no frontend code changes ship in Phase 19).
- Running `mvn` (local-maven-gap memory; the audit produces no test-runnable output).
- Picking the SpringDoc version verbatim — Section 3 walks the candidate versions (`2.6.0` line as of Spring Boot 3.5 release; `2.7.x` if newer Spring Boot 3.5.x patch landed) and lands on one only after WebFetch confirms the springdoc-openapi-starter-webmvc-ui release notes.
- Adding any OIDC, OAuth2, or external identity-provider integration code (forbidden by `docs/SELF_HOSTED_CONTRACT.md` sections 0 + 7.2).
- Implementing the OpenAPI customizer (the `OpenApiConfig` Java class) — that is a 19-02 plan deliverable; this audit only describes its expected shape.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the public REST surface and catalog controllers + auth filters</name>
  <files>.planning/phases/19-api-contract-docs/19-01-AUDIT.md</files>
  <action>
  Read every controller listed by `Glob backend/src/main/java/com/workouthub/**/*Controller.java` (31 files). Capture the file path, the class-level `@RequestMapping` base path (if any), and the count of mapping annotations per file (already gathered: see context block). Also read `SecurityConfig.java`, `JwtAuthenticationFilter.java`, `ForwardAuthFilter.java`, `HealthController.java`, `PrometheusMetricsController.java`, `GlobalExceptionHandler.java`, `ApiError.java`. Open `application.yml` and `application-prod.yml` to capture management endpoint exposure (`management.endpoints.web.exposure.include = health,info,prometheus`).

  Create `19-01-AUDIT.md` with frontmatter matching the 18-01-AUDIT shape (`phase: 19-api-contract-docs`, `plan: 01`, `deliverable: audit`). Write Sections 1 and 2.

  Section 1 - REST Surface Inventory:

  Sub-section 1A - Controller catalog. One row per controller:
    | # | Package | Controller (file:line of class decl) | Class-level @RequestMapping base | Mapping count | Auth tier | Documented surface? |
  Rows in feature-package alphabetical order, then by file name within the package:

    1. `achievements/AchievementsController` - 2 mappings - `/api/achievements` - JWT - public.
    2. `admin/AdminChallengesController` - 5 mappings - `/api/admin/challenges` - JWT (ADMIN role) - admin.
    3. `admin/AdminExercisesController` - 4 mappings - `/api/admin/exercises` - JWT (ADMIN role) - admin.
    4. `admin/AdminUsersController` - 7 mappings - `/api/admin/users` - JWT (ADMIN role) - admin.
    5. `analytics/AnalyticsController` - 6 mappings - `/api/analytics` - JWT - public.
    6. `analytics/ExerciseAnalyticsController` - 3 mappings - `/api/exercises/{id}` (last-performance + progress) - JWT - public.
    7. `audit/AuditController` - 2 mappings - `/api/audit` - JWT (ADMIN role) - admin.
    8. `auth/AuthController` - 3 mappings - `/api/auth` (login, refresh, logout) - permitAll on POST `/login` + `/refresh` (`SecurityConfig.java:37-40`); JWT on others - public/anonymous.
    9. `auth/PasswordResetController` - 2 mappings - `/api/auth` (reset-password) - permitAll on POST `/reset-password` (`SecurityConfig.java:40`) - anonymous.
    10. `challenges/ChallengesController` - 2 mappings - `/api/challenges` - JWT - public.
    11. `common/web/HealthController` - 2 mappings - `/livez`, `/healthz` - permitAll (`SecurityConfig.java:43-45`) - operator.
    12. `common/web/PrometheusMetricsController` - 1 mapping - `/metrics` - permitAll (`SecurityConfig.java:43-45`) - operator.
    13. `exercises/ExerciseController` - 4 mappings - `/api/exercises` - JWT - public.
    14. `exports/ExportController` - 17 mappings - `/api/export` - JWT - public.
    15. `health/HealthImportController` - 4 mappings - `/api/health-import` - JWT - public.
    16. `metrics/MetricsController` - 4 mappings - `/api/metrics` (note collision with operator `/metrics`; the path is `/api/metrics`, no collision in practice) - JWT - public.
    17. `nutrition/NutritionController` - 6 mappings - `/api/nutrition` - JWT - public.
    18. `push/PushController` - 4 mappings - `/api/push` - JWT - public.
    19. `sessions/SessionsController` - 6 mappings - `/api/sessions` - JWT - public.
    20. `sessions/SessionSetsController` - 4 mappings - `/api/sessions/{id}/sets` - JWT - public.
    21. `supplements/SupplementsController` - 5 mappings - `/api/supplements` - JWT - public.
    22. `twofa/TwoFactorController` - 5 mappings - `/api/2fa` - JWT - public.
    23. `users/PasswordChangeController` - 2 mappings - `/api/users` (password change) - JWT - public.
    24. `users/SessionsController` - 3 mappings - `/api/users/me/sessions` (refresh-token sessions, NOT workout sessions; namespace collision risk for SpringDoc operationId) - JWT - public.
    25. `users/UsersController` - 3 mappings - `/api/users` - JWT - public.
    26. `water/WaterController` - 4 mappings - `/api/water` - JWT - public.
    27. `webhooks/ScaleWebhookController` - 2 mappings - `/api/webhooks/scale` - permitAll (`SecurityConfig.java:41-42`); HMAC token in header - operator.
    28. `webhooks/WebhookTokensController` - 4 mappings - `/api/webhook-tokens` - JWT (ADMIN role) - admin.
    29. `workouts/WorkoutDayByIdController` - 2 mappings - `/api/workout-days` - JWT - public.
    30. `workouts/WorkoutDaysController` - 8 mappings - `/api/workout-plans/{id}/days` - JWT - public.
    31. `workouts/WorkoutPlansController` - 8 mappings - `/api/workout-plans` - JWT - public.

  Mapping count total: 134 (matches the `Grep` count from the context block; serves as the audit cross-check).

  Cross-check verification: file count = 31, total mappings = 134, distinct base paths under `/api/` = 27 (the 4 non-`/api` paths are `/livez`, `/healthz`, `/metrics`, `/error`). Auth tier breakdown: 4 admin-only (`AdminChallenges`, `AdminExercises`, `AdminUsers`, `WebhookTokens`, `Audit` = 5; revise count if Audit is restricted), 3 operator-only (`Health`, `Prometheus`, `ScaleWebhook`), 1 mixed permitAll/JWT (`AuthController` + `PasswordResetController`), 22 JWT-protected user paths.

  Sub-section 1B - Two collisions to flag for SpringDoc:
    - Two classes named `SessionsController`: `com.workouthub.sessions.SessionsController` (workout sessions; `/api/sessions`) and `com.workouthub.users.SessionsController` (refresh-token sessions; `/api/users/me/sessions`). SpringDoc auto-derives `operationId` from method name + class simple name; this clashes if both have e.g. `list()`. Plan 19-02 must annotate one or both with `@Operation(operationId = "...")` to disambiguate.
    - `MetricsController` (user-facing body metrics; `/api/metrics`) AND `PrometheusMetricsController` (operator Prometheus; `/metrics`) AND the actuator `/actuator/prometheus`. The path namespaces do not collide (`/api/metrics` vs `/metrics`), but the OpenAPI tag derived from class simple name will clash if both surface in the document. Plan 19-02 must EXCLUDE `/metrics` and `/actuator/**` from SpringDoc's scan path so only `/api/**` surfaces.

  Sub-section 1C - Auth filter wiring:
  Walk `SecurityConfig.java:27-57`:
    - Line 32: CSRF disabled.
    - Line 33: CORS via `Customizer.withDefaults` (delegates to `CorsConfig` Bean).
    - Line 34: stateless session policy.
    - Lines 36-48: route matchers (permitAll for auth + webhook + operator paths; authenticated for `/api/**`).
    - Line 51-52: 401 entry point via `HttpStatusEntryPoint(UNAUTHORIZED)`.
    - Line 53: `JwtAuthenticationFilter` mounted before `UsernamePasswordAuthenticationFilter`.
    - Lines 54-55: `Optional<ForwardAuthFilter>` mounted before `JwtAuthenticationFilter` IFF the bean exists.
  Walk `ForwardAuthFilter.java:33-34`: `@ConditionalOnProperty("app.auth.mode" = "forward-auth")`. Default mode is `builtin` (`application.yml:43-44`).
  Walk `JwtAuthenticationFilter.java`: extracts Bearer token from `Authorization` header; resolves `AppUserPrincipal` via `JwtService.parseAccess(token)`; populates `SecurityContextHolder`. No second auth scheme.

  Sub-section 1D - Error envelope:
  Walk `ApiError.java`: record components `(int status, String error, String message, String path, String code)`. `code` is the typed string from Phase 15-04 (e.g. `SESSION_FINISHED`). Walk `GlobalExceptionHandler.java` to catalog every `@ExceptionHandler` and the HTTP status it returns. Audit must confirm the OpenAPI error schema is `ApiError`, not an ad-hoc shape per endpoint.

  Total file/line count for the surface inventory at the bottom of the section: 31 controllers + 6 common/security + 1 common/web/ApiError + 1 common/web/GlobalExceptionHandler + 2 application.yml/application-prod.yml.

  Constraint: read-only on Java source. Do not run mvn or modify any source file. The only file written by this task is `19-01-AUDIT.md`.
  </action>
  <verify>
  - `19-01-AUDIT.md` exists with frontmatter and Sections 1A through 1D.
  - Section 1A lists 31 controllers numbered 1-31 with package, base path, mapping count, auth tier, public/admin/operator label.
  - Section 1A total mapping count line says `134` (matches the upstream `Grep` count).
  - Section 1B flags both collisions (SessionsController dual-class + MetricsController/PrometheusMetricsController/actuator) with the SpringDoc disambiguation hint.
  - Section 1C walks SecurityConfig line-by-line for the auth filter chain (5 line citations minimum).
  - Section 1D documents the ApiError envelope shape and confirms it is the project-wide error response.
  - `git diff --stat` shows zero changes under `backend/` and `docs/`.
  </verify>
  <done>
  Sections 1A-1D give the reader a complete view of the public REST surface, the auth filter wiring, and the error envelope without opening a single Java source file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Current docs state, SpringDoc 2.x version verification, and integration shape</name>
  <files>.planning/phases/19-api-contract-docs/19-01-AUDIT.md</files>
  <action>
  Read `docs/API.md` (current 2-line placeholder), `docs/EXPORT_FORMAT.md` (post-18-04), `docs/DEPLOYMENT.md`, `docs/OBSERVABILITY.md`, and `docs/SELF_HOSTED_CONTRACT.md` sections 6, 7, 9. Append Sections 2 and 3 to the audit file.

  Section 2 - Current Documentation State:

  Part 2A - `docs/API.md` state:
  - Line count: 3 (header + blank + placeholder line).
  - Content verbatim: "Placeholder. The full REST surface is documented here starting in PHASE 1."
  - Phase 1 is closed (v0.3); the placeholder text is stale by 2 milestones. ROADMAP Phase 19 owns the rewrite.

  Part 2B - `docs/EXPORT_FORMAT.md` state (post-18-04):
  - Documents the `FullExportDto` wire shape (camelCase per Direction C from 18-03), the `ClaudeSummaryDto` wire shape (snake_case per `@JsonNaming`), the round-trip drift section (Plan 18-04 added: `BodyMetric.id`, `Supplement.id`, `exportedAt` non-preservation; timing validator-vs-importer asymmetry).
  - Phase 19 must NOT re-document the export wire shape; it must LINK from `docs/API.md` to `docs/EXPORT_FORMAT.md` for the export endpoints' field-level reference.

  Part 2C - `docs/SELF_HOSTED_CONTRACT.md` section 7 alignment review:
  Cite the four sub-sections with file:line citations:
    - 7.1 Built-in auth (`docs/SELF_HOSTED_CONTRACT.md:264-269`): "The app handles login and (optionally) registration. Use a maintained library; never hand-roll JWT validation."
    - 7.2 Reverse-proxy forward-auth (`docs/SELF_HOSTED_CONTRACT.md:271-290`): the four env vars (`AUTH_MODE`, `AUTH_HEADER_USER`, `AUTH_HEADER_EMAIL`, `AUTH_HEADER_GROUPS`, `TRUSTED_PROXIES`); the trust gate is non-optional.
    - 7.3 Sessions (`docs/SELF_HOSTED_CONTRACT.md:292-296`): "Use Redis (Postgres apps) or a dedicated SQLite file (SQLite apps)." NOTE: WorkoutHub uses stateless JWT (no Redis); the audit must declare this is acceptable per the contract's intent (the section 7.3 wording is about session storage IF sessions exist; stateless JWT replaces sessions in this app).
  Walk `application.yml:43-49` (auth mode + trusted proxies + headers env vars) and confirm the env var names match the contract verbatim:
    - `app.auth.mode` <- `APP_AUTH_MODE` (default `builtin`).
    - `app.auth.trusted-proxies` <- `APP_AUTH_TRUSTED_PROXIES` (default empty).
    - `app.auth.headers.user/email/groups` <- `APP_AUTH_HEADER_USER/EMAIL/GROUPS` (defaults match contract: `X-Forwarded-User`, `X-Forwarded-Email`, `X-Forwarded-Groups`).
  Cross-check: contract uses `AUTH_MODE`/`TRUSTED_PROXIES` as bare names; app reads `APP_AUTH_MODE`/`APP_AUTH_TRUSTED_PROXIES` (with `APP_` prefix). This drift is consistent across other env vars (`APP_JWT_SECRET`, `APP_CORS_ALLOWED_ORIGINS`, etc.) and is documented in `.env.example`. Audit confirms no contract violation; the prefix convention is project-specific.

  Section 3 - SpringDoc 2.x Artifact + Version Verification:

  Part 3A - Candidate artifact selection:
  Three options walked:
    - Option A1: `org.springdoc:springdoc-openapi-starter-webmvc-ui` - Spring Boot 3.x compatible; bundles `springdoc-openapi-starter-webmvc-api` + Swagger UI assets. Default UI at `/swagger-ui.html`, default JSON at `/v3/api-docs`. Recommended for new integrations.
    - Option A2: `org.springdoc:springdoc-openapi-starter-webmvc-api` - JSON-only, no Swagger UI. Useful if operators do not want a discoverable UI bundled. Half the dependency footprint of A1.
    - Option A3: `org.springdoc:springdoc-openapi-webmvc-core` (no -starter-) - manual configuration; bypassed by Boot's auto-config story. Rejected; not idiomatic in Spring Boot 3.x.
  Lean Option A1 (Swagger UI bundled) UNLESS Section 4 Part D (operator-restriction stance) decides Swagger UI must NOT be exposed in the publishable image. In that case Option A2 is correct. Plan 19-02 verdict.

  Part 3B - Version pin verification (**REQUIRES WEB FETCH**):
  Plan 19-01 executor MUST run a WebFetch against the SpringDoc release notes and the Spring Boot 3.5 compatibility matrix:
    - `WebFetch https://springdoc.org/` to capture the latest stable line.
    - `WebFetch https://central.sonatype.com/artifact/org.springdoc/springdoc-openapi-starter-webmvc-ui/versions` to capture the most recent published versions (or use `mcp__context7__` if available for the springdoc/springdoc-openapi documentation).
    - `WebFetch https://github.com/springdoc/springdoc-openapi/releases` to capture the changelog and any Spring Boot 3.5.x compatibility notes.
  Record findings:
    - Latest stable version (as of 2026-05-06).
    - Minimum Spring Boot version supported by that release line.
    - Known incompatibilities with Spring Boot 3.5.14 (e.g. Jakarta EE 10 vs 11; Hibernate Validator transitive version pin).
    - Released vs. SNAPSHOT designation.
  Recommend a single version string (e.g. `2.7.0`) for plan 19-02. Mark it as a Section 6 deliverable for the `pom.xml` edit (with the protected-file warning).

  If WebFetch is unavailable in the executor's environment, fall back to: cite the SpringDoc 2.6.0 release as the conservative pin (released circa Spring Boot 3.4.x; still compatible with 3.5.x per SemVer guarantees) and recommend that Plan 19-02 verifies the pinned version against CI before merging.

  Part 3C - Integration shape (zero-touch defaults):
  With `springdoc-openapi-starter-webmvc-ui` on the classpath, SpringDoc auto-configures:
    - `GET /v3/api-docs` (JSON, default path; configurable via `springdoc.api-docs.path`).
    - `GET /v3/api-docs.yaml` (YAML).
    - `GET /swagger-ui.html` (UI; redirects to `/swagger-ui/index.html`).
    - `GET /swagger-ui/**` (UI assets).
  The default scan covers all `@RestController` beans across the application.
  Without any further annotation, the OpenAPI document will:
    - Group endpoints by class simple name (tag = `SessionsController` not `Workout sessions`).
    - Use Jackson DTO record-component name + `@JsonNaming` for schema property names (correct behavior - confirmed by SpringDoc 2.x release notes for Jackson interop).
    - Auto-derive HTTP method, path, request body type, and response body type from the controller method signature.
    - Default to no security scheme (every endpoint shows as public).
  Plan 19-02 must ADD an `OpenApiConfig` Java class (under `common/config/`) declaring the two security schemes (JWT + forward-auth) AND must explicitly EXCLUDE actuator + `/livez` + `/healthz` + `/metrics` paths via `springdoc.paths-to-match` or `springdoc.paths-to-exclude` config keys.

  Part 3D - Required `application.yml` config keys (for Plan 19-02 reference):
    - `springdoc.api-docs.path` = default `/v3/api-docs` (recommend keep default).
    - `springdoc.swagger-ui.path` = default `/swagger-ui.html` (recommend keep default OR redirect to `/api/docs` for operator-friendly URL; verdict in Section 4).
    - `springdoc.paths-to-match` = `/api/**` (recommend; excludes `/livez`, `/healthz`, `/metrics`, `/actuator/**`, `/error`).
    - `springdoc.swagger-ui.disable-swagger-default-url` = `true` (recommend; suppress default petstore demo).
    - `springdoc.swagger-ui.tags-sorter` = `alpha` (recommend; predictable navigation).
    - `springdoc.swagger-ui.operations-sorter` = `method` (recommend; group by HTTP verb).
  Section 4 must decide whether to expose Swagger UI in production (operator concern) or only the JSON via `/v3/api-docs` (operator can render externally).

  Constraint: no Java files modified. WebFetch is a read-only network call permitted by the GSD workflow.
  </action>
  <verify>
  - Section 2A captures the verbatim 3-line `docs/API.md` placeholder text.
  - Section 2B confirms `docs/EXPORT_FORMAT.md` is the reference for export wire shape and Phase 19 LINKS rather than re-documents.
  - Section 2C cites the four sub-sections of contract section 7 with line ranges and confirms `application.yml` env vars match (modulo `APP_` prefix convention).
  - Section 3A walks 3 SpringDoc artifact options and lands on Option A1 or A2 with rationale.
  - Section 3B includes WebFetch findings (or the conservative-pin fallback) with a single version recommendation.
  - Section 3C describes the zero-touch defaults and the four auto-exposed paths.
  - Section 3D enumerates the 6 application.yml config keys recommended for Plan 19-02 with default-vs-recommend distinctions.
  - `git diff --stat` shows only `.planning/phases/19-api-contract-docs/19-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 2 and 3 give plan 19-02+ the SpringDoc artifact + version verdict, the integration shape, and the contract section 7 alignment baseline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Security-scheme + actuator-exclusion analysis, contract alignment, recommended Phase 19 plan-02+ scope</name>
  <files>.planning/phases/19-api-contract-docs/19-01-AUDIT.md</files>
  <action>
  Append Sections 4, 5, and 6 to the audit file.

  Section 4 - Behavioral Analysis:

  Part 4A - Two security schemes (contract section 7.1 + 7.2):
  Walk both auth modes and document the OpenAPI 3.x security scheme declaration shape required for each.
    - Built-in JWT (default mode): `securitySchemes.bearerAuth` of `type: http`, `scheme: bearer`, `bearerFormat: JWT`. Applies as the global `security: [{bearerAuth: []}]` to every `/api/**` endpoint EXCEPT the permitAll matchers (`POST /api/auth/login`, `POST /api/auth/refresh`, `POST /api/auth/reset-password`, `POST /api/webhooks/scale/**`).
    - Forward-auth (opt-in): `securitySchemes.forwardAuth` of `type: apiKey`, `in: header`, `name: X-Forwarded-Email` (or `X-Forwarded-User`; the email is the keying value per `ForwardAuthFilter.java:66-72`). The OpenAPI document declares this as an alternative scheme; client tooling shows it as "Pick one" rather than "Both required". Note: `apiKey` is a documentation-only declaration; the trust gate (source IP must be in `TRUSTED_PROXIES`) is enforced by the application, not by client-side OpenAPI consumers. Audit must explicitly state this asymmetry: the OpenAPI document CANNOT model the source-IP trust gate (it is not part of the request shape); it can only declare the header; the security boundary is documented separately in `docs/API.md` prose plus `docs/SELF_HOSTED_CONTRACT.md` 7.2 link.
  Verdict: declare both schemes. Mark `bearerAuth` as the default (applied globally); operators flipping to forward-auth must read `docs/API.md` to learn the alternative scheme is gated by reverse-proxy header injection.

  Part 4B - Actuator + management exclusion:
  Walk `application.yml:60-74`:
    - `management.endpoints.web.exposure.include = health,info,prometheus`.
    - `management.endpoint.health.show-details = never`.
    - `management.metrics.tags.application = workouthub`.
    - `management.prometheus.metrics.export.enabled = true`.
  Three management endpoints exposed publicly:
    - `/actuator/health` (Spring Boot Actuator default; not for user docs).
    - `/actuator/info` (build info; not for user docs).
    - `/actuator/prometheus` (Prometheus scrape; operator concern).
  Plus the contract-required app-owned endpoints:
    - `/livez`, `/healthz` via `HealthController`.
    - `/metrics` via `PrometheusMetricsController`.
  None of these belong in the publicly-documented OpenAPI surface (the surface for user-facing clients = `/api/**`). Plan 19-02 must add `springdoc.paths-to-match: "/api/**"` to suppress the actuator + operator paths.
  Verdict: confirm `/api/**` include scope.
  Note for Section 6: a separate operator-targeted documentation page in `docs/OBSERVABILITY.md` already covers the operator paths (verify via Read in Task 1); cross-link from `docs/API.md` to `docs/OBSERVABILITY.md` for those paths.

  Part 4C - `docs/API.md` rewrite shape (single most important Phase 19 verdict):
  Walk three directions:
    - Direction C1: hand-write a per-endpoint reference (table of endpoint, method, request shape, response shape, error codes, auth requirement). Cost: O(N) prose for N=134 mappings; high maintenance burden (every plan in v0.5+ that adds an endpoint requires a doc edit; no compile-time link to the actual code). Pros: human-readable; Cons: out-of-date the day after merging.
    - Direction C2: replace `docs/API.md` with a thin navigational README that LINKS to:
      - The deployed `/v3/api-docs` JSON endpoint (operator-deployable Swagger UI at `/swagger-ui.html`).
      - A static export of the OpenAPI JSON committed under `docs/openapi.json` (snapshot per release tag; refreshed by a CI job on tag push or by `maven-resources-plugin` + `springdoc-openapi-maven-plugin`).
      - `docs/EXPORT_FORMAT.md` for export wire shape.
      - `docs/SELF_HOSTED_CONTRACT.md` section 7 for auth.
      - `docs/OBSERVABILITY.md` for operator paths.
      The page also contains: a security-scheme overview (built-in JWT + forward-auth), a list of typed `ApiError.code` values with the path that emits each, and a brief "How to consume the OpenAPI document for client codegen" pointer.
    - Direction C3: hybrid - hand-write the auth + error-code overview (~50 lines) AND embed an iframe / link to Swagger UI for the rest. Pros: best human read-time; Cons: most maintenance; doc duplication.
  Recommendation: Direction C2. Spring Boot 3.5 + SpringDoc 2.x makes the OpenAPI document the single source of truth; `docs/API.md` becomes navigational. Static export under `docs/openapi.json` is OPTIONAL for v0.4 (defer to Phase 42 / v1.0); minimum viable Phase 19 ships only the navigational README + the runtime endpoint.
  Verdict: Direction C2 (navigational, linking to runtime + per-section docs); static `docs/openapi.json` export deferred to Phase 42.

  Part 4D - Swagger UI exposure stance (operator concern):
  Walk three directions:
    - Direction D1: expose Swagger UI in production (`/swagger-ui.html`); operator decides to gate via reverse proxy. Pros: zero operator work; Cons: information disclosure (every endpoint visible to anyone reaching the bound interface; the contract section 3.2 says default `BIND_ADDR=127.0.0.1` so the surface is not publicly reachable by default).
    - Direction D2: ship Swagger UI but require an env flag `APP_OPENAPI_UI_ENABLED=false` (default) to disable. Operator opts in. Pros: safer default; Cons: extra env var + extra config wiring.
    - Direction D3: never bundle Swagger UI in production; ship JSON only via `/v3/api-docs`. Operator renders via a local Swagger UI or Postman. Maps to Option A2 from Section 3A.
  Recommendation: Direction D1. The contract's `BIND_ADDR=127.0.0.1` default means Swagger UI is only reachable from the operator's host by default. Operators who expose `BIND_ADDR=0.0.0.0` (against the contract) accept the discoverability risk. Documented in `docs/API.md` "Discoverability" sub-section.
  Verdict: Direction D1 (Swagger UI bundled, no env flag); document the bind-address-as-the-trust-boundary stance in `docs/API.md`.

  Part 4E - `ApiError.code` catalog:
  Walk every `ConflictException` and `ResponseStatusException` throw site in the codebase via Grep (`Grep "ConflictException\\(" --type java` plus `Grep "ResponseStatusException" --type java`). Catalog the typed codes:
    - `SESSION_ALREADY_ACTIVE` (sessions/SessionsService).
    - `SESSION_ALREADY_FINISHED` (sessions/SessionsService).
    - `SESSION_FINISHED` (sessions/SessionSetsService).
    - `SET_NUMBER_DUPLICATE` (sessions/SessionSetsService).
    - Any others surfaced by Grep.
  Audit must confirm the OpenAPI document's `ApiError.code` enum mirrors this list. Plan 19-02 may either:
    - Hand-curate the enum on the OpenAPI schema annotation.
    - Generate the enum at build time from a Java enum (`ApiErrorCode.java`) — but introducing a new enum is OUT of scope for Phase 19; the v0.4 cycle did not budget for it. Defer to Phase 42 if a maintainer wants strict compile-time enforcement.
  Verdict: hand-curate the OpenAPI annotation in Plan 19-02; mark "extract `ApiErrorCode` enum" as a Phase 42 candidate.

  Section 5 - Cross-Package Coupling Map and Verdicts:

  Part 5A - SpringDoc consumers (zero today):
  Currently zero project files import `io.swagger.v3.oas.annotations.*` or `org.springdoc.*`. `Grep "io.swagger.v3.oas.annotations|org.springdoc" --type java` returns no results. Phase 19 introduces the package boundary cleanly. Plan 19-02 will add:
    - `OpenApiConfig` (under `common/config/`) declaring `@Bean OpenAPI` with the two security schemes + project info.
    - Per-controller `@Tag` annotations OR a single `springDoc-openapi.group-configs` config key (audit verdict in 5B).
    - Per-method `@Operation(summary = "...", operationId = "...")` for the two SessionsController collisions (Section 1B). The full project does NOT need per-method annotations — SpringDoc auto-derives reasonable defaults.

  Part 5B - Tag strategy:
  Walk three options:
    - Option B1: rely on SpringDoc class-name auto-tagging. Tags: `auth-controller`, `users-controller`, `sessions-controller`, etc. Pros: zero annotation cost; Cons: ugly tags ("controller" suffix), tag collisions on dual-class names.
    - Option B2: add `@Tag(name = "Auth")` etc. to every controller. 31 single-line annotations. Pros: clean tags; Cons: 31 file touches.
    - Option B3: configure `springdoc.group-configs` in `application.yml` per package. Pros: zero Java annotation; Cons: less granular than per-controller `@Tag`.
  Verdict: Option B2 (per-controller `@Tag`). 31 single-line edits; one tag per controller; matches the feature-package-as-tag mental model. Plan 19-02 absorbs all 31 edits in a single commit.

  Part 5C - SecurityConfig changes (if any):
  `SecurityConfig.java:43-45` already permitAlls `/livez`, `/healthz`, `/metrics`, `/actuator/health`, `/actuator/info`, `/actuator/prometheus`. SpringDoc paths to add to the permitAll list:
    - `/v3/api-docs/**` (the OpenAPI JSON; must be reachable for tooling).
    - `/swagger-ui/**` (UI assets; if Direction D1).
    - `/swagger-ui.html` (UI entry).
  Plan 19-02 must extend the permitAll matcher in `SecurityConfig.java:43-45` to add these three patterns. Without this, the JWT filter will return 401 for any unauthenticated GET on the OpenAPI document — defeating the purpose.
  Verdict: 1 new matcher line in SecurityConfig.

  Part 5D - Build wiring:
  Three build-tool options:
    - Option E1: pom.xml dependency only. SpringDoc serves OpenAPI at runtime via `/v3/api-docs`. No static export.
    - Option E2: pom.xml dependency + `springdoc-openapi-maven-plugin` to generate `target/openapi.json` at build time AND copy to `docs/openapi.json` via `maven-resources-plugin`. Pros: snapshot doc in git; Cons: extra plugin config; build-time HTTP call to a temporary started Spring context.
    - Option E3: pom.xml + a CI job step `gh workflow on tag push` that runs `mvn springdoc-openapi-maven-plugin:run` and commits `docs/openapi.json`. Pros: doc in git only on releases; Cons: protected `pom.xml` + CI workflow file (also protected) edit.
  Verdict: Option E1 for v0.4; Option E2 deferred to Phase 42. Audit confirms NO `pom.xml` plugin block edit beyond the single dependency add.

  Part 5E - Test strategy:
  New integration test class `OpenApiSurfaceIntegrationTest` (under `backend/src/test/java/com/workouthub/common/web/`) extending `AbstractIntegrationTest`. Tests:
    - `openApiJsonIsReachableUnauthenticated` (200 on GET `/v3/api-docs`).
    - `openApiJsonContainsApiPathsAndExcludesActuator` (assert `paths.["/api/sessions"]` exists and `paths.["/actuator/health"]` does not).
    - `openApiJsonDeclaresBothSecuritySchemes` (assert `components.securitySchemes.bearerAuth` and `components.securitySchemes.forwardAuth`).
    - `swaggerUiIsReachableUnauthenticated` (200 on GET `/swagger-ui.html` if Direction D1; ignore if D3).
    - `apiErrorCodeEnumMatchesGlobalExceptionHandler` (assert the `ApiError` schema's `code` field enum lists all typed codes).
  Five tests minimum; one new test file ~80 lines.

  Section 6 - Recommended Phase 19 plan-02+ Scope:

  Bulleted list of concrete tasks plan 19-02+ should plan, each with: target files, expected change shape, and which test file picks up the regression coverage. Group by bucket:

  - `springdoc-dependency` — add `org.springdoc:springdoc-openapi-starter-webmvc-ui` (Option A1, version pinned per Section 3B) to `backend/pom.xml`. **This file is PROTECTED per `CLAUDE.md` Protected Files; the executor MUST halt and ask the user for confirmation before editing.** Single `<dependency>` block under `<dependencies>`. No version property entry needed if pin is hard-coded; recommend defining `<springdoc.version>` in `<properties>` for consistency with `<jjwt.version>` precedent. Targets: `backend/pom.xml` (1 dependency block, 1 property entry). Depends on Section 3 verdicts.

  - `openapi-config-class` — new file `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java`. Declares `@Bean OpenAPI customOpenAPI()`:
    - `info.title = "WorkoutHub API"`, `info.version = ${app.version}` (read from `IDENTITY.yaml` or via Spring `@Value`), `info.description = "Self-hosted multi-user fitness tracker. See docs/API.md."`.
    - `components.securitySchemes.bearerAuth` (`type: http`, `scheme: bearer`, `bearerFormat: JWT`).
    - `components.securitySchemes.forwardAuth` (`type: apiKey`, `in: header`, `name: X-Forwarded-Email`).
    - Global `security: [{bearerAuth: []}]` (operators in forward-auth mode override per-deployment).
    - `servers: []` left empty (operator's PUBLIC_URL is per-deployment; SpringDoc default is the request's own scheme/host).
    Targets: `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java` (~50 lines new file). Depends on Section 4 Part 4A verdict.

  - `application-yml-springdoc-keys` — add 6 `springdoc.*` keys under `application.yml` (confirm against Section 3D). Same keys do NOT need to live in `application-prod.yml` unless the prod overrides differ. Targets: `backend/src/main/resources/application.yml` (~6 lines added; non-protected). Depends on Sections 3D + 4B verdicts.

  - `security-config-permitall` — extend `SecurityConfig.java:43-45` permitAll matcher with `/v3/api-docs/**`, `/swagger-ui/**`, `/swagger-ui.html`. Targets: `backend/src/main/java/com/workouthub/common/config/SecurityConfig.java` (1 line edit, ~3 path patterns added). Depends on Section 5 Part 5C verdict.

  - `controller-tag-annotations` — add `@Tag(name = "...")` to all 31 `@RestController` classes. One-line annotation per class. Tag names mirror feature package names with proper casing (`Auth`, `Users`, `Sessions`, `Workouts`, `Metrics`, `Analytics`, `Exercises`, `Exports`, `Nutrition`, `Supplements`, `Water`, `Achievements`, `Challenges`, `Push`, `Webhooks`, `Health Import`, `Audit`, `2FA`, `Admin`, `Health` operator, `Prometheus` operator). Targets: 31 `*Controller.java` files (1 line each). Depends on Section 5 Part 5B Option B2 verdict.

  - `operationid-disambiguation` — add `@Operation(operationId = "...")` to the dual-class collision in `users.SessionsController` vs `sessions.SessionsController`. Audit must Grep for any other dual-class collisions (e.g. `MetricsController` is unique because `PrometheusMetricsController` is excluded from SpringDoc scan via `paths-to-match`). Targets: 1-2 `*Controller.java` files (3-6 method-level annotations expected). Depends on Section 1B verdict.

  - `apierror-code-enum-annotation` — add `@Schema(description = "Typed error code", allowableValues = {...})` to `ApiError.code` with the catalogued codes from Section 4 Part 4E. Targets: `backend/src/main/java/com/workouthub/common/web/ApiError.java` (1 annotation update). Depends on Section 4 Part 4E verdict.

  - `docs-api-md-rewrite` — replace the 3-line placeholder with the navigational README per Section 4 Part 4C Direction C2. Sections expected:
    - One-paragraph intro (what this doc is, what it links to).
    - "Runtime endpoints" sub-section (`/v3/api-docs`, `/swagger-ui.html`, link to live).
    - "Authentication" sub-section (built-in JWT + forward-auth; link to `docs/SELF_HOSTED_CONTRACT.md` section 7).
    - "Error envelope" sub-section (`ApiError` shape + typed `code` table).
    - "Export format" sub-section (link to `docs/EXPORT_FORMAT.md`).
    - "Operator endpoints" sub-section (link to `docs/OBSERVABILITY.md`).
    - "Discoverability" sub-section (BIND_ADDR-as-trust-boundary stance per Section 4 Part 4D).
    Targets: `docs/API.md` (~80-120 lines, full rewrite). Depends on Sections 4 Part 4C + 4D + 4E verdicts.

  - `openapi-surface-test` — new integration test per Section 5 Part 5E. Targets: `backend/src/test/java/com/workouthub/common/web/OpenApiSurfaceIntegrationTest.java` (~80 lines new file). Depends on Section 5 Part 5E verdict.

  - `defer` — explicit non-goals for v0.4: static `docs/openapi.json` export under git (Phase 42 candidate; Section 4 Part 4C); `springdoc-openapi-maven-plugin` build wiring (Phase 42; Section 5 Part 5D); `ApiErrorCode` enum extraction (Phase 42; Section 4 Part 4E); per-method `@Operation` annotations beyond the two collision spots (low ROI on the v0.4 budget; SpringDoc auto-derived shapes are sufficient); `@Schema` per-DTO annotations beyond `ApiError.code` (DTO records auto-derive correctly); OpenAPI 3.1 spec (SpringDoc 2.x emits OpenAPI 3.1 by default; no extra config needed); per-environment server overrides via `springdoc.servers`.

  For each `springdoc-dependency`, `openapi-config-class`, `application-yml-springdoc-keys`, `security-config-permitall`, `controller-tag-annotations`, `operationid-disambiguation`, `apierror-code-enum-annotation`, `docs-api-md-rewrite`, `openapi-surface-test` entry, write a one-paragraph rationale tied to a specific section verdict.

  Plan-count recommendation: how many plans (19-02, 19-03, ...) the integration warrants given comprehensive depth (`config.json: depth: comprehensive`) and the Phase 14/15/16/17/18 precedent (one bug-shape per plan; cross-cutting touch in its own plan when it crosses package boundaries).

  Suggested boundary template (the audit may revise):
  - **Plan 19-02: springdoc-integration** (medium-large plan). Bundles `springdoc-dependency` + `openapi-config-class` + `application-yml-springdoc-keys` + `security-config-permitall`. Touches: `pom.xml` (PROTECTED), `OpenApiConfig.java` (new), `application.yml`, `SecurityConfig.java`. ~4 file touches. Halt-on-pom.xml warning embedded in the plan.
  - **Plan 19-03: tag-and-collision-fix** (medium plan). Bundles `controller-tag-annotations` + `operationid-disambiguation`. Touches: 31 `*Controller.java` files (1 line each) + 1-2 method-level annotations on the SessionsController collision. ~31-33 file touches.
  - **Plan 19-04: error-schema-and-test** (small-medium plan). Bundles `apierror-code-enum-annotation` + `openapi-surface-test`. Touches: `ApiError.java` (1 annotation) + new `OpenApiSurfaceIntegrationTest.java` (~80 lines). ~2 file touches.
  - **Plan 19-05: docs-api-md-rewrite** (small-medium plan). Touches: `docs/API.md` only. ~1 file rewrite. Depends on 19-02 + 19-04 because the content references runtime endpoints + error-code enum.

  If Plan 19-03's 31-file scope is uncomfortable, split into 19-03a (per-feature `@Tag` for `/api/**` controllers, ~22 files) + 19-03b (admin + operator + edge controllers, ~9 files). Audit recommends KEEPING as a single plan since tags are mechanical.

  Constraint: do not edit any Java file. Do not propose architectural rewrites. The goal is the smallest set of changes that delivers the OpenAPI surface and respects the v0.4 budget + the protected-file rule on `pom.xml`.

  Final length check: target under 600 lines for the entire `19-01-AUDIT.md` (this audit is larger than the typical 500-line cap because it spans 31 controllers + a new external dependency + 3 documentation files). Trim verbose narrative; tables and ranked lists belong here, not prose.
  </action>
  <verify>
  - Section 4 Part 4A documents both security schemes with `bearerAuth` (http/bearer/JWT) and `forwardAuth` (apiKey/header) shapes; calls out the source-IP trust gate as un-modelable in OpenAPI.
  - Section 4 Part 4B walks the actuator + management exclusion and lands on `springdoc.paths-to-match: "/api/**"`.
  - Section 4 Part 4C walks 3 `docs/API.md` directions and lands on Direction C2 (navigational, link to runtime).
  - Section 4 Part 4D walks 3 Swagger UI exposure stances and lands on Direction D1 (bundled, BIND_ADDR is the trust boundary).
  - Section 4 Part 4E catalogs the `ApiError.code` typed values from Phase 15-04 (4+ codes minimum).
  - Section 5 Part 5A confirms zero current SpringDoc usage.
  - Section 5 Part 5B walks 3 tag strategies and lands on Option B2 (per-controller `@Tag`).
  - Section 5 Part 5C identifies the 3 SecurityConfig path patterns to add.
  - Section 5 Part 5D walks 3 build-wiring options and defers Option E2/E3 to Phase 42.
  - Section 5 Part 5E enumerates 5 integration tests for `OpenApiSurfaceIntegrationTest`.
  - Section 6 has one entry per applicable bucket (9 buckets minimum); empty buckets are stated explicitly as "None - no findings" or "Deferred to Phase 42 / v1.0".
  - Section 6 explicitly flags `pom.xml` as PROTECTED in the `springdoc-dependency` bucket and instructs the executor to halt and ask the user before editing.
  - Plan-count recommendation is explicit: 4 plans (19-02 through 19-05) with one-line objective each.
  - Every Section 6 rationale paragraph references the section that produced the verdict.
  - Total `19-01-AUDIT.md` length is under 600 lines.
  - `git diff --stat` shows only `.planning/phases/19-api-contract-docs/19-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 4, 5, and 6 give plan 19-02+ a security-scheme verdict, an actuator-exclusion verdict, a `docs/API.md` rewrite shape, a Swagger UI exposure stance, an `ApiError.code` enum catalog, a tag strategy, an integration-test plan, and explicit plan boundaries. The protected-file warning on `pom.xml` is surfaced as a halt-and-ask-the-user gate before any 19-02 work begins.
  </done>
</task>

</tasks>

<verification>
Before declaring the plan complete:
- [ ] `19-01-AUDIT.md` exists and contains Sections 1A through 6.
- [ ] Every controller, security filter, config key, contract section, and DTO referenced cites `file:line` (or migration line / contract section line).
- [ ] Section 1A lists all 31 controllers + 134 mapping count cross-check + auth tier breakdown.
- [ ] Section 1B flags the dual-class `SessionsController` and the metrics-namespace situation.
- [ ] Section 1C walks SecurityConfig line-by-line.
- [ ] Section 1D documents `ApiError` as the project-wide error envelope.
- [ ] Section 2A captures the verbatim `docs/API.md` placeholder.
- [ ] Section 2B confirms `docs/EXPORT_FORMAT.md` is the export-shape reference.
- [ ] Section 2C cites contract section 7 sub-sections with line ranges.
- [ ] Section 3A walks 3 SpringDoc artifact options and lands on one.
- [ ] Section 3B includes WebFetch-verified version pin (or conservative-pin fallback).
- [ ] Section 3C describes zero-touch defaults.
- [ ] Section 3D enumerates 6 application.yml config keys.
- [ ] Section 4 Part 4A walks both security schemes with concrete OpenAPI 3.x shape.
- [ ] Section 4 Part 4B walks actuator exclusion.
- [ ] Section 4 Part 4C walks `docs/API.md` rewrite shape and lands on Direction C2.
- [ ] Section 4 Part 4D walks Swagger UI exposure stance.
- [ ] Section 4 Part 4E catalogs `ApiError.code` typed values.
- [ ] Section 5 covers zero-current-usage, tag strategy, SecurityConfig changes, build wiring options, and 5 integration tests.
- [ ] Section 6 lists 9 buckets with file-level entries; `pom.xml` PROTECTED warning embedded.
- [ ] Plan-count recommendation is 4 plans (19-02 through 19-05) with explicit boundaries.
- [ ] Total length of `19-01-AUDIT.md` is under 600 lines.
- [ ] `git diff --stat` shows only `.planning/phases/19-api-contract-docs/` changes.
</verification>

<success_criteria>
- All three tasks complete.
- `19-01-AUDIT.md` is the single Java-free deliverable.
- Phase 19 plan-02+ has a concrete, file-level fix list bucketed by intent.
- SpringDoc artifact choice (Option A1 starter-webmvc-ui vs A2 api-only vs A3 manual) is settled with rationale.
- SpringDoc version pin is settled (or marked for WebFetch verification at execution time).
- Two security schemes (built-in JWT + forward-auth) have concrete OpenAPI 3.x declaration shapes.
- Actuator + management endpoint exclusion is settled (`springdoc.paths-to-match: "/api/**"`).
- `docs/API.md` rewrite shape is settled (Direction C2 navigational README).
- Swagger UI exposure stance is settled (Direction D1 bundled, BIND_ADDR boundary).
- `ApiError.code` typed values are catalogued for the OpenAPI schema annotation.
- Tag strategy is settled (Option B2 per-controller `@Tag`).
- Plan-count for Phase 19 implementation (19-02..19-05) is explicit.
- Protected-file warning on `pom.xml` is surfaced and the executor knows to halt-and-ask before any 19-02 edit.
- No Java sources, migrations, tests, pom.xml, or other documentation files are modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/19-api-contract-docs/19-01-SUMMARY.md` with frontmatter and body following the Phase 18-01 audit summary shape:

---
phase: 19-api-contract-docs
plan: 01
subsystem: docs, common/config, common/web
tags: [audit, openapi, springdoc, jwt-bearer, forward-auth, actuator-exclusion, contract-alignment, planning]

requires:
  - phase: 18-export-refinement
    plan: 04
    reason: Phase 18 close fixed the post-FullExportDto wire shape; Phase 19 inherits exactly and references docs/EXPORT_FORMAT.md from the new docs/API.md
  - phase: 17-body-metrics
    plan: 04
    reason: MetricsService.UpsertResult 200/201 split must be reflected accurately on the OpenAPI document for the POST /api/metrics endpoint
  - phase: 15-sessions-core
    plan: 04
    reason: ApiError.code typed envelope is the project-wide error response shape; the OpenAPI ApiError schema must list the catalogued codes
  - phase: v0.3 (Phase 5)
    reason: ForwardAuthFilter + AUTH_MODE=forward-auth was landed in v0.3; the OpenAPI document declares the forward-auth security scheme based on this filter

provides:
  - REST surface inventory (31 controllers, 134 mappings, auth tier breakdown, two namespace collisions flagged)
  - current docs/API.md placeholder state and docs/EXPORT_FORMAT.md reference role
  - SpringDoc 2.x artifact + version verdict (Option A1 starter-webmvc-ui or Option A2 api-only; pin per WebFetch)
  - integration shape (zero-touch defaults + 6 application.yml keys recommended)
  - two security schemes (bearerAuth http/bearer/JWT + forwardAuth apiKey/header)
  - actuator-and-management exclusion verdict (springdoc.paths-to-match = /api/**)
  - docs/API.md rewrite shape (Direction C2 navigational README; static export deferred)
  - Swagger UI exposure stance (Direction D1 bundled; BIND_ADDR is the trust boundary)
  - ApiError.code typed-values catalog with Grep-driven coverage
  - tag strategy (Option B2 per-controller @Tag)
  - SecurityConfig permitAll path additions for OpenAPI endpoints
  - integration test plan (5 tests in OpenApiSurfaceIntegrationTest)
  - protected-file warning on pom.xml for plan 19-02 dependency add
  - explicit plan boundaries (19-02 through 19-05) with one-line objectives

affects:
  - 19-api-contract-docs/19-02 (springdoc-integration: pom.xml PROTECTED + OpenApiConfig + application.yml + SecurityConfig)
  - 19-api-contract-docs/19-03 (tag-and-collision-fix: 31 @Tag annotations + 1-2 @Operation operationId)
  - 19-api-contract-docs/19-04 (error-schema-and-test: ApiError @Schema + OpenApiSurfaceIntegrationTest)
  - 19-api-contract-docs/19-05 (docs-api-md-rewrite: full docs/API.md replacement per Direction C2)
  - 20-release-v0-4 (CHANGELOG.md user-visible entry: "OpenAPI document at /v3/api-docs; Swagger UI at /swagger-ui.html"; docs/MIGRATION.md "no migration steps" entry)
  - ISSUES.md (new candidates if surfaced: SpringDoc + Spring Boot 3.5 transitive issue, ApiError.code enum extraction deferred to Phase 42)

tech-stack:
  added: ["org.springdoc:springdoc-openapi-starter-webmvc-ui (Option A1, version per Section 3B)"]
  patterns: ["@Tag per-controller (Option B2)", "@Operation operationId disambiguation for dual-class collisions"]

key-files:
  created:
    - .planning/phases/19-api-contract-docs/19-01-AUDIT.md
  modified: []

key-decisions:
  - "[SpringDoc artifact: Option A1 starter-webmvc-ui (UI bundled) OR Option A2 api-only (JSON only)]"
  - "[SpringDoc version pin: per Section 3B WebFetch findings or conservative 2.6.0 fallback]"
  - "[docs/API.md rewrite shape: Direction C2 (navigational README) OR Direction C1 (hand-written reference) OR Direction C3 (hybrid)]"
  - "[Swagger UI exposure stance: Direction D1 (bundled) OR Direction D2 (env-flag opt-in) OR Direction D3 (no UI; JSON only)]"
  - "[Tag strategy: Option B2 per-controller @Tag OR Option B1 auto-tag OR Option B3 group-configs]"
  - "[Build wiring: Option E1 (runtime only, no static export) OR Option E2 (maven plugin static export) OR Option E3 (CI tag-driven export)]"
  - "[ApiError.code curation: hand-write @Schema enum on ApiError.code OR extract ApiErrorCode Java enum (Phase 42)]"
  - "[Plan-count for Phase 19 implementation: 4 plans (19-02 through 19-05) with explicit boundaries]"
  - "[Protected-file gate on pom.xml: executor must halt and ask user before adding the SpringDoc dependency in plan 19-02]"
---

# Phase 19 Plan 01: api-contract-docs audit Summary

**[One-line audit outcome — what landed, what is now planning-ready]**

## Accomplishments
- REST surface inventory: 31 controllers, 134 mappings, auth tier (5 admin, 3 operator, 23 user-JWT), 2 namespace collisions flagged.
- docs/API.md placeholder confirmed (3 lines stale by 2 milestones); docs/EXPORT_FORMAT.md reference role confirmed; contract section 7 alignment mapped.
- SpringDoc artifact verdict: [Option A1 starter-webmvc-ui / Option A2 api-only].
- SpringDoc version pin: [WebFetch-verified version / conservative 2.6.0 fallback].
- Two security schemes (bearerAuth + forwardAuth) declared with concrete OpenAPI 3.x shape; source-IP trust gate documented as un-modelable.
- Actuator-and-management exclusion verdict: springdoc.paths-to-match = /api/**.
- docs/API.md rewrite shape verdict: Direction C2 (navigational; static export deferred).
- Swagger UI exposure stance: Direction D1 (bundled; BIND_ADDR is the trust boundary).
- ApiError.code typed-values cataloged for the OpenAPI schema annotation.
- Tag strategy verdict: Option B2 (per-controller @Tag, 31 single-line edits).
- SecurityConfig permitAll additions: 3 paths (/v3/api-docs/**, /swagger-ui/**, /swagger-ui.html).
- Integration test plan: 5 tests in new OpenApiSurfaceIntegrationTest.
- Protected-file warning on pom.xml surfaced for plan 19-02.
- Plan-count recommendation: 4 plans (19-02 springdoc-integration, 19-03 tag-and-collision-fix, 19-04 error-schema-and-test, 19-05 docs-api-md-rewrite).

## Files Created/Modified
- `.planning/phases/19-api-contract-docs/19-01-AUDIT.md` - audit deliverable.

## Decisions Made
- SpringDoc artifact + version line.
- docs/API.md rewrite shape direction.
- Swagger UI exposure stance.
- Tag strategy.
- Build wiring (runtime-only for v0.4).
- ApiError.code curation strategy.
- Phase 19 plan boundaries.
- Protected-file gate on pom.xml.

## Issues Encountered
- Possible NEW issues surfaced (raise as i-11+ in plan 19-02 if confirmed):
  - SessionsController dual-class operationId collision (definitely needs disambiguation).
  - ApiError.code enum drift if more codes are added post-19-04 (defer to Phase 42 enum extraction).
  - Local mvn unavailability means SpringDoc smoke test runs only in CI on first push; surface as a v0.4 risk.

## Next Phase Readiness
- Ready for Phase 19 plan-02+ planning: yes.
- Open questions for plan 19-02+: confirm SpringDoc version pin via WebFetch at execution time; confirm `pom.xml` edit authorization at the protected-file halt gate.
</output>
</content>
</invoke>