---
phase: 19-api-contract-docs
plan: 02
type: execute
domain: backend
---

<objective>
Land the SpringDoc 2.x runtime integration so a green CI build serves a populated OpenAPI 3.x JSON document at `GET /v3/api-docs` and a bundled Swagger UI at `GET /swagger-ui.html`, with the user-facing surface (`/api/**`) included and the operator surface (`/livez`, `/healthz`, `/metrics`, `/actuator/**`, `/error`) excluded, both security schemes (`bearerAuth` http/bearer/JWT applied globally and `forwardAuth` apiKey/header `X-Forwarded-Email` declared as alternative) present in `components.securitySchemes`, and the SpringDoc paths added to the `SecurityConfig` permitAll allowlist so unauthenticated requests reach the JSON + UI without a 401. Realises 19-01-AUDIT Section 6 buckets `springdoc-dependency`, `openapi-config-class`, `application-yml-springdoc-keys`, and `security-config-permitall`. Mid-plan of Phase 19 (4-plan structure: 01 audit + 02 springdoc-integration + 03 tag-and-collision-fix + 04 error-schema-and-test + 05 docs-api-md-rewrite).

Purpose: 19-02 is the load-bearing wiring plan for Phase 19. Without it, the 28 `@Tag` annotations from plan 19-03 have no document to land on, the `ApiError.code` `@Schema` annotation from plan 19-04 has no schema to land on, the `OpenApiSurfaceIntegrationTest` from plan 19-04 has no endpoint to assert against, and the `docs/API.md` rewrite from plan 19-05 has no runtime endpoint to link to. 19-02 ships the dependency add (`org.springdoc:springdoc-openapi-starter-webmvc-ui`), the `OpenApiConfig` Java bean (info block + two security schemes + global `bearerAuth` requirement), the 6 `application.yml` keys (`paths-to-match=/api/**`, default api-docs/swagger-ui paths, `disable-swagger-default-url=true`, `tags-sorter=alpha`, `operations-sorter=method`), and the 3 SpringDoc URL patterns added to the `SecurityConfig` permitAll matcher (`/v3/api-docs/**`, `/swagger-ui/**`, `/swagger-ui.html`). After 19-02: `GET /v3/api-docs` returns a 200 with non-empty `paths` map containing at least one `/api/**` entry; `GET /swagger-ui.html` returns a 200 with the bundled UI shell; the actuator + operator paths do NOT appear in the document; both security schemes are declared.

Output: `backend/pom.xml` gains a `<springdoc.version>2.6.0</springdoc.version>` `<properties>` entry (mirroring the `<jjwt.version>` precedent at `pom.xml:22`) plus a single `<dependency>` block on `org.springdoc:springdoc-openapi-starter-webmvc-ui:${springdoc.version}` in the existing `<dependencies>` list. New file `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java` (~50 lines) declares an `@Bean OpenAPI customOpenAPI()` populating `info.title`, `info.version`, `info.description`, `components.securitySchemes.bearerAuth`, `components.securitySchemes.forwardAuth`, and a global `security: [{bearerAuth: []}]` requirement (no per-method `security: []` overrides for permitAll endpoints in this plan; that disambiguation lands in plan 19-03 if needed but is documented as out-of-scope here per the audit's plan-boundary verdict). `backend/src/main/resources/application.yml` gains a `springdoc:` block at the top-level (alongside `spring:`, `server:`, `app:`, `management:`, `logging:`) with the 6 keys per audit Section 3D. `backend/src/main/java/com/workouthub/common/config/SecurityConfig.java` extends the existing operator-paths permitAll matcher at `:43-45` with 3 SpringDoc URL patterns. NO smoke test added in this plan (the `OpenApiSurfaceIntegrationTest` is plan 19-04's deliverable). NO `@Tag` annotations on controllers (plan 19-03). NO `@Schema` on `ApiError.code` (plan 19-04). NO `docs/API.md` edits (plan 19-05).

The `backend/pom.xml` edit is the protected-file halt-and-ask gate per `CLAUDE.md` "Protected Files". Task 1 below is the dependency add and the executor MUST pause and request explicit user confirmation before invoking the Edit tool on `pom.xml`. The orchestration reference (`~/.claude/get-shit-done/references/checkpoints.md`) documents this as the canonical "executor diff would add an external dependency... Always pause" pattern.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md
@.planning/phases/19-api-contract-docs/19-01-AUDIT.md
@.planning/phases/19-api-contract-docs/19-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@docs/SELF_HOSTED_CONTRACT.md
@backend/pom.xml
@backend/src/main/resources/application.yml
@backend/src/main/resources/application-prod.yml
@backend/src/main/java/com/workouthub/common/config/SecurityConfig.java
@backend/src/main/java/com/workouthub/common/security/JwtAuthenticationFilter.java
@backend/src/main/java/com/workouthub/common/security/ForwardAuthFilter.java

**Tech stack available (carried from Phase 18 close + 19-01 audit):** Java 21, Spring Boot 3.5.14 (`pom.xml:9-11`), Spring Web, Spring Data JPA, Spring Security (stateless JWT chain at `SecurityConfig.java:35`, optional `ForwardAuthFilter` mounted before JWT at `:54-55` iff `app.auth.mode=forward-auth`), Spring Boot Actuator (already at `pom.xml:73-76`; `management.endpoints.web.exposure.include = health,info,prometheus` at `application.yml:60-64`), Jackson 2.x with `serializationInclusion=NON_NULL` (`JacksonConfig`), Hibernate `@UuidGenerator`, Bean Validation via `jakarta.validation`. Local mvn unavailable on this Windows host (memory: `local_maven_gap`); the dependency add is CI-tested only on the next push.

**Established patterns (carried from prior plans + audits):**
- `pom.xml` version-pin pattern at `:22` (`<jjwt.version>0.13.0</jjwt.version>`) is the precedent for the new `<springdoc.version>2.6.0</springdoc.version>` property entry. Mirroring this keeps version literals out of the `<dependencies>` block.
- `SecurityConfig.java:43-46` operator-permitAll matcher already lists `/livez`, `/healthz`, `/metrics`, `/actuator/health`, `/actuator/info`, `/actuator/prometheus`, and `/error`. Adding 3 more SpringDoc paths is a 1-line Java edit using the same `requestMatchers(...)` chain.
- `application.yml` top-level keys are flush-left (`spring:`, `server:`, `app:`, `management:`, `logging:`); the new `springdoc:` block follows the same convention. Production override file `application-prod.yml` is consulted; per audit Section 3D the SpringDoc keys do NOT need a prod override.
- New `@Configuration` files under `common/config/` follow the existing precedent (`SecurityConfig.java`, `CorsConfig.java`, `JacksonConfig.java`, `ClockConfig.java`). `OpenApiConfig` slots into the same package alongside them.
- `@Bean` methods in `common/config/` use `@Bean` package-private return types per existing precedent (`SecurityFilterChain securityFilterChain(...)` is package-private). The `OpenAPI` bean follows the same access modifier.
- Existing controllers (31 today) carry NO SpringDoc annotations. The OpenAPI document SpringDoc auto-generates without annotations groups every endpoint by class simple name (e.g. `SessionsController` tag); plan 19-03 lands per-controller `@Tag` annotations to override these defaults. Plan 19-02 produces a working but ugly-tag document; that ugliness is INTENTIONAL boundary between plans.
- Test discipline (carried for plan 19-04, NOT touched here): integration tests extend `AbstractIntegrationTest` (real Postgres via Testcontainers); MockMvc + Hamcrest matchers; no direct repository writes.

**Constraining decisions (sourced from 19-01-AUDIT):**
- **SpringDoc artifact: Option A1, single starter.** Add ONLY `org.springdoc:springdoc-openapi-starter-webmvc-ui`. The starter brings `springdoc-openapi-starter-webmvc-api` transitively; declaring the api-only artifact alongside is redundant and is rejected per audit Section 3A. NO `<exclusions>` block on the dependency (the transitive footprint is intentional; UI assets, Webjars, swagger-core 2.2.x).
- **SpringDoc version pin: 2.6.0.** Conservative pin per audit Section 3B; compatible with Spring Boot 3.5.x via SemVer guarantees. The plan does NOT WebFetch a newer version at execution time; staying on the pinned 2.6.0 is the deterministic verdict the audit committed to. Bumping to 2.7.x is a one-line follow-up if a future plan opts in.
- **Security schemes: declare both, no per-method overrides in this plan.** `bearerAuth` (http/bearer/JWT) is the global requirement applied via `OpenApiConfig.addSecurityItem(new SecurityRequirement().addList("bearerAuth"))`. `forwardAuth` (apiKey, in: header, name: `X-Forwarded-Email`) is declared in `components.securitySchemes` but NOT applied as a global requirement. The OpenAPI consumer sees both schemes available; tooling presents the bearer scheme as default. Per-method `security: []` overrides on the 4 permitAll endpoints (`POST /api/auth/login`, `POST /api/auth/refresh`, `POST /api/auth/reset-password`, `POST /api/webhooks/scale/**`) are deferred (audit Section 4A acknowledges them but the plan-boundary verdict is "global declaration is sufficient for v0.4 reading; per-method disambiguation is a v1.0 polish item"). This plan does NOT touch any controller method.
- **Source-IP trust gate is un-modelable in OpenAPI.** Per audit Section 4A asymmetry note, OpenAPI 3.x has no mechanism to express "header trusted only when source IP is in TRUSTED_PROXIES". `forwardAuth` is declared as a header-presence scheme; the trust boundary is documented in prose-side `docs/API.md` rewrite (plan 19-05's deliverable). 19-02 does NOT attempt to encode this constraint in the OpenAPI document.
- **Actuator + operator path exclusion via `paths-to-match=/api/**`.** Single application.yml key suppresses `/livez`, `/healthz`, `/metrics`, `/actuator/health`, `/actuator/info`, `/actuator/prometheus`, `/error` from the public surface. `paths-to-exclude` is NOT used (would duplicate the include list's complement). Per audit Section 4B, the catch-all include is the single source of truth.
- **Swagger UI bundled (Direction D1).** The `springdoc-openapi-starter-webmvc-ui` artifact bundles the UI assets. No env flag (Direction D2 rejected). Operator's `BIND_ADDR=127.0.0.1` default per `docs/SELF_HOSTED_CONTRACT.md` is the trust boundary for UI exposure; documented prose-side in plan 19-05's `docs/API.md` rewrite, NOT in this plan.
- **Build wiring: Option E1 runtime-only.** Single `<dependency>` block, no `<plugin>` block. The `springdoc-openapi-maven-plugin` for static `target/openapi.json` generation is deferred to Phase 42. This plan does NOT touch `<build><plugins>` in `pom.xml`.
- **`springdoc.api-docs.path` and `springdoc.swagger-ui.path` keep defaults.** `/v3/api-docs` and `/swagger-ui.html` are SpringDoc's conventional URLs; tooling expects them. The application.yml entries are present for documentation visibility but the values are the defaults SpringDoc would use without them. This is a deliberate "make the contract explicit in YAML" stance per audit Section 3D.
- **`springdoc.swagger-ui.disable-swagger-default-url=true`.** Suppresses the default petstore demo URL the UI loads on first paint. Cosmetic; the document the UI fetches is `/v3/api-docs` directly.
- **`springdoc.swagger-ui.tags-sorter=alpha` and `operations-sorter=method`.** Predictable navigation across the 28 user/admin tags landing in plan 19-03. `method` sorts within each tag by HTTP verb (DELETE, GET, HEAD, OPTIONS, PATCH, POST, PUT); reads come before writes alphabetically. These are SpringDoc-supported values; no custom tooling.
- **`servers: []` left empty.** SpringDoc default infers the server URL from the request's scheme + host + context path. `docs/SELF_HOSTED_CONTRACT.md` does not pin a `PUBLIC_URL`; per audit Section 6 `openapi-config-class` bucket the `servers` array is intentionally not pre-populated.
- **`info.version` is read from `IDENTITY.yaml` via `@Value` injection.** WorkoutHub's version-of-truth is `IDENTITY.yaml`; the `application.yml` does not currently expose a `${app.version}` key. This plan adds `app.version: ${APP_VERSION:0.3.2}` to `application.yml` as a single new key alongside the existing `app:` block, AND injects `@Value("${app.version:0.0.0}") String version` into `OpenApiConfig`. The default `0.3.2` matches the current `IDENTITY.yaml` and `pom.xml:16` value as of v0.4 in-flight (the exact value lives in IDENTITY.yaml; the application.yml fallback is for local dev when the env var is not set). NOTE: a more rigorous link to IDENTITY.yaml is deferred (Phase 42 candidate); this plan keeps the wiring simple.
- **Single SecurityConfig edit, 3 patterns added.** Insert `/v3/api-docs/**`, `/swagger-ui/**`, `/swagger-ui.html` into the existing `requestMatchers(...)` permitAll chain at `SecurityConfig.java:43-45`. Match the existing comma-separated string-list style.

**Constraining decisions (this planner):**
- **4 atomic tasks total.** Task 1 = pom.xml dependency add (PROTECTED halt-and-ask gate). Task 2 = `OpenApiConfig.java` new file. Task 3 = `application.yml` 6 SpringDoc keys + 1 `app.version` key. Task 4 = `SecurityConfig.java` 3-pattern permitAll extension. Tasks 2/3/4 are independent of each other but all depend on Task 1's dependency being present on the classpath. Splitting Task 1 further is impossible (one file, one logical change). Splitting Task 2 further (info block vs schemes) would scatter a 50-line file across two commits without a logically valid intermediate state. Tasks 3 and 4 each touch one file and one logical change; they are correctly atomic at the per-file granularity.
- **Task ordering: Task 1 first, then Tasks 2/3/4 in any order.** The plan executes them sequentially as written for commit-cadence consistency (mirrors prior plan ordering across 18-04). Each task ends with a focused git commit per the project's commit conventions; phase verification gate runs once at the end (Task 5).
- **PROTECTED file gate at Task 1.** The executor must explicitly pause before running `Edit` on `backend/pom.xml`. The pause prompt is "Phase 19-02 Task 1 requires editing `backend/pom.xml` to add the SpringDoc starter dependency. This file is protected per CLAUDE.md. Confirm authorization to proceed?" If the user denies, the executor records the block in the plan's status note and exits the task; the plan is marked blocked at the orchestration level. If the user authorizes (one-time per task), the executor proceeds with the Edit.
- **Imports thrash check on `OpenApiConfig.java`.** New file; imports list is built from scratch. Expected: `io.swagger.v3.oas.models.OpenAPI`, `io.swagger.v3.oas.models.Components`, `io.swagger.v3.oas.models.info.Info`, `io.swagger.v3.oas.models.security.SecurityScheme`, `io.swagger.v3.oas.models.security.SecurityRequirement`, `org.springframework.beans.factory.annotation.Value`, `org.springframework.context.annotation.Bean`, `org.springframework.context.annotation.Configuration`. NO `io.swagger.v3.oas.annotations.*` (those are controller-side annotations for plan 19-03). NO Lombok (project does not use Lombok).
- **Imports thrash check on `SecurityConfig.java`.** Existing imports cover `requestMatchers(String...)`. The 3 new path patterns are string literals appended to the existing matcher chain. NO new imports needed.
- **Imports thrash check on `application.yml`.** YAML; no imports. The new `springdoc:` block is a sibling of the existing top-level keys.
- **Imports thrash check on `pom.xml`.** XML; no imports. The new `<dependency>` block sits in the existing `<dependencies>` list at the appropriate alphabetical-ish position (the file is loosely grouped by category: spring-boot-starter-* first, then flyway, postgres, jjwt, web-push, etc.). The plan picks "after the actuator starter at `:73-76`" so SpringDoc lives next to the other documentation/observability-adjacent dependencies.
- **`OpenApiConfig` `info.description` value.** "Self-hosted multi-user fitness tracker. Authentication: built-in JWT (default) or reverse-proxy forward-auth. See docs/API.md for navigational reference and docs/SELF_HOSTED_CONTRACT.md section 7 for the auth contract." 2 sentences; mentions both auth modes; cross-links the docs the operator/consumer needs. Stays under 300 characters.
- **`OpenApiConfig` `info.title` value.** "WorkoutHub API". Matches the IDENTITY.yaml `display_name` casing.
- **No verification gate that requires running the backend.** Per `local_maven_gap` memory, this plan does NOT execute `mvn verify` or `mvn spring-boot:run`. CI is the test gate (Task 5 verifies the structural correctness of the YAML/XML/Java edits via re-reading the files; the runtime smoke test lives in plan 19-04).
- **No `application-prod.yml` edit.** Per audit Section 3D, the SpringDoc keys are dev/prod identical; no prod override needed.
- **No `.env.example` edit.** The `app.version` key has a fallback (`${APP_VERSION:0.3.2}`); operators do not need to set it explicitly. If a future plan decides to make `APP_VERSION` operator-required, that is a separate plan-edit.
- **No frontend changes.** The OpenAPI document is consumed by client tooling (potential codegen) at the operator's discretion; no frontend code wires to it in v0.4.

**Issues being addressed:**
- 19-01-AUDIT Section 6 buckets: `springdoc-dependency` (closed by Task 1), `openapi-config-class` (closed by Task 2), `application-yml-springdoc-keys` (closed by Task 3), `security-config-permitall` (closed by Task 4).
- 19-01-AUDIT Section 4 Part A (security schemes): closed structurally by Task 2's `SecurityScheme` declarations.
- 19-01-AUDIT Section 4 Part B (actuator exclusion): closed by Task 3's `paths-to-match=/api/**` key.
- 19-01-AUDIT Section 4 Part D (Swagger UI exposure stance D1): closed by Task 1's starter-webmvc-ui artifact + Task 4's permitAll allowlist for `/swagger-ui/**` + `/swagger-ui.html`.
- ROADMAP Phase 19 deliverable "Generate OpenAPI via SpringDoc": closed by 19-02 (the runtime endpoint). The "refresh docs/API.md" sub-deliverable is plan 19-05.
- ISSUES.md: this plan does NOT close i-1..i-9 (none belong to Phase 19) and does NOT introduce new entries (i-NEW-A/B/C from 19-01-AUDIT are recorded in plan 19-04's docs task; opening ISSUES.md is out of scope here).

**Out of scope (deferred to other plans within Phase 19):**
- Per-controller `@Tag(name = "...")` annotations. 28 mechanical edits; plan 19-03 owns it.
- `@Operation(operationId = "...")` disambiguation for the dual `SessionsController` collision. Plan 19-03 owns it.
- `@Schema(allowableValues = {...})` annotation on `ApiError.code`. Plan 19-04 owns it.
- `OpenApiSurfaceIntegrationTest`. Plan 19-04 owns it.
- `docs/API.md` rewrite. Plan 19-05 owns it.
- ISSUES.md edits for i-NEW-A/B/C. Plan 19-04 owns the ISSUES.md update (alongside the test).
- Static `docs/openapi.json` export under git. Phase 42 (v1.0) candidate.
- `springdoc-openapi-maven-plugin` build wiring. Phase 42 candidate.
- `ApiErrorCode` Java enum extraction (the durable fix for i-NEW-A drift risk). Phase 42 candidate.
- Per-method `security: []` overrides on permitAll endpoints. v1.0 polish.
- Per-DTO `@Schema` annotations beyond `ApiError.code`. v1.0 polish; SpringDoc auto-derives correct schemas from Jackson defaults.
- OpenAPI 3.1 spec opt-in. SpringDoc 2.x emits OpenAPI 3.0 by default; 3.1 is a separate `springdoc.api-docs.version=openapi_3_1` key, deferred.
- Per-environment server overrides via `springdoc.servers`. Operator's `PUBLIC_URL` is per-deployment; SpringDoc's request-derived default is correct for v0.4.
</context>

<tasks>

<task type="manual_review">
  <name>Task 1: Add SpringDoc dependency to backend/pom.xml (PROTECTED FILE - HALT AND ASK)</name>
  <files>backend/pom.xml</files>
  <action>
**PROTECTED FILE GATE.** `backend/pom.xml` is protected per `CLAUDE.md` "Protected Files" section. The executor MUST pause and request explicit user confirmation before invoking the `Edit` tool on this file. Pause prompt:

> "Phase 19-02 Task 1 requires editing `backend/pom.xml` to add the SpringDoc starter dependency (`org.springdoc:springdoc-openapi-starter-webmvc-ui:2.6.0`) and a `<springdoc.version>` property entry. This file is protected per CLAUDE.md. Confirm authorization to proceed (yes/no)?"

If the user replies anything other than an unambiguous yes, mark this task blocked, stop the plan execution, and surface the block to the orchestrator.

If authorized, proceed with the two sub-edits below.

**Sub-edit 1.1: Add `<springdoc.version>` property.**

In the `<properties>` block (currently lines `:20-30`), insert a new property entry after the existing `<jjwt.version>` line at `:22`. Final form (post-edit, the new line is the second one in this fragment):

```xml
<jjwt.version>0.13.0</jjwt.version>
<springdoc.version>2.6.0</springdoc.version>
<testcontainers.version>1.20.4</testcontainers.version>
```

The 2.6.0 pin is the audit's deterministic verdict (Section 3B, conservative pin compatible with Spring Boot 3.5.x). Do NOT WebFetch a newer version. Do NOT alter the surrounding properties.

**Sub-edit 1.2: Add the SpringDoc starter `<dependency>` block.**

In the `<dependencies>` list, insert a new `<dependency>` block AFTER the existing actuator starter at lines `:73-76` (the block that declares `spring-boot-starter-actuator`). Final form (post-edit, the new block follows the actuator one):

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>

<dependency>
    <groupId>org.springdoc</groupId>
    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    <version>${springdoc.version}</version>
</dependency>

<dependency>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-core</artifactId>
</dependency>
```

Use the `${springdoc.version}` property reference; do NOT inline the version literal. No `<exclusions>` block (the transitive footprint is intentional). No `<scope>` (default `compile` is correct for runtime auto-config).

Do not touch any other section of `pom.xml` (no `<build><plugins>` edit; the SpringDoc maven plugin for static export is Phase 42).

Verification at end of task: re-read `pom.xml` lines `:18-32` (properties block) and the dependency block following the actuator starter to confirm the exact structure above.

Commit message: `chore(deps): add springdoc-openapi-starter-webmvc-ui 2.6.0 (phase 19-02)` per `CLAUDE.md` "Git and Commits" convention.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create OpenApiConfig.java declaring two security schemes + global bearer requirement + info block</name>
  <files>backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java</files>
  <action>
Create a new file at `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java`. The file declares one `@Configuration` class with one `@Bean OpenAPI customOpenAPI()` method. Approximate length: 50-55 lines including imports and Javadoc-free body (project convention: no Javadoc on new files unless module-level rationale is non-obvious; here the audit + plan are the rationale).

Final file content:

```java
package com.workouthub.common.config;

import io.swagger.v3.oas.models.Components;
import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.security.SecurityRequirement;
import io.swagger.v3.oas.models.security.SecurityScheme;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {

    private static final String BEARER_AUTH = "bearerAuth";
    private static final String FORWARD_AUTH = "forwardAuth";

    private final String version;

    public OpenApiConfig(@Value("${app.version:0.0.0}") String version) {
        this.version = version;
    }

    @Bean
    OpenAPI customOpenAPI() {
        return new OpenAPI()
                .info(new Info()
                        .title("WorkoutHub API")
                        .version(version)
                        .description(
                                "Self-hosted multi-user fitness tracker. "
                                        + "Authentication: built-in JWT (default) or "
                                        + "reverse-proxy forward-auth. See docs/API.md for "
                                        + "navigational reference and "
                                        + "docs/SELF_HOSTED_CONTRACT.md section 7 for the "
                                        + "auth contract."))
                .components(new Components()
                        .addSecuritySchemes(BEARER_AUTH, new SecurityScheme()
                                .type(SecurityScheme.Type.HTTP)
                                .scheme("bearer")
                                .bearerFormat("JWT"))
                        .addSecuritySchemes(FORWARD_AUTH, new SecurityScheme()
                                .type(SecurityScheme.Type.APIKEY)
                                .in(SecurityScheme.In.HEADER)
                                .name("X-Forwarded-Email")))
                .addSecurityItem(new SecurityRequirement().addList(BEARER_AUTH));
    }
}
```

Notes on each block:
- Package `com.workouthub.common.config` matches the existing precedent (`SecurityConfig.java`, `CorsConfig.java`, `JacksonConfig.java`, `ClockConfig.java`).
- Imports list contains EIGHT entries (5 swagger-models + 3 spring). NO `io.swagger.v3.oas.annotations.*` (those are controller-side annotations for plan 19-03). NO Lombok.
- Class is `public` (Spring needs to instantiate it; package-private would also work but the convention in `common/config/` is `public`).
- Constructor injection of `@Value("${app.version:0.0.0}") String version`: fallback `0.0.0` is used only if the application.yml entry is missing AND the `APP_VERSION` env var is unset. Both are set in this plan (Task 3 adds the application.yml line; the fallback there is `0.3.2`); the constructor fallback is a safety net.
- `customOpenAPI()` method visibility is package-private (matches `SecurityFilterChain securityFilterChain(...)` in `SecurityConfig.java:28`).
- `addSecurityItem(...)` registers `bearerAuth` as the GLOBAL security requirement applied to every operation in the document. Per-method `security: []` overrides on permitAll endpoints are deferred per the audit's plan-boundary verdict; OpenAPI consumers see the bearer requirement on every operation, including login/refresh/reset-password/webhooks (this is acceptable v0.4 behavior; tooling-side polish lands in v1.0).
- `forwardAuth` is declared in `components.securitySchemes` but NOT applied as a global requirement. The OpenAPI consumer sees the scheme in the components map; tooling presents it as available but not required.
- `info.title` matches IDENTITY.yaml `display_name` casing ("WorkoutHub API").
- `info.description` is exactly two sentences: one stating what the app is, one cross-linking the docs. Stays under 300 characters.

Constants `BEARER_AUTH` and `FORWARD_AUTH` are private static finals so the security-scheme key names appear once in source. Plan 19-04's `OpenApiSurfaceIntegrationTest` references them by string value; the constants are an implementation detail of this file.

Verification at end of task: re-read the file to confirm the imports list count (8), the constructor signature, the bean method signature, the two `addSecuritySchemes` calls (bearer + forward), and the single `addSecurityItem` call (bearer only).

Commit message: `feat(api): add OpenApiConfig with bearerAuth + forwardAuth schemes (phase 19-02)`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Add 6 springdoc.* keys + 1 app.version key to backend/src/main/resources/application.yml</name>
  <files>backend/src/main/resources/application.yml</files>
  <action>
Two sub-edits in this YAML file. Both are additive; no existing line is changed.

**Sub-edit 3.1: Add `app.version` key under the existing `app:` block.**

Insert a new `version: ${APP_VERSION:0.3.2}` line as the FIRST sub-key under `app:` (currently line `:32`). Match the indentation of the existing `app.jwt.secret` (2-space indent under `app:`). Final form of the top of the `app:` block (post-edit, lines `:32-37` in approximate post-edit numbering):

```yaml
app:
  version: ${APP_VERSION:0.3.2}
  jwt:
    secret: ${APP_JWT_SECRET}
    access-ttl-seconds: ${APP_JWT_ACCESS_TTL_SECONDS:3600}
    refresh-ttl-seconds: ${APP_JWT_REFRESH_TTL_SECONDS:1209600}
```

The `0.3.2` fallback matches the current `pom.xml:16` and `IDENTITY.yaml` value as of v0.4 in-flight. Future version bumps (Phase 20 release) update `IDENTITY.yaml` -> `pom.xml` -> this fallback in lock-step. An ISSUES.md candidate "single source of truth for version literal" is documented in 19-01-AUDIT issue i-NEW-A drift mitigation (Phase 42 candidate); 19-02 does not extract it.

**Sub-edit 3.2: Add a top-level `springdoc:` block.**

Insert a new `springdoc:` top-level block AFTER the existing `management:` block and BEFORE the `logging:` block. The existing top-level keys (in order): `spring`, `server`, `app`, `management`, `logging`. New order: `spring`, `server`, `app`, `management`, `springdoc`, `logging`. Match flush-left indentation of the sibling top-level keys.

Final form of the new block:

```yaml
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
    tags-sorter: alpha
    operations-sorter: method
  paths-to-match: /api/**
```

Six keys in total:
1. `springdoc.api-docs.path = /v3/api-docs` (default value made explicit per audit Section 3D).
2. `springdoc.swagger-ui.path = /swagger-ui.html` (default value made explicit).
3. `springdoc.swagger-ui.disable-swagger-default-url = true` (suppresses the petstore demo URL on first paint).
4. `springdoc.swagger-ui.tags-sorter = alpha` (predictable navigation across the 28 tags).
5. `springdoc.swagger-ui.operations-sorter = method` (HTTP-verb sort within each tag).
6. `springdoc.paths-to-match = /api/**` (excludes `/livez`, `/healthz`, `/metrics`, `/actuator/**`, `/error`, `/v3/api-docs`, `/swagger-ui*` from the public surface; the document itself is not self-referential).

Indentation: 2 spaces per nesting level (matches `spring.datasource.*`, `app.jwt.*`, `management.endpoints.web.exposure.*` style). The `paths-to-match` value is a single string; SpringDoc accepts a YAML list `[ "/api/**" ]` as well, but a single value is conventional when there is only one pattern.

Do NOT add the `springdoc:` block to `application-prod.yml`; per audit Section 3D no prod override is needed.

Verification at end of task: re-read the entire `application.yml` and confirm (1) `app.version` is the first sub-key under `app:`, (2) the new top-level `springdoc:` block is positioned between `management:` and `logging:`, (3) the 6 springdoc keys are present with the exact values above, (4) no existing line was modified.

Commit message: `feat(api): wire springdoc config keys + app.version in application.yml (phase 19-02)`.
  </action>
</task>

<task type="auto">
  <name>Task 4: Add 3 SpringDoc URL patterns to SecurityConfig permitAll matcher</name>
  <files>backend/src/main/java/com/workouthub/common/config/SecurityConfig.java</files>
  <action>
Single edit in `SecurityConfig.java`: extend the existing `requestMatchers(...)` permitAll chain at lines `:43-45` with three SpringDoc URL patterns.

Current form (lines `:41-46`):

```java
.requestMatchers(HttpMethod.POST,
        "/api/webhooks/scale/**").permitAll()
.requestMatchers("/livez", "/healthz", "/metrics",
        "/actuator/health", "/actuator/info",
        "/actuator/prometheus").permitAll()
.requestMatchers("/error").permitAll()
```

Final form (post-edit, the new permitAll matcher inserts as a SEPARATE chain after the operator-paths matcher):

```java
.requestMatchers(HttpMethod.POST,
        "/api/webhooks/scale/**").permitAll()
.requestMatchers("/livez", "/healthz", "/metrics",
        "/actuator/health", "/actuator/info",
        "/actuator/prometheus").permitAll()
.requestMatchers("/v3/api-docs/**",
        "/swagger-ui/**",
        "/swagger-ui.html").permitAll()
.requestMatchers("/error").permitAll()
```

The new matcher is a SEPARATE chain (its own `.requestMatchers(...).permitAll()` call) rather than mixed into the operator-paths matcher; this keeps the operator-vs-documentation grouping legible. Three patterns:
1. `/v3/api-docs/**` (covers the JSON `/v3/api-docs`, the YAML `/v3/api-docs.yaml`, and any group sub-paths a future plan might enable).
2. `/swagger-ui/**` (covers the UI assets: index.html, JS, CSS, fonts).
3. `/swagger-ui.html` (the UI entry redirect).

NO new imports are needed; `requestMatchers(String...)` is already imported and used at the existing `:43-45` chain.

NO change to any other line in `SecurityConfig.java` (no edit to the `/api/**` authenticated catch-all at `:47`, no edit to the JWT filter mounting at `:53`, no edit to the optional `ForwardAuthFilter` mounting at `:54-55`).

Verification at end of task: re-read `SecurityConfig.java` lines `:41-50` and confirm the new chain inserts cleanly between the operator-paths chain and the `/error` chain. Confirm the existing `/api/**` authenticated and `anyRequest().denyAll()` lines are unchanged.

Commit message: `feat(api): permitAll springdoc + swagger-ui paths in SecurityConfig (phase 19-02)`.
  </action>
</task>

<task type="auto">
  <name>Task 5: Phase verification gate (re-read all 4 touched files; confirm structural correctness)</name>
  <files>.planning/phases/19-api-contract-docs/19-02-SUMMARY.md (verification log; not the SUMMARY file itself)</files>
  <action>
This task is a verification gate, not a SUMMARY write. The SUMMARY file is written by the orchestrator-spawned summarizer after the executor completes; this task captures the verification checklist the executor walks before signaling task-complete.

Per `local_maven_gap` memory, this plan does NOT execute `mvn verify` or `mvn spring-boot:run` locally. CI is the test gate. The verification gate here is structural-correctness only:

**Verification checklist (the executor walks each item; surfaces a blocker if any item fails):**

1. `backend/pom.xml`:
   - `<springdoc.version>2.6.0</springdoc.version>` is present in the `<properties>` block.
   - `<dependency>` block for `org.springdoc:springdoc-openapi-starter-webmvc-ui` is present in the `<dependencies>` list with `<version>${springdoc.version}</version>`.
   - No other property or dependency was added or removed.
   - No `<plugin>` block was edited.

2. `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java`:
   - File exists with package `com.workouthub.common.config`.
   - 8 imports present (5 swagger-models + 3 spring); 0 `io.swagger.v3.oas.annotations.*` imports.
   - `@Configuration` annotation on the class.
   - Constructor takes `@Value("${app.version:0.0.0}") String version`.
   - `@Bean OpenAPI customOpenAPI()` method exists with package-private visibility.
   - Two `addSecuritySchemes` calls present: one for `bearerAuth` (HTTP/bearer/JWT), one for `forwardAuth` (APIKEY/HEADER/X-Forwarded-Email).
   - One `addSecurityItem` call applying `bearerAuth` globally.
   - `info.title = "WorkoutHub API"`, `info.version = version` (the injected value), `info.description` is the two-sentence string described in Task 2.

3. `backend/src/main/resources/application.yml`:
   - `app.version: ${APP_VERSION:0.3.2}` is the FIRST sub-key under `app:`.
   - Top-level `springdoc:` block exists between `management:` and `logging:`.
   - 6 springdoc keys present with the exact values from Task 3.
   - No existing line was modified.

4. `backend/src/main/java/com/workouthub/common/config/SecurityConfig.java`:
   - New `requestMatchers("/v3/api-docs/**", "/swagger-ui/**", "/swagger-ui.html").permitAll()` chain present between the operator-paths matcher and the `/error` matcher.
   - No other line modified.
   - No new imports.

5. CI smoke check (deferred to plan 19-04, but record the expected state here so the orchestrator knows what passes):
   - On the next CI run, `mvn verify` succeeds (compile + tests pass).
   - The application boots without `BeanCreationException` from SpringDoc (a misconfigured `OpenApiConfig` would surface here).
   - The `OpenApiSurfaceIntegrationTest` from plan 19-04 will hit `GET /v3/api-docs` and assert non-empty `paths` map; that assertion is OUT OF SCOPE for 19-02 but is the binding green-CI gate for the integration.

If any of items 1-4 fail: the executor stops, surfaces the blocker, and does NOT proceed to commit-and-push. If all 4 pass: each task's commit lands as a separate commit per the per-task commit-message conventions; the executor then signals the orchestrator that 19-02 is complete and ready for the SUMMARY.md write.

NOTE on commit cadence: Tasks 1-4 each end with their own commit per the plan-format convention (one logical change per commit). The full plan delivers 4 commits. The optional SUMMARY.md commit (5th) is the orchestrator's responsibility, not this task's.
  </action>
</task>

</tasks>

<verification>

**Structural correctness (executor walks before signaling task-complete; per Task 5 checklist):**

- `backend/pom.xml`: `<springdoc.version>2.6.0</springdoc.version>` present; `<dependency>` block for `springdoc-openapi-starter-webmvc-ui` present with `<version>${springdoc.version}</version>`; no other edits.
- `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java`: file exists; package matches; 8 imports; `@Configuration`; constructor takes `@Value("${app.version:0.0.0}")`; `@Bean OpenAPI customOpenAPI()` method present; two security schemes (`bearerAuth` http/bearer/JWT, `forwardAuth` apiKey/header/X-Forwarded-Email); global `bearerAuth` security requirement.
- `backend/src/main/resources/application.yml`: `app.version` key present as first sub-key under `app:`; top-level `springdoc:` block between `management:` and `logging:` with 6 keys per Task 3.
- `backend/src/main/java/com/workouthub/common/config/SecurityConfig.java`: new `requestMatchers(...)` permitAll chain with `/v3/api-docs/**`, `/swagger-ui/**`, `/swagger-ui.html` inserted between operator-paths matcher and `/error` matcher; no other edits; no new imports.

**CI gate (next push after the plan lands):**

- `mvn verify` on CI passes (compile + tests + jacoco coverage gate). Per `local_maven_gap` memory, this is the only test gate available.
- Application boots without `BeanCreationException` from SpringDoc (a misconfigured `OpenApiConfig` would surface as a startup failure).
- `OpenApiSurfaceIntegrationTest` (plan 19-04) on a future push will hit `GET /v3/api-docs` and assert (1) HTTP 200, (2) non-empty `paths` map, (3) `paths` contains at least one `/api/**` entry, (4) `paths` does NOT contain `/actuator/**`, `/livez`, `/healthz`, `/metrics`, (5) `components.securitySchemes.bearerAuth` and `forwardAuth` both declared, (6) `GET /swagger-ui.html` returns HTTP 200. That assertion suite is the binding green-light gate for the 19-02 integration; 19-04 is the plan that runs it.

**Tests added in this plan: zero.** Per the plan-boundary verdict, the integration test belongs to 19-04. The structural verification of 19-02's edits is sufficient for the plan to ship; the runtime smoke is delegated to the next plan.

**Files touched:** 4 modified (`backend/pom.xml`, `backend/src/main/resources/application.yml`, `backend/src/main/java/com/workouthub/common/config/SecurityConfig.java`) + 1 created (`backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java`) = 4 file touches across 4 commits.

**Files NOT touched (audit boundary):** any controller (28 user/admin-facing controllers + 3 operator-facing), `ApiError.java`, `application-prod.yml`, `.env.example`, `IDENTITY.yaml`, `docs/API.md`, `docs/EXPORT_FORMAT.md`, `docs/SELF_HOSTED_CONTRACT.md`, `docs/OBSERVABILITY.md`, `docs/DEPLOYMENT.md`, any frontend file, any migration file, any test file, `ISSUES.md`, `STATE.md`, `ROADMAP.md`, `CHANGELOG.md`.

</verification>

<success_criteria>

- All 4 tasks land their respective edits with the structural shape described in their `<action>` blocks.
- Task 1 explicitly halts before the `pom.xml` edit and proceeds only after user authorization (per `CLAUDE.md` "Protected Files" + the orchestration `checkpoints.md` reference).
- The Phase 5 verification gate (Task 5) passes all 4 file checks before the executor signals plan-complete.
- 4 commits land in order: `chore(deps): add springdoc-openapi-starter-webmvc-ui 2.6.0 (phase 19-02)`, `feat(api): add OpenApiConfig with bearerAuth + forwardAuth schemes (phase 19-02)`, `feat(api): wire springdoc config keys + app.version in application.yml (phase 19-02)`, `feat(api): permitAll springdoc + swagger-ui paths in SecurityConfig (phase 19-02)`.
- On the next CI push, the build is green: SpringDoc dependency resolves, the application boots, no `BeanCreationException` from `OpenApiConfig`. The runtime endpoint `GET /v3/api-docs` is reachable unauthenticated and returns a populated OpenAPI 3.x JSON document; that assertion lives in plan 19-04's `OpenApiSurfaceIntegrationTest` but the structural correctness of 19-02 is what enables it.
- Plan 19-03 (tag-and-collision-fix) can land next; 19-02 leaves the SpringDoc auto-derived class-name tags in place as the intentional intermediate state.
- The `pom.xml` edit is the ONLY protected-file touch in this plan; no other guarded file is modified.

</success_criteria>

<output>

- `backend/pom.xml` (modified): `<springdoc.version>2.6.0</springdoc.version>` property entry; `<dependency>` block for `org.springdoc:springdoc-openapi-starter-webmvc-ui:${springdoc.version}` in the `<dependencies>` list.
- `backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java` (created, ~50 lines): `@Configuration` class with one `@Bean OpenAPI customOpenAPI()` method declaring `bearerAuth` (http/bearer/JWT) + `forwardAuth` (apiKey/header/X-Forwarded-Email) security schemes and a global `bearerAuth` security requirement. `info.title = "WorkoutHub API"`, `info.version` injected from `${app.version:0.0.0}`, `info.description` cross-links `docs/API.md` and `docs/SELF_HOSTED_CONTRACT.md` section 7.
- `backend/src/main/resources/application.yml` (modified): `app.version: ${APP_VERSION:0.3.2}` as first sub-key under `app:`; new top-level `springdoc:` block (6 keys per audit Section 3D) inserted between `management:` and `logging:`.
- `backend/src/main/java/com/workouthub/common/config/SecurityConfig.java` (modified): new `requestMatchers("/v3/api-docs/**", "/swagger-ui/**", "/swagger-ui.html").permitAll()` chain inserted between the operator-paths permitAll chain and the `/error` chain.

After 19-02 ships and CI is green: SpringDoc auto-derives an OpenAPI document at `GET /v3/api-docs` covering all 31 controllers (with class-name auto-tags as the intentional intermediate state for plan 19-03), the bundled Swagger UI is reachable at `GET /swagger-ui.html` (BIND_ADDR is the trust boundary per audit Direction D1), the document declares both security schemes per audit Section 4A, and the actuator + operator paths are excluded per audit Section 4B. Plans 19-03, 19-04, 19-05 can land in sequence on top of this foundation.

</output>
