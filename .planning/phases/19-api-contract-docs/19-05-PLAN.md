---
phase: 19-api-contract-docs
plan: 05
type: execute
domain: docs
---

<objective>
Land the navigational rewrite of `docs/API.md` per 19-01-AUDIT Section 4C Direction C2 verdict, replacing the 3-line Phase-1-era placeholder ("Placeholder. The full REST surface is documented here starting in PHASE 1.") with a thin pointer-style reference that names the runtime SpringDoc surface as the single source of truth (`GET /v3/api-docs` JSON + `GET /swagger-ui.html` UI), documents the two-mode auth model (built-in JWT bearerAuth + opt-in `AUTH_MODE=forward-auth` X-Forwarded-Email apiKey), surfaces the typed `ApiError.code` catalog from Phase 15-04 as the wire-level error contract, and cross-links to the binding self-hosted contract (Section 7), the export wire-shape document (`docs/EXPORT_FORMAT.md`), and the operator-endpoint document (`docs/OBSERVABILITY.md`). Realises 19-01-AUDIT Section 6 bucket `docs-api-md-rewrite` and is the FINAL plan of Phase 19 (4-plan implementation structure: 01 audit + 02 springdoc-integration + 03 tag-and-collision-fix + 04 error-schema-and-test + 05 docs-api-md-rewrite).

Purpose: 19-05 is the documentation closeout for Phase 19. After 19-02 the OpenAPI document is live at `GET /v3/api-docs` with both security schemes; after 19-03 every user/admin-facing controller carries a hand-curated `@Tag` and the dual `SessionsController` operationId collision is resolved; after 19-04 the `ApiError` envelope renders to OpenAPI with the 4-value typed `code` enum and an `OpenApiSurfaceIntegrationTest` acts as a CI-enforced runtime smoke that the document reaches reachability + structural-shape contract. The remaining gap is `docs/API.md` itself: the file is still a 3-line placeholder pointing at "starting in PHASE 1" — stale by 2 milestones (v0.3 + v0.4 = 17 phases of REST surface added). 19-05 closes that gap with a navigational rewrite per audit Direction C2: NO hand-maintained per-endpoint tables (134 mappings would impose O(N) per-plan maintenance burden in v0.5+ — audit Section 4C Direction C1 explicitly REJECTED), NO embedded Swagger UI iframe duplication (audit Direction C3 REJECTED), but instead a thin README-style pointer document that links readers to the runtime surface for the endpoint catalog and to the per-section docs for cross-cutting concerns (auth, errors, exports, operator paths, discoverability). The runtime is THE source of truth; `docs/API.md` is the navigational entry point.

Output: `docs/API.md` is fully rewritten from 3 lines to ~120-160 lines spanning 7 sections (intro, runtime endpoints, authentication, error envelope, export format, operator endpoints, discoverability) per audit Section 6 `docs-api-md-rewrite` bucket. Optional 1-line touch on `README.md` to refine the existing `docs/API.md` link description from "REST API reference" to a phrase that signals the navigational nature of the rewrite (e.g. "navigational entry point to the runtime OpenAPI document and per-section API contracts"); this README touch is OPTIONAL and the planner authorizes its inclusion as a same-commit small refinement if and only if the executor reads `README.md:160` and confirms the existing description is misleading post-rewrite. NO Java edit (Phase 19 production code is closed at 19-04). NO test edit (the `OpenApiSurfaceIntegrationTest` from 19-04 already CI-asserts the runtime contract this document references). NO `pom.xml` edit (`pom.xml` was the only protected touch in Phase 19; landed at 19-02). NO `application.yml`, `OpenApiConfig.java`, `SecurityConfig.java`, `ApiError.java`, controller, DTO, service, repository, mapper, migration, or frontend file edit. NO `docs/EXPORT_FORMAT.md`, `docs/SELF_HOSTED_CONTRACT.md`, `docs/OBSERVABILITY.md`, `docs/DEPLOYMENT.md`, `docs/MIGRATION.md`, `docs/DATA_SCHEMA.md`, `docs/BACKUP.md`, `IDENTITY.yaml`, `CHANGELOG.md`, `ROADMAP.md`, `STATE.md`, `ISSUES.md`, `PROJECT.md` edit. NO new file creation under `docs/` or anywhere else (e.g. NO `docs/openapi.json` static export — audit explicitly defers this to Phase 42).

The plan's mechanical scope is small (1 file rewrite, optionally 1-line README refinement) but the prose IS the deliverable. Section content, link targets, ApiError code catalog, and BIND_ADDR-as-trust-boundary stance are pre-locked at plan time so the executor does not re-litigate phrasing during implementation. Local mvn unavailable on this Windows host (`local_maven_gap` memory) is a non-issue for this plan: it is documentation-only, no compile or test runs are required. Verification is structural-correctness only (Task 3 verification gate: re-read the rewritten file; confirm 7 sections present; confirm all link targets resolve to real files in the repo; confirm no broken cross-references).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md
@.planning/phases/19-api-contract-docs/19-01-AUDIT.md
@.planning/phases/19-api-contract-docs/19-01-SUMMARY.md
@.planning/phases/19-api-contract-docs/19-02-PLAN.md
@.planning/phases/19-api-contract-docs/19-03-PLAN.md
@.planning/phases/19-api-contract-docs/19-04-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@docs/API.md
@docs/SELF_HOSTED_CONTRACT.md
@docs/EXPORT_FORMAT.md
@docs/OBSERVABILITY.md
@README.md
@backend/src/main/java/com/workouthub/common/web/ApiError.java
@backend/src/main/java/com/workouthub/common/config/OpenApiConfig.java
@backend/src/main/resources/application.yml

**Tech stack available (carried from 19-02 + 19-03 + 19-04 closes):** Phase 19's runtime is fully wired by the time 19-05 runs. SpringDoc 2.6.0 serves OpenAPI 3.x JSON at `GET /v3/api-docs` and the Swagger UI at `GET /swagger-ui.html`. Both security schemes (`bearerAuth` http/bearer/JWT and `forwardAuth` apiKey/header `X-Forwarded-Email`) are declared. 28 hand-curated `@Tag` names group the user/admin-facing controllers. The `ApiError` envelope carries 11 `@Schema` annotations (1 class-level + 7 per-component on `ApiError` + 1 class-level + 2 per-component on `FieldError`); the `code` field enumerates the 4 typed values from Phase 15-04 (`SESSION_ALREADY_ACTIVE`, `SESSION_ALREADY_FINISHED`, `SESSION_FINISHED`, `SET_NUMBER_DUPLICATE`). The `OpenApiSurfaceIntegrationTest` CI-asserts reachability + tags + security schemes + the `ApiError.code` enum on every push. NONE of the above changes in 19-05 — 19-05 is documentation-only and its sole job is to make the runtime + per-section documentation discoverable from a single navigational `docs/API.md`.

**Established patterns (carried from prior plans):**
- `docs/*.md` files use H1 for the doc title and H2 for major sections; H3 only when a section needs sub-grouping (verified against `docs/SELF_HOSTED_CONTRACT.md`, `docs/OBSERVABILITY.md`, `docs/EXPORT_FORMAT.md`, `docs/DEPLOYMENT.md`, `docs/BACKUP.md`, `docs/MIGRATION.md`, `docs/DATA_SCHEMA.md`).
- Cross-doc links use the markdown `[text](relative/path)` form. From `docs/API.md`, peer docs in the same directory are referenced as `[text](FILENAME.md)` (no `docs/` prefix); the `README.md` at repo root is referenced as `[text](../README.md)`. Per audit Section 2C the link to the binding contract uses the form `[contract section 7](SELF_HOSTED_CONTRACT.md#7-authentication)` — verify the heading anchor by reading `SELF_HOSTED_CONTRACT.md` at line 260 (the audit confirms `## 7. Authentication` is the H2 heading, which produces the GitHub-flavored Markdown anchor `#7-authentication`).
- Code blocks use fenced `\`\`\`http`, `\`\`\`yaml`, `\`\`\`json`, `\`\`\`bash`, or `\`\`\`text` per content. Inline code uses backticks. Inline literal HTTP method + path uses `\`GET /api/...\`` form (verified against `docs/SELF_HOSTED_CONTRACT.md` Section 9 examples).
- Tables use the standard pipe-form with a header separator row. Two-column tables for short reference (e.g. typed code -> meaning) follow `| Code | Meaning |` header convention (verified against `docs/EXPORT_FORMAT.md` enum-value tables).
- No emojis anywhere per global CLAUDE.md + project CLAUDE.md.
- English-only doc content. Audit Section 2A confirms all `docs/*.md` are English; user-facing UI strings live in `frontend/src/i18n/messages/*.json` and are out of scope here.
- Per CLAUDE.md "No emojis anywhere", "Do not refactor surrounding code when fixing a bug", "Do not make changes beyond what was explicitly requested": this plan touches `docs/API.md` (and optionally one line of `README.md`); no other file is touched, no surrounding doc is reformatted, no emojis are added.
- Per CLAUDE.md "Do not add docstrings, comments, or type annotations to code you did not change": this plan does NOT touch any code file, so the rule is moot here. The rule about "no Javadoc" does not apply to markdown documentation.

**Constraining decisions (sourced from 19-01-AUDIT + 19-02 + 19-03 + 19-04 closes):**
- **Direction C2 from audit Section 4C: navigational rewrite, NOT hand-maintained per-endpoint reference.** Rejected directions: C1 (hand-write per-endpoint reference; O(N=134) prose per audit Section 1A, high maintenance burden), C3 (hybrid embed Swagger UI iframe; doc duplication). Verdict: navigational README that LINKS to the runtime + per-section docs; static `docs/openapi.json` export deferred to Phase 42.
- **Direction D1 from audit Section 4D: bundled Swagger UI; BIND_ADDR=127.0.0.1 default is the trust boundary.** Audit Section 4D rejects D2 (env-flag opt-in, extra config wiring) and D3 (no UI; JSON only). The "Discoverability" section in `docs/API.md` documents the BIND_ADDR-as-trust-boundary stance.
- **Two security schemes per audit Section 4A.** `bearerAuth` (http/bearer/JWT) is the default mode (built-in JWT auth) and is applied globally to every `/api/**` endpoint EXCEPT the permitAll matchers. `forwardAuth` (apiKey/header `X-Forwarded-Email`) is the alternative scheme declared when operators set `APP_AUTH_MODE=forward-auth` and run an SSO gateway in front. The source-IP trust gate (`TRUSTED_PROXIES`) is un-modelable in OpenAPI 3.x (audit Section 4A "Asymmetry note"); the prose-side documentation cross-links to `SELF_HOSTED_CONTRACT.md` Section 7.2 for the operator-side setup.
- **ApiError.code enumeration: 4 typed values per audit Section 4E.** The catalog is locked: `SESSION_ALREADY_ACTIVE` (live-session start; HTTP 409), `SESSION_ALREADY_FINISHED` (already-finished session; HTTP 409), `SESSION_FINISHED` (mutation on a finished session; HTTP 409), `SET_NUMBER_DUPLICATE` (duplicate set number on a session; HTTP 409). The catalog is sourced from `backend/src/main/java/com/workouthub/sessions/SessionsService.java:25-27` (3 codes) and `backend/src/main/java/com/workouthub/sessions/SessionSetsService.java:29` (1 code). Audit Section 4E records 8 additional `ConflictException` throw sites that emit `code: null` today; the prose acknowledges these explicitly with the audit-recommended "May be null on legacy paths" wording (matching the `@Schema.description` text from 19-04 Task 1 on the `ApiError.code` field).
- **Cross-link targets are deterministic:**
  - `[SELF_HOSTED_CONTRACT.md Section 7](SELF_HOSTED_CONTRACT.md#7-authentication)` — auth model (binding contract).
  - `[SELF_HOSTED_CONTRACT.md Section 7.1](SELF_HOSTED_CONTRACT.md#71-built-in-auth-default)` — built-in JWT mode.
  - `[SELF_HOSTED_CONTRACT.md Section 7.2](SELF_HOSTED_CONTRACT.md#72-reverse-proxy-forward-auth-opt-in)` — forward-auth mode + `TRUSTED_PROXIES` trust gate.
  - `[EXPORT_FORMAT.md](EXPORT_FORMAT.md)` — full-export JSON wire shape; canonical reference for `GET /api/export/full` + `POST /api/export/import`.
  - `[OBSERVABILITY.md](OBSERVABILITY.md)` — operator-facing endpoints (`/livez`, `/healthz`, `/metrics`, `/actuator/prometheus`, `/actuator/health`, `/actuator/info`).
  - `[../README.md](../README.md)` — Quick start, configuration, exposure (already lists `docs/API.md` in its Reference section).
  - Verify each link target's anchor by reading the target file at plan-execution time. The audit confirms these section headings exist in their respective files; the executor re-confirms before writing. If any anchor differs in casing or wording at execution time (e.g. SELF_HOSTED_CONTRACT.md Section 7.1's H3 "Built-in auth (default)" produces anchor `#71-built-in-auth-default`; the `(default)` parenthetical may or may not be slug-friendly across renderers), use the bare H2-section anchor (`#7-authentication`) as the conservative fallback and note in plan-time decisions that GitHub-flavored Markdown's slug generator strips parentheticals.
- **Tag-listing approach: link to runtime, do NOT enumerate.** The audit's Section 5B table lists 28 hand-curated tag names, but enumerating them in `docs/API.md` would force a per-plan maintenance burden every time a new feature controller adds a tag (which is the exact thing Direction C2 was designed to avoid). The prose mentions "28 tag groupings (see the runtime tags listing at /v3/api-docs)" or equivalent without enumerating. Audit verbatim: "Mention the 28 tag groupings (or point to runtime tag listing)" — the verdict is to point, not enumerate. This plan honors that directive.
- **No endpoint listing.** `docs/API.md` does NOT list HTTP method + path for any endpoint beyond:
  - `GET /v3/api-docs` (the OpenAPI JSON itself; load-bearing for the doc).
  - `GET /v3/api-docs.yaml` (YAML alternative; mentioned briefly).
  - `GET /swagger-ui.html` (the UI; load-bearing for Direction D1 trust-boundary discussion).
  - `GET /actuator/health`, `GET /actuator/prometheus`, `GET /livez`, `GET /healthz`, `GET /metrics` (only as cross-link bait to OBSERVABILITY.md; brief mention).
  All other endpoints stay in the runtime document and the runtime document only.
- **No example request/response payload outside the load-bearing OpenAPI snippet.** A single `\`\`\`json` block illustrating the ApiError envelope shape IS in scope (it's the project-wide error contract and the audit Section 4E catalog grounds it). Per-endpoint examples are NOT — those live in the runtime Swagger UI's interactive try-it-out flow.
- **No tooling/codegen recipe per audit Section 4C.** The audit suggests a "How to consume the OpenAPI document for client codegen" pointer but stops short of recommending specific tools (openapi-generator, kiota, etc.). The plan honors that boundary: ONE sentence pointing readers at the openapi-generator or similar via the standard `/v3/api-docs` JSON URL, no specific tool recommendation, no version pin.
- **CHANGELOG.md note: out of scope here.** ROADMAP/SUMMARY for Phase 19 (and Phase 20-release) own the CHANGELOG entry. 19-05 is documentation-only and does NOT touch CHANGELOG.md, ROADMAP.md, or STATE.md (the orchestrator-spawned summarizer handles those at phase close).
- **No frontmatter in `docs/API.md`.** Project-wide convention (verified across all 7 existing `docs/*.md` files) is no YAML frontmatter on operator-facing docs. The H1 is the title.

**Constraining decisions (this planner):**
- **Section structure: 7 sections, in fixed order.** Recording the section list verbatim so the executor does not re-litigate ordering during implementation:
  1. **Intro** (4-6 sentences). What this document is, what it is NOT. The runtime OpenAPI document is THE source of truth; this file is the navigational entry. Mentions `GET /v3/api-docs` JSON and `GET /swagger-ui.html` UI as the runtime surface.
  2. **Runtime endpoints**. Documents `GET /v3/api-docs` (JSON), `GET /v3/api-docs.yaml` (YAML), `GET /swagger-ui.html` (UI). One paragraph + one fenced code block showing a sample `curl` against `/v3/api-docs`. Brief note: SpringDoc 2.x emits OpenAPI 3.1 by default; the document includes both `bearerAuth` and `forwardAuth` security schemes; 28 hand-curated tags group the user/admin-facing endpoints with the precise runtime listing visible at `/v3/api-docs#/tags`.
  3. **Authentication**. Two-mode summary. Built-in JWT (bearerAuth) is the default; clients send `Authorization: Bearer <token>` with tokens minted by `POST /api/auth/login`. Forward-auth (`APP_AUTH_MODE=forward-auth`) is the opt-in mode for operators running an SSO gateway in front; the app trusts `X-Forwarded-Email` (and friends) ONLY when the request source IP matches `APP_AUTH_TRUSTED_PROXIES`. Cross-link to `SELF_HOSTED_CONTRACT.md` Section 7 for the binding contract; cross-link to `SELF_HOSTED_CONTRACT.md` Section 7.2 for the trust gate. Mention the `permitAll` endpoints by class (login, refresh, reset-password, scale webhook) without enumerating method + path; the runtime document is the canonical listing.
  4. **Error envelope**. The `ApiError` shape inline. One `\`\`\`json` example showing a typical 409 conflict response with the typed `code` field populated. Two-column table listing the 4 typed values + their HTTP-409-conflict meanings (sourced from audit Section 4E catalog). Sentence acknowledging `code: null` on legacy throw sites.
  5. **Export format**. One-paragraph pointer to `EXPORT_FORMAT.md` for the `GET /api/export/full` and `POST /api/export/import` wire-shape contract. Brief note: full-export JSON, round-trip restoration, no-id-collision discipline.
  6. **Operator endpoints**. One-paragraph pointer to `OBSERVABILITY.md`. Lists the 5 operator paths inline as a brief inventory (`GET /livez`, `GET /healthz`, `GET /metrics`, `GET /actuator/health`, `GET /actuator/info`, `GET /actuator/prometheus`) but explicitly notes these are excluded from the runtime OpenAPI document via `springdoc.paths-to-match=/api/**`. Cross-link to `OBSERVABILITY.md`.
  7. **Discoverability**. The BIND_ADDR-as-trust-boundary stance. The runtime SpringDoc surface (JSON + UI) is reachable WITHOUT authentication by design; the application's public-network exposure is bounded by `BIND_ADDR=127.0.0.1` (compose default) + the operator's reverse proxy. Operators who flip `BIND_ADDR=0.0.0.0` accept the discoverability risk explicitly. Cross-link to `SELF_HOSTED_CONTRACT.md` (the contract decides binding addresses) and to `../README.md` (the README's "Exposure" section already documents the reverse-proxy expectation).
- **Section heading wording (locked):**
  - `# API` (H1, mirroring the existing placeholder's H1 to minimize commit-noise; the H1 stays the same single-word title).
  - `## Runtime endpoints`
  - `## Authentication`
  - `## Error envelope`
  - `## Export format`
  - `## Operator endpoints`
  - `## Discoverability`
  The intro paragraph sits between the H1 and the first H2 with no intermediate heading, matching the existing convention in `docs/SELF_HOSTED_CONTRACT.md` and `docs/EXPORT_FORMAT.md`.
- **Word budget: ~120-160 lines total.** The audit's Section 6 `docs-api-md-rewrite` bucket records "~80-120 lines"; this plan widens the budget slightly to 120-160 to account for the locked-in 7 sections (each ~10-25 lines) plus the ApiError JSON example (~10 lines) plus the typed-code table (~8 lines). 200 lines is a hard cap per the project's "Max ~200 lines per file" discipline (CLAUDE.md "File Organization"). The executor stays under 200 by design.
- **Optional README touch: ONE line.** Read `README.md` at line 160 to confirm the existing description ("REST API reference"). If post-rewrite the description is misleading (the file is no longer a "REST API reference" in the traditional sense; it is a navigational entry point), the executor MAY refine the line in the SAME commit as the `docs/API.md` rewrite. Authorized refinement: `[\`docs/API.md\`](docs/API.md) — navigational entry point to the runtime OpenAPI document and per-section API contracts.` If the executor judges the existing line is acceptable as-is, leave `README.md` untouched. Either choice is acceptable to this planner; the planner does NOT mandate the touch. This is the ONLY allowed `README.md` change in this plan.
- **3 atomic tasks total.** Task 1 = `docs/API.md` rewrite (single-file edit; full content replacement). Task 2 = optional `README.md` line refinement (single-file Edit; 1-line touch; SKIP entirely if executor decides existing line is acceptable). Task 3 = phase verification gate (re-read `docs/API.md`; confirm 7 sections present; confirm all link targets resolve to real files; confirm no broken cross-references; confirm word count stays under 200 lines). Task ordering: 1 -> 2 (optional) -> 3. Splitting Task 1/2 captures the natural one-commit-per-logical-change cadence even if Task 2 lands. Task 1 is a 1-file rewrite; Task 2 is a 1-line edit (or skip); Task 3 has no file write.
- **Commit cadence: 1 or 2 commits.** One per Task 1 (the `docs/API.md` rewrite). One per Task 2 if and only if Task 2 lands (the optional `README.md` refinement). Task 3 (verification) does NOT commit. Commit messages follow the project's `docs(api): ...` convention with `(phase 19-05)` suffix.
- **No verification gate that requires running the backend.** Per `local_maven_gap` memory + the docs-only nature of this plan, this plan does NOT execute `mvn verify`, `mvn spring-boot:run`, `pnpm build`, `pnpm test`, or any other compile/test step. The runtime smoke (the `OpenApiSurfaceIntegrationTest` from 19-04) already runs in CI and CI-asserts the runtime contract this document references. Task 3 verifies markdown structural correctness via re-read only.
- **Link verification at execution time.** Task 3 explicitly walks each link target. The executor reads:
  - `docs/SELF_HOSTED_CONTRACT.md` and confirms the H2 "## 7. Authentication" heading exists -> anchor `#7-authentication`.
  - `docs/SELF_HOSTED_CONTRACT.md` and confirms the H3 "### 7.1 Built-in auth (default)" heading exists -> anchor `#71-built-in-auth-default` (or fall back to `#7-authentication` if the H3 anchor differs by parenthetical-stripping).
  - `docs/SELF_HOSTED_CONTRACT.md` and confirms the H3 "### 7.2 Reverse-proxy forward-auth (opt-in)" heading exists -> anchor `#72-reverse-proxy-forward-auth-opt-in` (or fall back to `#7-authentication`).
  - `docs/EXPORT_FORMAT.md` exists.
  - `docs/OBSERVABILITY.md` exists.
  - `README.md` exists at repo root (linked as `../README.md` from `docs/API.md`).
  - The runtime endpoints (`/v3/api-docs`, `/swagger-ui.html`) — these are documented by `OpenApiConfig.java` and `application.yml` as already-wired in 19-02; no need to verify at link-time.
  Any anchor mismatch surfaces as a structural blocker; the executor falls back to the bare H2 anchor as documented above.
- **No new YAML frontmatter, no new file.** The rewrite REPLACES the 3-line file content; the file path stays `docs/API.md`. No new file, no rename.
- **No markdown linter errors.** The rewrite uses the project's existing markdown conventions (verified against `docs/EXPORT_FORMAT.md` and `docs/SELF_HOSTED_CONTRACT.md` style): blank lines between H2 sections, fenced code blocks with language hints (`json`, `bash`, `http`, `yaml`), table headers + separator rows, no trailing whitespace, single newline at end of file.
- **No frontend changes.** Documentation-only.
- **No SQL migration, no Flyway change.** Pure documentation.
- **No `ISSUES.md` edit.** Phase 19's i-NEW-A (ApiError.code drift) and i-NEW-B (SpringDoc + Boot 3.5 first-integration risk) ISSUES.md candidates remain candidates — 19-04 explicitly deferred ISSUES.md edits to the orchestrator's discretion (per 19-04-PLAN line 132). 19-05 inherits that deferral; this plan does NOT touch `ISSUES.md`.

**Issues being addressed:**
- 19-01-AUDIT Section 6 bucket: `docs-api-md-rewrite` (closed by Task 1).
- 19-01-AUDIT Section 4C verdict (Direction C2 navigational rewrite): realised by Task 1.
- 19-01-AUDIT Section 4D verdict (Direction D1 BIND_ADDR-as-trust-boundary): documented in Task 1's Section 7 "Discoverability".
- 19-01-AUDIT Section 2A "stale by 2 milestones" placeholder finding: closed by Task 1.
- 19-01-AUDIT Section 4E ApiError.code catalog (4 typed values): surfaced as a wire-level error contract in Task 1's Section 4 "Error envelope".
- ROADMAP Phase 19 deliverable "docs/API.md rewrite" (the final Phase 19 sub-deliverable): closed by 19-05.

**Out of scope (deferred to other plans / phases):**
- Static `docs/openapi.json` export under git (snapshot of the runtime surface per release tag). Phase 42 candidate per audit Section 4C.
- `springdoc-openapi-maven-plugin` build wiring (generates `target/openapi.json` at build time). Phase 42 per audit Section 5D Option E2.
- CI tag-push step that commits `docs/openapi.json` per release. Phase 42 per audit Section 5D Option E3.
- `ApiErrorCode` Java enum extraction (the durable i-NEW-A drift fix). Phase 42 per audit Section 4E.
- Per-DTO `@Schema` annotations beyond `ApiError.code` (e.g. on `ClaudeSummaryDto`, `FullExportDto.SetRow`, etc.). v1.0 polish per audit Section 6 `defer`.
- Per-method `@Operation(summary = "...", description = "...")` for any controller beyond the dual-SessionsController operationId disambiguation (closed in 19-03). v1.0 polish.
- Tag i18n (Turkish translations of OpenAPI tags). Not aligned with the developer-tooling-English convention.
- Embedded Swagger UI iframe in `docs/API.md` (audit Direction C3 explicitly REJECTED).
- Hand-written per-endpoint reference tables in `docs/API.md` (audit Direction C1 explicitly REJECTED).
- Method + path enumeration for every controller in `docs/API.md`.
- Specific client-codegen tool recommendation (openapi-generator, kiota, etc.). Audit Section 4C boundary: pointer only, no specific tool.
- ISSUES.md edits for i-NEW-A / i-NEW-B / i-NEW-C. Inherited deferral from 19-04.
- CHANGELOG.md entry. Owned by Phase 20-release plan.
- ROADMAP.md / STATE.md / IDENTITY.yaml / `.planning/PROJECT.md` updates. Owned by orchestrator-spawned summarizer at phase close.

**Out of scope (NOT in Phase 19 at all):**
- OIDC client / OAuth flow documentation. Per `SELF_HOSTED_CONTRACT.md` "scope of this file" the app is explicitly NOT shipping an identity provider.
- Frontend API-consumer documentation. The runtime OpenAPI document IS the consumer-facing reference; the frontend's TanStack Query layer is wired against it.
- Rate-limit documentation. WorkoutHub does not currently rate-limit at the application layer; rate-limiting is the operator's reverse-proxy concern.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/API.md as the navigational entry point per Direction C2</name>
  <files>
    docs/API.md
  </files>
  <action>
Single-file rewrite. The 3-line placeholder is replaced with the 7-section navigational document per the audit Section 4C Direction C2 verdict. Use the Write tool (full content replacement) since the existing 3 lines do not survive the rewrite.

**Step A: Verify link-target anchors at execution time.**

Before writing, the executor performs three Read operations to confirm the anchor strings the Step B content references:
1. Read `docs/SELF_HOSTED_CONTRACT.md` lines 260-296 to confirm the H2 "## 7. Authentication" heading and the H3 "### 7.1 Built-in auth (default)" + "### 7.2 Reverse-proxy forward-auth (opt-in)" headings exist verbatim. Audit Section 2C confirms these headings exist; the verification re-confirms.
2. Read `docs/EXPORT_FORMAT.md` lines 1-25 to confirm the file exists and the H1 is `# WorkoutHub Full-Export JSON Format` (or similar). The cross-link from `docs/API.md` only targets the file (no anchor).
3. Read `docs/OBSERVABILITY.md` lines 1-25 to confirm the file exists. The cross-link only targets the file (no anchor).

If any link target fails to resolve at execution time (e.g. the heading was renamed in a parallel plan), the executor falls back to the conservative bare H2 anchor (`#7-authentication`) for SELF_HOSTED_CONTRACT links and surfaces a verification note for the orchestrator. The plan does NOT block on a missing anchor since the bare-section anchor is always safe; only file-not-found surfaces as a hard blocker.

**Step B: Write the rewritten content.**

Replace the entire file content with the following 7-section structure. Section content is locked at plan time; the executor writes verbatim with one-line latitude on phrasing where the prose is locked-but-could-flex (e.g. the closing sentence of each section).

Final file content (template; the executor adapts only the explicit `<verify>` placeholders if Step A surfaced a fallback):

```markdown
# API

WorkoutHub exposes a REST API under `/api/**`. The runtime SpringDoc surface is
the authoritative reference: a live OpenAPI 3.x document at
`GET /v3/api-docs` (JSON, YAML at `/v3/api-docs.yaml`) and an interactive
Swagger UI at `GET /swagger-ui.html`. This document is the navigational entry
point — it links to the runtime catalog, summarizes the auth model and the
project-wide error envelope, and points readers at the per-section contracts
(export wire shape, operator endpoints, self-hosted contract). It does not
attempt to re-document every endpoint by hand; the runtime document is the
single source of truth and the [`OpenApiSurfaceIntegrationTest`](../backend/src/test/java/com/workouthub/common/web/OpenApiSurfaceIntegrationTest.java)
asserts its structural shape on every CI run.

## Runtime endpoints

The OpenAPI document is rendered by SpringDoc 2.x against every
`@RestController` under `/api/**`. Operator-facing endpoints (`/livez`,
`/healthz`, `/metrics`, `/actuator/**`) are excluded from the document via
`springdoc.paths-to-match=/api/**` and stay documented in
[`OBSERVABILITY.md`](OBSERVABILITY.md).

| Endpoint | Purpose |
|----------|---------|
| `GET /v3/api-docs` | OpenAPI 3.x JSON document. The canonical machine-readable contract. |
| `GET /v3/api-docs.yaml` | Same document, YAML serialization. |
| `GET /swagger-ui.html` | Bundled interactive Swagger UI; redirects to `/swagger-ui/index.html`. |

The document declares two security schemes (`bearerAuth` and `forwardAuth`;
see [Authentication](#authentication)), 28 hand-curated tag groupings (one per
user- or admin-facing controller), and the project-wide `ApiError` envelope
schema (see [Error envelope](#error-envelope)). For client codegen, point your
tool of choice (openapi-generator, kiota, etc.) at the JSON URL:

```bash
curl -s http://127.0.0.1:8080/v3/api-docs | jq '.info, (.tags | length), (.paths | length)'
```

## Authentication

WorkoutHub supports the two-mode auth model defined in
[`SELF_HOSTED_CONTRACT.md` Section 7](SELF_HOSTED_CONTRACT.md#7-authentication).
The OpenAPI document declares both schemes; runtime behavior depends on
`APP_AUTH_MODE`.

### Built-in JWT (default)

Default mode (`APP_AUTH_MODE=builtin`). Clients obtain a token from
`POST /api/auth/login` and present it on subsequent requests:

```http
Authorization: Bearer <jwt>
```

OpenAPI scheme: `bearerAuth` (`type: http`, `scheme: bearer`,
`bearerFormat: JWT`). Applied globally; the permitAll endpoints
(`POST /api/auth/login`, `POST /api/auth/refresh`,
`POST /api/auth/reset-password`, and the HMAC-token-gated
`POST /api/webhooks/scale/**`) are reachable without a Bearer token. See
[contract Section 7.1](SELF_HOSTED_CONTRACT.md#71-built-in-auth-default).

### Reverse-proxy forward-auth (opt-in)

Set `APP_AUTH_MODE=forward-auth` when an SSO gateway (Authentik, Authelia,
Keycloak, oauth2-proxy) terminates auth in front of the app. The application
trusts identity headers injected by the proxy:

```
APP_AUTH_HEADER_USER=X-Forwarded-User
APP_AUTH_HEADER_EMAIL=X-Forwarded-Email
APP_AUTH_HEADER_GROUPS=X-Forwarded-Groups
APP_AUTH_TRUSTED_PROXIES=10.0.0.0/8,100.64.0.0/10
```

The headers are trusted **only** when the request source IP matches
`APP_AUTH_TRUSTED_PROXIES`. This is the single safety mechanism that prevents
header injection from external clients; OpenAPI 3.x cannot model the source-IP
gate, so the document declares the `forwardAuth` apiKey scheme (header
`X-Forwarded-Email`) and leaves the trust gate to the operator. See
[contract Section 7.2](SELF_HOSTED_CONTRACT.md#72-reverse-proxy-forward-auth-opt-in).

The application does not ship an OIDC client, an OAuth flow, or a session
backed by an external identity provider — that layer lives in the operator's
reverse proxy.

## Error envelope

Every `4xx` and `5xx` response uses the project-wide `ApiError` envelope. The
shape is rendered to OpenAPI under `components.schemas.ApiError` with each
field annotated; the field-level validation case adds an `errors[]` array of
`FieldError` records.

```json
{
  "timestamp": "2026-05-06T12:34:56Z",
  "status": 409,
  "error": "Conflict",
  "message": "Session already finished",
  "path": "/api/sessions/123/sets",
  "code": "SESSION_FINISHED"
}
```

The `code` field is a typed string populated for conflict responses tied to
known business rules. Phase 15-04 catalogued four values; the OpenAPI schema
enumerates them as `allowableValues` on `ApiError.code`:

| Code | HTTP | Meaning |
|------|------|---------|
| `SESSION_ALREADY_ACTIVE` | 409 | Attempt to start a workout session while another is still active for the same user. |
| `SESSION_ALREADY_FINISHED` | 409 | Attempt to finish a session that has already been finalized. |
| `SESSION_FINISHED` | 409 | Mutation attempt (set add/update) against a finished session. |
| `SET_NUMBER_DUPLICATE` | 409 | Set number collision within the same session-exercise pair. |

`code` may be `null` on legacy conflict paths (eight `ConflictException`
throw sites today emit a 409 without a typed code; Jackson `NON_NULL` strips
the field from the wire). Tooling should branch on `status` and `code`, not
on the human-readable `message` field, which is not stable across releases.

## Export format

The full-export and import endpoints (`GET /api/export/full`,
`POST /api/export/import`) ship a self-describing JSON payload designed for
round-trip with chat-style LLMs. The wire shape, enum values, round-trip
preservation rules, and acceptable drift are documented in
[`EXPORT_FORMAT.md`](EXPORT_FORMAT.md). The runtime OpenAPI document
references the same DTOs (`FullExportDto`, `ClaudeSummaryDto`) under
`components.schemas`.

## Operator endpoints

Operator-facing endpoints are intentionally excluded from the OpenAPI
document and live in [`OBSERVABILITY.md`](OBSERVABILITY.md):

- `GET /livez` — process is alive.
- `GET /healthz` — application is ready to serve traffic (DB reachable,
  migrations applied).
- `GET /metrics` — Prometheus scrape on the main listener.
- `GET /actuator/health` — Spring Boot Actuator health endpoint.
- `GET /actuator/info` — build info (version, commit).
- `GET /actuator/prometheus` — Spring Boot Actuator's Prometheus exporter.

These paths are `permitAll` at the application layer per
[`SELF_HOSTED_CONTRACT.md` Section 9](SELF_HOSTED_CONTRACT.md#9-metrics-and-health);
their access is bounded by `BIND_ADDR` and the operator's reverse proxy, not
by application auth.

## Discoverability

The OpenAPI JSON and Swagger UI are reachable without authentication by
design. The application's public-network exposure is bounded at the operator
layer:

- `BIND_ADDR=127.0.0.1` (compose default) restricts the listener to the
  loopback interface; only the operator's host can reach the OpenAPI surface.
- Operators who set `BIND_ADDR=0.0.0.0` (against the contract) accept the
  discoverability risk explicitly.
- The reverse-proxy layer (Caddy, Traefik, nginx — see [`../README.md`](../README.md)
  "Exposure" section) decides whether `/v3/api-docs` and `/swagger-ui.html`
  are reachable from the public DNS name. Restrict by path, by source IP, or
  by basic-auth at the proxy if the deployment requires it.

This stance is the v0.4 verdict from the [Phase 19
audit](../.planning/phases/19-api-contract-docs/19-01-AUDIT.md) Section 4D
and is reviewed at v1.0 (Phase 42).
```

**Step C: Verification within the task.**

After Write, the executor re-reads the entire `docs/API.md` and confirms:
- The H1 is `# API` (single line, matches the existing placeholder's H1 to keep commit-noise low).
- Exactly 7 H2 sections in order: `## Runtime endpoints`, `## Authentication`, `## Error envelope`, `## Export format`, `## Operator endpoints`, `## Discoverability`. (The intro paragraph sits between H1 and the first H2 with no intermediate heading.)
- The "Authentication" section has 2 H3 sub-sections: `### Built-in JWT (default)`, `### Reverse-proxy forward-auth (opt-in)`.
- All cross-link targets are valid relative paths from `docs/API.md`:
  - `SELF_HOSTED_CONTRACT.md#7-authentication`
  - `SELF_HOSTED_CONTRACT.md#71-built-in-auth-default`
  - `SELF_HOSTED_CONTRACT.md#72-reverse-proxy-forward-auth-opt-in`
  - `SELF_HOSTED_CONTRACT.md#9-metrics-and-health`
  - `EXPORT_FORMAT.md`
  - `OBSERVABILITY.md`
  - `../README.md`
  - `../backend/src/test/java/com/workouthub/common/web/OpenApiSurfaceIntegrationTest.java`
  - `../.planning/phases/19-api-contract-docs/19-01-AUDIT.md`
- The 4 typed `ApiError.code` values listed in the table exactly match the audit Section 4E catalog: `SESSION_ALREADY_ACTIVE`, `SESSION_ALREADY_FINISHED`, `SESSION_FINISHED`, `SET_NUMBER_DUPLICATE`.
- The 4 typed values in the table exactly match the `allowableValues` set rendered into `ApiError.code` by 19-04 Task 1.
- Total line count is between 120 and 200 (well under the 200-line `CLAUDE.md` discipline cap).
- No emojis anywhere.
- No trailing whitespace; single trailing newline at end of file.
- No frontmatter (no YAML `---` block at the top).
- No reference to specific client-codegen tools beyond the parenthetical `(openapi-generator, kiota, etc.)` (audit Section 4C boundary: pointer only).
- No per-endpoint method + path enumeration beyond the 3 runtime endpoints + the 6 operator endpoints + the explicit permitAll set in the Authentication section + the 2 export-endpoint paths.

NO change to any other file in this task. NO change to `README.md` (Task 2 owns it).

Commit message: `docs(api): rewrite as navigational entry point to runtime OpenAPI surface (phase 19-05)`.
  </action>
</task>

<task type="auto">
  <name>Task 2 (optional): Refine README.md API-doc link description if existing wording is misleading post-rewrite</name>
  <files>
    README.md
  </files>
  <action>
Optional single-line edit. SKIP entirely if the executor judges the existing line acceptable post-rewrite.

**Step A: Read the existing line.**

Read `README.md` at line 160. The existing content:

```
- [`docs/API.md`](docs/API.md) — REST API reference.
```

**Step B: Decide.**

Decision criterion: is "REST API reference" misleading after the navigational rewrite?
- Argument for refining: post-rewrite, `docs/API.md` is NOT a "REST API reference" in the traditional sense (no per-endpoint table); the runtime OpenAPI document is. The README description should signal the navigational nature so a reader doesn't open `docs/API.md` expecting a comprehensive endpoint catalog.
- Argument against refining: the existing line is short, conventional, and not actively wrong (the file still references the API; it just does so via runtime pointers). The 1-line touch adds commit-noise for marginal benefit.

The planner authorizes either choice. If the executor decides to refine, replace the existing line with:

```
- [`docs/API.md`](docs/API.md) — navigational entry point to the runtime OpenAPI document and per-section API contracts.
```

The replacement is a single-line edit using the Edit tool (find + replace). No other line in `README.md` is touched.

**Step C: Verification within the task.**

If Task 2 lands:
- The Edit changed exactly one line in `README.md`.
- The line is at or near line 160 (the original location).
- The surrounding `## Reference` H2 section is unchanged in structure.
- No other lines in `README.md` are touched.

If Task 2 is SKIPPED:
- `README.md` is unchanged.
- The Task 2 verification step records the skip decision in the executor's notes (e.g. "Task 2 skipped: existing 'REST API reference' description acceptable as-is.").

Commit message (only if Task 2 lands): `docs(readme): refine docs/API.md link to reflect navigational rewrite (phase 19-05)`.

NO change to any other file. NO addition or removal of `README.md` lines beyond the one targeted line. NO restructuring of the `## Reference` section.
  </action>
</task>

<task type="auto">
  <name>Task 3: Phase verification gate (re-read docs/API.md; confirm structural correctness; confirm no broken links)</name>
  <files>(verification only; no file write)</files>
  <action>
This task is a verification gate, not a SUMMARY write. The SUMMARY file is written by the orchestrator-spawned summarizer after the executor completes; this task captures the verification checklist the executor walks before signaling task-complete.

Per `local_maven_gap` memory + the docs-only nature of this plan, this plan does NOT execute any compile or test step. The verification gate here is structural-correctness and link-resolution only.

**Verification checklist (executor walks each item; surfaces a blocker if any item fails):**

1. **`docs/API.md` updated in Task 1:**
   - The file exists at `docs/API.md`.
   - The H1 is `# API` (single line).
   - Exactly 7 H2 sections in order: `## Runtime endpoints`, `## Authentication`, `## Error envelope`, `## Export format`, `## Operator endpoints`, `## Discoverability`. The intro paragraph sits between H1 and the first H2.
   - The "Authentication" section has exactly 2 H3 sub-sections (`### Built-in JWT (default)`, `### Reverse-proxy forward-auth (opt-in)`).
   - The "Error envelope" section contains exactly one fenced `\`\`\`json` block illustrating the ApiError shape AND a 4-row table with columns `Code | HTTP | Meaning` listing the 4 typed values from audit Section 4E (`SESSION_ALREADY_ACTIVE`, `SESSION_ALREADY_FINISHED`, `SESSION_FINISHED`, `SET_NUMBER_DUPLICATE`).
   - The "Runtime endpoints" section contains exactly one fenced `\`\`\`bash` block (the `curl ... | jq ...` example) and one 3-row table.
   - The "Authentication" section's "Built-in JWT" sub-section contains exactly one fenced `\`\`\`http` block (`Authorization: Bearer <jwt>`).
   - The "Authentication" section's "forward-auth" sub-section contains exactly one fenced code block (no language hint or `\`\`\`text`) showing the 4 `APP_AUTH_*` env keys.
   - Total line count between 120 and 200.
   - No emojis.
   - No trailing whitespace; single trailing newline.
   - No YAML frontmatter at the top.

2. **Cross-link targets resolve to real files:**
   - `docs/SELF_HOSTED_CONTRACT.md` exists and contains the H2 "## 7. Authentication" heading. Re-read at execution time to confirm.
   - `docs/SELF_HOSTED_CONTRACT.md` contains the H3 "### 7.1 Built-in auth (default)" heading. Re-read to confirm.
   - `docs/SELF_HOSTED_CONTRACT.md` contains the H3 "### 7.2 Reverse-proxy forward-auth (opt-in)" heading. Re-read to confirm.
   - `docs/SELF_HOSTED_CONTRACT.md` contains the H2 "## 9. Metrics and health" heading. Re-read to confirm.
   - `docs/EXPORT_FORMAT.md` exists.
   - `docs/OBSERVABILITY.md` exists.
   - `README.md` exists at repo root.
   - `backend/src/test/java/com/workouthub/common/web/OpenApiSurfaceIntegrationTest.java` exists (created by 19-04 Task 2).
   - `.planning/phases/19-api-contract-docs/19-01-AUDIT.md` exists.
   - If any anchor mismatches GitHub-flavored Markdown's slug generator (e.g. parenthetical-stripping changes the slug shape), the executor falls back to the bare H2 anchor (`#7-authentication`) and updates the in-document link accordingly. This is acceptable per the planner-time decision recorded in the `<context>` block.

3. **`README.md` Task 2 outcome (if Task 2 ran):**
   - If Task 2 LANDED: the line at or near `README.md:160` reflects the refined description; no other line in `README.md` is touched.
   - If Task 2 SKIPPED: `README.md` is unchanged.
   - The decision (LAND or SKIP) is recorded in the executor's task-complete signal so the orchestrator knows whether to expect 1 or 2 commits.

4. **No extraneous edits:**
   - No Java file is touched.
   - No test file is touched.
   - No `pom.xml`, `application.yml`, `OpenApiConfig.java`, `SecurityConfig.java`, `ApiError.java`, controller, DTO, service, repository, mapper file is touched.
   - No SQL migration is touched.
   - No frontend file is touched.
   - No `IDENTITY.yaml`, `CHANGELOG.md`, `ROADMAP.md`, `STATE.md`, `ISSUES.md`, `PROJECT.md`, `.planning/codebase/*`, `.planning/phases/19-*` (other than this plan's own placement) is touched.
   - No `docs/EXPORT_FORMAT.md`, `docs/SELF_HOSTED_CONTRACT.md`, `docs/OBSERVABILITY.md`, `docs/DEPLOYMENT.md`, `docs/MIGRATION.md`, `docs/DATA_SCHEMA.md`, `docs/BACKUP.md` is touched.
   - No new file is created under `docs/` (no `docs/openapi.json`, no `docs/auth.md`, etc.).

5. **CI smoke check (no new gate, but the existing 19-04 gate stays green):**
   - On the next CI run, `mvn verify` continues to pass.
   - The `OpenApiSurfaceIntegrationTest` from 19-04 continues to pass (5 tests; the runtime contract `docs/API.md` references is CI-asserted independently).
   - The frontend build (`pnpm build`) is unaffected (no frontend file touched).
   - Documentation-only plans do not introduce a new CI test; the existing 19-04 test is the enforcement.

If any of items 1-4 fail: the executor stops, surfaces the blocker, and does NOT proceed to commit. If all pass: each task's commit lands as a separate commit (1 commit if Task 2 SKIPPED; 2 commits if Task 2 LANDED) per the per-task commit-message conventions; the executor then signals the orchestrator that 19-05 is complete and ready for the SUMMARY.md write.

NOTE on commit cadence: Task 1 ends with its own commit. Task 2 (if it lands) ends with its own commit. Task 3 (verification) does NOT commit. The full plan delivers 1 or 2 commits. The optional SUMMARY.md commit is the orchestrator's responsibility, not this task's.
  </action>
</task>

</tasks>

<verification>

**Structural correctness (executor walks before signaling task-complete; per Task 3 checklist):**

- `docs/API.md` carries: 1 H1 (`# API`); 7 H2 sections in fixed order; 2 H3 sub-sections under "Authentication"; one `\`\`\`json` block in "Error envelope"; one `\`\`\`bash` block in "Runtime endpoints"; one `\`\`\`http` block + one bare-fenced env block in "Authentication"; one 3-row table in "Runtime endpoints" (Endpoint | Purpose); one 4-row table in "Error envelope" (Code | HTTP | Meaning); cross-links to `SELF_HOSTED_CONTRACT.md` (4 anchors), `EXPORT_FORMAT.md`, `OBSERVABILITY.md`, `../README.md`, `../backend/src/test/.../OpenApiSurfaceIntegrationTest.java`, `../.planning/phases/19-api-contract-docs/19-01-AUDIT.md`.
- The 4 typed values in the "Error envelope" table match the audit Section 4E catalog AND the `ApiError.code` `allowableValues` set rendered by 19-04 Task 1: `SESSION_ALREADY_ACTIVE`, `SESSION_ALREADY_FINISHED`, `SESSION_FINISHED`, `SET_NUMBER_DUPLICATE`.
- Total line count is between 120 and 200; no emojis; no trailing whitespace; single trailing newline; no frontmatter.
- `README.md` is either unchanged (Task 2 SKIPPED) or carries exactly one refined line at or near line 160 (Task 2 LANDED).
- No Java, test, config, migration, frontend, IDENTITY, CHANGELOG, ROADMAP, STATE, ISSUES, PROJECT, or peer-docs file is touched.

**CI gate (next push after the plan lands):**

- `mvn verify` on CI continues to pass (no Java change in this plan).
- The `OpenApiSurfaceIntegrationTest` from 19-04 continues to pass (no runtime change; the test asserts the runtime contract `docs/API.md` references but does NOT consume `docs/API.md` itself).
- Frontend build is unaffected.

**Tests added in this plan: 0** (the runtime smoke from 19-04 already covers the contract this document references).

**Files touched:** 1 modified (`docs/API.md`) + 0 or 1 modified (`README.md`, optional) = 1 or 2 file touches across 1 or 2 commits.

**Files NOT touched (audit + plan boundary):** `pom.xml`, `application.yml`, `application-prod.yml`, `OpenApiConfig.java`, `SecurityConfig.java`, `ApiError.java`, `GlobalExceptionHandler.java`, `ConflictException.java`, `OpenApiSurfaceIntegrationTest.java`, all 31 `*Controller.java` files, all DTOs, all services, all repositories, all mappers, all migrations, all frontend files, `IDENTITY.yaml`, `CHANGELOG.md`, `ROADMAP.md`, `STATE.md`, `ISSUES.md`, `PROJECT.md`, all `.planning/codebase/*` files, all peer `docs/*.md` files (`SELF_HOSTED_CONTRACT.md`, `EXPORT_FORMAT.md`, `OBSERVABILITY.md`, `DEPLOYMENT.md`, `MIGRATION.md`, `DATA_SCHEMA.md`, `BACKUP.md`).

</verification>

<success_criteria>

- Task 1's edit replaces `docs/API.md`'s 3-line placeholder with the 7-section navigational document per the audit Section 4C Direction C2 verdict, with all the structural elements described in its `<action>` Step C verification list (1 H1, 7 H2, 2 H3 under Authentication, 4 fenced code blocks, 2 tables, 9 cross-links, the 4-value typed-code catalog from audit Section 4E).
- Task 2 either lands a 1-line `README.md` refinement (with the executor-judged decision recorded) or is explicitly SKIPPED with the skip decision recorded.
- The Task 3 verification gate passes all file-checks and link-resolution-checks before the executor signals plan-complete.
- 1 or 2 commits land in order (1 if Task 2 SKIPPED, 2 if Task 2 LANDED): `docs(api): rewrite as navigational entry point to runtime OpenAPI surface (phase 19-05)` and optionally `docs(readme): refine docs/API.md link to reflect navigational rewrite (phase 19-05)`.
- On the next CI push, the build is green: `mvn verify` continues to pass; the `OpenApiSurfaceIntegrationTest` from 19-04 continues to pass; the frontend build is unaffected.
- Phase 19 (api-contract-docs) is closed: the audit (19-01), the springdoc-integration (19-02), the tag-and-collision-fix (19-03), the error-schema-and-test (19-04), and the docs-api-md-rewrite (19-05) are all landed. The OpenAPI document at `GET /v3/api-docs` is the runtime source of truth; `docs/API.md` is the navigational entry point that links readers to the runtime + per-section docs; the binding contract (`SELF_HOSTED_CONTRACT.md` Section 7) is cross-linked; the wire-level error contract (`ApiError.code` 4-value enum) is documented in prose AND CI-asserted; the operator-paths boundary (`OBSERVABILITY.md`) is cross-linked; the BIND_ADDR-as-trust-boundary stance is on the public-doc surface.
- No protected-file touch in this plan (`pom.xml` was the only protected touch in Phase 19; landed in 19-02). `README.md` is NOT in the project's protected-files list.

</success_criteria>

<output>

- `docs/API.md` is fully rewritten from the 3-line Phase-1-era placeholder to a ~120-160 line, 7-section navigational entry point per audit Section 4C Direction C2 (Task 1).
- (Optional) `README.md` line 160's `docs/API.md` link description is refined from "REST API reference" to "navigational entry point to the runtime OpenAPI document and per-section API contracts" if the executor judges the existing wording misleading post-rewrite (Task 2; SKIP allowed).

After 19-05 ships and CI is green: Phase 19 (api-contract-docs) is closed end-to-end. The runtime OpenAPI document at `GET /v3/api-docs` (live since 19-02) is the single source of truth for the REST surface; the 28 hand-curated tag groupings (closed in 19-03) navigate the document; the typed `ApiError.code` enum (annotated in 19-04 Task 1) surfaces in the schema and is CI-asserted by the 5-test `OpenApiSurfaceIntegrationTest` (added in 19-04 Task 2); `docs/API.md` (rewritten in 19-05) is the navigational entry point that links readers to the runtime, the binding self-hosted contract Section 7, the export format document, the operator-endpoints document, the OpenAPI surface integration test, and the README. Phase 19 has zero remaining open deliverables and ROADMAP can advance to Phase 20 (release-v0-4 cut).

</output>
