---
phase: 15-sessions-core
plan: 03
type: execute
---

<objective>
Expose `heartRateAvgBpm` (Short, nullable) read-only on `SessionDto` and `SessionSummaryDto` so Garmin .fit imports become visible to clients via `GET /api/sessions/{id}`, `/active`, `/start`, `/finish`, and `GET /api/sessions/history`.

Purpose: Closes audit Section 3 field-level drift "heartRateAvgBpm exposure gap" and audit Section 6 FG1. The V20 column has been live since v0.2 but is currently invisible to every API caller; the Garmin importer at `HealthImportService.java:68` is the sole writer and its data is silently dropped on the wire.

Output: 2 modified DTOs, 1 modified mapper, 2 modified integration tests, 1 SUMMARY.md. No migration, no controller change, no service change.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/phases/15-sessions-core/15-01-SUMMARY.md
@.planning/phases/15-sessions-core/15-02-SUMMARY.md

# Source files (will be modified):
@backend/src/main/java/com/workouthub/sessions/dto/SessionDto.java
@backend/src/main/java/com/workouthub/sessions/dto/SessionSummaryDto.java
@backend/src/main/java/com/workouthub/sessions/SessionsMapper.java

# Source files (read-only reference):
@backend/src/main/java/com/workouthub/sessions/domain/WorkoutSession.java
@backend/src/main/java/com/workouthub/health/HealthImportService.java

# Test files (will be modified):
@backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java
@backend/src/test/java/com/workouthub/sessions/SessionHistoryIntegrationTest.java

**Tech stack available:** Java 21, Spring Boot 3.5, Jackson with `serializationInclusion = NON_NULL` (omits null fields), JUnit 5, MockMvc, Testcontainers (per `AbstractIntegrationTest`).

**Established patterns (from 15-02):**
- Record-component additions land at the end of the record signature to preserve positional ordering for any reflective consumer.
- Mapper updates pass entity getter values directly without null defaulting; Jackson NON_NULL drops nulls automatically.
- Integration tests inject `WorkoutSessionRepository` via `@Autowired` to mutate fields that have no API write path (mimics the Garmin importer's contract).
- One commit per task; CI is the verification gate (local Maven unavailable on Windows host per `.planning/memory/local_maven_gap.md`).

**Constraining decisions (from audit):**
- Phase 15: read-only exposure only. No client write path (no `heartRateAvgBpm` on `FinishSessionRequest`, no PATCH endpoint). Garmin .fit importer at `HealthImportService.java:68` remains the sole writer.
- Phase 15: do NOT modify `HealthImportService` or any `health/` package code. The exposure decision is downstream-only.
- Phase 15: do NOT touch `WorkoutSession.java` entity, `WorkoutSessionRepository.java`, or `V20__sessions_heart_rate.sql`. Column shape is settled.
- Phase 15: do NOT modify `SessionSetDto.newPr` or `PrDetector` (Phase 16 territory).

**Issues being addressed:** None. Audit Section 3 field-level drift and Section 6 FG1 are contract-completeness items, not deferred issues in `ISSUES.md`.

**Concerns being verified:**
- The Jackson `NON_NULL` global configuration must drop the new `heartRateAvgBpm` field when null on legacy sessions (no V20-pre history rows have non-null values). Test asserts both null-omitted and value-present cases.
- `SessionSummaryDto` has 8 components today; adding a 9th does not break Jackson record deserialization for any consumer (the frontend reads via TanStack Query without strict shape validation per `frontend/src/lib/`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add heartRateAvgBpm to SessionDto, SessionSummaryDto, and SessionsMapper</name>
  <files>backend/src/main/java/com/workouthub/sessions/dto/SessionDto.java, backend/src/main/java/com/workouthub/sessions/dto/SessionSummaryDto.java, backend/src/main/java/com/workouthub/sessions/SessionsMapper.java</files>
  <action>
1. `SessionDto.java`: append `Short heartRateAvgBpm` as the LAST record component (after `List<SessionSetDto> sets`). Use boxed `Short` to allow null (matches entity field type).
2. `SessionSummaryDto.java`: append `Short heartRateAvgBpm` as the LAST record component (after `Short energyLevel`). Same boxed `Short` rationale.
3. `SessionsMapper.toDto`: pass `s.getHeartRateAvgBpm()` as the new last argument to the `SessionDto` constructor.
4. `SessionsMapper.toSummary`: pass `s.getHeartRateAvgBpm()` as the new last argument to the `SessionSummaryDto` constructor.

DO NOT add validation annotations - this is a read-only response field; validation only applies to request DTOs.
DO NOT change record component order before the new field - existing consumers (tests, frontend) read by JSON property name, not position, but other Java callers (mapper, tests) read positionally and breaking the order would cause silent compile-time-OK / runtime-wrong field swaps.
DO NOT add a no-arg overload or builder - records have a canonical constructor only; mapper call sites are the single boundary.
DO NOT touch `SessionSetDto`, `WorkoutSession.java`, or any other DTO. The change is scoped to two DTOs and one mapper class.
  </action>
  <verify>
- `grep -n "heartRateAvgBpm" backend/src/main/java/com/workouthub/sessions/dto/SessionDto.java` returns one line at the end of the record signature.
- `grep -n "heartRateAvgBpm" backend/src/main/java/com/workouthub/sessions/dto/SessionSummaryDto.java` returns one line at the end of the record signature.
- `grep -n "getHeartRateAvgBpm" backend/src/main/java/com/workouthub/sessions/SessionsMapper.java` returns exactly two matches (one in `toDto`, one in `toSummary`).
- No other source file under `backend/src/main/java/com/workouthub/sessions/` has been modified (verify with `git diff --stat backend/src/main/java/com/workouthub/sessions/`).
  </verify>
  <done>
- Both DTOs declare `Short heartRateAvgBpm` as their last record component.
- Both mapper methods populate the new field from `s.getHeartRateAvgBpm()`.
- No production code outside the three named files is touched.
- Commit message follows pattern: `feat(15-03): expose heartRateAvgBpm on session DTOs`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests asserting heart-rate exposure on detail and history endpoints</name>
  <files>backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java, backend/src/test/java/com/workouthub/sessions/SessionHistoryIntegrationTest.java</files>
  <action>
1. `SessionLifecycleIntegrationTest.java`:
   - Inject `WorkoutSessionRepository sessions` via `@Autowired` (add field; import `com.workouthub.sessions.domain.WorkoutSessionRepository`).
   - Add `@Test void heartRateAvgBpmIsNullByDefaultAndPopulatedAfterImporterWrite()`:
     a. Seed a user, start a session, capture `sessionId`.
     b. GET `/api/sessions/{id}`: assert `$.heartRateAvgBpm` is missing (use `jsonPath("$.heartRateAvgBpm").doesNotExist()`) - Jackson NON_NULL omits nulls.
     c. Mutate via repository: `WorkoutSession s = sessions.findById(sessionId).orElseThrow(); s.setHeartRateAvgBpm((short) 142); sessions.saveAndFlush(s);` - mimics the Garmin importer's write path at `HealthImportService.java:68` without booting the .fit pipeline.
     d. GET `/api/sessions/{id}` again: assert `$.heartRateAvgBpm` equals 142.
     e. Also assert `/api/sessions/active` returns the same `heartRateAvgBpm` value (proves the field appears on every `SessionDto`-returning endpoint, not just detail).

2. `SessionHistoryIntegrationTest.java`:
   - Inject `WorkoutSessionRepository sessions` via `@Autowired` (add field; import `com.workouthub.sessions.domain.WorkoutSessionRepository`).
   - Add `@Test void historyRowsExposeHeartRateAvgBpm()`:
     a. Seed a user, start-and-finish a session (reuse the existing `startAndFinish` helper), capture `sessionId`.
     b. Mutate via repository: set `heartRateAvgBpm = 138` on the finished session, `saveAndFlush`.
     c. GET `/api/sessions/history`: assert `$.content[0].heartRateAvgBpm` equals 138.
     d. Start-and-finish a second session WITHOUT setting heart rate.
     e. GET `/api/sessions/history`: assert `$.content[0].heartRateAvgBpm` does NOT exist (most-recent is the no-heart-rate session, NON_NULL omitted).

DO NOT add tests to `SessionSetsIntegrationTest.java`, `PrDetectionIntegrationTest.java`, or `ExerciseAnalyticsIntegrationTest.java` - heart-rate is a session-level field, not a set-level or analytics field.
DO NOT mock `HealthImportService` - direct repository write is the cleaner test path. Booting the Garmin .fit importer would couple this test to `health/` package internals (out of Phase 15 scope).
DO NOT use `Short.valueOf((short) 142)` - prefer `(short) 142` cast literal for brevity; both compile to the same boxed `Short`.
DO NOT delete or modify the existing test methods. Append only.
  </action>
  <verify>
- `grep -n "heartRateAvgBpm" backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java` returns at least 4 matches (assertion lines for missing, present-on-detail, present-on-active).
- `grep -n "heartRateAvgBpm" backend/src/test/java/com/workouthub/sessions/SessionHistoryIntegrationTest.java` returns at least 2 matches (assertion line for value, assertion line for doesNotExist).
- `grep -n "WorkoutSessionRepository" backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java backend/src/test/java/com/workouthub/sessions/SessionHistoryIntegrationTest.java` shows both files import and inject the repository.
- No other test file under `backend/src/test/java/com/workouthub/sessions/` is modified.
- CI verification gate: this commit triggers backend test execution on the next push; local Maven is unavailable per memory. The grep checks above are the deterministic local proof of structural correctness.
  </verify>
  <done>
- `SessionLifecycleIntegrationTest` has one new test method covering detail + active endpoints, asserting both null-omitted and 142-present cases.
- `SessionHistoryIntegrationTest` has one new test method covering history pagination row exposure with and without value.
- Both tests use direct repository mutation (mimicking the Garmin importer) - no health package coupling.
- Commit message follows pattern: `test(15-03): cover heartRateAvgBpm exposure on session detail and history`.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --stat` shows exactly 5 modified files: 2 DTOs, 1 mapper, 2 integration tests.
- [ ] No file in `backend/src/main/java/com/workouthub/health/` is modified.
- [ ] No file in `backend/src/main/resources/db/migration/` is added or modified.
- [ ] No file under `backend/src/main/java/com/workouthub/sessions/domain/` is modified.
- [ ] Both record DTOs compile structurally (canonical constructor signature matches mapper call sites).
- [ ] Two atomic commits land on the branch, one per task, in declared order.
</verification>

<success_criteria>
- Audit Section 3 field-level drift "heartRateAvgBpm exposure gap" closed.
- Audit Section 6 FG1 (heart-rate exposure on `SessionDto` + `SessionSummaryDto`) shipped.
- Audit Section 6 C3 coverage gap (heart-rate field assertion) closed via the two new integration tests.
- Phase 15 plan-03 of 4 complete; plan 15-04 (auto-numbering verdict + typed 409 codes) is the only remaining hardening plan in Phase 15.
- No regression: existing tests in `SessionLifecycleIntegrationTest` and `SessionHistoryIntegrationTest` continue to pass (CI gate).
- Garmin .fit importer (Phase 17 territory) remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/15-sessions-core/15-03-SUMMARY.md` matching the 15-02-SUMMARY.md frontmatter shape:

---
phase: 15-sessions-core
plan: 03
subsystem: sessions
tags: [dto, mapper, integration-test, heart-rate, contract-completeness]

requires:
  - phase: 15-sessions-core
    plan: 01
    reason: audit Section 3 field-level drift and Section 6 FG1 identified the exposure gap closed by this plan

provides:
  - heartRateAvgBpm read-only exposure on SessionDto (detail/active/start/finish responses)
  - heartRateAvgBpm read-only exposure on SessionSummaryDto (history response rows)
  - integration test pattern for write-via-repository mimicking the Garmin importer

affects:
  - 19-api-contract-docs (OpenAPI surface for the new field; document import-fed semantics)

tech-stack:
  added: []
  patterns:
    - direct repository mutation in integration tests as a stand-in for upstream-package writers (here Garmin .fit importer)

key-files:
  created:
    - .planning/phases/15-sessions-core/15-03-SUMMARY.md
  modified:
    - backend/src/main/java/com/workouthub/sessions/dto/SessionDto.java
    - backend/src/main/java/com/workouthub/sessions/dto/SessionSummaryDto.java
    - backend/src/main/java/com/workouthub/sessions/SessionsMapper.java
    - backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java
    - backend/src/test/java/com/workouthub/sessions/SessionHistoryIntegrationTest.java

key-decisions:
  - "Read-only exposure: no FinishSessionRequest field, no PATCH endpoint. Garmin .fit importer at HealthImportService.java:68 remains the sole writer; Phase 17 owns the data source."
  - "Field placement: appended as last record component to preserve positional ordering for mapper call sites."
  - "Test write path: direct WorkoutSessionRepository.saveAndFlush after setHeartRateAvgBpm mimics the importer without booting health/ package internals (out of Phase 15 scope)."
  - "Jackson NON_NULL omission: legacy sessions with null heartRateAvgBpm have the field absent from the JSON response; the test asserts both omitted and present cases."

duration: [measured]
completed: 2026-05-04
---

# Phase 15 Plan 03: heart-rate exposure on session DTOs Summary

**[One-line shipped summary - what changed and why]**

## Performance
[duration, tasks, files]

## Accomplishments
[Bulleted - DTOs added, mapper updated, tests added, audit gaps closed]

## Task Commits
1. **Task 1: ...** - `<sha>`
2. **Task 2: ...** - `<sha>`

## Files Created/Modified
[5 files with one-line descriptions]

## Decisions Made
[Inherit from frontmatter key-decisions]

## Deviations from Plan
[None expected]

## Issues Encountered
[None expected; CI is the verification gate per local Maven gap]

## Next Phase Readiness
- Plan 15-04 (auto-numbering verdict + typed 409 codes) is the last remaining Phase 15 hardening plan.
- Phase 19 (api-contract-docs) inherits the OpenAPI doc note for heartRateAvgBpm import-fed semantics.

---
*Phase: 15-sessions-core*
*Completed: 2026-05-04*
</output>
