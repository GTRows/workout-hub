---
phase: 15-sessions-core
plan: 04
type: execute
---

<objective>
Close audit Section 4b PARTIAL (typed 409 codes) and Section 4c FAIL (auto-numbering verdict). Add an optional `code` field to the shared `ApiError` envelope so the IndexedDB drainer can branch on `SET_NUMBER_DUPLICATE` vs `SESSION_FINISHED` (and on `SESSION_ALREADY_ACTIVE` / `SESSION_ALREADY_FINISHED` for tab-conflict cases) instead of guessing from the human-readable message. Lock the auto-numbering verdict: keep the live-online `count + 1` fallback at `SessionSetsService.java:55-57` but document the contract in Javadoc - offline-queue drainers MUST send explicit `setNumber` to preserve client-truth ordering. Land the C4 documentation test asserting `$.sets[*].newPr` is omitted on detail re-read.

Purpose: Plan 15-02 (idempotency key) made 200 unambiguous on the create path. Plan 15-04 finishes the offline-first sync contract by making the remaining 409s machine-readable and by writing down the auto-numbering rule a drainer must obey. The drainer can now treat status + code as the full decision input: 200 = persisted, 201 = newly created, 409 + `SET_NUMBER_DUPLICATE` = renumbering bug on the client, 409 + `SESSION_FINISHED` = drop the queued row.
Output: `code` component added to `ApiError`; `ConflictException` two-arg constructor; `GlobalExceptionHandler` propagates the code; four sessions/ throw sites carry codes; two existing integration tests assert `$.code`; one new `newPr` re-read test; `SessionSetsService.add` Javadoc documents the auto-numbering contract.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/phases/15-sessions-core/15-02-SUMMARY.md
@.planning/phases/15-sessions-core/15-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md

# Source files to modify
@backend/src/main/java/com/workouthub/common/web/ApiError.java
@backend/src/main/java/com/workouthub/common/web/ConflictException.java
@backend/src/main/java/com/workouthub/common/web/GlobalExceptionHandler.java
@backend/src/main/java/com/workouthub/sessions/SessionsService.java
@backend/src/main/java/com/workouthub/sessions/SessionSetsService.java

# Test files
@backend/src/test/java/com/workouthub/common/GlobalExceptionHandlerTest.java
@backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java
@backend/src/test/java/com/workouthub/sessions/SessionSetsIntegrationTest.java

**Tech stack available:** Spring Boot 3.5.x, Java 21 records, Jackson `@JsonInclude(NON_NULL)` on ApiError (legacy responses keep current shape - new field absent when null), JUnit 5, Mockito, MockMvc, Testcontainers Postgres 16-alpine.

**Established patterns (from prior phases and 15-02 / 15-03):**
- Feature-sliced packages; DTOs are records under `<feature>/dto/`
- Constructor injection only; no `@Autowired` on fields
- `ConflictException` -> 409, `NotFoundException` -> 404 via `GlobalExceptionHandler`
- Optional record components are appended last to keep diffs minimal (15-02 did this with `clientSetId` on `AddSetRequest`; 15-03 did this with `heartRateAvgBpm` on `SessionDto` / `SessionSummaryDto`)
- Integration tests extend `AbstractIntegrationTest`, seed users via `TestAuthHelpers.seed`, assert response bodies via `jsonPath`
- `GlobalExceptionHandlerTest` is a pure unit test with `MockHttpServletRequest`, no Spring context

**Constraining decisions:**
- Audit 15-01 Section 4b: drainer cannot decide from status alone whether 409 means "saved earlier" or "session is finished". Plan 15-02 already removed the "saved earlier" 409 (replay is now 200). The remaining 409s on the create path are `SET_NUMBER_DUPLICATE` (V5 unique violation) and `SESSION_FINISHED` (active-session guard). Both must carry codes.
- Audit 15-01 Section 4c: auto-numbering at `SessionSetsService.java:55-57` is order-sensitive. Out-of-order drain WITHOUT explicit `setNumber` corrupts row order. Plan 15-02 already landed `outOfOrderDrainPreservesSetNumberOrdering` proving the explicit-number path is safe; the open question for 15-04 is what to do about the implicit path.
- Audit 15-01 Section 6 CF2 verdict: keep auto-numbering for the live-online ad-hoc-add UX (the frontend session-execution screen taps "next set" without a number), but document the contract: offline-queue drainers MUST send explicit `setNumber`. Removing the fallback would force every UI caller to track a counter, which is hostile to the live path; the offline path already needs explicit numbering to survive out-of-order drain (proven by C2 test in 15-02).
- Audit 15-01 Phase 16 leak guard (still in force): MUST NOT modify `PrDetector`, `ExerciseAnalyticsController/Service`, `LastPerformanceDto`, `ProgressPointDto`, or `SessionSetRepository.findHistoricalByUserAndExercise`. The `newPr` flag stays as-is on the create response and stays absent on detail re-reads (the C4 test in Task 3 documents this).
- Codebase blast radius: `ApiError.code` is a NEW optional component on a shared envelope. Every existing 4xx/5xx response gets `code = null` (Jackson NON_NULL omits it). No client-visible regression. Only the four sessions/ throw sites in this plan emit non-null codes; other features can adopt codes incrementally in their own phases.
- Code-string convention: `SCREAMING_SNAKE_CASE` ASCII string constants. Stored as Java `String` constants on `ConflictException` (no enum) so callers in any feature can pass their own codes without coupling to a central registry. Drainer reads the literal string.

**Issues being addressed:** None from `ISSUES.md`. Audit Section 4b PARTIAL and 4c FAIL.

**Out of scope (deferred):**
- Adopting typed codes in other features (auth/, users/, workouts/, etc.) - infrastructure lands here; per-feature adoption is each feature's own concern.
- Body diffing on idempotent replay - Phase 19 at earliest (15-02 decision).
- Removing the auto-numbering fallback entirely - explicitly rejected by the CF2 verdict above.
- Persisting `is_pr` on `session_sets` - Phase 16 (sessions-analytics).
- OpenAPI doc surface for the new code field - Phase 19 (api-contract-docs).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed `code` to ApiError, ConflictException constructor variant, GlobalExceptionHandler propagation, GlobalExceptionHandlerTest assertion</name>
  <files>backend/src/main/java/com/workouthub/common/web/ApiError.java, backend/src/main/java/com/workouthub/common/web/ConflictException.java, backend/src/main/java/com/workouthub/common/web/GlobalExceptionHandler.java, backend/src/test/java/com/workouthub/common/GlobalExceptionHandlerTest.java</files>
  <action>
**`ApiError.java`:**
- Append `String code` as the LAST record component (after `errors`). The component is optional - callers pass `null` for legacy 4xx/5xx responses. Jackson is already configured with `@JsonInclude(NON_NULL)` at the global level (`JacksonConfig`), so a `null` code is omitted from the JSON output and existing client expectations are preserved.
- Update the inline Javadoc comment if any (none today; do not invent one).

**`ConflictException.java`:**
- Add a private final `String code` field.
- Keep the existing single-arg `ConflictException(String message)` constructor unchanged - it must call `this(null, message)` so existing throw sites in other packages (auth/, users/, etc.) keep working with `code = null`.
- Add a new constructor `ConflictException(String code, String message)` that stores the code and forwards the message via `super(message)`.
- Add a `public String code()` accessor (record-style getter naming, matching the existing codebase style on records and DTOs).

**`GlobalExceptionHandler.java`:**
- Update `handleConflict` to read `ex.code()` and pass it down to `build(...)`.
- Update the private `build(...)` helper to accept an additional `String code` parameter as the LAST argument (mirrors ApiError's component order). All callers default to `null` except `handleConflict`. Update every other caller (`handleValidation`, `handleNotFound`, `handleConstraintViolation`, `handleMissingParam`, `handleTypeMismatch`, `handleBadCredentials`, `handleDenied`, `handleUnreadableBody`, `handleResponseStatus`, `handleUnhandled`) to pass `null` as the trailing argument.
- Construct the new `ApiError(...)` with the code as the trailing argument.

**`GlobalExceptionHandlerTest.java`:**
- Extend the existing `conflictMapsTo409` test to assert `response.getBody().code()` is `null` when constructed with the single-arg `ConflictException("email taken")`. Verifies back-compat.
- Add a new test `conflictWithCodePropagatesCodeToApiError`: construct `new ConflictException("EMAIL_TAKEN", "email taken")`, invoke `handler.handleConflict(...)`, assert status 409, message "email taken", AND `response.getBody().code()` equals "EMAIL_TAKEN". Verifies the new constructor wires through.
- Optionally extend `notFoundMapsTo404WithPath` to assert `response.getBody().code()` is `null` (NotFoundException does NOT carry a code in this plan; codes are conflict-only for v0.4 scope). This is documentation, not strictly required - skip if it adds churn to the test.

**Avoid and WHY:**
- Do NOT make `code` a required component on `ApiError`. Every existing 4xx/5xx producer would need updating; the blast radius would explode beyond Phase 15 scope. Optional + null = zero regression.
- Do NOT add code support to `NotFoundException`. The audit's typed-code requirement is conflict-only (drainer disambiguation). Adding code to NotFoundException would extend the scope without a concrete consumer.
- Do NOT introduce a code enum. A `String` constant per throw site is sufficient and avoids a central registry that every feature would have to import. Drainers compare against literal strings.
- Do NOT change the position of any existing `ApiError` component. Re-ordering would be a JSON-shape change. Append-only.
- Do NOT remove the single-arg `ConflictException` constructor. It is used in auth/, workouts/, etc. and removing it would create a multi-package edit cascade that this plan explicitly avoids.
  </action>
  <verify>
- `grep -n "String code" backend/src/main/java/com/workouthub/common/web/ApiError.java` shows the new component as the last position.
- `grep -nE "code\(\)|String code" backend/src/main/java/com/workouthub/common/web/ConflictException.java` shows the field, two-arg constructor, and getter; the single-arg constructor still compiles by delegating.
- `grep -n "ex.code()" backend/src/main/java/com/workouthub/common/web/GlobalExceptionHandler.java` shows handleConflict reads the code; `grep -n "build(" backend/src/main/java/com/workouthub/common/web/GlobalExceptionHandler.java` shows every caller passes a trailing argument (null for non-conflict paths).
- `grep -nE "conflictWithCodePropagates|conflictMapsTo409" backend/src/test/java/com/workouthub/common/GlobalExceptionHandlerTest.java` shows the new + extended tests.
- In CI: `./mvnw -pl backend test -Dtest=GlobalExceptionHandlerTest` passes.
- In CI: `./mvnw -pl backend compile` succeeds (no other package broke - the single-arg constructor delegating preserves source compatibility everywhere).
  </verify>
  <done>
`ApiError` has an optional trailing `String code`; `ConflictException` accepts an optional code via a two-arg constructor while preserving the single-arg constructor; `GlobalExceptionHandler` propagates the code into the 409 body; `GlobalExceptionHandlerTest` asserts both null-code and explicit-code behavior; no other package needs changes for the build to pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply typed codes to sessions/ 409 throw sites and assert codes in existing integration tests</name>
  <files>backend/src/main/java/com/workouthub/sessions/SessionsService.java, backend/src/main/java/com/workouthub/sessions/SessionSetsService.java, backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java, backend/src/test/java/com/workouthub/sessions/SessionSetsIntegrationTest.java</files>
  <action>
**`SessionsService.java` throw sites (3 places):**
- L40 in `start`: change `throw new ConflictException("You already have an active session")` to `throw new ConflictException("SESSION_ALREADY_ACTIVE", "You already have an active session")`.
- L76 in `finish`: change `throw new ConflictException("Session already finished")` to `throw new ConflictException("SESSION_ALREADY_FINISHED", "Session already finished")`.
- L92 in `findActiveOwnedOrThrow`: change `throw new ConflictException("Session is finished and cannot be modified")` to `throw new ConflictException("SESSION_FINISHED", "Session is finished and cannot be modified")`. This site fires from POST /sets and PUT /sets/{setId} and DELETE /sets/{setId} when the session is finished - the drainer's primary disambiguator.

**`SessionSetsService.java` throw site (1 place):**
- L77-79 in `add` (the `catch (DataIntegrityViolationException)` block): change to `throw new ConflictException("SET_NUMBER_DUPLICATE", "Set number " + setNumber + " already recorded for this exercise")`.
- DO NOT touch the message text format - the existing message is part of the human-readable contract; clients that already display it must keep working.

**Code-string registry:** Place the four code constants as `private static final String` fields at the top of `SessionsService` (three fields) and `SessionSetsService` (one field). Mirrors the pattern of compile-time string constants used elsewhere in the codebase. Names match the codes exactly:
```java
private static final String CODE_SESSION_ALREADY_ACTIVE = "SESSION_ALREADY_ACTIVE";
private static final String CODE_SESSION_ALREADY_FINISHED = "SESSION_ALREADY_FINISHED";
private static final String CODE_SESSION_FINISHED = "SESSION_FINISHED";
```
And in `SessionSetsService`:
```java
private static final String CODE_SET_NUMBER_DUPLICATE = "SET_NUMBER_DUPLICATE";
```

**`SessionLifecycleIntegrationTest.java` assertions:**
- Locate the existing `startActiveFinishHistoryFullFlow` test (the test contains the two 409 cases at lines ~64-69 and ~84-89).
- After the "Second start while one is active -> 409" expectation (line ~69), append `.andExpect(jsonPath("$.code").value("SESSION_ALREADY_ACTIVE"))`.
- After the "Finishing a finished session is 409" expectation (line ~89), append `.andExpect(jsonPath("$.code").value("SESSION_ALREADY_FINISHED"))`.
- Do NOT split this into a new test method - the existing flow is the natural carrier for these assertions and the test name already covers the lifecycle.

**`SessionSetsIntegrationTest.java` assertions:**
- `duplicateExplicitSetNumberReturns409` (line ~71): on the second POST that returns 409, append `.andExpect(jsonPath("$.code").value("SET_NUMBER_DUPLICATE"))`.
- `differentClientSetIdSameSetNumberReturns409` (line ~234): on the colliding POST, append `.andExpect(jsonPath("$.code").value("SET_NUMBER_DUPLICATE"))`. Cements that the code fires for the genuine V5-unique collision, not the idempotency-key path.
- `updateAfterFinishReturns409` (line ~95): on the PUT that returns 409 because the session is finished, append `.andExpect(jsonPath("$.code").value("SESSION_FINISHED"))`. This proves the `findActiveOwnedOrThrow` path's code reaches the response body for the SET CRUD path (not just lifecycle).
- Do NOT add new test methods for code assertions - extend the existing tests so the code-vs-status pairing is documented in-place.

**Avoid and WHY:**
- Do NOT modify any non-conflict throw site in this task (e.g., NotFoundException calls). Codes are conflict-only for this plan.
- Do NOT extract a `SessionConflictCodes` shared class. The four constants are tightly coupled to two service classes; a separate class would add a new file with no caller benefit. Inline `private static final` strings keep the diff focused.
- Do NOT change any 409 message text. Drainers that fall back to message parsing (until they upgrade to code-based branching) must keep working.
- Do NOT add `$.code` assertions to OTHER existing 409 tests in this file or others (e.g., the auth/ register-conflict tests). Codes have not been added to those throw sites; asserting code presence elsewhere would fail.
- Do NOT make any backend behavioral change beyond carrying the code through. Status codes, response shapes (other than the new optional field), and DB writes are unchanged.
  </action>
  <verify>
- `grep -nE "CODE_SESSION_ALREADY_ACTIVE|CODE_SESSION_ALREADY_FINISHED|CODE_SESSION_FINISHED" backend/src/main/java/com/workouthub/sessions/SessionsService.java` shows the three constants and three throw-site usages.
- `grep -n "CODE_SET_NUMBER_DUPLICATE" backend/src/main/java/com/workouthub/sessions/SessionSetsService.java` shows the constant and the throw-site usage.
- `grep -nE "SESSION_ALREADY_ACTIVE|SESSION_ALREADY_FINISHED" backend/src/test/java/com/workouthub/sessions/SessionLifecycleIntegrationTest.java` shows the two new `$.code` assertions.
- `grep -nE "SET_NUMBER_DUPLICATE|SESSION_FINISHED" backend/src/test/java/com/workouthub/sessions/SessionSetsIntegrationTest.java` shows the three new `$.code` assertions across the three existing 409 tests.
- In CI: `./mvnw -pl backend test -Dtest=SessionLifecycleIntegrationTest,SessionSetsIntegrationTest` passes; no regression in the other tests in either file.
- In CI: full backend `./mvnw -pl backend verify` is green (line coverage stays >= 70%).
  </verify>
  <done>
All four sessions/ 409 paths emit a non-null `$.code` matching the constant defined at the throw site. The five existing 409 tests across `SessionLifecycleIntegrationTest` and `SessionSetsIntegrationTest` now assert the code value in addition to the status. Audit Section 4b PARTIAL closed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document the auto-numbering contract in Javadoc and assert `newPr` is omitted on detail re-read</name>
  <files>backend/src/main/java/com/workouthub/sessions/SessionSetsService.java, backend/src/test/java/com/workouthub/sessions/SessionSetsIntegrationTest.java</files>
  <action>
**`SessionSetsService.java` Javadoc on `add`:**
- Add a method-level Javadoc block above the existing `public AddSetResult add(UUID userId, UUID sessionId, AddSetRequest req)` declaration (around line 42). The Javadoc must be concise and precise - this is the binding written-down contract referenced by audit Section 4c FAIL closure.
- Content (English; no emojis):
  ```java
  /**
   * Adds a set to an active session.
   *
   * <p>Auto-numbering contract: when {@code req.setNumber()} is null, the server
   * assigns {@code countBySessionIdAndExerciseId + 1}. This is safe ONLY for the
   * live-online "next set" UX where a single client adds sets sequentially.
   *
   * <p>Offline-queue drainers MUST send an explicit {@code setNumber} on every
   * POST. Out-of-order drain (e.g. sets {1, 3, 2}) without explicit numbers
   * corrupts row order because each implicit insert sees a different in-flight
   * count. The integration test
   * {@code SessionSetsIntegrationTest.outOfOrderDrainPreservesSetNumberOrdering}
   * proves the explicit-number path tolerates out-of-order drain.
   *
   * <p>Idempotency contract: when {@code req.clientSetId()} is non-null and a
   * row already exists for {@code (sessionId, clientSetId)}, the persisted row
   * is returned with {@code idempotentHit=true}. The PR detector and
   * achievement evaluator are not re-fired on a replay.
   */
  ```
- Do NOT change any executable code in this method. The fallback at lines 55-57 stays exactly as-is. The Javadoc IS the verdict.

**`SessionSetsIntegrationTest.java` C4 documentation test:**
- Add a new test method `newPrFlagPresentOnCreateButOmittedOnDetailReread` after the existing tests but before the `setBody` private helpers. Reuses the `setUp` fixture (fresh user, fresh session, fresh exercise) so `bestPriorOneRm` returns null on the first POST and `PrDetector.beatsPriorBest(null, weight, reps)` returns true (a fresh user's first completed set is by definition a PR).
- Steps:
  1. POST `setBody(1, 10, 60.0, null)` -> assert 201 AND `jsonPath("$.newPr").value(true)`. (Confirms the create response carries `newPr=true` on a PR-setting set; this is the live behavior shipped in the Phase 16 leak.)
  2. GET `/api/sessions/{sessionId}` -> assert 200 AND `jsonPath("$.sets.length()").value(1)` AND `jsonPath("$.sets[0].newPr").doesNotExist()`. (Confirms `SessionsMapper.toDto` calls `toSetDto(x)` without the newPr arg, so on detail re-read the field is null and Jackson NON_NULL omits it.)
- Test name MUST start with `newPr` so it sorts naturally next to other newPr-related tests should they be added in Phase 16.
- DO NOT assert anything else about the set body in this test (e.g., `repsDone`, `weightKg`). The audit's C4 is scoped to the `newPr` field's create-vs-reread behavior; asserting other fields would dilute the regression intent.

**Avoid and WHY:**
- Do NOT modify `SessionsMapper.toDto`, `toSetDto`, or `PrDetector` in this task. Phase 16 leak guard. The C4 test documents existing behavior; it is not a feature change.
- Do NOT remove or modify the auto-numbering fallback at `SessionSetsService.java:55-57`. The CF2 verdict in this plan is documented continuation, not removal. Removing it would force every live-online UI caller to track a counter, breaking the session-execution UX.
- Do NOT add a 400-on-missing-setNumber path. That was the rejected Option A from the audit; the verdict is keep + document.
- Do NOT seed historical exercise sessions to set up the PR scenario. A fresh user's first POST already triggers `newPr=true` because `bestPriorOneRm` returns null and `PrDetector.beatsPriorBest(null, w, r)` returns true when oneRm is non-null. Extra seeding would couple the test to history-query implementation.
  </action>
  <verify>
- `grep -nE "Auto-numbering contract|Offline-queue drainers MUST" backend/src/main/java/com/workouthub/sessions/SessionSetsService.java` shows the Javadoc block.
- `grep -n "newPrFlagPresentOnCreateButOmittedOnDetailReread" backend/src/test/java/com/workouthub/sessions/SessionSetsIntegrationTest.java` shows the new test method.
- `grep -n "doesNotExist" backend/src/test/java/com/workouthub/sessions/SessionSetsIntegrationTest.java` shows at least the existing newPr assertion (in `sameClientSetIdRetryReturns200WithSameId`) and the new one (in `newPrFlagPresentOnCreateButOmittedOnDetailReread`).
- In CI: `./mvnw -pl backend test -Dtest=SessionSetsIntegrationTest` passes; the eight pre-existing tests in the file (four legacy + four added in 15-02) plus the one new test from this task all pass.
- Manually inspect the Javadoc rendering style in an IDE or via `./mvnw -pl backend javadoc:javadoc` (best-effort - not strictly required if CI does not run javadoc).
  </verify>
  <done>
`SessionSetsService.add` has a Javadoc block stating the auto-numbering contract (live-online safe; offline drainers MUST send explicit setNumber) and the idempotency contract (clientSetId replay does not re-fire side effects). `SessionSetsIntegrationTest` has a `newPrFlagPresentOnCreateButOmittedOnDetailReread` test that asserts `$.newPr` is true on the create response and absent on the detail re-read. No code behavior changes. Audit Section 4c FAIL verdict documented; audit Section 6 C4 closed.
  </done>
</task>

</tasks>

<verification>
Before declaring plan 15-04 complete:
- [ ] `./mvnw -pl backend verify` succeeds in CI (line coverage stays >= 70%)
- [ ] `ApiError` has an optional trailing `String code` component; existing JSON responses with `code = null` omit the field (Jackson NON_NULL)
- [ ] `ConflictException` accepts an optional code via a new two-arg constructor; the single-arg constructor still works (no other package broken)
- [ ] `GlobalExceptionHandler.handleConflict` propagates the code into the 409 body; all other handlers pass `null` for the code argument
- [ ] `GlobalExceptionHandlerTest` asserts both null-code (back-compat) and explicit-code (new contract) outcomes
- [ ] All four sessions/ 409 paths emit a non-null `$.code`: `SESSION_ALREADY_ACTIVE`, `SESSION_ALREADY_FINISHED`, `SESSION_FINISHED`, `SET_NUMBER_DUPLICATE`
- [ ] Five existing 409 integration-test assertions across `SessionLifecycleIntegrationTest` and `SessionSetsIntegrationTest` now also assert `$.code`
- [ ] `SessionSetsService.add` carries a Javadoc block describing the auto-numbering contract and the idempotency contract; no executable code change in this method
- [ ] `SessionSetsIntegrationTest.newPrFlagPresentOnCreateButOmittedOnDetailReread` asserts `$.newPr` is true on create and absent on detail re-read
- [ ] No modifications to `PrDetector`, `ExerciseAnalyticsController`, `ExerciseAnalyticsService`, `LastPerformanceDto`, `ProgressPointDto`, or `SessionSetRepository.findHistoricalByUserAndExercise` (Phase 16 leak guard from audit Section 5)
- [ ] No modifications to any Flyway migration file (immutability rule)
- [ ] `git status` shows exactly the planned files: 3 modified common/web sources, 2 modified sessions sources, 1 modified common test, 2 modified sessions tests = 8 files modified, 0 created (other than the SUMMARY.md after completion)
</verification>

<success_criteria>
- All three tasks complete with one atomic commit each (`feat(api):` for Task 1, `feat(api):` for Task 2, `docs(api):` and/or `test(api):` for Task 3 - choose per audit/Phase 14 conventions; if Task 3 produces both Javadoc and test in one commit, `docs(sessions):` is acceptable since the Javadoc is the load-bearing artifact and the test asserts the documented contract)
- The IndexedDB drainer's branch table (post-15-04) is unambiguous:
  - 200 = persisted (replay or update path) -> mark queued row synced
  - 201 = newly created -> mark queued row synced + capture server `id`
  - 400 = client validation error (setNumber out of range, etc.) -> bubble to UI
  - 404 = exerciseId or sessionId unknown -> bubble to UI
  - 409 + code `SESSION_FINISHED` -> drop the queued row (session is closed)
  - 409 + code `SET_NUMBER_DUPLICATE` -> bubble to UI as a renumbering bug (NOT silently swallow)
  - 409 + code `SESSION_ALREADY_ACTIVE` (start path) -> drop the queued start, fetch /sessions/active and continue
  - 409 + code `SESSION_ALREADY_FINISHED` (finish path) -> drop the queued finish, refresh state
- Audit Section 4b PARTIAL verdict is closed.
- Audit Section 4c FAIL verdict is documented (no behavior change; Javadoc is the binding contract; explicit-number path is already proven safe by 15-02's `outOfOrderDrainPreservesSetNumberOrdering`).
- Audit Section 6 C4 coverage gap is closed.
- Phase 15 is complete (all four plans shipped: 15-01 audit, 15-02 idempotency key, 15-03 heart-rate exposure, 15-04 typed codes + auto-numbering verdict).
</success_criteria>

<output>
After completion, create `.planning/phases/15-sessions-core/15-04-SUMMARY.md` with frontmatter (phase, plan, subsystem, tags, requires, provides, affects, tech-stack, key-files, key-decisions, duration, completed) matching the 15-02-SUMMARY.md / 15-03-SUMMARY.md format, and a body covering:

# Phase 15 Plan 04: typed 409 codes + auto-numbering verdict Summary

**[Substantive one-liner: e.g. "Typed `code` field on `ApiError`, four sessions/ 409 throw sites carry codes, auto-numbering contract documented in Javadoc, `newPr` re-read documentation test landed."]**

## Performance
- Duration, started, completed, tasks (3), files modified count

## Accomplishments
- ApiError gains optional `code` component; ConflictException accepts an optional code via a two-arg constructor; GlobalExceptionHandler propagates code into the 409 body
- Four sessions/ 409 paths now emit machine-readable codes: SESSION_ALREADY_ACTIVE, SESSION_ALREADY_FINISHED, SESSION_FINISHED, SET_NUMBER_DUPLICATE
- Five existing 409 integration-test assertions extended with `$.code` value checks
- SessionSetsService.add Javadoc documents the auto-numbering contract and idempotency contract
- newPrFlagPresentOnCreateButOmittedOnDetailReread test documents the create-response-only PR flag behavior
- Audit Section 4b PARTIAL closed; 4c FAIL documented; Section 6 C4 closed
- Phase 15 complete (4/4 plans shipped)

## Task Commits
1. Task 1: ApiError + ConflictException + GlobalExceptionHandler infra + GlobalExceptionHandlerTest - <sha>
2. Task 2: Sessions/ throw sites + lifecycle/sets integration tests `$.code` assertions - <sha>
3. Task 3: SessionSetsService.add Javadoc + newPr re-read documentation test - <sha>

## Files Created/Modified
- (list with one-line descriptions; expected 8 files modified, 0 created besides SUMMARY.md)

## Decisions Made
- **Optional vs required `code` on ApiError:** Optional. Required would cascade an edit through every existing 4xx/5xx producer; this plan is conflict-only by design.
- **String constants vs enum for codes:** Constants. An enum would force a central registry every feature must import; per-throw-site `private static final String` keeps adoption local.
- **NotFoundException codes:** Out of scope. The audit's typed-code requirement is conflict-only (drainer disambiguation). Adding NotFoundException codes would extend scope without a concrete consumer.
- **Auto-numbering fallback (CF2 verdict):** Keep + document. Removing the fallback would break the live-online "next set" UX. The offline path is already proven safe with explicit numbers (15-02's outOfOrderDrainPreservesSetNumberOrdering).
- **C4 test seeding strategy:** Fresh user, no historical session. PrDetector.beatsPriorBest(null, w, r) returns true on the first completed set; extra seeding would couple the test to history-query internals.

## Issues Encountered
- (or "None")

## Next Phase Readiness
- Phase 15 complete; v0.4 ready to advance to Phase 16 (sessions-analytics) per ROADMAP.md.
- Phase 19 (api-contract-docs) inherits the OpenAPI doc note for the new ApiError.code field and the four sessions code values.
- Other features (auth/, users/, workouts/, etc.) can adopt typed conflict codes incrementally without further infrastructure work.
</output>
