---
phase: 17-body-metrics
plan: 02
type: execute
domain: backend
---

<objective>
Append optional `photoUrl` as the 9th record component on `UpsertBodyMetricRequest` and thread `req.photoUrl()` into `MetricsService.upsert` so the user-facing `POST /api/metrics` can set the column that already exists in V6, the entity, and the response DTO. Cover the round-trip with a new integration test.

Purpose: Closes the only column-level GAP across ProjectBrief / V6 / `BodyMetric` entity / `BodyMetricDto` response / `UpsertBodyMetricRequest` (`.planning/phases/17-body-metrics/17-01-AUDIT.md` Section 3 Part A row `photo_url`). Realizes ROADMAP Phase 17 deliverable "optional progress photo URL" on the API surface (`.planning/ROADMAP.md:78`). Today the only path that writes `BodyMetric.photoUrl` is `FullImportService.replaceMetrics` at `FullImportService.java:172`; the user-facing path cannot. Photo upload mechanics (multipart vs MinIO) stay deferred to Phase 28; this plan only opens the URL string field.

Output:
- `UpsertBodyMetricRequest` gains `String photoUrl` as the 9th and final component, validated `@Size(max = 500)` to mirror V6's `VARCHAR(500)` (`V6__body_metrics_and_supplements.sql:17`).
- `MetricsService.upsert` writes the field via `entity.setPhotoUrl(req.photoUrl())` between the existing setter block at `MetricsService.java:39-45`.
- `MetricsIntegrationTest` gains `postWithPhotoUrlIsRoundTripped` asserting POST with `photoUrl` then GET returns the same value.
- No migration; V6 already defines `photo_url VARCHAR(500)`.
- No new endpoint, no controller signature change, no `BodyMetricDto` change (response already carries `photoUrl` at `BodyMetricDto.java:17`).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-body-metrics/17-01-AUDIT.md
@.planning/phases/17-body-metrics/17-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@backend/src/main/java/com/workouthub/metrics/dto/UpsertBodyMetricRequest.java
@backend/src/main/java/com/workouthub/metrics/dto/BodyMetricDto.java
@backend/src/main/java/com/workouthub/metrics/MetricsService.java
@backend/src/main/java/com/workouthub/metrics/MetricsController.java
@backend/src/main/java/com/workouthub/metrics/domain/BodyMetric.java
@backend/src/main/resources/db/migration/V6__body_metrics_and_supplements.sql
@backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java
@backend/src/main/java/com/workouthub/exports/FullImportService.java

**Tech stack available (carried from Phase 16):** Java 21 records, Spring Boot 3.5.x, Spring Web, Spring Data JPA, Bean Validation via `jakarta.validation`, Hibernate `@UuidGenerator`, Jackson with `serializationInclusion = NON_NULL` (per `JacksonConfig.java:18`), Flyway (last applied: V27 from Phase 16-04), JUnit 5 + Testcontainers Postgres, MockMvc + JSONPath. No new dependencies.

**Established patterns (relevant here):**
- Feature-sliced DTO records under `<feature>/dto/`; `jakarta.validation` annotations on request DTOs (CONVENTIONS.md). `UpsertBodyMetricRequest` already carries `@NotNull`, `@DecimalMin/@DecimalMax`, `@Size`.
- Optional trailing record component for back-compat additions (Phase 15-04 `ApiError.code`, Phase 16-04 `SessionSetDto.newPr`); Jackson `NON_NULL` strips when null. Same shape applies here.
- `MetricsService.upsert` pattern (`MetricsService.java:30-47`): hand-written setter block writes every column unconditionally; new field plugs into the same block in declaration order.
- Test seeding via `TestAuthHelpers.seed` + `AbstractIntegrationTest` (real Postgres) + `@AutoConfigureMockMvc` + JSONPath assertions (TESTING.md). Existing tests use unique `nanoTime()`-suffixed emails to avoid cross-test bleed.
- Phase 16-04 import precedent: `FullImportService.replaceMetrics` already writes `m.setPhotoUrl(r.photoUrl())` at `FullImportService.java:172`; the user-facing path is the lone gap and the round-trip pattern is identical.

**Constraining decisions (sourced from STATE.md and 17-01-AUDIT):**
- 17-01-AUDIT Section 3 Part A: `photo_url` is the only column where `UpsertBodyMetricRequest` drifts from V6/entity/response DTO. Schema, entity, and response DTO already carry it - this plan adds the request side and threads it through.
- 17-01-AUDIT Section 6 photo-url-exposure bucket: 3 file touches (1 DTO append, 1 service line, 1 test). No migration. `@Size(max = 500)` mirrors V6 `VARCHAR(500)`.
- 17-01-AUDIT defer bucket: storage strategy (multipart, MinIO, signed URLs) is out of scope; this plan stores the URL string only. Phase 28 (metrics-ui) or a dedicated upload-pipeline plan owns the upload mechanics.
- Phase 14 saveAndFlush lesson: `BodyMetric` has no parent-child cascade; lesson does not apply.
- CLAUDE.md standing rules: English-only code/comments/identifiers, no emojis, conventional commit subjects, no `Co-Authored-By` trailers.
- Local Maven gap (memory: local_maven_gap): backend tests run only in CI on this Windows host; mvn verify gate is delegated to CI.

**Issues being addressed:** None from `.planning/ISSUES.md`. The gap is tracked entirely via 17-01-AUDIT Section 3 Part A and Section 6 photo-url-exposure bucket.

**Out of scope for this plan:**
- Time-series range read on `GET /api/metrics` (Plan 17-03).
- 200/201 status-code split on `POST /api/metrics` (Plan 17-04).
- Photo upload mechanics (multipart, MinIO, signed URLs) - deferred to Phase 28.
- Adding `BodyMetricValidator` static helper - deferred (17-04 absorb-if-slot or Phase 31).
- Touching `BodyMetricDto`, `BodyMetric` entity, V6 migration, or any other Flyway file - response DTO and schema already carry `photoUrl`.
- Touching `webhooks/`, `health/`, `exports/`, `notifications/` packages - out of scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append photoUrl to UpsertBodyMetricRequest and thread into MetricsService.upsert</name>
  <files>backend/src/main/java/com/workouthub/metrics/dto/UpsertBodyMetricRequest.java, backend/src/main/java/com/workouthub/metrics/MetricsService.java</files>
  <action>
1. Edit `UpsertBodyMetricRequest.java` to append `String photoUrl` as the 9th and final record component. The existing 8 components stay in declared order with their existing validation annotations. Annotate the new component `@Size(max = 500)` (mirrors V6 `photo_url VARCHAR(500)` at `V6__body_metrics_and_supplements.sql:17`). No `@NotNull`, no `@URL` - the field is optional and the audit explicitly does not prescribe URL syntax (storage strategy is deferred). Reuse the existing `import jakarta.validation.constraints.Size;` already present on line 6.

   Final record header order: `(recordedDate, weightKg, bodyFatPercent, waistCm, chestCm, armCm, thighCm, notes, photoUrl)`. Note `notes` stays at position 8 (its existing slot) and `photoUrl` is appended at position 9 - JSON shape is name-keyed so existing clients are unaffected.

2. Edit `MetricsService.upsert` (`MetricsService.java:30-47`): insert exactly one line `entity.setPhotoUrl(req.photoUrl());` into the existing setter block. Place it between `entity.setThighCm(req.thighCm());` (current line 44) and `entity.setNotes(req.notes());` (current line 45) so the setter order matches the V6 column order (`thigh_cm`, `photo_url`, `notes`). The `BodyMetric.setPhotoUrl(String)` setter already exists at `BodyMetric.java:97` - no entity change needed. Keep the `repo.save(entity)` and the private `toDto` mapper untouched.

3. Do NOT add a Javadoc block to either file: existing record/method has none and CONVENTIONS.md says "inline comments only for why, not what" - the `@Size(max = 500)` annotation explains itself.

What to AVOID and WHY:
- Do NOT change the position of any existing record component: clients read by name, but record-canonical-constructor positional invocations (none in this codebase, but `MetricsService` calls `req.weightKg()` etc. accessor-style and is unaffected) and downstream consumers stay safe only when existing components keep their slots. Append at the end.
- Do NOT add `@NotNull`, `@URL`, or any pattern check on `photoUrl`: ProjectBrief calls it "opsiyonel" and the audit defer bucket explicitly leaves URL syntax to Phase 28. Over-validating now constrains the upload-pipeline plan.
- Do NOT widen the `@Size` past 500: V6 is `VARCHAR(500)`. `ddl-auto: validate` would not catch a longer Java-side cap, but the SQL would silently truncate or reject. Mirror the column width exactly.
- Do NOT touch `BodyMetricDto`: it already carries `photoUrl` at `BodyMetricDto.java:17`, and `MetricsService.toDto` already reads `m.getPhotoUrl()` at `MetricsService.java:65`. The response side is complete.
- Do NOT touch the V6 migration or add a new V<n> file: the column already exists. ROADMAP Phase 17 has no schema work because every column is in V6.
- Do NOT touch `ScaleWebhookController`, `HealthImportService`, or `FullImportService`: each writer has its own `setPhotoUrl` discipline (the export round-trip path at `FullImportService.java:172` already writes the field). Cross-package validator extraction is the 17-04 absorb-if-slot bucket, not this plan.
- Do NOT inline the setter into a fluent chain: the existing setter block uses statement-per-line style; the new line follows the same pattern.
  </action>
  <verify>
- `git diff -- backend/src/main/java/com/workouthub/metrics/dto/UpsertBodyMetricRequest.java` shows: existing line 17 (`thighCm,`) suffix change to add `,` after `notes`, and one new line `@Size(max = 500) String photoUrl` as the 9th component. No other edits, no import changes.
- `git diff -- backend/src/main/java/com/workouthub/metrics/MetricsService.java` shows exactly one new line `entity.setPhotoUrl(req.photoUrl());` between `setThighCm` and `setNotes`. No method signature change. No import change.
- `rg "photoUrl" backend/src/main/java/com/workouthub/metrics` returns 5 hits: `BodyMetric.java` (3: field + getter + setter), `dto/BodyMetricDto.java` (1: response component), `dto/UpsertBodyMetricRequest.java` (1: new request component), `MetricsService.java` (2: setter call + toDto getter call).
- `rg "new UpsertBodyMetricRequest" backend/src` returns 0 hits (the record is constructed only by Jackson during request deserialization; no Java-side constructor call exists today and this plan does not add one).
- mvn compile/test gate delegated to CI per local-maven-gap memory; record any local mvn output if available, otherwise note "delegated to CI" in the summary.
  </verify>
  <done>
`UpsertBodyMetricRequest` has 9 components ending with `@Size(max = 500) String photoUrl`. `MetricsService.upsert` writes `entity.setPhotoUrl(req.photoUrl())` between `setThighCm` and `setNotes`. No other source files touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add round-trip integration test for photoUrl</name>
  <files>backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java</files>
  <action>
1. Add a new `@Test` method `postWithPhotoUrlIsRoundTripped` after `crossUserListIsIsolated` (currently the last test, ending at line 129). The test must:
   - Seed a user via `helpers.seed("photo-" + System.nanoTime() + "@test.local", SECRET, Role.USER)` and capture `auth = "Bearer " + user.accessToken()` (matches the existing `postStoresAndGetReturnsTheEntry` shape at lines 38-41).
   - POST `/api/metrics` with body `{"recordedDate":"2026-04-25","weightKg":78.0,"photoUrl":"https://example.test/photos/abc.jpg"}`. Assert `status().isCreated()` and `jsonPath("$.photoUrl").value("https://example.test/photos/abc.jpg")` to lock the response carries the value (the response DTO already had `photoUrl` pre-plan, so this also asserts the controller is now wired end-to-end).
   - GET `/api/metrics` with the same `Authorization` header. Assert `status().isOk()`, `jsonPath("$.length()").value(1)`, and `jsonPath("$[0].photoUrl").value("https://example.test/photos/abc.jpg")` to lock the round-trip through the V6 column.

2. Do not touch any existing test method or helper. Reuse the existing `SECRET`, `mvc`, `helpers`, `objectMapper` injection. The new test follows the same shape as `postStoresAndGetReturnsTheEntry` and stays under ~22 lines.

What to AVOID and WHY:
- Do NOT add a `@BeforeEach` shared seed: existing tests are intentionally self-contained (`nanoTime()`-suffixed emails per test) per TESTING.md. Adding shared setup couples tests and risks ordering bleed.
- Do NOT assert via `objectMapper.readTree` if a JSONPath assertion suffices: keep diff small. JSONPath is the existing pattern in 5 of 6 tests in this file.
- Do NOT add a separate "photoUrl too long" 400 test in this plan: `@Size(max = 500)` is standard Spring validation and the existing `invalidWeightRangeReturns400` already proves `@Valid` is wired on the controller. A field-specific size test inflates scope past the audit's "1 new test" budget for this bucket. If a regression motivates one later, Plan 17-04 (status-code-split) can absorb it.
- Do NOT use a real-looking external host in the URL: `https://example.test/...` is RFC-2606-style and signals to readers it is a placeholder. Avoid `http://localhost:...` (would imply a fixture URL needs to resolve).
- Do NOT assert on `$.id`, `$.createdAt`, `$.updatedAt`: those are server-assigned and out of scope for the photo-url-exposure verification.
- Do NOT split the test into POST-then-GET-as-two-tests: the round-trip is the assertion; splitting hides whether the value survived persistence vs whether the controller echoed it on create.
  </action>
  <verify>
- `git diff -- backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java` shows exactly one new `@Test` method `postWithPhotoUrlIsRoundTripped` appended after `crossUserListIsIsolated`. No edits to other tests.
- `rg "photoUrl" backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java` returns 3 hits (POST body string, POST response assertion, GET response assertion).
- Test method count in the file grows from 6 to 7 (`rg "@Test" backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java | wc -l` returns 7 - run via `Grep` with `count` mode if `wc` is unavailable).
- mvn test gate delegated to CI per local-maven-gap memory. If mvn becomes available, run `./mvnw -q test -Dtest=MetricsIntegrationTest` and capture the result; otherwise the summary notes "delegated to CI".
  </verify>
  <done>
`postWithPhotoUrlIsRoundTripped` covers POST -> GET round-trip for the new `photoUrl` field. Test method count: 7. No other tests changed; no helpers added.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git status --short` shows exactly 3 modified files: `UpsertBodyMetricRequest.java`, `MetricsService.java`, `MetricsIntegrationTest.java`. No new files created. No migrations created.
- [ ] `rg "photoUrl" backend/src/main/java/com/workouthub/metrics` returns 5 hits across 4 files (entity field/getter/setter, response DTO, request DTO, service setter call + toDto getter call).
- [ ] `rg "@Size\(max = 500\)" backend/src/main/java/com/workouthub/metrics/dto/UpsertBodyMetricRequest.java` returns 1 hit on the new `photoUrl` component.
- [ ] `rg "setPhotoUrl" backend/src/main/java/com/workouthub/metrics/MetricsService.java` returns 1 hit on the new line in `upsert`.
- [ ] No edits outside the metrics package and its integration test class.
- [ ] No edits to protected files (`pom.xml`, lockfiles, `compose.yml`, Dockerfiles, `.github/workflows/`, `scripts/`).
- [ ] No edits to V6 or any other Flyway migration; `ls backend/src/main/resources/db/migration | tail -3` shows the same V25/V26/V27 head as before.
- [ ] mvn verify delegated to CI; commit message references the deferred local gate.
- [ ] Phase 17 plan-count: 4 plans, this is plan 02/04. Plans 17-03 (time-series-range) and 17-04 (status-code-split) remain.
</verification>

<success_criteria>
- ROADMAP Phase 17 deliverable "optional progress photo URL" closed on the API surface: `POST /api/metrics` accepts `photoUrl`, `GET /api/metrics` returns it.
- 17-01-AUDIT Section 3 Part A `photo_url` GAP row resolves to `OK` across all 5 sources (ProjectBrief / V6 / entity / response DTO / upsert request).
- Existing 8 `UpsertBodyMetricRequest` components unchanged in name, type, validation, and order.
- Existing 12 `BodyMetricDto` components untouched.
- Existing 6 `MetricsIntegrationTest` methods untouched; test count grows by exactly 1.
- No new endpoint, no controller signature change, no migration, no entity change, no cross-package edit.
- Photo upload mechanics remain deferred (no multipart, no MinIO, no signed URLs introduced).
</success_criteria>

<output>
After completion, create `.planning/phases/17-body-metrics/17-02-SUMMARY.md`:

```yaml
---
phase: 17-body-metrics
plan: 02
subsystem: metrics
tags: [java, spring-boot, dto, validation, body-metrics, contract-drift]

requires:
  - phase: 17-body-metrics
    plan: 01
    reason: 17-01-AUDIT Section 3 Part A photo_url GAP row + Section 6 photo-url-exposure bucket prescribed the exact 3 file touches

provides:
  - photoUrl on UpsertBodyMetricRequest exposed end-to-end (POST request -> entity setter -> V6 column -> response DTO already wired)
  - round-trip integration test asserting POST + GET carry the URL string

affects:
  - 17-03 time-series-range (independent; no shared lines)
  - 17-04 status-code-split (independent; no shared lines)
  - 18-export-refinement (FullExportService.MetricRow.photoUrl alignment - already in sync, but verify on Phase 18 entry)
  - 28-metrics-ui (frontend can now write photoUrl; upload-pipeline plan still owns multipart/MinIO/signed-URL choice)

tech-stack:
  added: []
  patterns:
    - "Optional trailing record component with @Size mirroring SQL column width (V6 VARCHAR(500) -> @Size(max = 500))"

key-files:
  modified:
    - backend/src/main/java/com/workouthub/metrics/dto/UpsertBodyMetricRequest.java
    - backend/src/main/java/com/workouthub/metrics/MetricsService.java
    - backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java

key-decisions:
  - "Append-only DTO change: existing 8 components keep position, new 9th component is String photoUrl"
  - "@Size(max = 500) mirrors V6 VARCHAR(500); no @NotNull, no @URL syntax check (deferred to Phase 28 upload-pipeline plan)"
  - "No new endpoint, no migration: V6 + entity + response DTO already carry photoUrl"
  - "Single round-trip integration test; field-specific size-violation test deferred (existing invalidWeightRangeReturns400 already proves @Valid is wired)"

patterns-established:
  - "Pattern: opening a request DTO field that the response DTO and SQL column already carry is a 3-touch change (DTO append + service setter line + round-trip test)"

issues-created: []

duration: [fill at execution]
completed: [fill at execution]
---
```

Body sections:

# Phase 17 Plan 02: Photo-URL Exposure Summary

**[Substantive one-liner - what shipped, e.g., "Appended optional `photoUrl` to `UpsertBodyMetricRequest` (`@Size(max = 500)`); threaded into `MetricsService.upsert`; round-trip integration test locks POST + GET behavior."]**

## Accomplishments

- `UpsertBodyMetricRequest` extended to 9 components with optional `photoUrl`.
- `MetricsService.upsert` writes `setPhotoUrl(req.photoUrl())` between `setThighCm` and `setNotes`.
- `MetricsIntegrationTest.postWithPhotoUrlIsRoundTripped` asserts POST + GET carry the URL string.

## Files Created/Modified

- `backend/src/main/java/com/workouthub/metrics/dto/UpsertBodyMetricRequest.java` - appended `@Size(max = 500) String photoUrl`.
- `backend/src/main/java/com/workouthub/metrics/MetricsService.java` - one new setter line in `upsert`.
- `backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java` - one new round-trip test.

## Decisions Made

- Append-only record change preserves the existing 8 components.
- `@Size(max = 500)` mirrors V6's `VARCHAR(500)`.
- No URL syntax validation (deferred to Phase 28 upload-pipeline plan).
- Single round-trip test; size-violation test deferred (existing `invalidWeightRangeReturns400` proves `@Valid` is wired).

## Issues Encountered

[Fill at execution time, or "None - mechanical change per audit photo-url-exposure bucket."]

## Deviations from Plan

[Fill at execution time.]

## Next Step

Plan 17-03 (time-series-range): extend `GET /api/metrics` with optional `from`/`to` `LocalDate` query params; add `findByUserIdAndRecordedDateBetweenOrderByRecordedDateDesc` repo method; 2 new integration tests (range hit, empty range).
</output>
