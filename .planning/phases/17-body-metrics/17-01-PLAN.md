---
phase: 17-body-metrics
plan: 01
type: execute
domain: backend
---

<objective>
Inventory the `backend/src/main/java/com/workouthub/metrics/` package, map it against ProjectBrief Phase 5 page #7 (Vücut Metrikleri) and ROADMAP Phase 17 deliverables (weight, body measurements, optional progress photo URL, time-series read endpoints for the metrics UI), audit the cross-package consumers that write `BodyMetric` directly (scale webhook, health import, full import) bypassing `MetricsService`, and recommend a concrete Phase 17 plan-02+ hardening scope with explicit plan boundaries.

Purpose: The `metrics/` package is already substantially landed (6 source files, ~150 lines; V6 migration with all schema columns; 6 integration tests green). Phase 17 cannot plan "what to land" because the bulk is landed; it must plan "what to expose (photoUrl on the upsert request), what to add (time-series range filter for the chart UI), what status-code semantics to fix (POST returns 201 even on date-collision update), and which cross-package writers must adopt the same validation contract (or stay direct)." This audit produces the single source of truth for those decisions.

Output: `.planning/phases/17-body-metrics/17-01-AUDIT.md` containing six sections (metrics package inventory, endpoint + DTO catalog, ProjectBrief Phase 5 + ROADMAP Phase 17 gap matrix, behavioral analysis (POST status semantics + upsert-by-date), cross-package coupling map + verdicts, recommended Phase 17 plan-02+ scope). No Java source files, migrations, or tests are modified.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/ISSUES.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/13-workouts-audit/13-01-AUDIT.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/phases/16-sessions-analytics/16-01-AUDIT.md
@.planning/phases/16-sessions-analytics/16-04-SUMMARY.md
@ProjectBrief.md
@backend/src/main/java/com/workouthub/metrics/MetricsController.java
@backend/src/main/java/com/workouthub/metrics/MetricsService.java
@backend/src/main/java/com/workouthub/metrics/dto/BodyMetricDto.java
@backend/src/main/java/com/workouthub/metrics/dto/UpsertBodyMetricRequest.java
@backend/src/main/java/com/workouthub/metrics/domain/BodyMetric.java
@backend/src/main/java/com/workouthub/metrics/domain/BodyMetricRepository.java
@backend/src/main/resources/db/migration/V6__body_metrics_and_supplements.sql
@backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java
@backend/src/main/java/com/workouthub/webhooks/ScaleWebhookController.java
@backend/src/main/java/com/workouthub/health/HealthImportService.java
@backend/src/main/java/com/workouthub/exports/FullExportService.java
@backend/src/main/java/com/workouthub/exports/FullImportService.java
@backend/src/main/java/com/workouthub/notifications/ReminderJob.java

**Tech stack available (carried from Phase 16):** Java 21, Spring Boot 3.5.x, Spring Web, Spring Data JPA, Hibernate `@UuidGenerator`, Jackson with `serializationInclusion = NON_NULL` (per `JacksonConfig.java:18`), Flyway migrations (last applied: V27 `session_sets.is_pr` per Phase 16-04), JUnit 5 + Testcontainers Postgres, typed `ApiError.code` envelope from Phase 15-04, Bean Validation via `jakarta.validation` on request DTOs.

**Established patterns (carried from Phase 16):**
- Feature-sliced packages (`metrics/` already conforms: `Controller`, `Service`, `domain/{Entity, Repository}`, `dto/{Request, Response}`).
- Controller -> Service -> Repository, transactions on services, DTOs at HTTP boundary.
- Validation via `jakarta.validation` annotations on request DTOs (`@DecimalMin`, `@DecimalMax`, `@NotNull`, `@Size`).
- Hand-written mappers (in this package: a private `static toDto` inside `MetricsService.java:55-69`, no separate `MetricsMapper` class; a candidate for extraction depending on Section 6 verdict).
- Optional trailing record components for back-compat (`ApiError.code`, `SessionSetDto.newPr`); Jackson NON_NULL omits when null. Same pattern applies to any new optional field on `BodyMetricDto`.
- Phase 14 lesson: explicit child-repo `saveAndFlush(child)` after `parent.addChild()` to materialize `@UuidGenerator` ids; metrics package has NO parent-child cascade so this lesson does not apply directly, but the audit must confirm.
- Phase 15-02 status-code split (200 = idempotent replay, 201 = newly created) precedent for fixing the POST-always-201 drift on `/api/metrics`.

**Constraining decisions (sourced from STATE.md + prior summaries):**
- ROADMAP Phase 17 description (`.planning/ROADMAP.md:76-83`): "Complete the metrics/ package: weight, body measurements, optional progress photo URL, and time-series read endpoints for the metrics UI." Plan-count flagged as TBD in roadmap; this audit decides.
- ProjectBrief Phase 5 page #7 (Vucut Metrikleri, lines 360-363): "Kilo grafigi (haftalik/aylik/tum zamanlar)" + "Olcum ekleme formu" + "Progress fotografi yukleme (opsiyonel)". The chart-window phrasing implies a time-series read with date-range or window scope.
- ProjectBrief schema (lines 177-186): `body_metrics` has 10 user-facing columns (`recorded_date`, `weight_kg`, `body_fat_percent`, `waist_cm`, `chest_cm`, `arm_cm`, `thigh_cm`, `photo_url`, `notes`). Audit must do a column-by-column drift check between V6 SQL, `BodyMetric` entity, `BodyMetricDto` response, and `UpsertBodyMetricRequest`.
- `ddl-auto: validate` - schema cannot drift; any new column requires a new `V<n>__*.sql` (V28 is the next free slot; V27 was consumed by Phase 16-04 `session_sets.is_pr`).
- Phase 15-02 `clientSetId` precedent: when a POST has both create and replay semantics, return 201 on create / 200 on replay (or update). Audit applies the same rule to `MetricsController.upsert` (`MetricsController.java:35-41`) which currently returns 201 unconditionally even when the row existed.
- Cross-package writers exist outside `MetricsService`: `ScaleWebhookController.ingest` (`ScaleWebhookController.java:35-60`), `HealthImportService.apply` (`HealthImportService.java:80-91`), `FullImportService.replaceMetrics` (`FullImportService.java:155-178`). The audit must catalog them and decide whether each should adopt `MetricsService.upsert` or stay direct (each has different semantics: idempotent-by-date for scale, batched-import for health/export).

**Issues being addressed:**
- None directly from `.planning/ISSUES.md`. Open issues i-3..i-9 belong to other phases per the issue-to-phase mapping in STATE.md (i-4 v0.6, i-5/i-6/i-8 v1.0, i-7 v1.0, i-9 v0.6).
- The audit may surface NEW issues (e.g. validation bypass on direct writers, missing photoUrl on upsert request, no GET-by-id). Recording new issues is in scope; opening `ISSUES.md` edits is out of scope for this audit and belongs to plan 17-02+.

**Out of scope for this plan:**
- Writing Java fixes (plan 17-02+).
- Adding new endpoints, columns, DTOs, or migration files.
- Modifying ProjectBrief, ROADMAP, or any STATE/ISSUES file.
- Touching `webhooks/`, `health/`, `exports/`, `notifications/` packages beyond reading the entry points that write or read `BodyMetric`.
- Running `mvn` (local-maven-gap memory; the audit produces no test-runnable output).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory metrics package and catalog endpoints + DTOs</name>
  <files>.planning/phases/17-body-metrics/17-01-AUDIT.md</files>
  <action>
  Read every file under `backend/src/main/java/com/workouthub/metrics/` (`MetricsController.java`, `MetricsService.java`, `domain/BodyMetric.java`, `domain/BodyMetricRepository.java`, `dto/BodyMetricDto.java`, `dto/UpsertBodyMetricRequest.java`) and the test file `backend/src/test/java/com/workouthub/metrics/MetricsIntegrationTest.java`. Also open `backend/src/main/resources/db/migration/V6__body_metrics_and_supplements.sql` to record the SQL-side column shape.

  Create `17-01-AUDIT.md` with frontmatter matching the 16-01-AUDIT shape (`phase: 17-body-metrics`, `plan: 01`, `deliverable: audit`). Write Sections 1 and 2.

  Section 1 - Metrics Package Inventory:
  Tree of `metrics/`-owned files grouped by role (Controller, Service, Domain entity, Repository, DTOs, Tests). For each file: one-line responsibility plus line count measured 2026-05-05. Same shape as `15-01-AUDIT.md` Section 1 and `16-01-AUDIT.md` Section 1.

  Sub-table for the V6 SQL definition: list every column in `body_metrics` from V6 with its SQL type (`UUID`, `DATE`, `NUMERIC(p,s)`, `VARCHAR(n)`, `TEXT`, `TIMESTAMPTZ`), nullability, and constraints (UNIQUE, CHECK, FK CASCADE). Cite `V6__body_metrics_and_supplements.sql:6-24` for the table DDL and `:26-28` for the indexes (`idx_body_metrics_recorded_date`, `idx_body_metrics_user_date` ON `(user_id, recorded_date DESC)`).

  Total file/line count for `metrics/`-owned code at the bottom of the section.

  Section 2 - Endpoint + DTO Catalog:
  One row per HTTP endpoint exposed by `MetricsController`. Columns: HTTP method, path, controller method name (file:line), request DTO, response DTO, auth requirement (JWT user via `@AuthenticationPrincipal AppUserPrincipal`), 2xx status code, idempotency notes, query parameters with defaults and bounds.

  Three rows expected:
    - `GET /api/metrics` -> `list` (`MetricsController.java:30-33`). Request: none. Response: `List<BodyMetricDto>`. 200 OK. Idempotent. NO query parameters - returns full history.
    - `POST /api/metrics` -> `upsert` (`MetricsController.java:35-41`). Request: `UpsertBodyMetricRequest`. Response: `BodyMetricDto`. 201 Created (always - even on date-collision update; flag this in Section 4). Idempotent BY DATE: same `(userId, recordedDate)` is upserted in place, not duplicated, due to `MetricsService.upsert` `findByUserIdAndRecordedDate(...).orElseGet(...)` (`MetricsService.java:31-38`) and the SQL-level UNIQUE constraint `body_metrics_user_date_unique` (`V6__body_metrics_and_supplements.sql:21`).
    - `DELETE /api/metrics/{id}` -> `delete` (`MetricsController.java:43-49`). Request: none. Response: 204 No Content. 404 via `NotFoundException` if `findByIdAndUserId` misses (`MetricsService.java:49-52`). Idempotent on retry only if first call wins; second call returns 404.

  Then a DTO sub-section: list all 12 components of `BodyMetricDto` (`BodyMetricDto.java:8-20`) and all 8 components of `UpsertBodyMetricRequest` (`UpsertBodyMetricRequest.java:10-18`). For each `UpsertBodyMetricRequest` field, record its validation annotation (`@DecimalMin/@DecimalMax/@NotNull/@Size`) and the numeric range. Flag the asymmetry between the two DTOs: `BodyMetricDto` has `id`, `photoUrl`, `createdAt`, `updatedAt` that the request does not (expected for create-time fields), AND `BodyMetricDto.photoUrl` (`BodyMetricDto.java:17`) which is reachable on read but UNREACHABLE on write via this endpoint (no `photoUrl` in the request DTO). Cite both files line ranges.

  Test sub-section: list all 6 tests in `MetricsIntegrationTest` with file:line and a one-line outcome each: `unauthenticatedCallReturns401` (line 32), `postStoresAndGetReturnsTheEntry` (37), `repeatedPostForSameDateUpdatesInsteadOfInserting` (59), `invalidWeightRangeReturns400` (82), `deleteRemovesTheEntry` (94), `crossUserListIsIsolated` (113).

  Constraint: read-only on Java source. Do not run mvn or modify any source file. The only file written by this task is `17-01-AUDIT.md`.
  </action>
  <verify>
  - `17-01-AUDIT.md` exists with frontmatter and Sections 1 and 2.
  - Section 1 lists all 6 source files plus the V6 SQL column table; column table has every column from `body_metrics` plus the two indexes.
  - Section 2 has exactly three endpoint rows (GET, POST, DELETE) with `file:line` citations.
  - DTO sub-section lists all 12 `BodyMetricDto` components and all 8 `UpsertBodyMetricRequest` components.
  - Asymmetry flag is explicit: `photoUrl` is on the response but not on the request.
  - Test sub-section lists all 6 tests with line numbers.
  - `git diff --stat` shows zero changes under `backend/`.
  </verify>
  <done>
  Sections 1 and 2 give the reader a complete view of the metrics package's current scaffold without opening any source file.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProjectBrief + ROADMAP gap matrix and behavioral analysis</name>
  <files>.planning/phases/17-body-metrics/17-01-AUDIT.md</files>
  <action>
  Read `ProjectBrief.md` lines 177-186 (body_metrics schema) and lines 360-363 (Phase 5 page #7 Vucut Metrikleri requirements) and `.planning/ROADMAP.md` lines 76-83 (Phase 17 goal). Append Sections 3 and 4 to the audit file.

  Section 3 - ProjectBrief + ROADMAP Gap Matrix:
  Three-part table.

  Part A - Schema column drift (ProjectBrief lines 177-186 vs V6 vs entity vs response DTO vs upsert request):
  Column-by-column matrix with 5 sources:
    | Column | ProjectBrief schema | V6 SQL (line) | Entity field (line) | BodyMetricDto component (line) | UpsertBodyMetricRequest component (line) | Status |
  Rows: `recorded_date`, `weight_kg`, `body_fat_percent`, `waist_cm`, `chest_cm`, `arm_cm`, `thigh_cm`, `photo_url`, `notes`.

  For `photo_url`, the row should show: ProjectBrief = listed, V6 = `VARCHAR(500)` line 17, Entity = `BodyMetric.java:48` `photoUrl`, BodyMetricDto = `BodyMetricDto.java:17` `photoUrl`, UpsertBodyMetricRequest = **MISSING**, Status = **GAP** (Phase 17 ROADMAP deliverable: "optional progress photo URL"). Every other row should resolve to "all five sources align" / Status = `OK`.

  Cite each component's file:line in the cell. Use a status column with `OK` / `GAP` / `Drift`.

  Part B - ProjectBrief Phase 5 page #7 deliverables (lines 360-363) vs current state:
    - "Kilo grafigi (haftalik/aylik/tum zamanlar)" -> Status: `Partial`. Evidence: `GET /api/metrics` returns full history (`MetricsController.java:30-33`), but no time-window or range filter; the frontend can do client-side filtering but the chart UI semantics imply a server-side range read for "weekly/monthly/all-time" perf and consistency. ROADMAP explicitly says "time-series read endpoints" (`ROADMAP.md:78`); Phase 17 must add a date-range or window-scoped read.
    - "Olcum ekleme formu" -> Status: `Partial`. Evidence: `POST /api/metrics` accepts the form but missing `photoUrl` (Part A row). Validation ranges present (weight 20-500, body fat 0-100, etc.).
    - "Progress fotografi yukleme (opsiyonel)" -> Status: `Gap on the API surface`. Evidence: `BodyMetric.photoUrl` and SQL column `photo_url` exist; the only path that writes `photoUrl` today is `FullImportService.replaceMetrics` (`FullImportService.java:172`) - the user-facing `POST /api/metrics` cannot set it. Notes: this audit does NOT prescribe upload mechanics (multipart vs URL string vs MinIO). Phase 17 minimum is to expose `photoUrl` as an OPTIONAL string field on `UpsertBodyMetricRequest` so the import path's value can also reach the user-facing path; storage strategy is a Phase 5 frontend concern (deferred).

  Part C - ROADMAP Phase 17 deliverables (`ROADMAP.md:76-83`) vs current state:
    - "weight" -> `Implemented` with validation. Evidence: `weightKg` in entity, request, response. Validation: `@DecimalMin("20.0") @DecimalMax("500.0")` (`UpsertBodyMetricRequest.java:12`).
    - "body measurements" -> `Implemented` with validation. Evidence: 4 measurement fields (waist/chest/arm/thigh) in entity, request, response with bounded ranges.
    - "optional progress photo URL" -> `Schema/entity/response yes; request DTO no`. Cross-link to Part A photo_url row.
    - "time-series read endpoints for the metrics UI" -> `Missing`. Evidence: only `GET /api/metrics` exists, returns full history with no date-range filter and no per-field-projection. Cross-link to Part B "Kilo grafigi" row.

  Section 4 - Behavioral Analysis:

  Part A - POST status-code semantics:
  Walk `MetricsController.upsert` (`MetricsController.java:35-41`) and `MetricsService.upsert` (`MetricsService.java:30-47`):
    1. Service queries `findByUserIdAndRecordedDate(userId, req.recordedDate())`.
    2. If hit: existing entity is mutated and saved (UPDATE).
    3. If miss: new entity is created (INSERT).
    4. Controller returns `ResponseEntity.status(HttpStatus.CREATED)` (`MetricsController.java:39`) UNCONDITIONALLY.
  Drift verdict: 201 is wrong for the UPDATE branch. Per HTTP semantics, 200 OK fits the UPDATE branch and 201 Created fits the INSERT branch. Phase 15-02 (`clientSetId` idempotency) precedent: same shape (200 = replay/update, 201 = newly created). Walk both fix directions:
    - Direction A: keep upsert-by-date and split status. `MetricsService.upsert` returns a wrapper containing `(BodyMetricDto, boolean wasCreated)`; controller maps to 200 vs 201. Touches: 1 service method signature, 1 controller method, 6 tests.
    - Direction B: split into two endpoints. `POST /api/metrics` always creates and returns 409 on date collision. `PUT /api/metrics/{id}` updates by id. Touches: 1 controller method split into 2; service split into create + update; date-collision uniqueness moves from upsert-by-find to a 409 mapping path; 6 tests rewritten.
  Recommendation: pick one. Direction A is the smaller change and matches Phase 15-02's idempotency pattern. Direction B is more REST-pure but inconsistent with the V6 schema's "one row per (user, date)" design (UNIQUE constraint at SQL level). Lean Direction A unless Section 5 surfaces a reason to split.

  Part B - upsert-by-date semantics and the missing PUT-by-id:
  Document the current contract: there is no `PUT /api/metrics/{id}` and no `GET /api/metrics/{id}`. To edit an existing row by id, the client must know its `recordedDate` and POST that. List the implications:
    - Frontend MUST cache `recordedDate` per row (cheap; already in `BodyMetricDto`).
    - DELETE-then-POST is needed if the user changes the `recordedDate` of an existing row (because the unique constraint `body_metrics_user_date_unique` ties identity to date).
    - Per-field updates are not a separate concern: the upsert request requires the full mutable shape on every call (no PATCH semantics).
  Verdict: this is acceptable for v0.4 if the date-keyed model is intentional. Recommend keeping it (do not add PUT-by-id) unless Section 6 plan-count opens a slot for it. The brief's "Olcum ekleme formu" wording does not imply per-field PATCH.

  Part C - Validation bypass on cross-package writers (preview of Section 5):
  Note (do not resolve here) that `ScaleWebhookController.ingest` (`ScaleWebhookController.java:53-57`) writes a `BodyMetric` directly with NO Bean Validation - if a malformed scale payload sends `weightKg = 5.0`, the SQL precision check (`NUMERIC(5,2)` allows 0-999.99) would not catch the "weight out of human range" rule that `UpsertBodyMetricRequest` enforces (`@DecimalMin("20.0")`). Same applies to `HealthImportService.apply` (`HealthImportService.java:80-91`). Section 5 will recommend adoption strategy.

  Constraint: no Java files modified.
  </action>
  <verify>
  - Section 3 Part A is a 9-row column-drift matrix with all 5 source columns filled and a Status column; `photo_url` is the only `GAP` row.
  - Section 3 Part B has 3 ProjectBrief Phase 5 #7 deliverables with `Partial` / `Implemented` / `Gap` verdicts.
  - Section 3 Part C has 4 ROADMAP Phase 17 deliverables with status verdicts.
  - Section 4 Part A walks both fix directions for the 201-always drift and ends with a single recommendation.
  - Section 4 Part B documents the missing PUT-by-id with implications.
  - Section 4 Part C flags validation bypass on `ScaleWebhookController` and `HealthImportService` as a preview of Section 5.
  - `git diff --stat` shows only `.planning/phases/17-body-metrics/17-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 3 and 4 give plan 17-02+ a column-level fix list (`photoUrl` on upsert request), a status-code direction, and a flagged cross-package coupling concern.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-package coupling map, time-series endpoint shape, and recommended Phase 17 plan-02+ scope</name>
  <files>.planning/phases/17-body-metrics/17-01-AUDIT.md</files>
  <action>
  Append Sections 5 and 6 to the audit file.

  Section 5 - Cross-Package Coupling Map and Time-Series Endpoint Shape:

  Part A - Cross-package writers/readers of `BodyMetric` (bypass `MetricsService`):
  One row per call site outside `metrics/` that touches `BodyMetricRepository` or `BodyMetric` directly. Columns: caller (file:line), operation (read / write / delete), validation surface (Bean Validation? domain check?), idempotency strategy, semantics fit for adopting `MetricsService.upsert` (Y/N + reason).

  Five rows expected:
    - `webhooks/ScaleWebhookController.ingest` (`ScaleWebhookController.java:35-60`). Operation: write (idempotent by `(userId, recordedDate)` via `findByUserIdAndRecordedDate` check at line 49). Validation surface: payload-level `payload.weightKg() == null` check only (line 43-45); NO `@Valid` on `ScalePayload`; NO `MetricsService` weight-range check. Adopt `MetricsService.upsert`? `Maybe`: would gain weight-range validation, but loses the "first-write-wins" idempotency (the current path returns 200 if already present without re-writing the row, which is the right semantic for a passive sensor; an upsert path overwrites the existing row's weight value). Recommend: keep direct, but extract the weight-range check into a `BodyMetricValidator.validate(weightKg)` static helper and call it from both paths.
    - `health/HealthImportService.apply` (`HealthImportService.java:80-91`). Operation: write (per body-mass record; idempotent by `(userId, recordedDate)` via `findByUserIdAndRecordedDate` skip-check at line 81). Validation surface: NONE. Adopt `MetricsService.upsert`? `No`: HealthImport is a batch path that explicitly skips on conflict (logged as `bmSkipped`), not overwrite. The skip semantics are a feature (avoid clobbering manual edits with imported data). Same `BodyMetricValidator` recommendation.
    - `exports/FullExportService.buildMetrics` (`FullExportService.java:101-105`). Operation: read-only via `findByUserIdOrderByRecordedDateDesc`. Validation surface: N/A. Adopt? `No, intentional`. The export is a read-side projection.
    - `exports/FullImportService.replaceMetrics` (`FullImportService.java:155-178`). Operation: delete-all + insert-all (`metrics.deleteAll(existing)` then `metrics.saveAll(fresh)`). Validation surface: NONE (the `ImportValidator` runs at payload shape only, not field bounds). Adopt? `No`: the import path is "wholesale replace" semantics; per-row upsert would be O(n) round-trips and would not match the `ImportResultDto.metricsInserted` count semantic.
    - `notifications/ReminderJob.runWeightNudgeAt` (`ReminderJob.java:77-90`). Operation: read-only via `findUserIdsWithoutMetricsSince` (`BodyMetricRepository.java:19-26`). Validation surface: N/A. Adopt? `No, intentional`. Pure read for cron logic.

  Verdict (compact):
  Five direct call sites; only `ScaleWebhookController` and `HealthImportService` are write paths that bypass `UpsertBodyMetricRequest`'s weight-range validation. Recommend: extract a thin `BodyMetricValidator` helper (no Spring bean, just static methods) so the three writers (controller, scale webhook, health import) all share the bounds check; do NOT force adoption of `MetricsService.upsert` because each writer has different conflict semantics. Defer the validator extraction to plan 17-02+ if Section 6 plan-count opens a slot; otherwise mark as a Phase 31 (charts-stats) deferred-concern.

  Part B - Time-series endpoint shape:
  Walk three options for the missing time-series read:
    - Option 1: extend `GET /api/metrics` with `from` and `to` query parameters (both optional, both `LocalDate`). When omitted, returns full history (back-compat). When set, returns only rows with `recordedDate BETWEEN from AND to`. Repository adds `findByUserIdAndRecordedDateBetweenOrderByRecordedDateDesc`. Pros: zero new endpoints, single read path for both list and chart UIs, idiomatic. Cons: response payload still carries all 12 fields per row even when the chart only needs `(date, weightKg)`.
    - Option 2: add `GET /api/metrics/series?from=&to=&field=weightKg` returning `List<{recordedDate: LocalDate, value: BigDecimal}>` projection, lighter payload for chart UIs. Pros: matches "time-series read endpoint" wording in ROADMAP literally; cheap on the wire for charts. Cons: new DTO; new controller method; another endpoint to document; field-projection is overkill for v0.4 single-user load.
    - Option 3: hybrid - extend Option 1 query params AND add a lightweight chart endpoint. Pros: covers both UIs perfectly. Cons: 2x the change cost, not justified by v0.4 budget.
  Recommendation: pick one. Lean Option 1 unless Phase 21+ frontend audits surface a chart-payload size concern. Reuse the `(user_id, recorded_date DESC)` index from V6 (`V6__body_metrics_and_supplements.sql:27-28`) - both `from`/`to` filters are index-friendly on this index.

  Section 6 - Recommended Phase 17 plan-02+ Scope:

  Bulleted list of concrete tasks plan 17-02+ should plan, each with: target files, expected change shape (1-3 lines or "rewrite N lines" or "new file: lines ~N"), and which test file picks up the regression coverage. Group by bucket:
    - `photo-url-exposure` - add `photoUrl` to `UpsertBodyMetricRequest` (one new optional `String` component with `@Size(max = 500)` to mirror the V6 column width); thread `req.photoUrl()` into `MetricsService.upsert` (one line); test extension on `MetricsIntegrationTest` covering `photoUrl` round-trip on POST -> GET. Targets: `dto/UpsertBodyMetricRequest.java`, `MetricsService.java:39-45`, `MetricsIntegrationTest.java`. Depends on Section 3 Part A `photo_url` GAP row.
    - `time-series-range` - add `from` and `to` `@RequestParam(required = false)` `LocalDate` to `MetricsController.list`; add repository method `findByUserIdAndRecordedDateBetweenOrderByRecordedDateDesc(UUID, LocalDate, LocalDate)`; service branches on parameter presence; 2 new integration tests (range hit, empty range). Targets: `MetricsController.java:30-33`, `MetricsService.java:23-28`, `BodyMetricRepository.java`, `MetricsIntegrationTest.java`. Depends on Section 5 Part B Option 1 verdict (revise if audit picked Option 2).
    - `status-code-split` - return 200 on update vs 201 on create from `POST /api/metrics`. Service returns wrapper `(BodyMetricDto, boolean wasCreated)` or signals via Spring's `ResponseStatus` builder; controller maps to status; existing 6 tests revisited (one currently asserts 201 on the upsert-update path - `repeatedPostForSameDateUpdatesInsteadOfInserting` at line 59 asserts `isCreated()` on the second POST and must be flipped to `isOk()`); 1 new test asserting the create-then-update status pair. Targets: `MetricsController.java:35-41`, `MetricsService.java:30-47`, `MetricsIntegrationTest.java:67-78`. Depends on Section 4 Part A Direction A verdict.
    - `validation-extraction` (optional, may defer) - extract `BodyMetricValidator` static helper covering the weight + measurement bounds shared by `MetricsService.upsert`, `ScaleWebhookController.ingest`, `HealthImportService.apply`. Touches `metrics/BodyMetricValidator.java` (new file ~30 lines), `webhooks/ScaleWebhookController.java:53-57`, `health/HealthImportService.java:80-91`. Cross-package surface change. Depends on Section 5 Part A verdict.
    - `defer` - explicit non-goals for v0.4: PUT-by-id endpoint (Section 4 Part B; not requested by brief), photo upload mechanics (multipart/MinIO; Phase 5 frontend concern; this audit only opens the URL field), chart-projection endpoint (Section 5 Part B Option 2; revisit at Phase 31 if needed), `MetricsMapper` extraction (`MetricsService.toDto` is a private static; extract only if test count grows past 12 or a second feature consumes the mapping - Phase 31 candidate).

  For each `photo-url-exposure`, `time-series-range`, and `status-code-split` entry, write a one-paragraph rationale tied to a specific section verdict (e.g. "Section 3 Part A row `photo_url` shows the only column-level gap; touches: `dto/UpsertBodyMetricRequest.java` + 1 service line + 1 test").

  Plan-count recommendation: how many plans (17-02, 17-03, ...) the hardening warrants given comprehensive depth (`config.json: depth: comprehensive`) and the Phase 14/15/16 precedent (one bug-shape per plan; migration in its own plan if independent of code-level changes; Phase 17 has NO migration since `photoUrl` already exists in the V6 schema). Recommend explicit plan boundaries with one-line objective per plan.

  Suggested boundary template (the audit may revise):
  - Plan 17-02: photo-url-exposure (smallest change; closes the column-level gap; ~3 file touches).
  - Plan 17-03: time-series-range (adds the chart-friendly read; new repo method + new query params; ~4 file touches + 2 new tests).
  - Plan 17-04: status-code-split (semantic fix for upsert; tightens the contract; ~3 file touches + 1 new test + 1 flipped assertion).
  - Plan 17-05 (optional): validation-extraction (cross-package; only land if Section 5 Part A verdict justifies).

  If `validation-extraction` is deferred, mark it as a "Phase 17-04 may absorb if scope budget allows" note rather than a plan slot.

  Constraint: do not edit any Java file. Do not propose architectural rewrites - the goal is the smallest set of changes that closes the gaps and respects the v0.4 budget.

  Final length check: target under 500 lines for the entire `17-01-AUDIT.md`. Trim verbose narrative; tables and ranked lists belong here, not prose.
  </action>
  <verify>
  - Section 5 Part A has 5 cross-package call-site rows with adopt-or-stay verdicts.
  - Section 5 Part A ends with a compact verdict on the validator extraction direction.
  - Section 5 Part B walks 3 time-series endpoint shape options and ends with a single recommendation.
  - Section 6 has one entry per applicable bucket; empty buckets are stated explicitly as "None - no findings" or "Deferred to Phase 31."
  - Plan-count recommendation is explicit: `Plan 17-02: ...`, `Plan 17-03: ...`, etc., each with a one-line objective.
  - Every `photo-url-exposure`, `time-series-range`, and `status-code-split` rationale paragraph references the section that produced the verdict (Section 3 Part A, Section 5 Part B, Section 4 Part A respectively).
  - Total `17-01-AUDIT.md` length is under 500 lines.
  - `git diff --stat` shows only `.planning/phases/17-body-metrics/17-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 5 and 6 give plan 17-02+ a file-level scope, an unambiguous time-series shape verdict, an unambiguous status-code verdict, and explicit plan boundaries.
  </done>
</task>

</tasks>

<verification>
Before declaring the plan complete:
- [ ] `17-01-AUDIT.md` exists and contains Sections 1-6.
- [ ] Every controller method, service method, DTO record, repository query, and migration column referenced cites `file:line` (or migration line).
- [ ] Section 1 lists every file in `metrics/` plus the V6 column table.
- [ ] Section 2 has all 3 endpoints, all 12 + 8 DTO components, and all 6 test methods.
- [ ] Section 3 Part A is a 9-row column-drift matrix with `photo_url` as the only `GAP` row.
- [ ] Section 3 Part B + C cover ProjectBrief Phase 5 #7 and ROADMAP Phase 17 deliverables.
- [ ] Section 4 Part A delivers a single status-code verdict with both directions walked.
- [ ] Section 4 Part B documents the missing PUT-by-id with implications and a verdict.
- [ ] Section 5 Part A catalogs all 5 cross-package call sites with adopt verdicts.
- [ ] Section 5 Part B walks 3 time-series endpoint options and lands on one.
- [ ] Section 6 buckets are concrete and either populated with file-level entries or stated as empty.
- [ ] Plan-count recommendation in Section 6 is explicit so plan 17-02+ structure is decided.
- [ ] Total length of `17-01-AUDIT.md` is under 500 lines.
- [ ] `git diff --stat` shows only `.planning/phases/17-body-metrics/` changes.
</verification>

<success_criteria>
- All three tasks complete.
- `17-01-AUDIT.md` is the single Java-free deliverable.
- Phase 17 plan-02+ has a concrete, file-level fix list bucketed by intent.
- Time-series endpoint direction (Option 1 query-param extension vs Option 2 dedicated chart endpoint) is settled with rationale.
- Status-code direction (200/201 split via Direction A wrapper vs Direction B endpoint split) is settled with rationale.
- Cross-package validator-extraction direction (extract shared validator vs leave each writer with its own bounds) is settled with rationale.
- Plan-count for Phase 17 hardening (17-02..17-0N) is explicit.
- No Java sources, migrations, tests, or other planning files are modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/17-body-metrics/17-01-SUMMARY.md` with frontmatter and body following the Phase 16-01 audit summary shape:

---
phase: 17-body-metrics
plan: 01
subsystem: metrics
tags: [audit, gap-analysis, contract-drift, cross-package-coupling, planning]

requires:
  - phase: 16-sessions-analytics
    plan: 04
    reason: V27 migration ordering; V28 is the next free Flyway slot for any Phase 17 schema work (not anticipated for the photo-url path since the column exists in V6)
  - phase: 15-sessions-core
    plan: 02
    reason: clientSetId 200/201 status-code split precedent applied to MetricsController.upsert in Section 4 Part A

provides:
  - metrics package inventory (6 source files + 1 test class + V6 SQL column table)
  - endpoint + DTO catalog (3 endpoints, BodyMetricDto 12 components, UpsertBodyMetricRequest 8 components)
  - schema column drift matrix (9 rows, photo_url is the lone GAP)
  - ProjectBrief Phase 5 #7 + ROADMAP Phase 17 deliverable status
  - status-code semantics verdict for MetricsController.upsert (200 update / 201 create)
  - cross-package coupling map (5 direct BodyMetric call sites outside MetricsService)
  - time-series endpoint shape verdict (Option 1 query-param extension OR Option 2 dedicated chart endpoint)
  - validator-extraction verdict (shared static helper OR per-writer bounds)
  - recommended Phase 17 plan-02+ scope (photo-url-exposure / time-series-range / status-code-split / validation-extraction / defer buckets)
  - explicit plan boundaries with one-line objective per plan

affects:
  - 17-body-metrics/17-02
  - 17-body-metrics/17-03
  - 17-body-metrics/17-04 (or whatever the final plan-count resolves to)
  - 18-export-refinement (export DTO already carries photoUrl; verify alignment after 17-02)
  - 21-frontend-audit (time-series endpoint shape will inform metrics-ui chart wiring at Phase 28)

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/17-body-metrics/17-01-AUDIT.md
  modified: []

key-decisions:
  - "[Status-code verdict: 200 on update / 201 on create via service-returns-(dto, wasCreated) wrapper OR endpoint split into POST + PUT-by-id]"
  - "[Time-series verdict: Option 1 from/to params on GET /api/metrics OR Option 2 dedicated /api/metrics/series projection endpoint]"
  - "[Validator-extraction verdict: extract shared BodyMetricValidator static helper OR leave each writer with its own bounds]"
  - "[Plan-count for Phase 17 hardening: N plans (17-02..17-0N) with explicit boundaries]"
---

# Phase 17 Plan 01: body-metrics audit Summary

**[One-line audit outcome - what landed, what is now planning-ready]**

## Accomplishments
- Inventory complete: 6 metrics package source files + 1 test class + V6 column table.
- Endpoint + DTO catalog: 3 endpoints, 2 response/request DTOs with components enumerated, 6 tests cataloged.
- Schema column drift: 9 rows, `photo_url` is the only GAP across the 5 source columns (ProjectBrief / V6 / entity / response DTO / upsert request).
- ProjectBrief Phase 5 #7 deliverables: K-of-3 implemented, L partial.
- ROADMAP Phase 17 deliverables: K-of-4 implemented, L partial.
- Status-code semantics verdict: [Direction A wrapper / Direction B endpoint split] with rationale.
- Cross-package coupling: 5 call sites cataloged, M write paths bypass `MetricsService` validation.
- Time-series endpoint verdict: [Option 1 / Option 2 / Option 3] with rationale.
- Validator-extraction verdict: [extract / leave-direct] with rationale.
- Phase 17 plan-02+ scope: K photo-url-exposure, L time-series-range, M status-code-split, N validation-extraction, P defer.
- Plan-count recommendation: 17-02..17-0N with one-line objective each.

## Files Created/Modified
- `.planning/phases/17-body-metrics/17-01-AUDIT.md` - audit deliverable.

## Decisions Made
- Status-code semantics direction.
- Time-series endpoint shape.
- Cross-package validator extraction direction.
- Phase 17 plan boundaries.

## Issues Encountered
- Any inconsistency found between codebase docs and actual code.
- Any ProjectBrief drift requiring Phase 19 (api-contract-docs) clarification.

## Next Phase Readiness
- Ready for Phase 17 plan-02+ planning: yes / no with reason.
- Open questions for plan 17-02+: list any.
</output>
