---
phase: 16-sessions-analytics
plan: 01
type: execute
domain: backend
---

<objective>
Inventory the analytics scaffold currently living inside `backend/src/main/java/com/workouthub/sessions/`, map it against ProjectBrief Phase 4 special endpoints (lines 305-308) and ROADMAP Phase 16 deliverables (PR computation, volume aggregation, 1RM (Epley) projections at the query layer), and recommend a concrete Phase 16 plan-02+ hardening scope with explicit plan boundaries.

Purpose: All Phase 16 endpoints (`/api/exercises/{id}/last-performance`, `/api/exercises/{id}/progress`) are already physically implemented as Phase 15 audit (`15-01-AUDIT.md` Section 5) "in-package leaks" inside `sessions/`. Phase 16 cannot plan "what to land" because everything is landed; it must plan "what to formalize (package boundary), what to expose (Epley projection on `ProgressPointDto`), what to make durable (PR persistence), and what to push down to SQL (perf)." This audit produces the single source of truth for those decisions.

Output: `.planning/phases/16-sessions-analytics/16-01-AUDIT.md` containing six sections (analytics scaffold inventory, endpoint + DTO catalog, ProjectBrief Phase 4 + ROADMAP Phase 16 gap matrix, package-boundary verdict, perf baseline analysis, recommended Phase 16 plan-02+ scope). No Java source files, migrations, or tests are modified.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/ISSUES.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/13-workouts-audit/13-01-AUDIT.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/phases/15-sessions-core/15-02-SUMMARY.md
@.planning/phases/15-sessions-core/15-03-SUMMARY.md
@.planning/phases/15-sessions-core/15-04-SUMMARY.md
@ProjectBrief.md
@backend/src/main/java/com/workouthub/sessions/ExerciseAnalyticsController.java
@backend/src/main/java/com/workouthub/sessions/ExerciseAnalyticsService.java
@backend/src/main/java/com/workouthub/sessions/PrDetector.java
@backend/src/main/java/com/workouthub/sessions/SessionsMapper.java
@backend/src/main/java/com/workouthub/sessions/SessionSetsService.java
@backend/src/main/java/com/workouthub/sessions/dto/LastPerformanceDto.java
@backend/src/main/java/com/workouthub/sessions/dto/ProgressPointDto.java
@backend/src/main/java/com/workouthub/sessions/dto/SessionSetDto.java
@backend/src/main/java/com/workouthub/sessions/domain/SessionSet.java
@backend/src/main/java/com/workouthub/sessions/domain/SessionSetRepository.java
@backend/src/main/java/com/workouthub/sessions/domain/WorkoutSession.java
@backend/src/main/resources/db/migration/V5__sessions_and_sets.sql
@backend/src/main/resources/db/migration/V26__session_sets_client_id.sql
@backend/src/test/java/com/workouthub/sessions/ExerciseAnalyticsIntegrationTest.java
@backend/src/test/java/com/workouthub/sessions/PrDetectorTest.java
@backend/src/test/java/com/workouthub/sessions/PrDetectionIntegrationTest.java
@backend/src/test/java/com/workouthub/sessions/SessionSetsIntegrationTest.java

**Tech stack available (carried from Phase 15):** Java 21, Spring Boot 3.5.x, Spring Web, Spring Data JPA, Hibernate `@UuidGenerator`, Jackson with `serializationInclusion = NON_NULL`, Flyway migrations (last applied: V26 `session_sets.client_set_id`), JUnit 5 + Testcontainers Postgres, typed `ApiError.code` envelope (Phase 15-04: `SET_NUMBER_DUPLICATE`, `SESSION_FINISHED`, `SESSION_ALREADY_ACTIVE`, `SESSION_ALREADY_FINISHED`).

**Established patterns (carried from Phase 15):**
- Feature-sliced packages.
- Controller -> Service -> Repository, transactions on services, DTOs at HTTP boundary.
- Validation via `jakarta.validation` on request DTOs and `@Min`/`@Max` on `@RequestParam`.
- Hand-written mappers (`SessionsMapper`), no MapStruct.
- Optional trailing record components for back-compat (`ApiError.code`, `SessionSetDto.newPr`); Jackson NON_NULL omits when null.
- Per-throw-site `private static final String` constants for typed conflict codes (Phase 15-04 decision).
- Phase 14 lesson: explicit child-repo `saveAndFlush(child)` after `parent.addChild()` to materialize `@UuidGenerator` ids; sessions package already complies.
- Phase 15-04 Javadoc-as-binding-contract pattern: `SessionSetsService.add` documents auto-numbering and idempotency contracts in Javadoc; behavior unchanged.

**Constraining decisions:**
- Phase 15-01 audit Section 5 ownership map: `ExerciseAnalyticsController.java`, `ExerciseAnalyticsService.java`, `LastPerformanceDto.java`, `ProgressPointDto.java`, `PrDetector.java` are Phase 16 territory. `SessionSetRepository.findHistoricalByUserAndExercise` is shared but Phase 16 owns its analytics use. Plans 15-02/15-03/15-04 explicitly did NOT modify these files (Phase 15 leak guard upheld per 15-04 SUMMARY).
- Phase 15-04 `newPr` create-vs-reread verdict: `newPr=true` lands on POST response, omitted on detail re-read. Documented by `SessionSetsIntegrationTest.newPrFlagPresentOnCreateButOmittedOnDetailReread`. Phase 16 PR durability work, if undertaken, MUST keep this regression test green or replace it with a stronger durable-read assertion.
- ProjectBrief Phase 4 line 305-308 lists the two analytics endpoints under the sessions phase; ROADMAP places them in Phase 16. The phase split is a planning artifact, not a contract drift. Audit must call out that the brief and the roadmap agree on shape; only ownership differs.
- ROADMAP Phase 16 description (`.planning/ROADMAP.md:62-66`): "PR computation, volume aggregation, and 1RM (Epley) projections at the query layer." All three are Phase 16 deliverables. Audit must verify which are present and which are missing in current scaffold.
- `ddl-auto: validate` - schema cannot drift; any new column requires a new `V<n>__*.sql` (V27 is the next free slot; V23/V24/V25/V26 are taken).

**Issues being addressed:**
- None directly from `.planning/ISSUES.md`. Open issues i-3..i-9 belong to other phases per the issue-to-phase mapping in STATE.md.
- The audit may surface NEW issues (e.g. perf concern with full-history Java aggregation, missing Epley exposure on the progress DTO). Recording new issues is in scope; opening `ISSUES.md` edits is out of scope for this audit and belongs to plan 16-02+.

**Out of scope for this plan:**
- Writing Java fixes (plan 16-02+).
- Adding new endpoints, columns, or DTOs.
- Modifying ProjectBrief, ROADMAP, or any STATE/ISSUES file.
- Touching `sessions/` files that Phase 15-04 documented as stable (`SessionsService`, `SessionsController`, `SessionSetsController`, lifecycle DTOs). Phase 16 is allowed to touch shared call sites only via plan 16-02+; this audit is read-only.
- Touching workouts/, metrics/, export/, health/ packages beyond reading the call site that the analytics flow depends on (specifically `ExerciseRepository.findById` in `ExerciseAnalyticsService.lastPerformance` and `progress`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory analytics scaffold and catalog endpoints + DTOs</name>
  <files>.planning/phases/16-sessions-analytics/16-01-AUDIT.md</files>
  <action>
  Read every analytics-owned file under `backend/src/main/java/com/workouthub/sessions/` (Phase 15-01 Section 5 ownership map: `ExerciseAnalyticsController.java`, `ExerciseAnalyticsService.java`, `PrDetector.java`, `dto/LastPerformanceDto.java`, `dto/ProgressPointDto.java`) and the shared call sites that Phase 16 owns (`SessionSetRepository.findHistoricalByUserAndExercise`, `SessionSetsService.bestPriorOneRm`, `SessionsMapper.toSetDto(set, newPr)`). Also catalog the Phase 16 test surface: `ExerciseAnalyticsIntegrationTest`, `PrDetectorTest`, `PrDetectionIntegrationTest`, plus the `newPrFlagPresentOnCreateButOmittedOnDetailReread` test inside `SessionSetsIntegrationTest`.

  Create `16-01-AUDIT.md` with frontmatter matching `15-01-AUDIT.md` (`phase: 16-sessions-analytics`, `plan: 01`, `deliverable: audit`). Write Sections 1 and 2.

  Section 1 - Analytics Scaffold Inventory:
  Tree of Phase 16-owned files grouped by role (Controller, Service, Domain logic helper, DTOs, Repository query, Tests). For each file: one-line responsibility plus line count. For shared call sites (`SessionsMapper.toSetDto`, `SessionSetsService.bestPriorOneRm`, `SessionSetsService.add` PR-detection invocation), cite `file:line` and note "shared (Phase 15 entry-point + Phase 16 logic)." Total file/line count for Phase 16-owned code at the bottom. Same shape as `15-01-AUDIT.md` Section 1.

  Section 2 - Endpoint + DTO Catalog:
  One row per HTTP endpoint exposed by `ExerciseAnalyticsController`. Columns: HTTP method, path, controller method name (file:line), request DTO (none for both - `@PathVariable` + optional `@RequestParam`), response DTO, auth requirement (JWT user), 2xx status code (200 / 204 split for `last-performance`), idempotency notes (both GETs are idempotent), query parameters with defaults and bounds (`limit` default 10, `@Min(1) @Max(100)` per `ExerciseAnalyticsController.java:43`).

  Then a DTO sub-section: `LastPerformanceDto` and `ProgressPointDto` record components with their field types, semantics, and source (computed in `ExerciseAnalyticsService.summarize` vs returned verbatim from entity). For `ProgressPointDto`, explicitly list all six fields (`sessionId`, `startedAt`, `setCount`, `totalVolumeKg`, `maxWeightKg`, `topRepsDone`) and note which ROADMAP Phase 16 deliverables they cover: volume aggregation = `totalVolumeKg`, max weight = `maxWeightKg`, top reps = `topRepsDone`. Flag absent fields explicitly: NO `estimatedOneRmKg` (Epley projection), NO `prSetCount` (per-session PR count). Cite `ProgressPointDto.java:7-13` for the record shape.

  For `LastPerformanceDto`, list components (`sessionId`, `startedAt`, `endedAt`, `sets`) and note that the bundle re-uses `SessionSetDto` with `newPr=null` because `ExerciseAnalyticsService.lastPerformance` calls `SessionsMapper.toSetDto(s)` without the boolean overload (`ExerciseAnalyticsService.java:46`). State whether this matches the brief intent (brief line 307: "bu hareketi en son ne zaman, kac kg, kac tekrar yapti" - PR badge not implied; current shape is faithful).

  Constraint: read-only on Java source. Do not run mvn or modify any source file. The only file written by this task is `16-01-AUDIT.md`.
  </action>
  <verify>
  - `16-01-AUDIT.md` exists with frontmatter and Sections 1 and 2.
  - Section 1 lists all five Phase 16-owned files plus the three shared call sites with `file:line` citations.
  - Section 2 has one row per `@GetMapping` in `ExerciseAnalyticsController` (two rows expected).
  - DTO sub-section lists every component of `LastPerformanceDto` and `ProgressPointDto` with file:line citations.
  - Absent fields are explicitly flagged (`NO estimatedOneRmKg`, `NO prSetCount`).
  - `git diff --stat` shows zero changes under `backend/`.
  </verify>
  <done>
  Sections 1 and 2 give the reader a complete view of Phase 16's current scaffold without opening any source file.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProjectBrief + ROADMAP gap matrix and package-boundary verdict</name>
  <files>.planning/phases/16-sessions-analytics/16-01-AUDIT.md</files>
  <action>
  Read `ProjectBrief.md` lines 297-318 (Phase 4 endpoint list + offline-first strategy + Phase 4 outputs) and `.planning/ROADMAP.md` lines 62-66 (Phase 16 goal + research note). Append Sections 3 and 4 to the audit file.

  Section 3 - ProjectBrief Phase 4 + ROADMAP Phase 16 Gap Matrix:
  Two-part table.

  Part A: ProjectBrief Phase 4 special endpoints (lines 305-308) vs current state.
    - Row 1: `GET /api/exercises/:id/last-performance`. Status `Implemented`. Evidence: `ExerciseAnalyticsController.java:30`. Notes: bundle shape matches brief intent ("en son ne zaman, kac kg, kac tekrar"); 200/204 split is a defensible refinement.
    - Row 2: `GET /api/exercises/:id/progress`. Status `Implemented`. Evidence: `ExerciseAnalyticsController.java:39`. Notes: per-session aggregate. brief is silent on per-row shape; ROADMAP Phase 16 specifies the deliverables (next part).

  Part B: ROADMAP Phase 16 deliverables (`ROADMAP.md:62-66`) vs current state.
    - "PR computation": `Implemented at create-time`. Evidence: `SessionSetsService.add` at `SessionSetsService.java:80-82` invokes `PrDetector.beatsPriorBest`; Phase 15-04 documented create-only `newPr` flag. Notes: PR is NOT durable across detail re-read (Phase 15-04 verdict; tested by `newPrFlagPresentOnCreateButOmittedOnDetailReread`). Phase 16 must decide: persist `is_pr` on `session_sets` (V27) OR keep create-only and rely on `/progress` for durable PR view.
    - "Volume aggregation": `Implemented`. Evidence: `ExerciseAnalyticsService.summarize` at `ExerciseAnalyticsService.java:73-76` computes `totalVolumeKg = sum(weightKg * repsDone)` per session. Notes: Java-side aggregation over full `findHistoricalByUserAndExercise` result; perf assessed in Section 4.
    - "1RM (Epley) projections at the query layer": `Partial - logic exists, DTO does not expose`. Evidence: `PrDetector.epleyOneRm` at `PrDetector.java:12` computes Epley; `SessionSetsService.bestPriorOneRm` at `SessionSetsService.java:105-115` uses it for the create-time PR check; but `ProgressPointDto` (`ProgressPointDto.java:7-13`) has NO `estimatedOneRmKg` component and `ExerciseAnalyticsService.summarize` does NOT compute it. Notes: closing this gap is the cheapest Phase 16 deliverable (one DTO field + one mapper line); test extension trivial.

  Then a "Field-level drift" subsection listing absent shapes with rationale:
    - `ProgressPointDto.estimatedOneRmKg` (BigDecimal): missing. Phase 16 ROADMAP deliverable. Should equal `PrDetector.epleyOneRm(maxWeightKg, topRepsDone)` per session.
    - `ProgressPointDto.prSetCount` (int) or `ProgressPointDto.hasPr` (boolean): missing. Optional, depends on PR-durability verdict in Section 5. State as "candidate, not required by brief or roadmap; defer if PR durability stays create-only."
    - `LastPerformanceDto.sets[*].newPr`: always null on this read path because the bundle calls `toSetDto(s)` without the boolean overload (`ExerciseAnalyticsService.java:46`). State: "intentional - last-performance is a `current shape` view, not a PR view; do not flip without explicit user-facing motivation."

  Section 4 - Package-Boundary Verdict + Perf Baseline:

  Part A - Package-Boundary Verdict:
  Decide whether Phase 16 should extract analytics out of `sessions/` into a new `com.workouthub.analytics` feature package, or formalize ownership in place. Walk both directions explicitly:

  - Extract direction: move `ExerciseAnalyticsController.java`, `ExerciseAnalyticsService.java`, `LastPerformanceDto.java`, `ProgressPointDto.java` to `analytics/`. `PrDetector.java` is a judgment call: it is invoked from `SessionSetsService.add` (Phase 15 surface) so moving it forces a cross-package import on the hot path. Repository method `SessionSetRepository.findHistoricalByUserAndExercise` MUST stay in `sessions/` because it queries `SessionSet`/`WorkoutSession` entities owned by sessions; analytics imports the existing repo. Pros: matches CONVENTIONS feature-sliced layout, mirrors `metrics/` and `export/` patterns, makes Phase 31 (charts-stats) extension natural. Cons: 5 file moves + import churn across `SessionSetsService` (PrDetector reference) and 2 test classes + 1 integration test; risk of merge conflicts with any in-flight Phase 15 follow-up; Spring component scan is unaffected (root package is `com.workouthub`).
  - Stay-in-place direction: keep all five files inside `sessions/` and document the boundary via Javadoc package-info or a Phase 16-01 SUMMARY note. Pros: zero churn, mirrors current shipping shape, consistent with Phase 15-04's "Javadoc-as-binding-contract" pattern. Cons: violates feature-slicing convention; future analytics endpoints (Phase 31 charts-stats) accumulate in `sessions/` and the boundary erodes.

  Recommendation: pick one with rationale. Suggest extract direction if the audit confirms (a) `SessionSetsService` is the only cross-package consumer of `PrDetector`, (b) no other backend feature imports `LastPerformanceDto` or `ProgressPointDto`, (c) Phase 31 (charts-stats) per ROADMAP is the next analytics workload and benefits from the new package. Verify (a) and (b) by `grep -r "PrDetector\|LastPerformanceDto\|ProgressPointDto" backend/src/main/` and record findings inline.

  Part B - Perf Baseline:
  Walk `ExerciseAnalyticsService.progress` (`ExerciseAnalyticsService.java:55-69`):
  1. Calls `sets.findHistoricalByUserAndExercise(userId, exerciseId)` returning ALL completed sets across ALL finished sessions for this user+exercise (no LIMIT in the JPQL at `SessionSetRepository.java:21-30`).
  2. Java-side group-by session id via `LinkedHashMap`.
  3. `.limit(limit)` AFTER the group-by, so the SQL still scans every row.
  Identify the perf cost: assuming a power user with 200 finished sessions averaging 4 sets per exercise (= 800 rows), a single `/progress?limit=10` request fetches 800 rows just to use the most recent 10 groups. Lazy `@ManyToOne` on `SessionSet.session` (`SessionSet.java:26-28`) means each unique session_id triggers a SELECT on `workout_sessions` unless eager-loaded - quantify: in the Java `summarize`, `setsInSession.get(0).getSession()` triggers the lazy load per group => up to 200 SELECTs.

  State whether this is "acceptable for v0.4" (single user, low cardinality, `@Transactional(readOnly = true)` keeps a session open) or "must be pushed down to SQL" (use a JPQL aggregate `SELECT new ProgressRow(session.id, session.startedAt, COUNT(*), SUM(weightKg * repsDone), MAX(weightKg), MAX(repsDone)) FROM SessionSet s ... GROUP BY session.id ORDER BY session.startedAt DESC LIMIT :limit`). Walk both directions; recommend a verdict with rationale tied to the v0.4 milestone scope (single self-hosted user, not a multi-tenant SaaS). Note that ROADMAP says "1RM ... at the query layer" - clarify whether "query layer" means SQL or "the analytics query path in Java"; lean on convention here (the project has used Java-side aggregation in `WorkoutPlansService` and `MetricsService` too) and propose Java-side as the v0.4 default with a Phase 31 deferral marker for SQL pushdown if charts-stats workload requires it.

  Cite `SessionSetRepository.findHistoricalByUserAndExercise` (`SessionSetRepository.java:21-30`) for the current query; cite the `summarize` invocation (`ExerciseAnalyticsService.java:65-67`) for the Java-side fan-out.

  Constraint: no Java files modified. The grep confirmation in Part A is read-only.
  </action>
  <verify>
  - Section 3 Part A has both ProjectBrief endpoint rows with `file:line` evidence.
  - Section 3 Part B has all three ROADMAP Phase 16 deliverables (PR computation, volume aggregation, Epley projections) with status verdict (`Implemented` / `Partial` / `Implemented at create-time`).
  - Field-level drift subsection lists at least `estimatedOneRmKg` as missing with rationale.
  - Section 4 Part A walks both extract and stay-in-place directions and ends with a single recommendation backed by grep evidence (cite the grep command + a one-line summary of hits).
  - Section 4 Part B walks the `progress` query path with a row-count estimate and ends with a single perf verdict (acceptable for v0.4 OR must push down to SQL).
  - `git diff --stat` shows only `.planning/phases/16-sessions-analytics/16-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 3 and 4 give plan 16-02+ a concrete contract-level fix list (Epley DTO field, optional PR durability) and a settled perf direction.
  </done>
</task>

<task type="auto">
  <name>Task 3: PR durability decision and recommended Phase 16 plan-02+ scope</name>
  <files>.planning/phases/16-sessions-analytics/16-01-AUDIT.md</files>
  <action>
  Append Sections 5 and 6 to the audit file.

  Section 5 - PR Durability Decision:

  Phase 15-04 documented create-only `newPr` (test: `newPrFlagPresentOnCreateButOmittedOnDetailReread`). Phase 16 owns the durability decision per Phase 15-01 audit Section 6 D1 / FG2. Walk both directions:

  - Persist direction: add `is_pr BOOLEAN NOT NULL DEFAULT false` column to `session_sets` via V27 migration; backfill existing rows by computing best-1RM-per-(userId, exerciseId) and marking that set's row `is_pr = true` (one-shot SQL inside the migration); add `boolean isPr` field on `SessionSet` entity; flip `SessionSetsService.add` to set `set.setPr(isPr)` before persist; flip `SessionsMapper.toSetDto(set)` to read `set.isPr() ? Boolean.TRUE : null`; remove the `(set, Boolean newPr)` overload (or keep it as a no-op for back-compat). Update `newPrFlagPresentOnCreateButOmittedOnDetailReread` to assert PR survives detail re-read. Pros: closes Phase 15-04 contract gap; offline drainer's GET-after-POST shows the PR badge correctly; one-time backfill correctly attributes existing PRs. Cons: V27 migration with backfill SQL is non-trivial (window function or correlated subquery to find best-Epley per (user, exercise)); update path on `PUT /sets/:setId` becomes stateful (must re-evaluate is_pr if reps/weight change); coverage cost = 2-3 new integration tests.

  - Stay-create-only direction: keep `newPr` as a flash flag on the create response only; rely on `/api/exercises/{id}/progress` for the durable PR view; Phase 16 adds an explicit `prSessionId` field on `ProgressPointDto` (or recomputes per-session "was-pr" via Epley over the result set) for the frontend's PR list. Pros: no migration; no update-path complexity; matches the brief shape (PRs surfaced by progress, not by set list). Cons: PR badge UX is lost on detail re-read after offline-queue drain; frontend must always cross-reference `/progress` to render the PR badge.

  Recommendation: pick one. Suggest persist direction if (a) audit confirms the V27 backfill is feasible with a single window-function SQL (`MAX(epley) OVER (PARTITION BY user_id, exercise_id)`), (b) the `PUT /sets/:setId` update path is rare enough that re-evaluation cost is acceptable, (c) Phase 31 (charts-stats) and Phase 25 (session-execution UI) both want a stable PR badge. Verify feasibility (a) by drafting (NOT executing) the backfill SQL inline in the audit. Note that Postgres 16 supports window functions natively, but Epley = `weight * (1 + reps/30)` so the window must order by an SQL-side Epley expression (no UDF needed). State whether the migration test (`V27MigrationTest`) needs to seed pre-PR rows then assert `is_pr=true` on the expected one.

  Section 6 - Recommended Phase 16 plan-02+ Scope:

  Bulleted list of concrete tasks plan 16-02+ should plan, each with: target files, expected change shape (1-3 lines or "rewrite N lines" or "new file: lines ~N"), and which test file picks up the regression coverage. Group by bucket:
    - `package-extraction` - directory move + import update (depends on Section 4 Part A verdict).
    - `epley-projection` - `ProgressPointDto.estimatedOneRmKg` field + service computation + test extension (depends on Section 3 Part B "1RM" gap).
    - `pr-durability` - V27 migration + entity field + service mutation + mapper read + test (depends on Section 5 verdict; if stay-create-only, this bucket is empty and the alternative is `progress-prSessionId` enrichment).
    - `perf-pushdown` - SQL aggregate query for `/progress` (depends on Section 4 Part B verdict; likely deferred).
    - `defer` - explicit non-goals for v0.4 (Phase 31 charts-stats SQL pushdown, Phase 25 frontend integration).

  For each `package-extraction` and `pr-durability` entry, write a one-paragraph rationale tied to a specific section verdict (e.g. "Section 4 Part A recommends extract; touches: ExerciseAnalyticsController, ExerciseAnalyticsService, dto/LastPerformanceDto, dto/ProgressPointDto, optionally PrDetector").

  Plan-count recommendation: how many plans (16-02, 16-03, ...) the hardening warrants given comprehensive depth (`config.json: depth: comprehensive`) and the Phase 14/15 precedent (one bug-shape per plan; migration in its own plan if independent of code-level changes). Recommend explicit plan boundaries with one-line objective per plan.

  Suggested boundary template (the audit may revise):
  - Plan 16-02: package-extraction (analytics/ package). Depends on Section 4 Part A verdict.
  - Plan 16-03: epley-projection (ProgressPointDto.estimatedOneRmKg). Depends on Section 3 Part B verdict.
  - Plan 16-04: pr-durability (V27 + entity + service + mapper + tests). Depends on Section 5 verdict (skip if stay-create-only).

  If Section 5 verdict is stay-create-only, replace Plan 16-04 with `Plan 16-04: progress-prEnrichment` (`ProgressPointDto.prSessionId` field + per-session PR detection in the progress query). Phase 16 closes either way.

  Constraint: do not edit any Java file. Do not propose architectural rewrites - the goal is the smallest set of changes that closes the gaps and respects the v0.4 budget.

  Final length check: target under 500 lines for the entire `16-01-AUDIT.md`. Trim verbose narrative; tables and ranked lists belong here, not prose.
  </action>
  <verify>
  - Section 5 walks both persist and stay-create-only directions and ends with a single recommendation backed by feasibility evidence (V27 backfill SQL draft inline if persist is recommended).
  - Section 6 has one entry per applicable bucket; empty buckets are stated explicitly as "None - no findings" or "Deferred to Phase 31."
  - Plan-count recommendation is explicit: `Plan 16-02: ...`, `Plan 16-03: ...`, etc., each with a one-line objective.
  - Every `package-extraction` and `pr-durability` rationale paragraph references the section that produced the verdict (Section 4 Part A or Section 5).
  - Total `16-01-AUDIT.md` length is under 500 lines.
  - `git diff --stat` shows only `.planning/phases/16-sessions-analytics/16-01-AUDIT.md` changed.
  </verify>
  <done>
  Sections 5 and 6 give plan 16-02+ a file-level scope, an unambiguous PR durability verdict, and explicit plan boundaries.
  </done>
</task>

</tasks>

<verification>
Before declaring the plan complete:
- [ ] `16-01-AUDIT.md` exists and contains Sections 1-6.
- [ ] Every controller method, service method, DTO record, and repository query referenced cites `file:line`.
- [ ] Section 2 lists every component of `LastPerformanceDto` and `ProgressPointDto`.
- [ ] Section 3 Part A covers both ProjectBrief Phase 4 special endpoints; Part B covers all three ROADMAP Phase 16 deliverables.
- [ ] Section 4 Part A delivers a single package-boundary recommendation with grep evidence.
- [ ] Section 4 Part B delivers a single perf verdict with row-count estimation.
- [ ] Section 5 delivers a single PR durability verdict (persist OR stay-create-only) with feasibility evidence.
- [ ] Section 6 buckets are concrete and either populated with file-level entries or stated as empty.
- [ ] Plan-count recommendation in Section 6 is explicit so plan 16-02+ structure is decided.
- [ ] `git diff --stat` shows only `.planning/phases/16-sessions-analytics/` changes.
</verification>

<success_criteria>
- All three tasks complete.
- `16-01-AUDIT.md` is the single Java-free deliverable.
- Phase 16 plan-02+ has a concrete, file-level fix list bucketed by intent.
- Package-boundary direction (extract to `analytics/` vs stay-in-place) is settled with rationale.
- PR durability direction (persist `is_pr` vs stay-create-only) is settled with rationale.
- Perf direction (Java-side vs SQL pushdown) is settled with row-count evidence.
- No Java sources, migrations, tests, or other planning files are modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/16-sessions-analytics/16-01-SUMMARY.md` with frontmatter and body following the Phase 15-01 audit summary shape:

---
phase: 16-sessions-analytics
plan: 01
subsystem: analytics
tags: [audit, gap-analysis, package-boundary, pr-durability, planning]

requires:
  - phase: 15-sessions-core
    plan: 01
    reason: ownership map (Section 5) and Phase 16 scope-leak inventory inherited from this plan
  - phase: 15-sessions-core
    plan: 04
    reason: create-only newPr verdict and SessionSetsIntegrationTest.newPrFlagPresentOnCreateButOmittedOnDetailReread regression baseline that Phase 16 PR durability work must respect or replace

provides:
  - analytics scaffold inventory (5 Phase 16-owned files + 3 shared call sites)
  - endpoint + DTO catalog (2 endpoints, 2 response DTOs)
  - ProjectBrief Phase 4 + ROADMAP Phase 16 gap matrix
  - package-boundary verdict (extract OR stay-in-place)
  - perf baseline analysis with row-count estimate
  - PR durability verdict (persist V27 OR stay-create-only)
  - recommended Phase 16 plan-02+ scope (package-extraction / epley-projection / pr-durability / perf-pushdown / defer buckets)
  - explicit plan boundaries with one-line objective per plan

affects:
  - 16-sessions-analytics/16-02
  - 16-sessions-analytics/16-03
  - 16-sessions-analytics/16-04 (or whatever the final plan-count resolves to)
  - 31-charts-stats (perf-pushdown deferral marker)

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/16-sessions-analytics/16-01-AUDIT.md
  modified: []

key-decisions:
  - "[Package-boundary verdict: extract to com.workouthub.analytics OR stay-in-place inside sessions/]"
  - "[PR durability verdict: persist is_pr via V27 OR stay create-only and enrich progress DTO]"
  - "[Perf verdict: Java-side aggregation acceptable for v0.4 OR push down to SQL]"
  - "[Plan-count for Phase 16 hardening: N plans (16-02..16-0N) with explicit boundaries]"
---

# Phase 16 Plan 01: sessions-analytics audit Summary

**[One-line audit outcome - what landed, what is now planning-ready]**

## Accomplishments
- Inventory complete: N Phase 16-owned files + M shared call sites under `sessions/`.
- Endpoint + DTO catalog: 2 endpoints, 2 response DTOs, X record components.
- ProjectBrief Phase 4 special endpoints: 2-of-2 implemented; ROADMAP Phase 16 deliverables: K-of-3 fully landed, L partial.
- Package-boundary verdict: [extract / stay-in-place] with rationale.
- Perf baseline: [acceptable for v0.4 / must push down to SQL] with row-count evidence.
- PR durability verdict: [persist V27 / stay create-only] with feasibility evidence.
- Phase 16 plan-02+ scope: K package-extraction, L epley-projection, M pr-durability, N defer.
- Plan-count recommendation: 16-02..16-0N with one-line objective each.

## Files Created/Modified
- `.planning/phases/16-sessions-analytics/16-01-AUDIT.md` - audit deliverable.

## Decisions Made
- Package-boundary verdict.
- PR durability verdict.
- Perf verdict.
- Phase 16 plan boundaries.

## Issues Encountered
- Any inconsistency found between codebase docs and actual code.
- Any ProjectBrief drift requiring Phase 19 (api-contract-docs) clarification.

## Next Phase Readiness
- Ready for Phase 16 plan-02+ planning: yes / no with reason.
- Open questions for plan 16-02+: list any.
</output>
