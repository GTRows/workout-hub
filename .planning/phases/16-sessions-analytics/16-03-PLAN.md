---
phase: 16-sessions-analytics
plan: 03
type: execute
domain: backend-analytics
---

<objective>
Append `estimatedOneRmKg` (Epley 1RM projection) as the 7th record component on `ProgressPointDto`, and populate it in `ExerciseAnalyticsService.summarize` by picking the maximum `PrDetector.epleyOneRm` across all sets in the session group.

Purpose: Closes the ROADMAP Phase 16 deliverable "1RM (Epley) projections at the query layer" (`.planning/ROADMAP.md:64`). `PrDetector.epleyOneRm` already exists and is invoked at `SessionSetsService.bestPriorOneRm` for create-time PR detection, but is never materialized into the read-side `/progress` shape. This is the cheapest Phase 16 deliverable per audit Section 6 EP1 - one DTO field plus one mapper line plus one test extension.

Output:
- `ProgressPointDto` gains `BigDecimal estimatedOneRmKg` as a 7th record component (appended; Jackson serialization is name-keyed for clients, so position is internal-only).
- `ExerciseAnalyticsService.summarize` computes the max Epley across all sets in the group via `PrDetector.epleyOneRm`, picking the top-Epley set per session (matches `analytics/AnalyticsService.oneRepMax` precedent at `AnalyticsService.java:113-123`).
- `ExerciseAnalyticsIntegrationTest.progressReturnsPerSessionSummariesNewestFirst` asserts `$[0].estimatedOneRmKg`.
- New test `progressEstimatedOneRmIsNullForUnweightedSets` covers the `weightKg == null` path (Epley returns null per `PrDetector.java:13`).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sessions-analytics/16-01-AUDIT.md
@.planning/phases/16-sessions-analytics/16-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@backend/src/main/java/com/workouthub/analytics/dto/ProgressPointDto.java
@backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsService.java
@backend/src/main/java/com/workouthub/analytics/PrDetector.java
@backend/src/test/java/com/workouthub/analytics/ExerciseAnalyticsIntegrationTest.java

**Tech stack available:** Java 21 records, Spring Boot 3.5.x, JUnit 5, Testcontainers (real Postgres), MockMvc + JSONPath, AssertJ. No new dependencies.

**Established patterns (carry from prior phases):**
- DTO records under `<feature>/dto/`; never return entities (CONVENTIONS.md).
- Java-side aggregation in analytics query path (audit Section 4 Part B verdict; matches `AnalyticsService.weeklyVolume`/`oneRepMax`/`personalRecords`/`heatmap`).
- Per-top-set Epley semantic (max Epley across sets in a session) from `analytics/AnalyticsService.java:113-123` precedent. Do NOT use `epleyOneRm(maxWeight, topReps)` because `maxWeight` and `topReps` may belong to different sets, producing a non-physical 1RM.
- `PrDetector.epleyOneRm` returns `null` when `weightKg == null` or `reps <= 0` (`PrDetector.java:13`); aggregation must filter nulls so an unweighted-set group yields `estimatedOneRmKg = null`, not zero.
- Test integration pattern: `AbstractIntegrationTest` + `@AutoConfigureMockMvc` + `TestAuthHelpers.seed`; assert via `jsonPath` (TESTING.md).

**Constraining decisions:**
- 16-02 SUMMARY: `ProgressPointDto` is 6 components today; record component order preserved during the package move; appending the 7th component is what 16-02 explicitly deferred to 16-03.
- 16-01 AUDIT Section 4 Part B: SQL pushdown deferred to Phase 31. Stay Java-side for v0.4.
- 16-01 AUDIT Section 6 EP1: pick "per-top-set Epley" semantic, document the choice in service Javadoc.
- 16-01 AUDIT Section 6 D5: `ApiError` codes on 404 (unknown exerciseId) explicitly out of scope.
- CLAUDE.md: standing rules - English-only code/comments/identifiers, no emojis, conventional commits, no Co-Authored-By trailers, English-only commit subjects.
- Local Maven gap (memory: local_maven_gap): backend tests run in CI only on this Windows host; mvn verify gate is delegated to CI.

**Issues being addressed:** None (no open ISSUES.md entry for this work; tracked entirely via Phase 16 audit Section 6 EP1).

**Concerns being verified:** Phase 16 ROADMAP "1RM (Epley) projections at the query layer" gap closes with this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append estimatedOneRmKg to ProgressPointDto and compute in summarize</name>
  <files>backend/src/main/java/com/workouthub/analytics/dto/ProgressPointDto.java, backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsService.java</files>
  <action>
1. Edit `ProgressPointDto.java` to append `BigDecimal estimatedOneRmKg` as the 7th and final record component. Order: `(sessionId, startedAt, setCount, totalVolumeKg, maxWeightKg, topRepsDone, estimatedOneRmKg)`. Keep existing imports; do NOT add a Javadoc block (the existing record has none and CONVENTIONS says only "why" comments).

2. Edit `ExerciseAnalyticsService.summarize` (private static, currently lines 72-94):
   - After the existing `topReps` computation, add a `BigDecimal estimatedOneRm` computed as the max non-null `PrDetector.epleyOneRm(s.getWeightKg(), s.getRepsDone())` across all `setsInSession`. Use `setsInSession.stream().map(s -> PrDetector.epleyOneRm(s.getWeightKg(), s.getRepsDone())).filter(Objects::nonNull).max(Comparator.naturalOrder()).orElse(null)`.
   - Pass `estimatedOneRm` as the 7th argument to the `new ProgressPointDto(...)` constructor.
   - `PrDetector` is already in the same package (`com.workouthub.analytics`) post-16-02 so no import needed; `BigDecimal`, `Comparator`, `Objects` are already imported.

3. Add a single-line `// why` comment above the Epley aggregation explaining the per-top-set semantic vs the cheaper-but-incorrect `epleyOneRm(maxWeight, topReps)` shortcut. Audit Section 6 EP1 explicitly calls for documenting the choice.

What to AVOID and WHY:
- Do NOT introduce `epleyOneRm(maxWeight, topReps)` shortcut: `maxWeightKg` and `topRepsDone` may come from different sets in the same session, producing a non-physical 1RM that doesn't match `AnalyticsService.oneRepMax` precedent.
- Do NOT change record component order or rename any existing component: clients (frontend Zod schemas, eventual export) read by name, but backwards-compatibility on the JSON shape demands existing fields stay where they are; appending is safe.
- Do NOT collapse to a single stream that recomputes maxWeight/topReps: keep the three independent reductions (totalVolume, maxWeight, topReps, estimatedOneRm) so the diff stays readable and behavior is bisectable.
- Do NOT promote `summarize` from `private static` to instance: zero call sites outside the file.
- Do NOT touch `LastPerformanceDto` or `lastPerformance(...)`: audit Section 3 is explicit that `last-performance` is intentionally a "current shape" view, not a PR/projection view.
  </action>
  <verify>
- `git diff -- backend/src/main/java/com/workouthub/analytics/dto/ProgressPointDto.java` shows exactly one new component line `BigDecimal estimatedOneRmKg`, comma added on the previous line.
- `git diff -- backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsService.java` shows the new aggregation block, the constructor call's 7th arg, and the why-comment; no other behavior changes.
- Grep `rg "new ProgressPointDto"` returns exactly one call site (in `summarize`); the 7-arg signature compiles by inspection of the record header.
- Grep `rg "ProgressPointDto" backend/src` confirms only `ExerciseAnalyticsService.java` constructs it (no test file directly instantiates the record - tests assert via JSON only).
- mvn compile/test gate delegated to CI per local-maven-gap memory; record any local mvn output if available, otherwise note "delegated to CI" in the summary.
  </verify>
  <done>
ProgressPointDto has 7 components ending with `BigDecimal estimatedOneRmKg`. ExerciseAnalyticsService.summarize computes the max non-null Epley across all sets in the group and passes it as the 7th constructor arg. Why-comment documents the per-top-set semantic. No other files touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ExerciseAnalyticsIntegrationTest with Epley assertions</name>
  <files>backend/src/test/java/com/workouthub/analytics/ExerciseAnalyticsIntegrationTest.java</files>
  <action>
1. Extend the existing `progressReturnsPerSessionSummariesNewestFirst` test (lines 76-102) with two new assertions on the JSON path response:
   - `$[0].estimatedOneRmKg`: session s2 has one set (12 reps @ 55kg), Epley = 55 * (1 + 12/30) = 77.000. Assert via `jsonPath("$[0].estimatedOneRmKg").value(77.000)`.
   - `$[1].estimatedOneRmKg`: session s1 has two sets (10 reps @ 50kg each), Epley per set = 50 * (1 + 10/30) = 66.667 (rounded HALF_UP at scale=3 per `PrDetector.java:17`). Both sets identical so max-Epley = 66.667. Assert via `jsonPath("$[1].estimatedOneRmKg").value(66.667)`.
   - JSONPath value matchers compare via Jackson's `BigDecimal` deserialization; passing a `double` literal works because JSONPath compares numerically. Use 3 decimal places to mirror `PrDetector.epleyOneRm`'s `setScale(3, RoundingMode.HALF_UP)`.

2. Add a new test method `progressEstimatedOneRmIsNullForUnweightedSets` after `limitCountIsAtLeastOne` (last test in file, currently line 200). The test must:
   - Seed a user via `helpers.seed`, seed an exercise via `seedExercise`.
   - Use a private helper `addUnweightedSet(auth, sessionId, exerciseId, setNumber, reps)` that posts a set with `weightKg` omitted from the JSON body (rather than `weightKg=0`). Body shape: `{"exerciseId":"<id>","setNumber":<n>,"repsDone":<r>}`. Assert 201 on the POST.
   - Start a session, add one unweighted set (8 reps), finish, then GET `/api/exercises/{id}/progress?limit=10` and assert `$.length()==1`, `$[0].setCount==1`, `$[0].topRepsDone==8`, `$[0].maxWeightKg` is null (jsonPath assertion: `jsonPath("$[0].maxWeightKg").doesNotExist()` if Jackson NON_NULL omits null BigDecimals at field level - verify by inspecting existing serialization config; otherwise use `jsonPath("$[0].maxWeightKg").value(org.hamcrest.Matchers.nullValue())`).
   - Assert `$[0].estimatedOneRmKg` is null via `jsonPath("$[0].estimatedOneRmKg").value(org.hamcrest.Matchers.nullValue())` (or `.doesNotExist()` if Jackson omits null fields globally).
   - Reuse existing `startSession` and `finish` helpers; only `addUnweightedSet` is new.

3. Keep the existing `addSet(...)` helper unchanged (still used by 4 other tests).

What to AVOID and WHY:
- Do NOT modify other tests in the file (lastPerformance*, progressLimitClampedByLimitParam, progressOnAnotherUsersExerciseIsEmptyForCaller, unknownExerciseIdReturns404, limitCountIsAtLeastOne) - their fixtures and assertions are independent of the Epley field.
- Do NOT add a setup-shared seed in `@BeforeEach`: tests are intentionally self-contained (each seeds via nanoTime to avoid cross-test bleed; matches TESTING.md pattern).
- Do NOT compute Epley values in the test using `PrDetector.epleyOneRm` then assert equality: hard-code the expected decimal (77.000, 66.667) to lock the contract; if PrDetector behavior changes accidentally the test must catch it.
- Do NOT use `.isEqualTo(BigDecimal.valueOf(77.0))`: JSONPath compares numerically against the JSON value; mixing types via the AssertJ wrapper masks rounding bugs.
- Do NOT assert on `$[0].estimatedOneRmKg` shape (e.g., regex on the string form): assert numeric equality at the documented `setScale(3)` precision.
  </action>
  <verify>
- `git diff -- backend/src/test/java/com/workouthub/analytics/ExerciseAnalyticsIntegrationTest.java` shows: 2 new assertion lines inside `progressReturnsPerSessionSummariesNewestFirst`; one new `progressEstimatedOneRmIsNullForUnweightedSets` test method; one new `addUnweightedSet` private helper.
- `rg "estimatedOneRmKg" backend/src/test` returns at least 3 hits (2 in the extended test, 1+ in the new test).
- Test method count grows by 1 (was 7 `@Test`s in the file; now 8).
- mvn test gate delegated to CI per local-maven-gap memory; if mvn becomes available, run `./mvnw -q test -Dtest=ExerciseAnalyticsIntegrationTest` and capture the result.
  </verify>
  <done>
`progressReturnsPerSessionSummariesNewestFirst` asserts both Epley values (77.000 for s2, 66.667 for s1). New test `progressEstimatedOneRmIsNullForUnweightedSets` covers the null-weight path end-to-end. Test count: 8. No other tests changed.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git status --short` shows exactly 3 modified files: `ProgressPointDto.java`, `ExerciseAnalyticsService.java`, `ExerciseAnalyticsIntegrationTest.java`. No new files created.
- [ ] `rg "new ProgressPointDto" backend/src/main` returns exactly 1 hit (in `ExerciseAnalyticsService.summarize`) with 7 args.
- [ ] `rg "estimatedOneRmKg" backend/src` returns hits in: ProgressPointDto (1, the record component), ExerciseAnalyticsService (2: local var + constructor arg), ExerciseAnalyticsIntegrationTest (3+: 2 in extended test, 1+ in new test).
- [ ] No edits to protected files (`pom.xml`, lockfiles, compose.yml, Dockerfiles, GH workflows, scripts/).
- [ ] No edits outside the analytics package and its test class.
- [ ] mvn verify delegated to CI; commit message references the deferred local gate.
- [ ] Phase 16 plan-count: 4 plans, this is plan 03/04. Plan 16-04 (pr-durability) remains.
</verification>

<success_criteria>

- ROADMAP Phase 16 deliverable "1RM (Epley) projections at the query layer" closed: `ProgressPointDto.estimatedOneRmKg` is materialized in the `/api/exercises/{id}/progress` response for every session group.
- Per-top-set Epley semantic chosen and documented in source comment.
- Null-weight path covered by a dedicated integration test.
- Existing six progress fields unchanged in shape, type, ordering, and JSON serialization.
- Phase 16 audit Section 6 EP1 marker closed; D2 (Epley consolidation Phase 31) and D1/D3/D4/D5 deferred markers remain open.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-sessions-analytics/16-03-SUMMARY.md`:

# Phase 16 Plan 03: Epley Projection Summary

**[Substantive one-liner - what shipped, e.g., "Appended `estimatedOneRmKg` as 7th record component on `ProgressPointDto`; per-top-set Epley aggregation in `summarize`; integration test extended with one assertion + one null-weight test."]**

## Frontmatter

```yaml
---
phase: 16-sessions-analytics
plan: 03
subsystem: backend-analytics-readside
tags: [java, spring-boot, dto, analytics, epley, progress-endpoint]

requires:
  - phase: 16-sessions-analytics
    provides: 16-01 audit Section 6 EP1 + 16-02 package-extraction
provides:
  - ProgressPointDto.estimatedOneRmKg surfaced on /api/exercises/{id}/progress
  - per-top-set Epley aggregation pattern documented in source
affects: [16-04 pr-durability, 25 frontend session-execution, 31 charts-stats]

tech-stack:
  added: []
  patterns:
    - "Per-top-set Epley aggregation: max(epleyOneRm(weight, reps)) across sets in a session group"

key-files:
  modified:
    - backend/src/main/java/com/workouthub/analytics/dto/ProgressPointDto.java
    - backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsService.java
    - backend/src/test/java/com/workouthub/analytics/ExerciseAnalyticsIntegrationTest.java

key-decisions:
  - "Per-top-set Epley over (maxWeight, topReps) shortcut: maxWeight and topReps may belong to different sets"
  - "Append-only DTO change: existing 6 components keep position, new 7th component is BigDecimal estimatedOneRmKg"
  - "Null-weight => null Epley contract preserved end-to-end (PrDetector.epleyOneRm returns null; aggregation filters nulls)"

patterns-established:
  - "Pattern: read-side analytics DTOs accept null fields when the input physically yields null (Jackson NON_NULL omits at serialization)"

issues-created: []

duration: [fill]
completed: 2026-05-05
---
```

## Accomplishments

- ProgressPointDto record extended to 7 components.
- ExerciseAnalyticsService.summarize computes max-Epley per session via `PrDetector.epleyOneRm`.
- Integration test extended with two numeric assertions and one new null-weight test.

## Files Created/Modified

- `backend/src/main/java/com/workouthub/analytics/dto/ProgressPointDto.java` - appended `BigDecimal estimatedOneRmKg`
- `backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsService.java` - per-top-set Epley aggregation + 7th constructor arg
- `backend/src/test/java/com/workouthub/analytics/ExerciseAnalyticsIntegrationTest.java` - 2 new assertions + 1 new test + 1 new helper

## Decisions Made

- Per-top-set Epley semantic chosen and documented (matches `analytics/AnalyticsService.oneRepMax`).
- Null-weight contract: aggregation filters nulls, group with all-unweighted sets yields `estimatedOneRmKg = null`.

## Issues Encountered

[Fill at execution time, or "None - mechanical extension per audit EP1."]

## Deviations from Plan

[Fill at execution time.]

## Next Step

Plan 16-04 (pr-durability): V27 migration `is_pr BOOLEAN NOT NULL DEFAULT false` + backfill window-function update + entity field + service mutation + update-path re-evaluation + 3-4 integration test rewrites. Largest plan in phase.
</output>
