---
phase: 16-sessions-analytics
plan: 02
type: execute
---

<objective>
Extract the analytics scaffold from `com.workouthub.sessions` to the existing `com.workouthub.analytics` package per audit Section 4 Part A verdict and Section 6 PE1.

Purpose: Eliminate the cross-package leak that Phase 15-01 audit Section 5 flagged, co-locate per-exercise analytics with the existing `analytics/` package (Phase 31 territory landed pre-GSD), and unblock a future Epley consolidation between `PrDetector.epleyOneRm` and `AnalyticsService.epley`. This is a mechanical move with no behavior change.

Output: 5 source files plus 3 test files relocated to the analytics package; one import line added in `SessionSetsService`; full Maven test suite green under the new layout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sessions-analytics/16-01-AUDIT.md
@.planning/phases/15-sessions-core/15-01-AUDIT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@backend/src/main/java/com/workouthub/sessions/ExerciseAnalyticsController.java
@backend/src/main/java/com/workouthub/sessions/ExerciseAnalyticsService.java
@backend/src/main/java/com/workouthub/sessions/PrDetector.java
@backend/src/main/java/com/workouthub/sessions/dto/LastPerformanceDto.java
@backend/src/main/java/com/workouthub/sessions/dto/ProgressPointDto.java
@backend/src/main/java/com/workouthub/sessions/SessionSetsService.java
@backend/src/main/java/com/workouthub/sessions/SessionsMapper.java
@backend/src/main/java/com/workouthub/analytics/AnalyticsController.java
@backend/src/main/java/com/workouthub/analytics/AnalyticsService.java
@backend/src/test/java/com/workouthub/sessions/ExerciseAnalyticsIntegrationTest.java
@backend/src/test/java/com/workouthub/sessions/PrDetectorTest.java
@backend/src/test/java/com/workouthub/sessions/PrDetectionIntegrationTest.java

**Tech stack available:** Java 21, Spring Boot 3.5.x, Spring Data JPA, JUnit 5, Testcontainers (Postgres 16), Spring Security + JWT, Maven.

**Established patterns:**
- Feature-sliced packages under `com.workouthub.<feature>` (`auth`, `users`, `exercises`, `workouts`, `sessions`, `analytics`, etc.). Each owns Controller, Service, Repository, DTOs in `<feature>/dto/`.
- Public `static` mappers (e.g. `SessionsMapper`) callable cross-package without exposing entities outside their owning feature.
- `@Transactional(readOnly = true)` on read-side services.
- Component scan root is `com.workouthub`; analytics is already a peer of sessions.

**Constraining decisions:**
- Phase 16-01 audit Section 4 Part A verdict: extract to `com.workouthub.analytics`. Grep evidence: only `SessionSetsService` imports `PrDetector` outside the moved set; no other backend feature consumes the analytics DTOs.
- Phase 16-01 audit Section 6 PE1: 5 source files plus 3 test files; one-import touch on `SessionSetsService`; mechanical move with no behavior change.
- Public API paths (`/api/exercises/{id}/last-performance`, `/api/exercises/{id}/progress`) MUST stay byte-for-byte identical (controller `@RequestMapping` is unchanged - only the Java package containing the controller changes).

**Issues being addressed:** None from ISSUES.md (Phase 16 deliverable; not an open issue).

**Concerns being verified:**
- Phase 15-01 audit Section 5 in-package leaks (5 files / 191 lines) closed by relocation.
- Phase 15-04 SUMMARY note about Phase 16 owning formalization of these files honored.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move analytics scaffold to com.workouthub.analytics</name>
  <files>
    backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsController.java (new, replaces sessions/ExerciseAnalyticsController.java),
    backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsService.java (new, replaces sessions/ExerciseAnalyticsService.java),
    backend/src/main/java/com/workouthub/analytics/PrDetector.java (new, replaces sessions/PrDetector.java),
    backend/src/main/java/com/workouthub/analytics/dto/LastPerformanceDto.java (new, replaces sessions/dto/LastPerformanceDto.java),
    backend/src/main/java/com/workouthub/analytics/dto/ProgressPointDto.java (new, replaces sessions/dto/ProgressPointDto.java),
    backend/src/main/java/com/workouthub/sessions/SessionSetsService.java (add 1 import line),
    backend/src/test/java/com/workouthub/analytics/ExerciseAnalyticsIntegrationTest.java (new, replaces sessions/ExerciseAnalyticsIntegrationTest.java),
    backend/src/test/java/com/workouthub/analytics/PrDetectorTest.java (new, replaces sessions/PrDetectorTest.java),
    backend/src/test/java/com/workouthub/analytics/PrDetectionIntegrationTest.java (new, replaces sessions/PrDetectionIntegrationTest.java)
  </files>
  <action>
Mechanical relocation - no behavior changes, no logic edits, no DTO field additions, no API path changes. Steps:

1. Create the new files at the new paths and delete the old files in a single atomic operation. Each new file has the same content as the old file with these adjustments:

   a. Source file `analytics/ExerciseAnalyticsController.java`: change `package com.workouthub.sessions;` -> `package com.workouthub.analytics;`. Update DTO imports to `com.workouthub.analytics.dto.LastPerformanceDto` and `com.workouthub.analytics.dto.ProgressPointDto`. Keep `@RequestMapping("/api/exercises/{exerciseId}")` byte-for-byte identical (this is a public-API contract). Keep `AppUserPrincipal` import (already cross-package).

   b. Source file `analytics/ExerciseAnalyticsService.java`: change package decl to `com.workouthub.analytics;`. Update imports - drop the unqualified `SessionsMapper::toSetDto` reliance and add explicit `import com.workouthub.sessions.SessionsMapper;` (cross-package call works because both class and methods are public static). Update DTO imports: `LastPerformanceDto` and `ProgressPointDto` become same-package (drop the `import` lines for them). Keep `import com.workouthub.sessions.dto.SessionSetDto;` (still cross-package). Keep `SessionSet`, `SessionSetRepository`, `WorkoutSession` imports unchanged (already explicit cross-package). Keep `NotFoundException`, `ExerciseRepository` unchanged.

   c. Source file `analytics/PrDetector.java`: change package decl to `com.workouthub.analytics;`. No other changes (pure utility class, no Spring dependency, only `java.math` imports).

   d. Source file `analytics/dto/LastPerformanceDto.java`: change package decl to `com.workouthub.analytics.dto;`. Add `import com.workouthub.sessions.dto.SessionSetDto;` because `SessionSetDto` stays in `sessions/dto/` and is now cross-package.

   e. Source file `analytics/dto/ProgressPointDto.java`: change package decl to `com.workouthub.analytics.dto;`. No other changes (only `java.math.BigDecimal`, `java.time.Instant`, `java.util.UUID` imports).

   f. Edit `sessions/SessionSetsService.java`: add `import com.workouthub.analytics.PrDetector;` to the import block (alphabetically after `com.workouthub.achievements.AchievementEvaluator;`). The two call sites at lines 82 and 110 stay literally `PrDetector.beatsPriorBest(...)` and `PrDetector.epleyOneRm(...)` (resolved via the new import). Do NOT touch any other line in this file - the audit Section 6 PE1 explicitly scopes this to a one-import touch.

   g. Test file `analytics/PrDetectorTest.java`: change package decl to `com.workouthub.analytics;`. PrDetector reference stays unqualified (now same-package). No other changes.

   h. Test file `analytics/ExerciseAnalyticsIntegrationTest.java`: change package decl to `com.workouthub.analytics;`. The test never imports `ExerciseAnalyticsController/Service` or the DTOs by class name (it uses MockMvc + JsonPath), so no import additions are required. Do not change the test class name or any test method.

   i. Test file `analytics/PrDetectionIntegrationTest.java`: change package decl to `com.workouthub.analytics;`. Same MockMvc-driven shape; no class-name imports to update. Do not change test class name or method bodies.

2. Verify NO other file in `backend/src/main` or `backend/src/test` references the old fully-qualified names (`com.workouthub.sessions.PrDetector`, `com.workouthub.sessions.ExerciseAnalyticsController`, `com.workouthub.sessions.ExerciseAnalyticsService`, `com.workouthub.sessions.dto.LastPerformanceDto`, `com.workouthub.sessions.dto.ProgressPointDto`). If any are found, update the import. (Audit Section 4 Part A grep confirmed only `SessionSetsService` qualifies, but re-verify after the move.)

What to AVOID and WHY:
- Do NOT add new fields to any DTO. `ProgressPointDto.estimatedOneRmKg` is the deliverable of Plan 16-03, not 16-02.
- Do NOT touch `SessionsMapper` overload signatures or remove the `(SessionSet, Boolean newPr)` variant. PR-durability work is Plan 16-04.
- Do NOT change `@RequestMapping` paths, query-param names, response shapes, or status codes. Spring Boot routes by annotation, not by Java package - the public API surface MUST be identical.
- Do NOT alter the body of `bestPriorOneRm` in `SessionSetsService` or any logic line. The only edit in that file is one new import.
- Do NOT introduce a Javadoc package-info.java or a back-compat re-export shim for the old `com.workouthub.sessions.PrDetector` symbol. Audit Section 4 Part A explicitly favors a clean directory move.
- Do NOT reorder DTO record components - clients consume by name, but the audit's Phase 16-03 deliverable appends `estimatedOneRmKg` as the 7th component, so leave the 6-component shape intact.
  </action>
  <verify>
Build compiles cleanly with the new package layout:
```
cd backend && ./mvnw -q -DskipTests compile test-compile
```

No file under `backend/src` (excluding the planning directory) still references `com.workouthub.sessions.PrDetector`, `com.workouthub.sessions.ExerciseAnalyticsController`, `com.workouthub.sessions.ExerciseAnalyticsService`, `com.workouthub.sessions.dto.LastPerformanceDto`, or `com.workouthub.sessions.dto.ProgressPointDto`:
```
grep -rE "com\.workouthub\.sessions\.(PrDetector|ExerciseAnalyticsController|ExerciseAnalyticsService|dto\.(LastPerformanceDto|ProgressPointDto))" backend/src
# Expect: no matches
```

`SessionSetsService` has exactly one new import line and no other diff:
```
git diff --stat backend/src/main/java/com/workouthub/sessions/SessionSetsService.java
# Expect: 1 file changed, 1 insertion(+)
```

Files exist at the new paths:
```
ls backend/src/main/java/com/workouthub/analytics/{ExerciseAnalyticsController,ExerciseAnalyticsService,PrDetector}.java
ls backend/src/main/java/com/workouthub/analytics/dto/{LastPerformanceDto,ProgressPointDto}.java
ls backend/src/test/java/com/workouthub/analytics/{ExerciseAnalyticsIntegrationTest,PrDetectorTest,PrDetectionIntegrationTest}.java
```

Old source paths are gone:
```
ls backend/src/main/java/com/workouthub/sessions/{ExerciseAnalyticsController,ExerciseAnalyticsService,PrDetector}.java 2>&1
ls backend/src/main/java/com/workouthub/sessions/dto/{LastPerformanceDto,ProgressPointDto}.java 2>&1
ls backend/src/test/java/com/workouthub/sessions/{ExerciseAnalyticsIntegrationTest,PrDetectorTest,PrDetectionIntegrationTest}.java 2>&1
# Expect: all "No such file or directory"
```
  </verify>
  <done>
- 5 source files relocated to `com.workouthub.analytics` (3 in package root, 2 under `analytics/dto/`).
- 3 test files relocated to `com.workouthub.analytics` test package.
- `SessionSetsService` has exactly one added import (`com.workouthub.analytics.PrDetector`); no other change in that file.
- `git status` shows 5 source moves, 3 test moves, 1 source edit; no behavior diffs.
- `./mvnw compile test-compile` succeeds.
- No grep hits for the old fully-qualified names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify endpoint contracts unchanged</name>
  <files>none (verification-only)</files>
  <action>
Run the full backend Maven verify cycle and confirm the relocated tests still cover the analytics surface end-to-end. The relocation is mechanical, so EVERY test that passed before MUST still pass under the new layout. Any failure indicates an import or wiring slip from Task 1 - fix it, do not adjust the test expectations.

Steps:
1. Run `cd backend && ./mvnw -q test` - full suite.
2. If any test fails, diagnose by class name:
   - `analytics/PrDetectorTest` failure -> Task 1 broke the unqualified `PrDetector` reference; verify package decl matches new directory.
   - `analytics/ExerciseAnalyticsIntegrationTest` failure -> Spring component scan missed the relocated `@RestController`/`@Service`. Verify package starts with `com.workouthub` (component scan root).
   - `analytics/PrDetectionIntegrationTest` failure -> `SessionSetsService` import edit may be wrong; verify the new `import com.workouthub.analytics.PrDetector;` line is present and the call sites at lines 82 and 110 still compile.
   - `sessions/SessionSetsIntegrationTest.newPrFlagPresentOnCreateButOmittedOnDetailReread` failure -> same root cause; the `newPr` flow depends on `PrDetector.beatsPriorBest` which is now in `com.workouthub.analytics`.
3. Fix root causes ONLY in the file that broke; do not refactor adjacent code.

What to AVOID and WHY:
- Do NOT change any test assertion. The contract this plan preserves is "behavior unchanged after move"; any green-test edit means a behavior change snuck in.
- Do NOT silence or `@Disabled`-mark a flake. Repeat-run via `./mvnw test -Dtest=ClassName` to triage if needed.
- Do NOT rerun only a subset to "save time". A clean move must pass the WHOLE suite, including unrelated packages, because component scan and import errors ripple beyond the touched files.
  </action>
  <verify>
Full suite green:
```
cd backend && ./mvnw -q test
# Expect: BUILD SUCCESS, [INFO] Tests run: <N>, Failures: 0, Errors: 0, Skipped: <as before>
```

Skip count unchanged from pre-move baseline (run `git stash` of the move and compare if uncertain - audit Section 1 reports 6+9+3 = 18 tests across the three relocated test files, plus the existing `newPrFlagPresentOnCreateButOmittedOnDetailReread` in `SessionSetsIntegrationTest:282-294` which MUST stay green).

Public-API smoke check (manual sanity, not gating): inspect that the controller routes at the new path resolve identically. Already covered by `analytics/ExerciseAnalyticsIntegrationTest` running green - no separate curl needed.
  </verify>
  <done>
- `./mvnw test` exits zero with no failures and no errors.
- Skip count matches pre-move baseline.
- `analytics/PrDetectorTest` (9 tests), `analytics/ExerciseAnalyticsIntegrationTest` (~7 tests), `analytics/PrDetectionIntegrationTest` (3 tests) all pass under their new package.
- `sessions/SessionSetsIntegrationTest.newPrFlagPresentOnCreateButOmittedOnDetailReread` still green (the create-only `newPr` contract from Phase 15-04 survives the move).
  </done>
</task>

</tasks>

<verification>
Before declaring plan 16-02 complete:
- [ ] `./mvnw -q test` succeeds (full suite, all packages)
- [ ] `./mvnw -q -DskipTests package` succeeds (no Flyway / JPA validate failures - relevant because component scan covers the new package)
- [ ] No grep hits for `com.workouthub.sessions.(PrDetector|ExerciseAnalyticsController|ExerciseAnalyticsService|dto\.(LastPerformanceDto|ProgressPointDto))` anywhere in `backend/src`
- [ ] `git diff --stat` shows the move shape: 5 source moves, 3 test moves, 1 single-line edit in `SessionSetsService.java`
- [ ] No edits to migrations under `backend/src/main/resources/db/migration/`
- [ ] No edits to `application.yml`, `compose.yml`, or any docs/contract file (this is an internal package refactor)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced (plain `mvn -q test` exit zero, no new compiler warnings on relocated files)
- Phase 16-01 audit Section 4 Part A verdict (extract to `com.workouthub.analytics`) realized
- Public API surface byte-for-byte identical (paths, status codes, response bodies)
- Plan 16-03 (epley-projection) and Plan 16-04 (pr-durability) can pick up the relocated files without further moves
  </success_criteria>

<output>
After completion, create `.planning/phases/16-sessions-analytics/16-02-SUMMARY.md`:

# Phase 16 Plan 02: Package Extraction Summary

**[Substantive one-liner - e.g. "Relocated 5 analytics source files plus 3 tests from sessions/ to com.workouthub.analytics; one-import touch on SessionSetsService; full test suite green."]**

## Accomplishments

- Moved `ExerciseAnalyticsController`, `ExerciseAnalyticsService`, `PrDetector` from `com.workouthub.sessions` to `com.workouthub.analytics`.
- Moved `LastPerformanceDto`, `ProgressPointDto` from `sessions/dto` to `analytics/dto`.
- Moved `ExerciseAnalyticsIntegrationTest`, `PrDetectorTest`, `PrDetectionIntegrationTest` to `com.workouthub.analytics` test package.
- Added `import com.workouthub.analytics.PrDetector;` to `SessionSetsService` (single-line touch).
- Phase 15-01 audit Section 5 in-package leaks closed.
- Epley duplication between `analytics/PrDetector.epleyOneRm` and `analytics/AnalyticsService.epley` now visible in a single package; consolidation deferred to Phase 31 per audit Section 6 D2.

## Files Created/Modified

- `backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsController.java` - moved from `sessions/`
- `backend/src/main/java/com/workouthub/analytics/ExerciseAnalyticsService.java` - moved from `sessions/`; added cross-package imports for `SessionsMapper`, `SessionSetDto`
- `backend/src/main/java/com/workouthub/analytics/PrDetector.java` - moved from `sessions/`
- `backend/src/main/java/com/workouthub/analytics/dto/LastPerformanceDto.java` - moved from `sessions/dto/`; added cross-package import for `SessionSetDto`
- `backend/src/main/java/com/workouthub/analytics/dto/ProgressPointDto.java` - moved from `sessions/dto/`
- `backend/src/main/java/com/workouthub/sessions/SessionSetsService.java` - one new import (`com.workouthub.analytics.PrDetector`)
- `backend/src/test/java/com/workouthub/analytics/ExerciseAnalyticsIntegrationTest.java` - moved from `sessions/`
- `backend/src/test/java/com/workouthub/analytics/PrDetectorTest.java` - moved from `sessions/`
- `backend/src/test/java/com/workouthub/analytics/PrDetectionIntegrationTest.java` - moved from `sessions/`

## Decisions Made

- Picked Direction A (extract) per audit Section 4 Part A verdict; no late switch to stay-in-place.
- Kept `SessionsMapper` in `com.workouthub.sessions`. Cross-package call from `analytics/ExerciseAnalyticsService` is fine because both class and methods are public static. Moving `SessionsMapper` to a shared module is out of scope; it owns `SessionDto`/`SessionSummaryDto` mapping that belongs in `sessions/`.
- Did NOT introduce a back-compat shim or deprecated re-export; clean directory move per audit recommendation.

## Issues Encountered

[Document any compile errors hit during the move and how they were resolved, or "None - mechanical move with audit-listed import-path changes only."]

## Next Step

Ready for `16-03-PLAN.md` (epley-projection: append `estimatedOneRmKg` to `ProgressPointDto` and compute in `summarize`). Files now live at their final v0.4 location.
</output>
